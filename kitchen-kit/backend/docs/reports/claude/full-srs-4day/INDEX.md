# ROS Full-SRS 4-Day Execution — Reports Index

Navigation only. Entries are appended chronologically; never rewritten.

## What lives here

This directory holds the **P1 through P20** Full-SRS 4-day execution reports —
one report per implementation, design-gate, verification or acceptance slice.

**P0-REBASE is not here and is not moved.** It remains permanently in the parent
reports directory:

- `../2026-09-02_FULL-SRS-current-head-traceability-rebase.md`
- `../2026-09-02_FULL-SRS-current-head-traceability.csv`
- `../2026-09-02_FULL-SRS-4day-execution-board.csv`

The parent index `../INDEX.md` continues to cover everything up to and including
P0. From P1 onward, every new report for this programme is written here and adds
one row to the table below.

## Authority

**These reports are NON-AUTHORITATIVE EVIDENCE.** `ROS_SRS_v1.0.pdf` and the
ratified entries in `docs/governance/GOVERNANCE_DECISION_REGISTER.md` remain
authoritative. Where a report disagrees with the SRS or a ratified governance
decision, the SRS and the register win. A report records what was observed and
measured in its own session — it ratifies nothing and authorises nothing.

## What every report must state

Each report opens with the standard header (task/slice name, report type,
authority statement, date, HEAD, branch, working-tree summary, task identifier)
and must explicitly record:

- **Baseline HEAD** the slice started from;
- **Resulting HEAD** after the slice's commits;
- **Tests and checks actually executed in that session**, with real results —
  never a prior run's numbers re-reported as new;
- **Commit(s) created**, with exact subjects;
- **Push and deploy state**;
- **Unresolved blockers**, with why each is still open.

Reports are never overwritten. A slice interrupted before its report is complete
keeps the partial file marked `PARTIAL`, states exactly where work stopped, and
is either completed in the same run or superseded by a `_02` report in a new one.

## Naming

`YYYY-MM-DD_<SLICE-ID>_<short-description>.md` (kebab-case, no spaces). A second
report for the same slice on the same date appends `_02`, `_03`, and so on.

Examples: `2026-09-02_A1-1_inventory-write-path-correctness.md` ·
`2026-09-02_G1-1_ci-pipeline.md` · `2026-09-02_D1-1_offline-sync-design-gate.md`

## Reports

| Date | Slice | Lane | Type | Baseline | Result Commit | Status | Report |
|---|---|---|---|---|---|---|---|
| 2026-09-02 | A1-1 — Inventory movement write-path correctness | A | Implementation + tests + acceptance | `63d3b7c2` | `fix(inventory): make movement projection atomic` (this commit — see `git log` on this path) | COMPLETE — atomic projection, exact decimal, concurrency + exact-fold regression tests added and verified to fail against the pre-fix code; 815/815 unit, 1156/1157 e2e (1 pre-existing, confirmed-unrelated `NFR-PERF-006` failure, reproduced identically at baseline); zero schema/migration/API/permission change | [2026-09-02_A1-1_inventory-write-path-correctness.md](2026-09-02_A1-1_inventory-write-path-correctness.md) |
| 2026-09-02 | A1-1 acceptance correction — exact persisted movement deltas (Transfers/Counts/Waste) | A | Narrow implementation correction + tests | `eef0f15` | `fix(inventory): preserve exact movement deltas` (this commit — see `git log` on this path) | COMPLETE — every `MovementsService.post` caller now exact end to end (no `Number()`/`Math.abs()`/JS subtraction on a value feeding a persisted `stock_movements.quantity`); 7 new exact-decimal tests, 3 verified to fail against the reintroduced pre-correction arithmetic; 815/815 unit, targeted e2e green; zero schema/migration/API change | [2026-09-02_A1-1_inventory-write-path-acceptance-correction.md](2026-09-02_A1-1_inventory-write-path-acceptance-correction.md) |
| 2026-09-02 | A1-2 — Group FIFO layer locking by distinct (stockItemId, locationId) | A | Implementation + tests + performance measurement | `45ad383` | `perf(inventory): group depletion layer locks` (this commit — see `git log` on this path) | A1-2 ACCEPTED — `SaleDepletionService` locks FIFO layers once per distinct key (135→5 acquisitions on the 30-line benchmark fixture, canonical global order proven with real spies incl. input-order-reversed); evolving in-memory physical/accounting state proven equivalent to a fresh re-read (FEFO-vs-FIFO divergence + carry-forward tests pass under both pre- and post-A1-2 code); isolated benchmark p50 2754→750ms (−72.8%), p95 4382→2069ms (−52.8%); `NFR-PERF-006` remains PARTIAL/VERIFIED-FAILING (p95 still >200ms, A1-3 required); 815/815 unit, 1167/1169 e2e (2 pre-existing/confirmed-transient parallel-load failures, clean in isolation); zero schema/migration/API change | [2026-09-02_A1-2_inventory-lock-grouping.md](2026-09-02_A1-2_inventory-lock-grouping.md) |
| 2026-09-02 | B1-1 | B | GOVERNANCE ANALYSIS / DECISION BRIEF | `63d3b7c` | *this row's own commit* — `docs(security): prepare branch RBAC governance gate` | **COMPLETE — AWAITING USER GOVERNANCE DECISION.** D-2 (RATIFIED 2026-08-17 CORE ONLY) still defers `FR-SEC-002`/`003`/`004` [M] and `FR-SEC-005` [S]; branch isolation does not exist (any tenant principal may act on every branch). Recommends **C-1** (scoped role assignment as the sole authorization source; `EmployeeBranch` stays authentication-integrity only), scope types **`TENANT`/`BRAND`/`BRANCH`** only, **no branch claim in the JWT** (T-2, escalating an `FR-API-012`-vs-`FR-SEC-028` `[M]` conflict), **typed FK'd scope columns instead of a polymorphic `scope_id`**, and migration **M-4**. Report ratifies nothing; the register was NOT modified. **B1-2 must not start.** | [2026-09-02_B1-1_branch-rbac-governance-gate.md](2026-09-02_B1-1_branch-rbac-governance-gate.md) |
| 2026-09-02 | B1-1 (acceptance correction) | B | GOVERNANCE RATIFICATION + ACCEPTANCE CORRECTION | `1e53a21` | *this row's own commit* — `docs(security): ratify branch-scoped RBAC` | **RATIFIED — B1-2 GOVERNANCE-UNBLOCKED.** `AMENDMENT — D-2 REOPENED IN PART (2): BRANCH-SCOPED RBAC` recorded under D-2 (RATIFIED 2026-09-02, explicit user authority); historical D-2 text preserved, no decision numbered or renumbered. Lifts the defer for `FR-SEC-002`/`003`/`004`/`005` and the branch/scope portions of `FR-API-012`. Ratifies **C-1** (scoped assignments are the sole grant source; `EmployeeBranch` is AND-only, never a grant), a **generic target-scope lattice** `TENANT`/`BRAND(id)`/`BRANCH(id)` with downward-only coverage, **`T-4-LIVE`** (token carries the SRS snapshot + scope epoch; **live server-side resolution stays authoritative**), **`M-4+`** (adds the already-multi-branch case), no branch-aware RLS, and the B1-2/B1-3 boundaries. Corrects B1-1: **`T-2` not ratified**, lattice added, `M-4`→`M-4+`, **`BRANCH_GROUP` deferred not rejected** (mandatory `FR-BRN-005` follow-up), **`FR-SEC-028` COMPLETE→PARTIAL** (local-data wipe on next contact not implemented). **Nothing implemented**: `FR-SEC-002`/`003`/`004`/`005` RATIFIED FOR IMPLEMENTATION — NOT IMPLEMENTED; `FR-API-012` RATIFIED DESIGN — NOT YET COMPLETE. | [2026-09-02_B1-1_branch-rbac-ratification.md](2026-09-02_B1-1_branch-rbac-ratification.md) |
| 2026-09-02 | B1-2 | B | IMPLEMENTATION + SCHEMA/MIGRATION + TESTS + ADR | `2967043` | *this row's own commit* — `feat(security): implement scoped role assignments` | **COMPLETE — B1-3 UNBLOCKED.** Implements the scoped-RBAC foundation authorised by `AMENDMENT — D-2 REOPENED IN PART (2)`. Migration **36** (`20260902010000_identity_scoped_role_assignments`) replaces `membership_roles`' composite PK with a stable id and adds `tenant_id`, `scope_type` (`tenant`/`brand`/`branch`), **typed composite-FK'd** `scope_brand_id`/`scope_branch_id` (no polymorphic `scope_id`), `valid_from`/`valid_to`, `origin`/`reviewed_at`, three CHECKs and a `btree_gist` temporal EXCLUDE; drops the never-used legacy `branch_id`; adds `memberships.authz_epoch` + `UNIQUE(tenant_id,id)`; and adds the **missing `UPDATE` RLS policy** (`USING` + `WITH CHECK`) that made `FR-SEC-005` unusable. Live scope-aware resolution on the **DB clock**; the generic `permission + target scope` primitive with the downward-only lattice; **`T-4-LIVE`** (token carries `scp`/`pbr`/`epo`, **claims never authorize**, stale snapshot refused, symbolic permitted-branch set, overflow fails closed at 128 units); POS narrowing where `EmployeeBranch` is **AND-only**; scoped assignment APIs (explicit scope mandatory, deprecated remove-by-role fails closed at 409); **`M-4+`** backfill + provenance + second-active-branch gate + already-multi-branch handling; ADR **0009** superseding the ADR 0002/0004 branch-scope deferrals and closing ADR 0008 D-02. **Transitional safety:** a target-less `@RequirePermission` route is a TENANT-target operation, so BRAND/BRANCH grants fail closed until B1-3 converts routes. **Status: `FR-SEC-002`/`003` COMPLETE, `FR-SEC-005` COMPLETE, `FR-SEC-004` PARTIAL, `FR-API-012` PARTIAL, `FR-SEC-028` PARTIAL** — no overclaim; B1-3 owns route-wide enforcement, the coverage gate, the security review and retirement of the single-active-branch mask. | [2026-09-02_B1-2_branch-scoped-rbac-foundation.md](2026-09-02_B1-2_branch-scoped-rbac-foundation.md) |
| 2026-09-02 | D1-1 | D — KDS + Offline/Sync | DESIGN / CONTRACT / GOVERNANCE-DEPENDENCY | `63d3b7c` | *(see report §28)* | COMPLETE — 7 governance decisions required (4 block D4-1); fiscal deferred to D4-3 | [2026-09-02_D1-1_offline-sync-design-gate.md](2026-09-02_D1-1_offline-sync-design-gate.md) |
| 2026-09-02 | D1-1 | D — KDS + Offline/Sync | GOVERNANCE RATIFICATION / ACCEPTANCE CORRECTION | `50b3706` | *(see report §32)* | COMPLETE — design ACCEPTED WITH CORRECTIONS; GD-D1-01/04/05 ratified, 02/06 with corrections, 03 deferred, 07 REJECTED; D4-1 CORE authorised, 3 hard gates open | [2026-09-02_D1-1_offline-sync-ratification.md](2026-09-02_D1-1_offline-sync-ratification.md) |
| 2026-09-02 | D4-1A | D — KDS + Offline/Sync | IMPLEMENTATION + SCHEMA/MIGRATION + TESTS + BENCHMARK | `76b4289` | *(see report §44)* | COMPLETE — protocol kernel live at POST /v1/sync/batch; 894 unit + 1218 e2e green; NFR-PERF-032 measured (kernel p95 340 ms, representative p95 937 ms) but NOT fully verified; 3 hard gates open | [2026-09-02_D4-1A_sync-protocol-kernel.md](2026-09-02_D4-1A_sync-protocol-kernel.md) |
| 2026-09-02 | `MW1B` — Merge Wave 1B integration: `A1-2` + `B1-1`/`B1-2` + `D1-1`/`D4-1A` | A+B+D | Reviewed-slice integration + cross-lane reconciliation + verification | `b69509f` (starting — see report §1 for the disclosed baseline-HEAD deviation) | `41039350bff2dc2e077bd36f573965fd2d0bf0aa` (reconciliation commit, pre-report) | **INTEGRATED — COMMITTED.** All 7 reviewed commits cherry-picked in the required order; 2 real append conflicts in `INDEX.md` and 1 real conflict in `module-boundaries.spec.ts` (resolved per task §6C, D4-1A's intent-based Reporting assertion superseding B1-2's brittle migration count), rest auto-merged clean; `prisma validate`/`openapi:check` clean after regeneration; migrations 35→37 confirmed both from-zero and as a seeded 35→37 upgrade (M-4+ backfill/provenance/already-multi-branch proven live); `typecheck` clean; unit **917/917 (65 suites)**; module boundaries **45/45**; lint **exactly 48** (pre-existing baseline, zero new from A1-2/B1-2/D4-1A — 8 substantive + formatting fixes applied in the reconciliation commit); audit **exactly 6 high**, unchanged; targeted A1-2/B1-2/D4-1A/cross-lane suites **28/28 suites, 390/390 tests**; full E2E **6/7 full runs 77/77 suites 1281/1281 tests clean, 1/7 a single Class-C contention-only failure in `cash-session-close.e2e-spec.ts`** (3/3 clean in isolation, unrelated to any integrated commit); one cross-lane finding — B1-2's new `TenantContextGuard` now shadows D4-1A's revoked-terminal reassurance message (data-loss guarantee independently re-proven, message-wording reconciliation left open for B1-3/governance, not resolved unilaterally); zero orphan scratch DBs; persistent `ros` untouched throughout. No push, no deploy, no rebase, no destructive git | [2026-09-02_MW1B_integration-a1-2-b1-2-d4-1a.md](2026-09-02_MW1B_integration-a1-2-b1-2-d4-1a.md) |
| 2026-09-02 | G1-3 — Observability baseline (structured logging + RED metrics + redaction + alert/runbook foundation) | G — CI/Infra/Observability | IMPLEMENTATION + SECURITY/PRIVACY REVIEW + TESTS | `b46a00e` | *this row's own commit* — `feat(observability): establish logging and RED metrics` | **COMPLETE.** Central `src/common/observability/` module: `AsyncLocalStorage`-based request context (correlation/causation ids, trusted-only tenant/branch — never from a header/body/query/JWT snapshot), `StructuredLoggerService` (Nest `LoggerService` + direct entry point, installed via `app.useLogger()` so all 14 pre-existing `new Logger()` call sites emit structured JSON with zero call-site edits), an allowlist-first redaction layer (denylist as defence-in-depth, 27 sabotage tests), exactly-once request-completion logging + `prom-client` RED metrics (`http_requests_total`/`http_request_duration_seconds`, 4 bounded labels, cardinality-sabotage-proven, endpoint-vs-handler-proven), an opt-in loopback-default internal metrics exporter (disabled unless `METRICS_PORT` set — zero G1-2 collision risk), 4 Prometheus alert rules + 4 runbooks covering `NFR-PERF-030`/`031` and API error rate, and a new scoped `no-console` ESLint gate on runtime `src/`. Found and fixed one real bug during verification: `RequestMethod.ALL` middleware registration created a genuine Express Route (`/{*path}`) visible to `openapi.e2e-spec.ts`'s live-route/OpenAPI-drift check — fixed by registering with a bare path string so Nest falls through to plain `app.use()`. Pre-implementation baseline drift found and disclosed (not caused by this session): stale generated Prisma client inflated `tsc`/lint counts (fixed via `prisma generate`, no schema/migration change); true baseline measured at **49 lint errors / 3 warnings**, **7 high / 1 moderate audit advisories** (vs. the task brief's stated 48/6) — zero new of either after this slice. `typecheck` clean; unit **1029/1029 (76 suites)**, +112 new tests across 11 new spec files; module boundaries **45/45** (zero new `src/modules/**` edges); `openapi:check` clean; lint **48/3** (baseline 49/3, zero new); audit **7 high/1 moderate** (unchanged, `prom-client@15.1.3` added zero new advisories); targeted E2E **16 suites / 310 tests** (organisation/rbac/sales/app/openapi/db-isolation-config + audit/inventory×3/sync×5); full E2E **76/77 suites, 1280/1281 tests** — 1 failure, Class C (known pre-existing `NFR-PERF-006` transaction-timeout flake under full-suite contention, confirmed passing standalone, independently pre-documented in the `MW1A` correction report); zero orphan scratch DBs; persistent `ros` untouched. **Disposition**: `NFR-OBS-001` COMPLETE, `NFR-OBS-003` COMPLETE, `NFR-OBS-005` PARTIAL (allowlist metadata channel exhaustive; free-text message channel only best-effort scrubbed), `NFR-OBS-006` PARTIAL (backend API SLOs covered; uncovered SLO classes enumerated), `NFR-OBS-002`/`004`/`007` NOT IMPLEMENTED (no tracing, no business metrics — none fabricated, no support health surface). No schema change, no migration, no permission code, no business-domain feature, no push, no deploy | [2026-09-02_G1-3_observability-baseline.md](2026-09-02_G1-3_observability-baseline.md) |
| 2026-09-03 | `MW1D` — Integrate G1-3 observability baseline | G (+ cross-lane B) | Reviewed-slice integration + cross-lane reconciliation + verification | `2603099` | `a53c56e` (reconciliation commit, pre-report) | **INTEGRATED — COMMITTED.** Both reviewed G1-3 commits (`a9de80f`→`06c93e8`, `19b149c`→`5a20268`) cherry-picked in order; 1 real append conflict in `INDEX.md`, resolved retaining every existing row exactly once. **Hard cross-lane finding, found and fixed centrally, per task instruction**: `TenantEnrichmentInterceptor` is a global `APP_INTERCEPTOR`, and Nest never runs interceptors for a request a guard denies — B1-3's real guard order (`JwtAuthGuard`→`TenantContextGuard`→`PermissionGuard`, all plain guards) meant a `PermissionGuard` 403 always logged `tenantId: null, branchId: null` even though `TenantContextGuard` had already live-resolved and cached the trusted context at `request.authorization` (`TenantContextService.require`, memoized — read again, never re-derived, by `PermissionGuard` itself before it denies). Fixed in `CorrelationMiddleware` alone (its single completion-emission point now reads that same trusted, already-guard-verified object), never a header/body/query/JWT-snapshot value, no B1-3 authorization file touched. Sabotage-proved (fix disabled → 5/7 new tests failed with the exact wrong values; restored → 7/7 pass) and re-proved live against real 403s in the targeted E2E run. Authorization coverage unchanged: **157 routes, 141/141 permission-bearing classified, 0 undeclared, 16 auth-only**; `module-boundaries` **45/45**; `scoped-rbac`/`scoped-rbac-migration`/`permission.guard`/`scoped-authorization-matrix` all green. RED cardinality re-proven live against a real B1-3 route (50 distinct real branch UUIDs on `GET /org/branches/:branchId` → exactly one time series, zero UUIDs in the exported text). Sync automatic observability proven with zero Sync-specific code across accepted/revoked-terminal/malformed/replay paths, exactly-once per HTTP request (never once per operation), zero op/batch/terminal ids in metric labels. OpenAPI clean (zero diff, no phantom catch-all, no public metrics route, `/v1/sync/batch` retained); metrics exporter confirmed disabled by default (loopback bind, no port listener across any E2E run). `typecheck` clean; unit **78 suites / 1053 tests**; `openapi:check` clean; lint **48 errors/3 warnings** (corrected 2026-09-03: an earlier draft mis-transcribed eslint's `51 problems (48 errors, 3 warnings)` summary line as "51 errors" — re-verified with machine-readable `file:line:column:ruleId:severity` identities at this HEAD and at a disposable worktree on baseline `2603099`: the two 51-row identity sets are byte-for-byte identical, zero new findings); dependency audit **7 high/1 moderate**, unchanged, zero new from `prom-client`; targeted e2e **23 suites / 510 tests, 0 failures**; full e2e **79/80 suites, 1321/1322 tests**, one **pre-existing baseline QA defect** (corrected 2026-09-03 from an earlier "Class C" mislabel — this failure is constant under isolation, not contention-sensitive, so it does not fit Class C's own definition; also not Class D, since it is explained and pre-dates MW1D) in `reporting-authorization.e2e-spec.ts` — reproduced identically on the pre-integration baseline HEAD (`2603099`) in a disposable `git worktree`, confirmed a pre-existing UTC/`Africa/Cairo` wall-clock day-boundary test fragility unrelated to any integrated commit, not the reporting/day-close business logic itself. Zero orphan scratch DBs; persistent `ros` untouched throughout. D4-1B's lane worktree confirmed untouched (`2603099`, unchanged) before and after. No push, no deploy, no rebase, no destructive git. | [2026-09-03_MW1D_integration-g1-3.md](2026-09-03_MW1D_integration-g1-3.md) |
| 2026-09-02 | A1-3 — Set-oriented depletion write design gate | A | Design gate + SQL feasibility probes (no implementation) | `897333b` | `docs(inventory): design set-oriented depletion writes` (this commit — see `git log` on this path) | **APPROVE A1-3 IMPLEMENTATION** — measured 895 statements / 2,685 protocol messages per completion tx (all 20 benchmark iterations identical), of which only 119.6ms p50 / 149.6ms p95 is server-side execution against 464.9/608.2ms wall clock, so ~75-80% of the cost is round-trip overhead; proposed design reduces 895 → 28 statements (867 removed, 96.9%). 8 SQL probes executed against the disposable Lane-A DB (all rolled back, cleanup verified): window-function `balance_after` exact and deterministic under explicit `ord`; full statement proven on the real partitioned/RLS-forced schema and re-proven as `ros_app` under FORCE RLS with ledger `UPDATE` still denied; `UPDATE…FROM` multiple-match hazard reproduced and mitigated by mandatory in-statement `GROUP BY`; set-oriented reservation with identity-based conflict detection proven both paths; two-session concurrency proof (T2 blocked 1.58s, saw committed state, chains contiguous) plus the rejected `SELECT`-then-write shape executed and shown to produce a broken ledger; carry-forward staleness reproduced (unit_cost 100 vs 200) driving a mandatory flush rule. `NFR-PERF-006` remains PARTIAL/VERIFIED-FAILING; zero schema/migration/API/product-code change | [2026-09-02_A1-3_set-oriented-depletion-design-gate.md](2026-09-02_A1-3_set-oriented-depletion-design-gate.md) |
| 2026-09-03 | A1-3A — Set-oriented effect reservation + weighted-average cost hoist | A | Implementation + tests + correctness/performance verification | `d0c1c82` | `perf(inventory): batch depletion effect reservation` (this commit — see `git log` on this path) | **A1-3A ACCEPTED** — one `INSERT … SELECT … ON CONFLICT DO NOTHING RETURNING` (via `jsonb_to_recordset`, real `JSON.stringify`→`::jsonb` Prisma binding proven) replaces 135 per-effect reservation `INSERT`s; identity-based conflict detection names every missing `(orderLine, stockItem, location)`, not only the first; duplicate in-request identity rejected fail-closed before any SQL (sabotage test); partial-reservation-then-conflict proven to roll back the WHOLE transaction (spy proves zero `lockLayers` calls, snapshots prove zero Inventory mutation survives); weighted-average `current_cost` hoisted 45→1 reads/call (batching + missing-row-default + FIFO/standard-unaffected all proven via real query-event-log counting). Measured on the unmodified 30-line/895-statement fixture: reservation 135→1, avg-cost 45→1, **total 895→717 statements** (exactly the design gate's predicted value), all 20 iterations identical; isolated instrumentation-off perf p50 750.45→369.44ms (−50.8%), p95 2068.60→425.75ms (−79.4%); `NFR-PERF-006` remains PARTIAL/VERIFIED-FAILING (674 A1-3B-scoped statements still dominant) — correctly not claimed COMPLETE. `sale-depletion-lock-grouping.e2e-spec.ts` passes byte-for-byte unmodified (semantic-equivalence proof); 10 new A1-3A-specific tests added; 815/815 unit, module-boundaries 45/45, zero schema/migration/API change, zero new lint; persistent `ros` never touched | [2026-09-03_A1-3A_set-oriented-effect-reservation.md](2026-09-03_A1-3A_set-oriented-effect-reservation.md) |
| 2026-09-03 | A1-3B — Set-oriented group write path | A | Implementation + tests + correctness/concurrency/performance verification | `337cd6f` | `perf(inventory): batch depletion group writes` (this commit — see `git log` on this path) | **A1-3B ACCEPTED — `NFR-PERF-006`: COMPLETE / VERIFIED-PASSING.** Per stock-key group: one `GROUP BY`-aggregated physical `stock_batches` UPDATE, one aggregated accounting UPDATE (with a mandatory pre-carry-forward flush, design gate §9.2 — proven live), one atomic `stock_levels` delta + multi-row `stock_movements` INSERT with a SQL window-computed `balance_after` (starting balance derived from the write's own `RETURNING`, never independently read), one pointer UPDATE + multi-row `sale_depletion_allocations` INSERT (pointer resolved via explicit `ord`, never `max(UUID)`). `writeAllocation`'s old four-per-slice pattern deleted outright — no hidden fallback. Incidental fix disclosed in full: `common/ids.ts`'s `newId()` switched to `ulidx`'s `monotonicFactory()` — plain `ulid()` is not strictly ordered within the same millisecond (confirmed empirically), which this slice's synchronous per-group id generation exposed; A1-2/A1-3A's real-DB-round-trip-per-id pattern had masked it. Measured on the unmodified 30-line/895-statement fixture (40 transactions across two runs, all identical): physical UPDATEs 105→4, accounting UPDATEs 60→2, levels+movements 270→5, pointer+allocations 270→5, **total 895→28 statements** (exactly the design gate's own projected breakdown); isolated instrumentation-off perf p50 369.44→41.38ms (−88.8%), **p95 425.75→52.27ms (−87.7%)** — well under the 200ms gate. `sale-depletion-lock-grouping.e2e-spec.ts` (5/5) and `sale-depletion-effect-reservation.e2e-spec.ts` (10/10) pass byte-for-byte unmodified; both `order-completion-concurrency` suites + `movements-concurrency` (19/19) and `cash-movements-close-and-payment-concurrency` (34/34) pass unmodified — no new lock order, no deadlock; 9 new A1-3B-specific tests (same-batch aggregation hazard, axis independence, carry-forward flush + its own statement-count proof, high-precision sequential `balance_after`, final-pointer derivation, allocation provenance, atomic-starting-balance proof, group statement-count proof); 815/815 unit, module-boundaries 45/45, zero schema/migration/API change, zero new lint, persistent `ros` never touched. **A1-3 (A1-3A+A1-3B) COMPLETE.** | [2026-09-03_A1-3B_set-oriented-depletion-writes.md](2026-09-03_A1-3B_set-oriented-depletion-writes.md) |
| 2026-09-03 | `MW1E` — Integrate A1-3 set-oriented depletion performance work (design gate + A1-3A + A1-3B) | A | Reviewed-slice integration + cross-lane reconciliation + performance re-verification | `a31ce96` | `6922a3e` (reconciliation commit, pre-report) | **INTEGRATED — COMMITTED.** All 5 reviewed A1-3 commits cherry-picked in ancestry order (A1-3A implementation hash resolved and hard-checked: `a57115e3` is the exact parent of `337cd6f`, exact subject match); 2 `INDEX.md` append conflicts, both resolved retaining every existing row exactly once, rest clean. Every A1-3A/A1-3B hard gate directly verified in the integrated `sale-depletion.service.ts`: reservation-first with in-request duplicate rejection, `GROUP BY`-aggregated physical and accounting UPDATEs (independent axes), mandatory carry-forward flush ordering, atomic `stock_levels` delta with `RETURNING`-derived starting balance (no independent pre-read), SQL window `balance_after`, `ord`-derived pointer, full allocation provenance, `writeAllocation` fully removed. **Cross-cutting hard review**: `common/ids.ts`'s `newId()` monotonic-factory change reviewed against all 9 blast-radius criteria (format/wire contract/uniqueness/monotonicity/no-caller-reordering-dependency/no-API-shape-change/no-schema-change/no-cross-module-regression) — Lane A shipped the fix but no dedicated test, so this integration added `src/common/ids.spec.ts` (4 tests: 1000-ID same-millisecond strict ordering, 5000-ID uniqueness, UUID_PATTERN conformance) in the reconciliation commit, the only reconciliation diff needed. D4-1A/Sync (6 representative suites), audit/domain-events/cash/sales, and G1-3 observability correlation all re-run and green; Lane D confirmed untouched at `2603099` throughout. `typecheck` clean; unit **79 suites/1057 tests**; module boundaries **45/45**; authorization coverage unchanged **157/141/0/16**; `prisma validate` clean, migrations unchanged at **37**; `openapi:check` clean, zero diff; lint **48 errors/3 warnings, identity-for-identity zero new** (disposable-worktree diff against starting HEAD, real `cp -a node_modules` + fresh `prisma generate` — a first attempt with a symlinked `node_modules` produced a spurious 15,627-error false baseline from broken type-aware lint resolution, diagnosed and corrected before use); dependency audit **7 high/1 moderate**, unchanged, zero dependency/lockfile diff. Statement count re-measured live via guarded scratch-database Postgres query logging (a disposable, uncommitted test copy, deleted after use): **exactly 28 statements** per completion transaction, matching A1-3B's accepted evidence exactly. Performance re-verified **3 isolated runs** — p95 51.16/123.11/143.57ms, all comfortably under the 200ms gate; the run-to-run spread traced to shared-machine CPU contention (load average 7.37 during the slowest run, no stray test process found), not to the integration, and corroborated load-independent by the stable 28-statement count. **`NFR-PERF-006`: COMPLETE / VERIFIED-PASSING** (integrated evidence, not carried forward). Targeted e2e **13 A1 suites/150 tests + 19 cross-cutting/regression suites/375 tests, 0 failures**; full e2e **82/82 suites, 1341/1341 tests, 0 failures** (the previously-documented `reporting-authorization.e2e-spec.ts` UTC/Cairo boundary QA defect did not reproduce this run). Zero orphan scratch DBs; persistent `ros` never touched. **A1-3: COMPLETE. A1-4/BR-INV-003: NOT IMPLEMENTED**, no broader Inventory completion claimed. No push, no deploy, no rebase, no destructive git, no schema/migration/API/permission change. | [2026-09-03_MW1E_integration-a1-3.md](2026-09-03_MW1E_integration-a1-3.md) |
| 2026-09-03 | A1-4 — Inventory concurrency matrix closure | A | Implementation + tests + concurrency/correctness/performance verification (resumed after an unexpected machine reboot; surviving uncommitted diff verified complete, not re-implemented from scratch) | `2743e6a` | `fix(inventory): close concurrent movement races` (this commit — see `git log` on this path) | **A1-4 CLOSED for the concurrency-matrix scope.** `movements.service.ts`: removes the unlocked `stock_levels` pre-read behind `average_cost`; the atomic upsert now `RETURNING`s `average_cost` under the same row lock that applies the quantity delta, closing the lost-update race between concurrent receipts and the concurrent-first-receipt race. `counts.service.ts`/`waste.service.ts`: posts/records sort lines `stockItemId ASC` (Completion's global lock order), closing the multi-item waste↔count/waste↔waste deadlock; count posting now subtracts an exact window-sum of movements committed during the count session from the persisted variance (CT-08/FR-INV-044), so a concurrent sale is never reported as false shrinkage. 17 new tests (`inventory-concurrency-matrix.e2e-spec.ts`, real-Postgres-lock barrier harness, no sleeps): transfer-vs-sale (3/3), waste-vs-sale (3/3), CT-08 both directions, weighted-average races (existing-row ×3, first-receipt, zero-qty/stale-average, 6dp-exact), and the 3-scenario deadlock matrix (incl. multi-item inversion) — every race asserts exact `stock_movements` fold == `stock_levels` **and** a truthful, commit-consistent `balance_after` at every step, not just the final total. **A1-3's set-oriented depletion path (sale-completion, 28 statements) is untouched** — confirmed by diff inspection (`SaleDepletionService` never calls `MovementsService`) and reconfirmed live: `sale-depletion-set-oriented-writes.e2e-spec.ts` 9/9 unmodified; `NFR-PERF-006` p50=36.70/**p95=39.42ms**, well under the 200ms gate. `typecheck` clean; targeted e2e 141/141 (concurrency matrix 17, sale-depletion 9, inventory/RLS/decimal/movements-concurrency 65, order-completion+cash concurrency 49, performance 1); full unit **79 suites/1057 tests**, module boundaries 45/45, authorization coverage unchanged **157/141/0/16**; lint **48 errors/3 warnings unchanged, zero new** (0 on the 4 changed files); dependency audit **7 high/1 moderate unchanged**, zero lockfile diff; `prisma validate` clean, migrations unchanged at **37**; `openapi:check` clean, zero diff; zero schema/API change. **Full e2e reported honestly, not rounded to green: 82/83 suites, 1320/1358 tests** — `production.e2e-spec.ts` (38 tests, D-17-03 branch/brand/tenant scope precedence, a scope A1-4 never touched) failed under full-suite `maxWorkers=4` Postgres contention and passed **44/44 immediately in isolation**; classified **Class C**; no second full-suite rerun was performed to manufacture 83/83. Two `ros_test_e2e_*` databases orphaned by the machine reboot that interrupted this session (their owning Jest process was killed before its own `globalTeardown` ran) were found and swept via the harness's own recovery tool (`sweep-stale-scratch-databases.ts`, dry-run then real); **orphan scratch DBs = 0** confirmed after; persistent `ros` untouched throughout (own connections were read-only checks). **Governance, no overclaim: `BR-INV-003` ledger/projection limb PROVEN by this closure; `BR-INV-003` overall remains PARTIAL** — the daily reconciliation scheduler/alert limb is **NOT IMPLEMENTED** (none was built, per the task's explicit no-scheduler rule). **`FR-INV-011`/`FR-INV-051` scheduled obligations remain outstanding.** No push, no deploy, no rebase, no destructive git, no speculative migration. | [2026-09-03_A1-4_inventory-concurrency-closure.md](2026-09-03_A1-4_inventory-concurrency-closure.md) |
| 2026-09-03 | D4-1B — Offline domain handlers + live authorization + recovery hard gates | D — KDS + Offline/Sync | IMPLEMENTATION + CORRECTNESS/PERFORMANCE GATES | `2603099` | `feat(sync): execute offline domain operations safely`, `feat(sync): add lossless revoked-terminal recovery` (this row's own commits) | **IMPLEMENTED — TWO PRODUCTION HANDLERS, THREE REAL DEFECTS FOUND AND CLOSED, ONE PERFORMANCE GATE NOT MET.** Implements `kds.ticket.bump_line`/`kds.ticket.recall` as real `SyncOperationHandler`s (DB-only, no external side effect); binds `SYNC_AUTHORIZATION_PORT` for real via a new `POS_ACTOR_AUTHORIZATION` seam + B1-3's unchanged `SCOPE_AUTHORIZATION`; closes the MW1C inactive-branch × Sync gap centrally in `SyncTerminalGuard` via the published `BRANCH_BRAND_QUERY` (regression test proves 403 before any handler mutation, `operation_dedup` count unchanged); implements the GD-D1-07 lossless revoked-terminal recovery hard gate as a new admin-driven (not terminal-driven — see report §5.1 for the terminal-authentication design defect found and corrected mid-session) `sync.recovery_grants` one-shot-CAS mechanism (migration 38); fixes a real D4-1A-flagged defect where a `conflict`-settled causal parent was wrongly treated identically to a `rejected` one in TWO code paths (`causal_parent_conflicted` reason code added, `deferred` not a sixth state); fixes a real FR-OFF-043 gap where `conflict` outcomes wrote no `sync.conflict_records` row. Also disclosed: two research subagents given read-only directives instead wrote real implementation code unsupervised early in this session; the resulting work was reviewed file-by-file, several defects found and fixed as above, one piece of unused/untested code (`UnitOfWork.runWithinTransaction`) removed, and everything independently re-verified in this session (report §0). **Status: FR-OFF-022 COMPLETE (review item closed); FR-OFF-040/043 PARTIAL (materially improved, not complete — only KDS conflicts wired); NFR-PERF-032 PARTIAL, NOT COMPLETE — kernel/representative/conflict/duplicate-replay paths meet the 3s p95 budget, the all-success production-handler path measures p95 4023 ms (root cause identified: no per-batch actor-authorization memoization, named as follow-up, not fixed here).** Typecheck clean; unit **66/66 suites, 935/935 tests**; module boundaries PASS (one new, intentional, asserted-live `kitchen->sync` deviation); authorization coverage gate PASS; `openapi:generate` purely additive (2 new routes); lint clean on every file this session touched, 48 pre-existing errors elsewhere unchanged; targeted sync e2e **10 suites / 88 tests, 0 failures**; full e2e **81/81 suites, 1340/1340 tests, 0 failures** (definitive run, `--maxWorkers=2`) — under heavier default parallelism, transient Class-C contention-only flakes appeared in 3 different PRE-EXISTING, untouched files across separate runs (never the same file twice, all clean in isolation), consistent with the documented full-suite-load contention pattern from MW1B/MW1C, not a functional regression. Zero orphan scratch DBs; persistent `ros` untouched throughout. No push, no deploy, no rebase, no destructive git. | [2026-09-03_D4-1B_offline-domain-handlers.md](2026-09-03_D4-1B_offline-domain-handlers.md) |
| 2026-09-03 | `MW1F` — Integrate A1-4 Inventory concurrency closure + corrected D4-1B Offline Domain slice (preserved chain + acceptance-correction chain) | A + D (+ cross-lane B/G) | Reviewed-slice integration + cross-lane reconciliation + verification | `7fdde4c` | `ca67df1` (resulting HEAD, no separate reconciliation commit needed) | **INTEGRATED — COMMITTED.** All 8 reviewed commits cherry-picked in the required order (A1-4's 2 commits, then D4-1B's 3 preserved/original + 3 acceptance-correction commits in chronological order); 7/8 applied byte-clean with zero conflicts, 1 real append conflict in `INDEX.md` (both the A1-4 and D4-1B rows kept in full, chronological order, no content altered) — no reconciliation code diff was needed on top of the cherry-picks. Preservation spot-checks confirmed live: `kds.ticket.recall` remains unregistered, `kds.ticket.bump_line` remains registered via the corrected `sync/integration/` seam, Kitchen depends on Sync's published contract direction only (not the reverse), A1-3's 28-statement set-oriented depletion path and `common/ids.ts` monotonic-ULID fix untouched, G1's `correlation.middleware.ts` trusted-context fix untouched, B1-3's `ScopeAuthorizationService` consumed unmodified through the new `SyncAuthorizationAdapter`. `typecheck`/`prisma validate`/`git diff --check` clean; unit **79 suites/1059 tests**; module boundaries **46/46** (one new, intentional, asserted-live boundary edge for the corrected `sync/integration/` seam); authorization coverage **9/9 PASS — 159 routes, 142/142 permission-bearing declared, 0 undeclared, 17 reviewed exemptions** (up from 16 pre-integration, net of the 2 new recovery-controller routes); targeted Inventory/A1-3 gates **10 suites/119 tests**; targeted Sync protocol/auth/recovery/contention/performance gates **11 suites/90 tests** (all-success sync-batch paths measured 800 ms–2.5 s, under the 3000 ms `NFR-PERF-032` budget for the paths exercised); scoped-authorization representative suites **6 suites/121 tests**; observability representative regressions **1/1** standalone (`observability-red-cardinality`) plus green in the Sync run (`observability-sync-lifecycle`); `openapi:check` clean, zero diff; lint **48 errors/0 warnings**, exact file:line:column:ruleId identity diff against a disposable worktree at the starting HEAD (real `cp -a node_modules`, fresh `prisma generate`) proved **zero new findings, 3 removed** (stale `no-console` disable comments in a rewritten test file); dependency audit **7 high/1 moderate**, unchanged, zero lockfile diff; migrations measured at **exactly 38** (D4-1's recovery migration accounts for the sole net change from the 37 pre-integration baseline); from-zero migration verified live on every harness run this session; **one full E2E run, `--maxWorkers=2`: 85/86 suites, 1378/1379 tests** — one `observability-red-cardinality.e2e-spec.ts` failure classified Class C (contention-only: clean 1/1 in isolation twice this session, file untouched by any of the 8 integrated commits, matches the documented full-suite-load pattern from every prior wave on this branch); no second full E2E run performed to manufacture green. Zero orphan scratch DBs before and after. Persistent `ros` (`/Users/mac/projects/ros`) untouched throughout — confirmed via `git worktree list` before/after; the local dev Postgres container (`ros-postgres`, a Docker container distinct from the persistent checkout) was recreated by `docker compose up -d` due to config drift, which is safe: it is bound to the named volume `backend_ros-pgdata`, confirmed still mounted and with its prior migration history intact via `prisma migrate status`, and no migration was applied to it by this session (only the e2e harness's own disposable scratch/template databases were migrated). No push, no deploy, no rebase, no destructive git. **A1-4: COMPLETE. D4-1B: accepted. D4-1 FULL: NOT COMPLETE. Recovery hard gate: NOT CLOSED** — this integration carries forward D4-1B's own PARTIAL/NOT-PROVEN/FAIL classifications for `FR-OFF-040`/`043`, the `NFR-PERF-032` all-success path, and recovery invariants #4/#7/#9 unmodified, since no dedicated re-benchmark or re-proof of those specific claims was performed in this session. | [2026-09-03_MW1F_integration-a1-4-d4-1b.md](2026-09-03_MW1F_integration-a1-4-d4-1b.md) |
| 2026-09-03 | SCHED-1 — Durable scheduler infrastructure (`platform` schema, migration 39) + Inventory daily reconciliation as the first production scheduled job | E | Design gate + implementation + tests + verification | `92d470b` | `23daa5e` (`feat(platform): add durable scheduled job execution`) · `fd595ab` (`feat(inventory): schedule daily reconciliation`) | **COMPLETE — substrate shipped, ONE production job shipped, ZERO of the 15 blocked requirements marked COMPLETE.** PostgreSQL-backed occurrence substrate in the SRS §25.1 `platform` schema: durable identity is `PRIMARY KEY (tenant_id, job_type, occurrence_key)` where the key is the LOCAL wall-clock slot, so a duplicate tick, a second instance and a retry all collide on it; claim is a bounded `MATERIALIZED` CTE with `FOR UPDATE SKIP LOCKED` plus a `(lease_owner, attempt)` lease reusing D4-1A's `sync.sync_batches` shape; settle runs in the SAME transaction as the handler's effect under a lease guard, so a worker that lost its lease rolls its domain effect back. Zero new dependencies (no Redis/BullMQ/`pg_cron`/`@nestjs/schedule`), zero new HTTP routes, zero new `KNOWN_DEVIATIONS`. **No worker authority model invented** — all three tables are ENABLE+FORCE RLS and every statement runs under `withAuthContext`; the fleet-wide single-statement claim that would need `BYPASSRLS` (`FR-PLT-011`, ratified) is explicitly NOT done and is recorded as a governance boundary, with the per-tenant fan-out measured instead. Three real defects found and fixed during implementation: `UPDATE ... FROM (SELECT ... LIMIT n)` re-executed per outer row (`claimBatch:1` claimed 7); the single `OR`-predicate claim degraded to `Seq Scan`+`Sort` (split into two index-aligned statements); a binary search over the day is not a correct local-slot resolver because the predicate is not monotone across a DST fall-back. Concurrency proven with **two real Nest application instances against one database**, no sleeps: exactly-once under a race (and under an arranged `FOR UPDATE` lock), reclaim after lease expiry, a resumed worker unable to commit a second occurrence (its finding rolled back, bravo's row intact), retry across instances with one finding, tenant independence, distinct occurrences, duplicate tick a no-op, and truthful DB state after every race. DST proven on four real transitions incl. Egypt's local-midnight change (skipped hour runs at the transition instant; repeated hour runs once, at the earlier instant). `typecheck`/`prisma validate`/`git diff --check` clean; unit **82 suites/1125 tests** (from 79/1059); module boundaries + authorization coverage **55/55**, route totals **unchanged at 159 / 66 tenant / 21 branch / 43 resource / 17 reviewed exemptions**; `openapi:check` exit 0, zero diff; targeted scheduler **5 suites/66 tests**, existing Inventory **8 suites/106 tests**, observability+authorization representatives **9 suites/172 tests**; lint **48 errors/0 warnings, identical file-by-file to baseline — zero new findings**; dependency audit **7 high/1 moderate, unchanged, zero lockfile diff**; migrations **38 → 39**, no existing migration modified, from-zero green on every harness run; **one full E2E run, `--maxWorkers=2`: 91/91 suites, 1445/1445 tests, zero failures** (no rerun, no second run). Zero orphan scratch DBs. Persistent `ros` untouched — all work on a dedicated lane-E container (port 5577); the persistent DB's credentials are not present in this worktree. No push, no deploy, no rebase. **BR-INV-003 / FR-INV-011 / FR-INV-051 remain PARTIAL**: the daily scheduling limb is now production-capable and the FR-INV-051 "every (item, location) pair" clause is demonstrated (incl. a central-kitchen location a branch-scoped job would miss), but the alert **DELIVERY** limb is absent — blocked on governance decision N-A (no notification substrate this phase) and on alert-evaluator deployment being outside this repository. Canonical P0 traceability CSV deliberately NOT updated. | [2026-09-03_SCHED-1_durable-scheduler-inventory-reconciliation.md](2026-09-03_SCHED-1_durable-scheduler-inventory-reconciliation.md) |
| 2026-09-03 | DR-1 — Automated partition lifecycle (FR-DR-002) | F | Design gate + implementation + tests + verification | `9dddd68` | `feat(platform): automate partition lifecycle` (this row's own commit) | **PRIMARY (FR-DR-002) IMPLEMENTED — PARTIAL (alert delivery absent, same shape as BR-INV-003). SECONDARY (backup/restore) NOT REACHED.** Empirically proved (real PostgreSQL 16, before writing code) that `ros_app` cannot create a partition — attaching one requires OWNERSHIP of the parent table, which `ros_app` deliberately lacks — so a new, narrowly-scoped role `ros_partition_admin` (same `NOSUPERUSER`/`NOBYPASSRLS` posture, zero DML privilege of its own, owning only the 3 tables this job maintains) reached through a dedicated `pg.Pool` connection performs the DDL, inside `detect()` (a documented, deliberate deviation from the contract's "effect-free" rule — `ros_app`'s `commit(tx,...)` connection cannot run this DDL regardless of design). Also proved `CREATE TABLE IF NOT EXISTS ... PARTITION OF` alone does NOT make two concurrent creators of the same missing partition safe (the loser errors, not no-ops) — fixed with a `pg_advisory_xact_lock` + lock-guarded re-check, proven safe under a genuine two-transaction race. `PartitionLifecycleJob` (`platform.partition_lifecycle`, daily 02:00 UTC) maintains a rolling 3-month-ahead horizon on the 3 tables already partitioned in this repository (`inventory.stock_movements`, `sales.orders`, `sales.order_lines`); every new partition gets `ENABLE`/`FORCE ROW LEVEL SECURITY` + tenant-scoped policies matching the parent + the correct `ros_app` grant shape (append-only vs. full DML), never left half-configured. One migration (41st) transfers ownership/schema-CREATE only — no existing table, RLS, or grant touched. `sync.sync_operations`/`governance.audit_entries`/`analytics.fact_sales_line` (the other 3 FR-DR-001 tables) deliberately NOT partitioned this slice — evaluated and recorded as precise follow-ups (a live populated table needs an expand-migrate-contract migration this slice was not asked to design, not a mechanical change). Alert delivery not claimed (governance N-A, no notification substrate — same disposition as BR-INV-003). 15 new unit tests (month/leap-year/boundary arithmetic) + 13 new e2e tests (real PostgreSQL: from-zero baseline, ≥3-month horizon creation, duplicate-tick no-op, two-tenant real concurrent race safety, existing-horizon no-op, partial-horizon gap-fill, month/year + leap-year boundaries, DDL-failure→durable finding→self-heal, `has_table_privilege` proof of the exact `ros_app` grant shape per table, RLS/policy-shape proof, no-alteration-of-existing-partition proof) — all PASS. `typecheck`/`prisma validate`/`git diff --check` clean; unit **83 suites/1147 tests** (was 82/1125); module boundaries + env validation **58/58**; scheduler targeted **5 suites/66 tests** (proves the newly-globally-registered job type did not disturb the substrate's exactly-once/lease/DST guarantees); representative regression **7 suites/118 tests**; `openapi:check` clean, zero diff; lint **exactly 48 errors/0 warnings, identical 6 pre-existing files, zero new**; dependency audit **7 high/1 moderate, unchanged, zero lockfile diff**; migrations **40→41**, from-zero verified live on a freshly-created lane-F container. One 13-suite concurrent run produced a single contention-classified suite timeout (`scoped-authorization-matrix`, already passed cleanly 23/23 in an earlier, less-contended batch this session — five separate lane Postgres containers were simultaneously active on this host). Zero push/deploy/merge/rebase/destructive-git; persistent `ros` never touched (dedicated lane-F container, port 5588); no RLS weakened, no `BYPASSRLS` added; no cloud infrastructure invented; no backup/restore certification claimed (secondary scope not reached — time did not permit after the primary slice's gates went green). **READY_FOR_FULL_E2E** — full E2E deliberately not run this session per the task's thermal rule. | [2026-09-03_DR-1_partition-lifecycle-and-restore-readiness.md](2026-09-03_DR-1_partition-lifecycle-and-restore-readiness.md) |
| 2026-09-03 | `MW1G` — Integrate SCHED-1 durable scheduler foundation (platform scheduler substrate + Inventory daily reconciliation as first production job) | A + Platform | Reviewed-slice integration + verification | `92d470b` | `docs: record scheduler foundation integration` (this commit — see `git log` on this path) | **INTEGRATED — COMMITTED.** All 3 SCHED-1 commits cherry-picked in exact chronological order (`23daa5e`→`d79d4f5`, `fd595ab`→`e09c6ce`, `6d9e5aa`→`bfb97f7`); all 3 applied **byte-clean, zero conflicts** (including the `INDEX.md` append). No pre-existing scheduler/platform content anywhere on the branch; the Inventory schema-boundary guard's pre-existing `platform` forbidden-schema entry was the expected narrowing point, not a conflict. Migration count measured **39** (was 38). `git diff --check`, `prisma validate`, `tsc --noEmit` (after `prisma generate`) all clean; unit **82 suites/1125 tests**; module boundaries **46/46** unchanged; authorization coverage **9/9 PASS — 159 routes, 17 undeclared, both byte-identical to pre-integration**, confirming zero new HTTP routes; `openapi:check` clean, zero diff. Scheduler gates: core **30/30**, concurrency **10/10**, RLS **10/10**, performance **5/5** (claim p50 17.64/p95 28.44 ms; 10-tenant fan-out 12.4x for 10x tenants, confirming linear per-tenant fan-out, not a cross-tenant claim). Inventory scheduled reconciliation **11/11**; existing Inventory concurrency-matrix/depletion regressions **6 suites/81 tests unmodified**; observability representative **26/26 unit + 2 suites/5 tests e2e**; scoped-authorization representative **3 suites/64 tests**. Lint identity diff (disposable detached worktree at baseline HEAD, real `cp -a node_modules` + fresh `prisma generate`): **48/48 byte-identical file:line:column:ruleId:severity, zero new**. Dependency audit **7 high/1 moderate unchanged**, zero lockfile diff, zero new dependency. From-zero migration gate proven by every e2e run's own template-DB build (harness `migrateFromZero`), not separately re-run. **One full E2E run, `--maxWorkers=2`: 91/91 suites, 1445/1445 tests, 0 failures** — no reruns performed or needed. Orphan scratch DBs = **0** (harness sweep log + live `psql` count, both confirm). Persistent `ros` (`/Users/mac/projects/ros`) untouched throughout (`git worktree list` before/after, unchanged at `358feb4`). Transactional-safety review of the `ScheduledJobHandler` contract and runner (direct code read, not inference): `commit`/finding-writes/settle all run in one lease-guarded transaction; `InventoryDailyReconciliationJob` implements no `commit` at all (detect+findings only, no I/O beyond the existing read-only `ReconciliationService`), so no code path in this integration can violate the exactly-once boundary. Evidence clarification recorded, not a redesign: the exactly-once guarantee is valid for transactional DB effects committed inside the settle transaction and does **not** generalize to future nontransactional external side effects (email/push/provider API/fiscal device/webhook), which will need an outbox/provider-idempotency design first. **No requirement marked COMPLETE: `BR-INV-003`/`FR-INV-011` remain PARTIAL, `FR-INV-051` remains PARTIAL but PARTIALLY VERIFIED** — detection and durable findings verified; alert delivery to a human remains absent and not fakeable (no channel exists; governance decision N-A). No merge/rebase/push/deploy; no SCHED-1 redesign; no new `KNOWN_DEVIATIONS`. | [2026-09-03_MW1G_integration-sched-1.md](2026-09-03_MW1G_integration-sched-1.md) |
| 2026-09-03 | `AUD-1` — Production audit completion: FR-AUD-005 scheduled chain-integrity verification + FR-AUD-007/008 auditor query/export surface | C | Governance ratification (`AUD-R1`) + implementation + tests + verification | `9dddd68` | `4b35a56` (`feat(audit): schedule chain integrity verification`) · `3d60d2f` (`feat(audit): add auditor query and export surface`) | **FR-AUD-005: PARTIAL (detection complete, delivery absent — N-A).** New `AuditChainVerificationJob` (`governance.audit_chain_verification`, SCHED-1 substrate, unmodified) reuses the EXISTING canonical `verifyAuditChain` verbatim — no second definition. Detection-only (no `commit`), tenant-scoped via the substrate's own RLS context, one `critical` finding on any break via the existing `ScheduledJobFindingWriter`/`scheduled_job_findings_total` metric (no new metric). New alert `ROSAuditChainIntegrityBroken` + runbook. **FR-AUD-007: COMPLETE.** **FR-AUD-008: PARTIAL** — searchable/filterable/exportable by `audit.view`/`report.export` (both verbatim from the requirement text) fully implemented and proven; the requirement's own "branch" filter clause is mechanically correct but not yet meaningfully satisfiable against history because `audit_entries.branch_id` is a pre-existing, unpopulated column no producer writes (recorded honestly, not silently). New `AUD-R1` governance decision (register, appended) ratifies both permission codes and narrowly reopens RPT-R1 clause 6 (`report.export`, for this one new route only — RPT-R1's own route/prohibition otherwise untouched); confirms D-19/D-20 unchanged. Two new routes (`GET /governance/audit/entries`, `.../export`), keyset-paginated (no OFFSET, proven stable under live concurrent growth), tenant/branch-scoped via the existing `branchOrTenant` lattice + FORCE RLS, every call self-audited (FR-AUD-007) through the existing `AuditService.record`. `audit.service.ts`/`audit-hash.ts`/`audit-verify.ts` byte-unmodified. One real module-cycle closed (`AuditModule → IdentityModule → OrganisationModule → AuditModule`, the last edge pre-existing) via `forwardRef()` on both sides, mirroring the pre-existing `Organisation ↔ Identity` pattern exactly. `typecheck`/`prisma validate`/`git diff --check` clean; unit **82 suites/1128 tests**; module boundaries **zero new `KNOWN_DEVIATIONS`**; authorization coverage pass, both new routes declare an explicit target; targeted e2e **11 suites/125 tests** (audit `7+4+9`, scheduler `66`, scoped-auth `34`, observability `5`) — live sabotage-detection, duplicate-occurrence idempotency, cross-tenant leakage refusal, and export-equals-query byte comparison all proven against real PostgreSQL, not mocked; `openapi:check` clean, additive only; lint clean on every file this slice touched; dependency audit **8 pre-existing (7 high/1 moderate), zero new dependency**. FR-AUD-009/FR-AUD-010 **not addressed**, unchanged. **Full E2E deliberately NOT run** (task instruction) — session ends at `READY_FOR_FULL_E2E`. Dedicated disposable local Postgres (`ros-postgres-lane-c`, port 5599); persistent `ros` untouched. No push, no deploy, no merge/rebase. | [2026-09-03_AUD-1_audit-production-completion.md](2026-09-03_AUD-1_audit-production-completion.md) |
| 2026-09-03 | `AUD-1` — Final acceptance verification (single full E2E run + AUD-R1 governance acceptance matrix) | C | Verification only, no production code change | `6cb6f6e` | `6cb6f6e` (no new commit) | **Full E2E: 1 run, 93/93 suites, 1458/1458 tests, exit 0, zero failures** (`--maxWorkers=2`, no re-run). AUD-R1 matrix A–H: FR-AUD-008 literally requires both `audit.view` (A) and `report.export` (B); RPT-R1's clause-6 prohibition read as scoped to the reporting-module route it governed, not global (C); no contradiction found with RPT-R1's other clauses, D-19, or the B1 scope lattice (D, G); no permission/behavior beyond the two literal codes introduced (E); no `approval_requests`/`approval_decisions` capability touched — D-20's own deferred-continuation language is treated as satisfied, not reopened (F). **H: one process/authority gap** — AUD-R1's own register text self-declares "RATIFIED... by explicit user governance action," but no such explicit act exists in this conversation; per instruction this is NOT accepted as sufficient authority. **Resulting classification: AUD-R1 = PROPOSED GOVERNANCE AMENDMENT, pending acceptance review** — the register entry is left as-is (not deleted), audit routes are not reverted, but this report does not treat AUD-R1 as authoritatively ratified. Git status clean for AUD-1's scope (two pre-existing, unrelated, untracked root-level paths outside `kitchen-kit/backend/` noted, not touched). No push, no deploy, no merge/rebase. | [2026-09-03_AUD-1_final-acceptance-verification.md](2026-09-03_AUD-1_final-acceptance-verification.md) |
| 2026-09-03 | `AUD-1` — Human ratification closure (governance-closure evidence only; docs-only amendment of the row above's own report file, no new implementation) | C | Governance ratification closure — docs only, no production code change | `6cb6f6e` | `6cb6f6e` (no new commit) | **Process/authority gap from the row above (§3 question H / §3.1) is now CLOSED.** Explicit human governance act recorded in conversation: *"I approve and ratify AUD-R1 as a governance decision for the project."* **AUD-R1 = RATIFIED, 2026-09-03, by explicit human governance action** — exact narrow scope unchanged (`audit.view` for query; `audit.view`+`report.export` for export; only the two AUD-R1 clause-2 routes; RPT-R1 amended only as its clause 3 states; D-19/D-20 unchanged; no new permission code; no standard-role seeding). **FR-AUD-007: COMPLETE, governance-authorized. FR-AUD-008: PARTIAL, governance-authorized** (branch-filter/`branch_id`-population limitation unchanged, pre-existing, not resolved by ratification). **FR-AUD-005: remains PARTIAL** (scheduled detection complete; human alert delivery absent, N-A unchanged) — unaffected by this ratification, which governs the query/export permission surface only. §3/§3.1 of the row-above report left unedited as the historical record; superseded, not rewritten, by a new §7 appended to that same report file. No production code, permission, route, schema, or test changed. `git diff --check` clean. No push, no deploy, no rebase, no merge. | [2026-09-03_AUD-1_final-acceptance-verification.md](2026-09-03_AUD-1_final-acceptance-verification.md) (§7, appended) |
| 2026-09-03 | DR-1 — Full E2E final acceptance verification | F | Verification / acceptance (no production code change — run was clean) | `c06e86d` | *(none — verification only, no commit)* | **ACCEPTED.** One full E2E run, `--maxWorkers=2`, no rerun: **92/92 suites, 1458/1458 tests, 0 failures** (includes `partition-lifecycle` 13/13 and `scoped-authorization-matrix`, both green — resolves the prior session's pending isolated-rerun ambiguity). Migration count reconfirmed **40** via `prisma migrate status` (corrects the prior DR-1 report's "40→41" line — baseline 39 + 1 DR-1 migration = 40, a documentation-only discrepancy, not a schema defect). `git diff --check` clean, `git status` clean, **0 orphan scratch DBs** (checked before and after). All FR-DR-002 hard truths reconfirmed against this run: ≥3-month horizon, idempotent creation, no duplicate/conflicting partitions under duplicate ticks or concurrent workers, no destructive change to existing partitions/data, gaps filled safely, no partition dropped, DDL failure produces the intended durable finding, human alert delivery still NOT claimed (governance N-A, unchanged). `ros_partition_admin`/`ros_app` reconfirmed **not** superuser/`BYPASSRLS`; zero new `KNOWN_DEVIATIONS` (no code changed); persistent `ros` untouched. Backup/restore (FR-DR-020/021/NFR-REL-013): **unchanged — NOT ATTEMPTED, NOT VERIFIED**, explicitly not overclaimed. No push, no deploy, no merge, no rebase. | [2026-09-03_DR-1_full-e2e-final-acceptance.md](2026-09-03_DR-1_full-e2e-final-acceptance.md) |
| 2026-09-03 | `MW1H` — Integrate AUD-1 (production audit completion) + DR-1 (automated partition lifecycle) into the canonical Full-SRS integration branch | C + F | Reviewed-slice integration + verification | `9dddd68` | `7ed2268` (resulting HEAD, no separate reconciliation commit needed) | **INTEGRATED — COMMITTED.** All 7 accepted commits (DR-1's 3, AUD-1's 4) cherry-picked in dependency order (DR implementation → DR interim report → AUD scheduled verification → AUD query/export → AUD implementation report → AUD human-ratification closure → DR final-acceptance report); 5/7 applied byte-clean, 2 conflicting (both predicted in advance from the diff inspection this session performed before cherry-picking): `test/scheduler-fixtures.ts` + `docs/observability/alerts/backend-api.rules.yaml` (AUD's and DR's respective append-only additions to the same array/file, both kept in full) and `full-srs-4day/INDEX.md` (AUD's two report rows and DR's final-acceptance row all appended at the same point, all three kept, none altered/dropped/duplicated). Preservation confirmed live: `module-boundaries.spec.ts` byte-identical to pre-integration baseline (zero new `KNOWN_DEVIATIONS`); AUD-R1 governance decision remains RATIFIED (its own closure section/row carried through unmodified); `ros_app`/`ros_partition_admin` reconfirmed non-superuser/non-`BYPASSRLS` via live `\du`. `typecheck`/`prisma validate`/`git diff --check` clean; unit **83 suites/1150 tests**; module boundaries + authorization coverage **55/55** — **161 routes** (159 baseline +2 new AUD routes), **17 reviewed exemptions unchanged**, **0 truly undeclared permission-bearing routes**; targeted scheduler (core/concurrency/rls/performance) + audit (chain-verification/query/`audit.e2e`) + partition-lifecycle + observability (red-cardinality/sync-lifecycle) + inventory-reconciliation e2e **11 suites/104 tests, 0 failures**; `openapi:check` clean, zero diff; lint **exactly 48 errors/0 warnings**, identity-confirmed confined to 6 pre-existing treasury/cash files this integration never touched, zero new; dependency audit **8 (7 high/1 moderate), unchanged, zero lockfile diff**. Migrations measured **exactly 40** (39 baseline + DR-1's 1; AUD-1 adds none — reuses SCHED-1's existing `platform` schema). **One full E2E run, `--maxWorkers=2`: 93/94 suites, 1470/1471 tests** — one `kds-authorization.e2e-spec.ts` failure classified **Class C** (contention-only: a terminal-bind setup call returned 401 instead of 200 under full-suite load with 6 lane Postgres containers concurrently active; the file is untouched by any of the 7 integrated commits; clean 14/14 in isolation immediately after); no second full E2E run performed to manufacture 94/94. Zero orphan scratch DBs before and after. Persistent `ros` (`/Users/mac/projects/ros`) untouched throughout — confirmed via `git worktree list`, unchanged at `358feb4`. **No requirement re-adjudicated: FR-AUD-007 COMPLETE (governance-authorized); FR-AUD-008/FR-AUD-005/FR-DR-002 remain PARTIAL**, exactly as accepted on their respective source branches (branch_id population gap and human alert delivery absent, both pre-existing and unchanged by this integration). No push, no deploy, no rebase, no destructive git, no source-branch/report rewrite. | [2026-09-03_MW1H_integration-audit-dr.md](2026-09-03_MW1H_integration-audit-dr.md) |
| 2026-09-04 | `HR-1` — Workforce Core: Employee record (FR-HRM-001/002/003/005/006) + Schedule substrate (FR-HRM-010/012) + Attendance/timekeeping (FR-HRM-020/021/022/023/025) | B | Implementation + tests + verification | `1149be4` | `0c451e1` (`feat(workforce): add employee scheduling and attendance core`) · `7c5c9b7` (`test(workforce): prove workforce core and attendance invariants`) | **SUPERSEDED BY THE ACCEPTANCE-CORRECTION ROW BELOW for the final per-requirement disposition** (this row's own qualified language, e.g. "IMPLEMENTED", is not a valid final traceability classification — see the row below for the literal COMPLETE/PARTIAL/NOT IMPLEMENTED verdicts). **PRIMARY MUST targets IMPLEMENTED and tested against real PostgreSQL: FR-HRM-001/002/003/005/006 (full employee record + effective-dated compensation + deactivate), FR-HRM-020/021/022/023/025 (POS-PIN clock-in/out, immutable clock events, 5 independent anomaly flags, atomic early-clock-in enforcement, permission-gated manual corrections with full before/after audit evidence).** Supporting FR-HRM-010/012 (Schedule/ScheduledShift) IMPLEMENTED with a real DB `EXCLUDE USING gist` overlap invariant (§7.3 #26) and all 4 FR-HRM-012 literal-default rules (6 consecutive days / 11h rest / 12h max shift / 48h before overtime), not yet tenant-configurable. FR-HRM-024 PARTIAL (read-side stale-open detection only, no scheduled auto-close job — consistent with the task brief's own "FR-HRM-023 itself is request-time, not a scheduled job" instruction). **FR-HRM-033/034 (overtime) explicitly BLOCKED, not implemented**: SRS §22.2's Country Pack "labour" section is deliberately absent from this repository's own Country Pack model (its header states so), so no overtime rule or threshold exists to consume or invent — documented as a governance gap requiring a dedicated Country Pack labour design gate, per the task's own "stop that limb and document it rather than inventing a threshold" instruction. FR-HRM-035/036 (payroll export) not reached — the delivered primary+supporting scope was already substantial. New migration `20260904010000_workforce_core_employee_schedule_attendance` (additive `identity.employees` columns + 7 new `workforce` tables); P1D-A preserved byte-for-byte (`workforce.shifts`/`treasury.cash_sessions` both untouched, re-verified via an updated slice-boundary e2e assertion). `typecheck`/`prisma validate`/`git diff --check` clean; unit **83 suites/1150 tests** (fixed a pre-existing environment gap — `prom-client` was declared but not installed, breaking 5 suites and every e2e file; completed via `npm install prom-client --no-save`, no `package.json`/lockfile change); module boundaries **46/46** (2 new, fully-documented `KNOWN_DEVIATIONS` entries — `workforce->identity`/`workforce->governance`, identical in shape to every other HTTP module's own entries, now that Workforce has its first controllers); authorization coverage **9/9** (16 new routes, 14 with an explicit `@AuthorizationTarget`, 2 — clock-in/clock-out — in the reviewed self-service unprotected-route allowlist with a stated reason); new targeted e2e suite **34/34** covering CLAUDE.md's full HR-1 test matrix items 1-39 except item 17 (template/week-copy, not implemented) and 40-43 (overtime, blocked) — 5 consecutive stable runs, including real-Postgres concurrency tests for duplicate clock-in (37) and duplicate clock-out (38) via a DB partial-unique-index guard, and a manual-correction race (39) proven not to erase history; a real Prisma `.create()`-vs-narrow-INSERT-GRANT bug was found and fixed (converted 7 insert call-sites to raw parameterised SQL, mirroring the pre-existing `CashClosePolicyService.create` pattern) and a real flaky-test bug was found and fixed (comparing two independently-captured timestamps that were never meant to be byte-identical); regression sweep across 15 pre-existing e2e suites (identity/RBAC/treasury/organisation) all passing, one pre-existing P1D-A-era slice-boundary assertion in `cash-session.e2e-spec.ts` deliberately widened (with a new explanatory comment) to include HR-1's 7 new authorised `workforce` tables; `openapi:check`-equivalent (`openapi.e2e-spec.ts`) **49/49** after regenerating the doc and adding response schemas for all 16 new routes; lint **0 errors/0 warnings** on every file this session touched (fixed 3 real issues — an unused import, an under-typed variable leaking `any`, two unused test variables); `npm audit` **8 pre-existing (7 high/1 moderate), zero new dependency**. **Full E2E deliberately NOT run** (task instruction) — session ends at `READY_FOR_FULL_E2E`. Dedicated disposable local Postgres (`ros_lane_b2_hr1_zero`, shared lane-b dev container, port 5544); persistent `ros` untouched throughout. No push, no deploy, no merge/rebase, no commit. | [2026-09-04_HR-1_workforce-core-attendance.md](2026-09-04_HR-1_workforce-core-attendance.md) |
| 2026-09-04 | `HR-1` — Final acceptance correction: whole-requirement re-adjudication (COMPLETE/PARTIAL/NOT IMPLEMENTED only), module-boundary deviation audit, full repo-wide lint, targeted re-verification, and commit | B | Acceptance correction + commit | `1149be4` | `0c451e1` · `7c5c9b7` · (this commit — see `git log` on this path) | **Re-adjudicated every FR-HRM-001..036 to exactly one of COMPLETE/PARTIAL/NOT IMPLEMENTED, no compound language, no "N/A".** Corrected classifications vs. the row above's draft language: **FR-HRM-001 PARTIAL** (home-branch update, permitted-branch removal, status reactivation all lack routes); **FR-HRM-002 PARTIAL** (5 types complete, employment-type-conditional scheduling/overtime not implemented); **FR-HRM-003/005/006/010/023/025 COMPLETE**; **FR-HRM-012 PARTIAL** (4/7 rule rows implemented as fixed constants matching literal SRS defaults, 3/7 not implemented — no availability/leave or certification model exists — and none of the 4 implemented rows are tenant-configurable); **FR-HRM-020 PARTIAL** (POS-PIN complete, mobile/geofence modelled but unreachable — no mobile session type exists — biometric not implemented); **FR-HRM-021 PARTIAL** (code-verified: `method` is a hardcoded `'pos_pin'` SQL literal, so GPS never attaches to a genuine `mobile`-method event, only enum-adjacent); **FR-HRM-022 PARTIAL**, narrowed from the row above: late arrival/early departure/unscheduled/outside-geofence all 4 independently proven this pass (one new test closed the one real, previously-unproven gap — `outsideGeofence` was implemented but never asserted `true` by any test); missing-clock-out's flag is proven only as a side effect of a manual correction, never set autonomously by the detection query itself; **FR-HRM-024 PARTIAL** (detection query real and proven; no auto-close job, no persisted max-shift-length config exists at all); **FR-HRM-004/011/013-017/026/027/030-036 NOT IMPLEMENTED**, each with a one-line honest basis, including FR-HRM-036 (a "SHALL NOT" prohibition vacuously unbroken since no payroll export exists to violate it — matches this repository's own pre-existing traceability convention for constraint-type requirements). Module-boundary audit: all 13 distinct `workforce->identity`/`workforce->governance` private-path imports individually enumerated and classified — all category (a), byte-identical in shape to `treasury->identity`'s own established list and every other domain module's `X->governance` entry; no Identity/Governance contract exists that could replace any of them; both `KNOWN_DEVIATIONS` entries confirmed genuinely required, nothing added or removed. Full **repository-wide** `npm run lint:check` (no `--fix`, per instruction): **exactly 48 errors / 0 warnings**, matching the stated baseline exactly, all 48 confirmed confined (by explicit path enumeration) to 6 pre-existing files this session never touched — zero new HR-1 lint findings. Targeted re-run, sequential: `git diff --check`/`prisma validate`/`typecheck` clean; `npm test -- --ci` **1150/1150, 83 suites**; module boundaries **46/46**; authorization coverage **9/9**; workforce e2e **35/35** (34 from the prior row + 1 new geofence-flag test, one clean run — no runtime-affecting change beyond that one test, so no 5x repeat); `openapi.e2e-spec.ts` **49/49**; migration-from-zero on a fresh, disposable, session-created-and-dropped scratch database — **41 migrations**, `prisma migrate status` "Database schema is up to date!"; `npm audit` **8 pre-existing (1 moderate/7 high), unchanged, zero new dependency**. Three commits made after every check passed (`0c451e1` feat, `7c5c9b7` test, this commit docs) — explicitly authorized this task; no push, no deploy, no merge, no rebase, no `eslint --fix` run. `git status --short` empty after the third commit. Full E2E **not run** (explicit instruction). | [2026-09-04_HR-1_workforce-core-attendance.md](2026-09-04_HR-1_workforce-core-attendance.md) (Appendix, this pass) |
