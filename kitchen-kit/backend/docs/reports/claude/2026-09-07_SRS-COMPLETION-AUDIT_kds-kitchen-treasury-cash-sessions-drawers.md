# SRS Completion Audit — KDS/Kitchen (Routing, Fire, Amendments) & Treasury (Cash Sessions, Drawers, Day Close)

**Report type:** Backend-only SRS completion audit (read-only investigation; no files modified).

**Authority statement:** This report is non-authoritative evidence. The SRS
(`ROS_SRS_v1.0.pdf`) and the ratified entries in
`docs/governance/GOVERNANCE_DECISION_REGISTER.md` remain the sole authority
for requirements, permission semantics, and architecture decisions. Nothing
in this document creates, amends, or ratifies governance. Every requirement
classification below was re-verified against current source in this session
(via two parallel deep-read investigations of the full module file sets) —
prior reports were used only as evidence to be re-checked, never taken on
trust.

**Date:** 2026-09-07

**HEAD:** `bf91d6671a1c39fd0b3c0e8b4ba6c66aa8132ed8`

**Branch:** `full-srs/lane-d4-reporting-demo` (unintegrated into `origin/main`)

**Working tree summary (start and end of this task — no files modified by this task except this report and INDEX.md):**
```
?? docs/reports/claude/2026-09-05_PROD-DEMO-SMOKE_production-smoke-test-plan.md
?? docs/reports/claude/2026-09-06_DEMO-GOLDEN-PATH-AUDIT_consolidated-27-step-audit.md
?? docs/reports/claude/2026-09-06_GOLDEN-PATH-AUDIT_pos-branch-load-and-catalogue-sellable.md
?? docs/reports/claude/2026-09-06_GOLDEN-PATH-AUDIT_pos-create-fire-payment-receipt-refund.md
```
(all pre-existing/untracked before this task; untouched by this task)

**Task identifier:** SRS-COMPLETION-AUDIT — KDS/Kitchen + Treasury (Cash Sessions/Drawers) section

**Scope discipline:** Read-only. No code, schema, test, or config file was
changed. Investigation performed via full-file reads of every source file in
`src/modules/kitchen/**` and `src/modules/treasury/**`, the relevant e2e/unit
spec files, `prisma/schema.prisma` model definitions, targeted sections of
`GOVERNANCE_DECISION_REGISTER.md`, `ROS_SRS_v1.0.pdf` chapters 9 and 16, the
three named recent commits (`bf91d66`, `4d7a387`, and the drawer-authorization
proof commit), and the four named prior reports (re-verified against current
code, not trusted).

---

## 1. KDS / Kitchen — Stations & Routing (SRS §9.2)

| Req ID | Section | Title | Classification | Owner | Controller/API | Service/Domain | Prisma | Transactionality | AuthZ | Audit/Event | Tests | Evidence | Remaining work | Severity |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-KDS-001 [M] | §9.2 | Station definition per branch (name, colour, capacity) | COMPLETE_BACKEND | Organisation module (Kitchen only reads it) | `POST/GET/PATCH /org/branches/:branchId/stations`, `GET /org/stations/:stationId` (`organisation.controller.ts`) | `StationsService` (Organisation) | `Station` model `prisma/schema.prisma:864-891` (`name` VarChar64, `displayColour` VarChar9 line 871 "FR-KDS-001 [M]", `capacityConfig` Json line 868, branch FK, unique `(branchId,id)`/`(branchId,name)`) | N/A (simple CRUD) | `settings.branch.manage` (create/update), `settings.branch.read` (list/get), branch-scoped `@AuthorizationTarget` | Standard audit on write | Confirmed live in `2026-09-06_DEMO-OPS-HOTFIX-2` report (route inventory) and `2026-09-06_DEMO-OPS-HOTFIX-3` (branch-context fix) | `organisation.controller.ts`; Kitchen reads via `organisation/contract` (`ROUTING_CONFIG_QUERY`, `KDS_BRANCH_CONFIG_QUERY`, `STATION_DISPLAY_BINDING_QUERY`, `ORG_STATION_TARGET_RESOLVER` — `kitchen.controller.ts:53,60`) | None identified | N/A |
| FR-KDS-010 [M] | §9.2 | Route each order line to one/more stations, 5-tier precedence (line override → modifier rule → menu item station → category default → branch fallback) | COMPLETE_BACKEND | Kitchen | Invoked only internally from the Fire path (no direct HTTP route — resolution happens synchronously inside `POST` Fire) | `RoutingResolverService.resolve` — `src/modules/kitchen/routing/routing-resolver.service.ts:61-172`, exactly the 5-tier precedence with category-conflict detection (`resolveCategoryTier`, 123-158) and `RoutingNoDestinationError` (line 117) | `StationRoutingRule` (`schema.prisma:970-998`), `BranchKdsConfig.fallbackStationId` (`1007-1031`), `OrderLineStationOverride` (Sales-owned, `2201`) | Runs inside the Fire transaction (see §4 below), no own `$transaction` | N/A (internal, not HTTP) | N/A (not itself an audited action; its output IS the audited Ticket write) | `src/modules/kitchen/routing/routing-resolver.service.spec.ts` (20 `it()` cases, tiers 1-5, multi-station union, conflict, determinism); `test/sales-fire.e2e-spec.ts` "routing tiers through the REAL Fire producer (§19)" (tier 3, tier 5, multi-station, conflict→422+rollback, no-destination→422+rollback, sibling-line rollback) | None identified | N/A |
| FR-KDS-011 [M] | §9.2 | Single line routable to multiple stations | COMPLETE_BACKEND | Kitchen | (internal, via Fire) | `OrderLineFiredHandler` loops `resolution.stationIds` (`tickets/order-line-fired.handler.ts:81-160`), one `Ticket`+`TicketLine` per station | `TicketLine` has no `stationId` of its own — inherits from owning `Ticket` (`schema.prisma:1231-1296`) | Same Fire transaction | N/A | N/A | `test/kitchen-ticket-persistence.e2e-spec.ts` "same OrderLine may appear on two different station Tickets"; `test/sales-fire.e2e-spec.ts` "multi-station routing creates a line representation at EACH resolved station" | None identified | N/A |
| FR-KDS-012 [S] | §9.2 | Prep-Time-Aware staggered release | MISSING_BACKEND | Kitchen | none | none | `Ticket.targetReadyAt` column exists but is **always null**: `tickets/ticket-reader.types.ts:54` — "Always `null` until FR-KDS-044 `[S]` populates it — never fabricated." | N/A | N/A | N/A | none | No release-time computation exists anywhere in `src/modules/kitchen`. Column is a documented placeholder only. | DEFERRED |
| FR-KDS-013 [S] | §9.2 | Expediter (Pass) display | MISSING_BACKEND (DEFERRED_BY_GOVERNANCE) | Kitchen | none — `kitchen.controller.ts:74` docblock: "Deliberately ABSENT: `/serve` (FR-KDS-013 `[S]`, deferred)" | none | none | N/A | N/A | N/A | none | Governance register line ~6969: "`served`/Expediter behaviour (FR-KDS-013 `[S]`)... remain DEFERRED and NOT authorized" (KDS MVP Operator Lifecycle Ratification, 2026-08-30). No expediter route, no aggregate cross-station "complete" view. | DEFERRED |

---

## 2. KDS / Kitchen — Display & Interaction (SRS §9.3)

| Req ID | Section | Title | Classification | Owner | Controller/API | Service/Domain | Prisma | Transactionality/Concurrency | AuthZ | Audit/Event | Tests | Evidence | Remaining work | Severity |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-KDS-020 [M] | §9.3 | Ticket card display fields (order#, type, elapsed time, table/customer ref, lines w/ qty+modifiers+notes) | COMPLETE_BACKEND (data layer); rendering NOT_BACKEND_OWNED | Kitchen | `GET /kds/stations/:stationId/queue` | `TicketReaderService` (`tickets/ticket-reader.service.ts`), `TicketCardDto`/`TicketCardLineDto` (`tickets/ticket-reader.types.ts:1-64`) — elapsed time server-computed at response time (`ticket-reader.service.ts:169-172`) | `Ticket`/`TicketLine` | Read-only query | `kds.operate` + `KdsStationGuard` | N/A (GET never writes) | `test/kitchen-ticket-persistence.e2e-spec.ts` "renders a full card... from Kitchen tables only", "issues no query against sales.*/catalogue.* (proven by revoking `ros_app` SELECT)" | Backend data contract complete and self-contained (no cross-module read at request time); actual card rendering is a frontend concern | Visual rendering only | N/A |
| FR-KDS-021 [M] | §9.3 | Modifiers visually distinguished (removal vs addition) | COMPLETE_BACKEND (data layer); rendering NOT_BACKEND_OWNED | Kitchen | (part of queue read) | `ModifierKindSnapshot` enum persisted on `TicketLineModifier.kind` | `schema.prisma:1089-1095` (addition/removal/substitution) | Read-only | as above | N/A | `test/kitchen-ticket-persistence.e2e-spec.ts` "modifier kind substrate" (5 tests) | Type-safe `kind` field served; client rendering (`- no onion` / `+ extra cheese` styling) not backend's responsibility | Visual rendering only | N/A |
| FR-KDS-022 [M] | §9.3 | Colour-coding by elapsed time (neutral/amber/red/flashing red) | PARTIAL_BACKEND (config substrate) / NOT_BACKEND_OWNED (thresholds+rendering) | Kitchen | (part of queue read) | `elapsedSeconds` server-computed; `Station.displayColour` served | as FR-KDS-001 | Read-only | as above | N/A | covered incidentally by queue-read tests | No backend "target"/threshold config beyond the always-null `targetReadyAt` (see FR-KDS-012); amber/red thresholds and flashing are pure client logic with no server-side configuration surface | Add a per-station/branch configurable elapsed-time threshold if amber/red bands are meant to be tenant-configurable (currently would have to be hardcoded client-side) | P2_COMPLETENESS |
| FR-KDS-023 [M] | §9.3 | Configurable sort orders (FIFO, target completion, order-type priority, course sequence) | PARTIAL_BACKEND | Kitchen | `GET /kds/stations/:stationId/queue?sort=fifo` | `StationQueueQueryDto` (`kitchen.dto.ts:18-23`) `@IsIn(['fifo'])` — any other value 400s before the handler runs; `ticket-reader.service.ts:77` "FIFO only in this slice (design gate §17)" | n/a | Read-only | as above | N/A | queue-read tests (FIFO ordering asserted) | Only FIFO implemented. By-target-completion-time, by-order-type-priority, by-course-sequence sorts MISSING. Governance register (line ~6969) explicitly lists "sorting beyond FIFO" as DEFERRED. | DEFERRED |
| FR-KDS-024 [M] | §9.3 | Bump item / bump all | COMPLETE_BACKEND | Kitchen | `POST /kds/tickets/:ticketId/lines/:lineId/bump`, `POST /kds/tickets/:ticketId/bump-all` (`kitchen.controller.ts:362,404`) | `KdsOperationsService.bumpLine`/`bumpAllTx` (`tickets/kds-operations.service.ts:248-406`), `BUMP_ELIGIBLE_STATUSES={queued,started,ready}` (`tickets/ticket-projection.ts:65-69`) | `Ticket`/`TicketLine` write | SERIALIZABLE isolation + 3-attempt bounded retry (`KDS_SERIALIZABLE_RETRY`, `kds-operations.service.ts:40-43`) plus a separate 5-attempt CAS retry for the ticket-aggregate projection (`tickets/ticket-projection.service.ts`) | `kds.operate` + `KdsStationGuard`, per-request re-verification of `ticket.stationId === scope.stationId` from a freshly-loaded row inside the transaction (`kds-operations.service.ts:549-568`) | `TICKET_LINE_BUMPED`/`TICKET_BUMPED` audit actions, in-transaction; `ticket.bumped` domain event published (KDS-R11 permission ratification; §5.5.4 event, publisher Kitchen Ops → Sales/Analytics) | `test/kds-operator-lifecycle.e2e-spec.ts` (bump item / bump-all, 6 tests); `test/kds-concurrency.e2e-spec.ts` (12 real-Postgres concurrency tests A-L incl. a write-skew "GUARD" test proving plain READ COMMITTED would produce the anomaly SERIALIZABLE prevents) | None identified | N/A |
| FR-KDS-025 [M] | §9.3 | Recall function, default 30-min retention | COMPLETE_BACKEND, incl. KDS-R12 | Kitchen | `POST /kds/tickets/:ticketId/recall` — **Idempotency-Key REQUIRED** (`kitchen.controller.ts:445-492`, `@Idempotent()`) | `KdsOperationsService.recallTx` (`kds-operations.service.ts:423-545`) — checks `status==='bumped'`, elapsed vs `branchConfig.recallWindowSeconds`, 422 past window | `BranchKdsConfig.recallWindowSeconds` defaults **1800** (`schema.prisma:1014`, "FR-KDS-025 [M]... default 30 minutes") | Optimistic `version` CAS (`kds-operations.service.ts:493-506`); publishes `ticket.recalled` (KDS-R12, ratified 2026-08-30, governance register line 6902) inside the same UnitOfWork as the Sales reversion | `kds.operate` + `KdsStationGuard` | `TICKET_RECALLED` audit; `ticket.recalled` event, Sales reverts exactly the `ready→fired` lines it caused, per KDS-R12 | `test/kds-operator-lifecycle.e2e-spec.ts` recall block (5 tests: idempotency-key-required, window-expiry, replay); `test/kds-concurrency.e2e-spec.ts` I/J/K/L (recall-vs-bump race, cross-transaction Sales reversion, subscriber-failure rollback) | None identified | N/A |
| FR-KDS-026 [M] | §9.3 | Deliberate interaction to bump (long-press/double-tap) | NOT_BACKEND_OWNED | Kitchen | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Pure frontend UX gesture requirement; the backend bump endpoint is a single idempotent-ish POST with no "confirmation" parameter, appropriately, since the deliberateness is a client interaction pattern not a server contract concern | Frontend only | N/A |
| FR-KDS-027 [S] | §9.3 | Priority flags (rush/VIP/remake) | MISSING_BACKEND | Kitchen | none | none | No `priority`/`rush`/`vip`/`remake` field anywhere in `Ticket`/`TicketLine` (grep returned zero hits). Note: `StationRoutingRule.priority` exists but is explicitly documented as unrelated, unused semantics (`schema.prisma:978`: "retained physically with NO assigned semantics... resolver MUST NOT read it") | n/a | n/a | n/a | none | No implementation found. | DEFERRED |
| FR-KDS-028 [S] | §9.3 | Amendments to fired order appear as update on existing ticket (never new ticket), with alert | COMPLETE_BACKEND (persistence); alert rendering NOT_BACKEND_OWNED | Kitchen | (internal, via Fire on a subsequent course) | `OrderLineFiredHandler` reuses the ticket via natural key `(tenantId, orderId, businessDay, stationId)` (`ticket-persistence.service.ts:121-194`), unique constraint `uq_tickets_order_station` (`schema.prisma:1184`). Amendment reactivation logic at `order-line-fired.handler.ts:134-159` | as above | Same Fire transaction | n/a | ticket write is audited/eventing as for a normal fire | `test/kds-amendment.e2e-spec.ts` (amendment into already-bumped ticket reactivates correctly; replay is a no-op); `test/kitchen-ticket-persistence.e2e-spec.ts` "later fireBatchId on an already-ticketed station reuses the SAME Ticket" | Audible/visual alert is a frontend concern driven by the same event stream; no backend gap | N/A | N/A |
| FR-KDS-029 [M] | §9.3 | Cancelled lines struck through + alert, visible for configurable period | PARTIAL_BACKEND | Kitchen | (internal, via post-fire void) | `TicketPersistenceService.cancelTicketLinesForOrderLine` (`ticket-persistence.service.ts:357-373`, excludes `served`), triggered by `OrderLineVoidedPostFireHandler` (`tickets/order-line-voided-postfire.handler.ts`, POS-FIN-1/FR-POS-070-071) inside the same transaction as the Sales void | `BranchKdsConfig.cancelledLineVisibilitySeconds` field exists but **nullable, no default** (`schema.prisma:1015-1020`: "NULL = not yet configured; a future cancellation-handler slice supplies a value, not this one") | Same transaction as the Sales-side void | n/a | audited as part of the void transaction | `test/pos-financial-corrections.e2e-spec.ts` H1 "a real Fire creates a real Ticket+TicketLine; post-fire void cancels EXACTLY the matching TicketLine" | Strikethrough/alert rendering is frontend. No write path populates `cancelledLineVisibilitySeconds` — the "configurable period" itself is never set by any code path found. **Note:** this requirement was reported NOT_IMPLEMENTED as of the 2026-08-30 KDS operator-lifecycle report; cancellation persistence has since been added via POS-FIN-1 (`order-line-voided-postfire.handler.ts`), so the classification has materially improved since that prior report — re-verify this file's existence/date if cross-referencing older reports. | Populate `cancelledLineVisibilitySeconds` from a real config write path | P2_COMPLETENESS |
| FR-KDS-030 [S] | §9.3 | All-day counts (aggregate outstanding qty/item across active tickets) | MISSING_BACKEND | Kitchen | none | none | none | n/a | n/a | n/a | none | No aggregate-by-item endpoint or query found in `src/modules/kitchen`. | DEFERRED |
| FR-KDS-031 [C] | §9.3 | Icon/image mode | NOT_BACKEND_OWNED | Kitchen | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Frontend rendering mode; Catalogue already stores item images (FR-MNU-004), out of this audit's scope | Frontend only | N/A |

---

## 3. KDS / Kitchen — Timing & Performance Measurement (SRS §9.4)

| Req ID | Section | Title | Classification | Owner | Controller/API | Service/Domain | Prisma | Tests | Evidence | Remaining work | Severity |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-KDS-040 [M] | §9.4 | Timestamps per ticket/line: created, routed, first-viewed, started, ready, bumped, served | PARTIAL_BACKEND | Kitchen | (queue/card read) | 6 of 7 timestamps live; `servedAt` is a **dead column** | `Ticket`/`TicketLine` both carry `createdAt, routedAt, firstViewedAt, startedAt, readyAt, bumpedAt, servedAt` (`schema.prisma:1140-1151`, `1257-1263`) | queue/card-read tests exercise the 6 live timestamps | `contract/kds-summary.query.ts:15` — "`servedAt` is a schema placeholder that no write path in this codebase ever populates," consistent with FR-KDS-013 (Expediter/serve) being deferred | `servedAt` needs a write path once Expediter/serve ships | P2_COMPLETENESS |
| FR-KDS-041 [M] | §9.4 | Avg prep time by item/station/hour/employee/order type | PARTIAL_BACKEND | Kitchen (query) / Reporting (surface) | No direct Kitchen HTTP route; consumed by Reporting's overview endpoint (out of this audit's module scope) | `KdsSummaryQueryService.forBranch` (`tickets/kds-summary.query.service.ts:16-53`) — a **later, narrower** slice (RPT-DEMO-1) than what the KDS-R11/R12 ratification (2026-08-30) explicitly deferred ("FR-KDS-041/042 analytics... DEFERRED and NOT authorized" at that time). Computes only a single branch/business-day-wide `averagePrepDurationSeconds` (mean of `bumpedAt-startedAt`) plus `statusCounts` — **not** broken down by item/station/hour/employee/order-type as required. | n/a | `test/reporting-demo-analytics.e2e-spec.ts` "kds: ticket count, status counts, and average prep duration are correct" | Coarse overview aggregate exists; the required multi-dimensional breakdown does not. Real requirement work remains. | P2_COMPLETENESS |
| FR-KDS-042 [M] | §9.4 | "Ticket time" = bump − fire; "order time" = last-line-ready − order-open | PARTIAL_BACKEND | Kitchen | none | Raw facts exist (`TicketFireBatch.firedAt`, `Ticket.bumpedAt`) but **no service computes either named metric**. `KdsSummaryQueryService` computes `bumpedAt−startedAt` (a *different* metric — prep duration, not ticket time). No "order time" computation found at all. | n/a | none | Same governance deferral caveat as FR-KDS-041 | Implement both named metrics as distinct computations from existing raw timestamps | P2_COMPLETENESS |
| FR-KDS-043 [S] | §9.4 | Bottleneck station identification (queue-depth/throughput ratio per hour) | MISSING_BACKEND | Kitchen | none | none | none | none | grep for "bottleneck" returns zero hits | No implementation. | DEFERRED |
| FR-KDS-044 [S] | §9.4 | Configurable per-item target prep time, default recipe `prep_time_seconds` | PARTIAL_BACKEND | Catalogue/Production (data) / Kitchen (application) | none in Kitchen | `MenuItem.prepTimeSeconds`/`Recipe.prepTimeSeconds` exist in Catalogue/Production (`schema.prisma:1577,4294`, `catalogue/menu-items/menu-items.service.ts:353`, `production/versions/recipe-versions.service.ts:204`) but **Kitchen never reads them** — `Ticket.targetReadyAt` stays permanently null | n/a | none (Kitchen side) | Data substrate is COMPLETE_BACKEND upstream (Catalogue/Production, out of this audit's scope); the KDS-side *application* of it (populating `targetReadyAt`) is MISSING | Wire `targetReadyAt` computation into the routing/ticket-creation path | P2_COMPLETENESS |
| FR-KDS-045 [C] | §9.4 | Capacity warning (queued items > configured throughput for next 15 min) | MISSING_BACKEND | Kitchen | none | none | none | none | grep returns zero hits | No implementation. | DEFERRED |
| NFR-USA-006 [M] | §9.5 | Legible at 2m, min 24pt effective font | NOT_BACKEND_OWNED | Kitchen | n/a | n/a | n/a | n/a | n/a | Frontend rendering concern | N/A |
| NFR-PERF-004 [M] | §9.5 | Fired order appears on target station display within 1s p95 | NOT_VERIFIED | Kitchen | n/a | n/a | n/a | No benchmark executed this session or found in a prior evidenced report | KDS is poll/read-based (no gateway/socket code in Kitchen module per `2026-09-07_GOLDEN-PATH-BACKEND-CLOSURE` trace) — end-to-end latency depends on client poll interval, not verified here | Needs a real perf benchmark | NEEDS_VERIFICATION |
| NFR-REL-002 [M] | §9.5 | KDS continues to display/accept bumps for already-received tickets during outage, buffering locally | PARTIAL_BACKEND | Kitchen/Sync | `KdsOfflineTicketOperationsService.bumpLine` (`tickets/kds-offline-ticket-operations.service.ts:53-141`) — takes caller-supplied `occurredAt`, preserving the original timestamp; wired through `modules/sync/integration/kds-ticket-bump-line.sync-handler.ts` | n/a | n/a | `test/sync-kds-handlers.e2e-spec.ts` "kds.ticket.bump_line" (5 tests, incl. live-authorization revalidation and replay safety) | Only **bump** is exposed offline — no offline `start`/`recall`/`acknowledgeViewed` handler was found registered. Explicitly listed as DEFERRED at KDS-R11/R12 ratification time (2026-08-30); this fork's investigation found later, narrower offline-bump work postdating that deferral note, not a contradiction of "peer discovery"/full offline parity. | Offline start/recall/first-viewed handlers remain unimplemented | P2_COMPLETENESS |
| NFR-REL-003 [M] | §9.5 | Local peer discovery for KDS↔POS routing when internet is down | MISSING_BACKEND | Kitchen | none | none | none | none | Governance register (line ~6969) lists "peer discovery (NFR-REL-003)" as explicitly DEFERRED; no later work found | No implementation. | DEFERRED |

---

## 4. KDS / Kitchen — Cross-Cutting: Fire Atomicity, Concurrency, Authorization, Audit (special checks)

| Check | Classification | Evidence | Tests | Severity |
|---|---|---|---|---|
| **Routing resolution & fire transaction atomicity — no partial writes on routing failure** | COMPLETE_BACKEND | `OrderLineFiredHandler.handle` runs inside the SAME transaction the Fire command opened (`UnitOfWork.execute → dispatcher.drain(ctx)` → this handler, all before `PrismaService.withAuthContext`'s `$transaction` commits — `order-line-fired.handler.ts:33-46`). `RoutingNoDestinationError`/`RoutingConfigurationConflictError` propagate unmodified through the dispatcher (verified non-swallowing: `domain-event-dispatcher.spec.ts:148`) and are only caught **after** Prisma has already rolled back, purely to translate to HTTP 422 (`sales-fire.service.ts:370-375`). No side effect (event, cache, queue, websocket) occurs outside the transaction boundary — Kitchen has no gateway/socket code at all; KDS clients poll a plain read query. Independently confirmed by `2026-09-07_GOLDEN-PATH-BACKEND-CLOSURE_kds-refund-cashclosepolicy.md` §1, which traced this exact chain and found **no production code defect**. | `test/sales-fire.e2e-spec.ts`: "conflicting category routing sets → ROUTING_CONFIGURATION_CONFLICT → 422 + full rollback", "zero routing destinations → ROUTING_NO_DESTINATION → 422 + full rollback", and — the decisive multi-line case — "multi-line fire where one line resolves and a sibling line has zero routing destinations → the WHOLE fire rolls back, including the line that would have resolved (no partial Kitchen write survives)" (added 2026-09-07, `bf91d66`). Also `test/kitchen-ticket-persistence.e2e-spec.ts`: "a routing NO-DESTINATION failure rolls back the entire Fire transaction," "...CONFIGURATION CONFLICT failure rolls back the entire Fire transaction," "the handler runs inside the SAME transaction as the Fire command — exactly one `$transaction` call." | N/A |
| **Ticket persistence / idempotency / race safety** | COMPLETE_BACKEND | Raw `INSERT ... ON CONFLICT ... DO NOTHING RETURNING` (not catch-P2002) used deliberately to avoid PostgreSQL's abort-on-conflict semantics inside a shared transaction, documented rationale at `ticket-persistence.service.ts:79-118`. Header-mismatch detection (`assertHeaderUnchanged`/`TicketHeaderMismatchError`) prevents silent overwrite on a legitimately conflicting replay. Ticket-aggregate status (`projectTicketStatus`, a pure function, `ticket-projection.ts`) updated via version-column CAS with 5-attempt bounded retry (`ticket-projection.service.ts`), independent of the outer whole-UnitOfWork SERIALIZABLE 3-attempt retry used for bump/bump-all/recall. | `test/kitchen-ticket-concurrency.e2e-spec.ts` (8 tests, real-Postgres races on Ticket/FireBatch/TicketLine/Modifier creation, FK-violation-vs-conflict distinction, end-to-end double-Fire race); `test/kds-concurrency.e2e-spec.ts` (12 tests, SERIALIZABLE bump/recall races) | N/A |
| **First-viewed** | COMPLETE_BACKEND | `POST /kds/stations/:stationId/tickets/view` acknowledges first-viewed; GET queue never writes `firstViewedAt` (proven by test); FR-AUD-001 requires this be audited — "one entry per newly-first-viewed Ticket... a zero-row replay writes no entry" (recorded, not merely discretionary, per the KDS-R11/R12 ratification's own "Future standard-role intent" note) | `test/kds-first-viewed.e2e-spec.ts` (incl. "D. replaying...writes zero new audit entries") | N/A |
| **Start/bump concurrency** | COMPLETE_BACKEND | See FR-KDS-024 row above; SERIALIZABLE + bounded retry explicitly recorded by the KDS-R11/R12 ratification as an "engineering correctness mechanism," not itself a governance choice, but present and tested | `test/kds-concurrency.e2e-spec.ts` (12 tests incl. write-skew "GUARD" proof) | N/A |
| **KDS Authorization (`kds.operate`)** | COMPLETE_BACKEND | Single coarse permission `kds.operate` (`kitchen.permissions.ts:34`) — an explicit, user-ratified exception to the zero-invented-permission-code discipline (KDS-R11, governance register line 6835), the third such exception after `pos.order.fire` and `pos.payment.capture`. Guard chain: `JwtAuthGuard → TenantContextGuard → PermissionGuard(kds.operate) → KdsStationGuard` (`kitchen.controller.ts:207-209`). `KdsStationGuard` (`auth/kds-station.guard.ts:52-117`) fails closed on: non-terminal-bound session, terminal not active/not type `kds`, terminal bound to zero or more-than-one station, path `:stationId` mismatch. Every ticket-scoped mutation independently re-verifies station ownership from a freshly-loaded row inside the transaction, never trusting the guard alone. | `test/kds-authorization.e2e-spec.ts` (13 tests: positive queue+mutation, no-permission, no-terminal, wrong-terminal-type ×2, disabled/revoked terminal, zero/two-station binding, stationId mismatch, missing-employee, cross-tenant 404-not-403, tenant-vs-station-layer independence) | N/A |
| **Audit trail** | COMPLETE_BACKEND | `AuditService` calls for `TICKET_VIEWED`, `TICKET_LINE_STARTED`, `TICKET_LINE_BUMPED`, `TICKET_BUMPED` (bump-all), `TICKET_RECALLED` — one entry per operator action; audit deliberately ordered LAST so its per-tenant advisory lock is never load-bearing for correctness (explicit design note + test) | `test/kds-concurrency.e2e-spec.ts` test H "audit advisory lock is NOT load-bearing" | N/A |
| **UC-KDS-01 steps 7-9 (Expediter aggregate, expediter bump→served, order time, POS notify)** | MISSING_BACKEND | No Expediter surface (FR-KDS-013 deferred), no `served` transition writer, no "order time" computation, no POS notification beyond the generic `ticket.bumped` event | n/a | DEFERRED_BY_GOVERNANCE |
| **UC-KDS-01 alternate 6a (void-after-fire → struck through, waste disposition prompt)** | COMPLETE_BACKEND (Kitchen side) | Kitchen-side cancellation complete (see FR-KDS-029); "waste disposition prompt" is Sales' `PostFireVoidRecord` (POS-FIN-1, out of this audit's module scope) | `test/pos-financial-corrections.e2e-spec.ts` H1 | N/A |

---

## 5. Treasury — Cash Session Model (SRS §16.2)

| Req ID | Section | Title | Classification | Owner | Controller/API | Service/Domain | Prisma | Transactionality/Concurrency | AuthZ | Audit/Event | Tests | Evidence | Remaining work | Severity |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-FIN-001 [M] | §16.2 | Cash drawers as branch-level entities; one open session per drawer at any time | COMPLETE_BACKEND | Treasury | `POST /cash-sessions` (`treasury.controller.ts:379-438`), `POST/GET /branches/:branchId/drawers` (`drawers.controller.ts`) | `CashSessionsService.open` (`cash-sessions/cash-sessions.service.ts:225-239`), `DrawersService.create/listForBranch` | `Drawer` (`schema.prisma:2970-2996`, branch-scoped, `@@unique([tenantId,branchId,id])`); one-open-session enforced by a **partial unique index** `uq_one_open_session_per_drawer` (migration `20260820160000_shift_drawer_cash_session_open/migration.sql:188`) — DB-level, race-proof, not application logic | Concurrent-open race resolved at the DB (unique index), caught as Prisma `P2002` → HTTP 409 | `cash.session.open` (open), `settings.branch.manage` (drawer admin, reused — no invented drawer-admin permission) | Standard audit on session open (`CASH_SESSION_OPENED`, `SHIFT_OPENED`) | `test/cash-session.e2e-spec.ts` ("refuses a SECOND sequential open", "admits exactly ONE of two concurrent opens", "is a PARTIAL index, not UNIQUE(drawer_id,status)", "permits MANY closed sessions on one drawer"); `test/drawers-provisioning.e2e-spec.ts` (9 tests) | Drawer administration surface added 2026-09-06 (`4d7a387`, `DEMO-OPS-HOTFIX-3`) — re-verified present and correctly guarded in this session, not merely trusted from the prior report | None identified | N/A |
| FR-FIN-002 [M] | §16.2 | Session bound to exactly one employee; two employees SHALL NOT share an open session on the same drawer | COMPLETE_BACKEND | Treasury | `POST /cash-sessions` | `CashSessionsService.open` derives employee only from the trusted POS session (`cash-sessions.service.ts:141-159`, `treasury.controller.ts:794-813 requirePosIdentity`) | `CashSession.employeeId` NOT NULL (`schema.prisma:3026`), four-column composite FK to `Shift` | N/A | Employee identity always server-derived, never client-supplied | N/A | `test/cash-session.e2e-spec.ts` "refuses a session whose employee differs from its shift employee" | None identified | N/A |
| FR-FIN-003 [S] | §16.2 | Shared-drawer mode (branch policy, multi-cashier, reduced-control indication) | MISSING_BACKEND | Treasury | none | none | No branch-policy flag, no reduced-control-environment indication found (`grep -rn "shared.drawer|sharedDrawer|reduced.control"` → zero hits) | n/a | n/a | none | No implementation. Should-priority, not required for current phase. | DEFERRED |
| FR-FIN-004 [M] | §16.2 | Expected Cash = OpeningFloat + CashSales + CashTips + PayIns − CashRefunds − PayOuts − SafeDrops ± Rounding | COMPLETE_BACKEND (one term structurally zero by design) | Treasury | (internal, part of close-declare) | `CashSessionCloseService.computeExpectedCash` (`cash-session-close/cash-session-close.service.ts:751-789`), exact eight-term formula | `CashSessionCloseAttempt` (`schema.prisma:3109-3148`), DB CHECK `ck_csca_formula`. `CASH_TIPS_TOTAL` is **structurally zero** (`cash-session-close.service.ts:118-119`, DB CHECK `ck_csca_tips_structurally_zero_p1g1`) — no tip mechanism exists anywhere in the codebase, documented and DB-enforced, not a hidden defect. `cashRefundsTotal` is real (sourced from `sales.refunds` via `CashSessionTenderTotalsQuery`, POS-FIN-1). | Declared once, immutable thereafter | N/A | audited as part of close-declare | `test/cash-session-close.e2e-spec.ts` "exact match: variance 0…"; `test/cash-movements-close-and-payment-concurrency.e2e-spec.ts` (pay-in/pay-out/safe-drop vs close races) | The Cash Tips term is a documented no-op pending a future tips feature — not a gap this task should re-flag as incomplete without qualification | P3_HARDENING (tips term) |
| FR-FIN-005 [M] | §16.2 | Cash variance = Counted − Expected, recorded on session | COMPLETE_BACKEND | Treasury | (internal) | `variance = declaredTotal - facts.expectedCash` (`cash-session-close.service.ts:314`), DB CHECK `ck_csca_variance`, copied onto `CashSession.variance` at CLOSED transition (`cash-session-close.service.ts:454-466`, `schema.prisma:3044-3046`) | as above | as above | n/a | n/a | `test/cash-session-close.e2e-spec.ts` "boundary: variance exactly == tolerance is WITHIN" | None identified | N/A |
| FR-FIN-006 [M] | §16.2 | Variance beyond tolerance requires reason + approval by `cash.variance.approve` holder who SHALL NOT be the session owner | COMPLETE_BACKEND | Treasury (+ Governance Approval Runtime) | `POST /cash-sessions/:id/close` (declare), `POST /cash-sessions/:id/close/finalize` (manager decision) | `declareClose` computes `approvalRequired` from resolved policy tolerance (`cash-session-close.service.ts:315-317`); above-tolerance freezes session `open→closing` with **no disclosure** until committed (blind-count control, FR-POS-095); `finalizeClose` (`:487-674`) verifies manager PIN (`treasury.controller.ts:760-765`), calls Governance's `ApprovalCommands.createRequest` with `excludedApproverUserId: ownerUserId` (self-approval structurally excluded, `:582-591`) | tolerance is a real, versioned, per-branch policy — see §6 below | `TREASURY_PERMISSIONS.CASH_VARIANCE_APPROVE = 'cash.variance.approve'` (`treasury.permissions.ts:75`), checked by the Approval Runtime against the **verified manager's** own permission set, never the caller's | `CASH_VARIANCE_DECLARED`, `CASH_SESSION_CLOSED` audit actions; `cash.variance.detected` domain event published once per newly-created close attempt (`treasury/contract/events.ts:40-83`), matching SRS §5.5.4's catalogue exactly | `test/cash-session-close.e2e-spec.ts` "self-approval is blocked", "owner's Employee has no linked Identity User → finalize fails closed (FR-SEC-016)", "R-6(a): explicit REJECTION commits…" | Rejection-recovery semantics (R-6, ratified 2026-08-30) fully implemented: session stays `CLOSING`, count immutable, retry with a new `ApprovalRequest`/`ApprovalDecision`, no reopen/recount/escalation | None identified | N/A |
| FR-FIN-007 [M] | §16.2 | Sessions immutable once closed; corrections as adjusting entries referencing the session | PARTIAL_BACKEND | Treasury | n/a (no correction endpoint exists) | Immutability itself is real and DB-enforced: `ros_app` has no UPDATE/DELETE grant on `cash_session_close_attempts`/`cash_count_denominations`; only 10 named columns on `cash_sessions` are updatable at all | as above | RLS `WITH CHECK` prevents a jump straight from `open` to `closed` without an anchor | n/a | n/a | `test/cash-session-close.e2e-spec.ts` "ros_app has no UPDATE/DELETE…", "ros_app cannot UPDATE a cash_sessions column outside the 10 close-related columns", "...cannot jump open→closed without an anchor" | **No adjusting-entry mechanism exists** — the SRS's second clause ("Corrections SHALL be recorded as adjusting entries referencing the session") has zero implementation. Explicitly and deliberately absent per the controller docblock (design gate §12, "NOT SOURCE-DECIDABLE"), not an oversight. | P2_COMPLETENESS |
| FR-FIN-010 [M] | §16.3 | Record totals by tender type per session/day: cash, each card scheme, wallet, gift card, voucher, on-account, aggregator-settled | PARTIAL_BACKEND | Treasury | (internal query, consumed by close/day-close) | `CashMovementTotalsQueryService`/`CashSessionTenderTotalsQuery` (per session), `DayCloseService` (per day) | n/a | n/a | n/a | n/a | covered by close/day-close test suites | Only `cash` and `manual_external_card` tender families implemented; card scheme, wallet, gift card, voucher, on-account, aggregator-settled all MISSING (`grep` for `cardScheme|wallet|giftCard|voucher|onAccount|aggregator` in `treasury` → zero hits). Self-declared as PARTIAL in the runtime response's own `scope` block, not silently missing. | P1_MAJOR_GAP |
| FR-FIN-011 [S] | §16.3 | Card-total reconciliation against terminal batch settlement report | MISSING_BACKEND | Treasury | none | none | none | n/a | n/a | n/a | none | No implementation found (`grep` for "batch settlement"/reconciliation returns nothing relevant). | DEFERRED |
| FR-FIN-012 [S] | §16.3 | Aggregator payout reconciliation (net of commission, flagging discrepancies) | MISSING_BACKEND | Treasury | none | none | none | n/a | n/a | n/a | none | No implementation found. | DEFERRED |
| FR-FIN-015..018 [S] | §16.4 | Expenses (recording, petty-cash pay-out linkage, approval threshold, recurring templates) | MISSING_BACKEND | Treasury | none | none | No dedicated Expense model/module found anywhere in the repository | n/a | n/a | n/a | none | `treasury.controller.ts:486` references "cash removed from the drawer for an expense" only as a **pay-out** (FR-POS-091, unrelated to a structured Expense record with category/supplier/attachment/approval). No Expense entity exists. | DEFERRED |

---

## 6. Treasury — Day Close (SRS §16.5)

| Req ID | Section | Title | Classification | Owner | Controller/API | Service/Domain | Prisma | Transactionality/Concurrency | AuthZ | Audit/Event | Tests | Evidence | Remaining work | Severity |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-FIN-020 [M] | §16.5 | Business-day close operation per branch | COMPLETE_BACKEND | Treasury | `POST /branches/:branchId/day-closes/:businessDay` (`day-close.controller.ts:300`) | `DayCloseService.post` (`day-close/day-close.service.ts:251-741`) — service docblock states "FR-FIN-020/021/023/024, complete" | `DayClose`, `DayCloseActivation`, `DayCloseTaxClassTotal`, `DayCloseOrderTypeTotal`, `DayCloseSession` (`schema.prisma:3320-3520`) | Fully transactional (`UnitOfWork`), idempotent | `cash.day.close` | `DAY_CLOSE_ACTIVATED`/`DAY_CLOSED` audit actions (`audit.constants.ts:80-81`); `day.closed` domain event published (`contract/events.ts:110-142`), matching SRS §5.5.4 exactly (publisher Treasury, subscribers Analytics/Fiscal/Reporting — zero registered subscribers currently, correct per DC-R1) | `test/day-close.e2e-spec.ts` (31 tests), `test/day-close-cutover-race.e2e-spec.ts`, `test/day-close-znumber-concurrency.e2e-spec.ts` | Ratified DC-R1 (2026-08-31): "the state-changing Treasury DayClose aggregate... AUTHORISED to be implemented now" — FR-FIN-020 "in full" | None identified | N/A |
| FR-FIN-021 [M] | §16.5 | Day close blocked while any cash session open; blocking sessions listed | COMPLETE_BACKEND | Treasury | (same route as FR-FIN-020) | `day-close.service.ts:450-463` — queries `cashSession.findMany({status: {not:'closed'}})`, **unqualified by business day, over every branch session** (broader than the literal SRS wording, deliberately, per code comment) | as above | as above | as above | as above | `test/day-close.e2e-spec.ts` "an open session (with or without payments/movements) and a closing session both block", "a blocking session belongs to the branch/tenant — another tenant/branch never blocks" | Ratified DC-R1: "in full — both limbs" | None identified | N/A |
| FR-FIN-022 [M] | §16.5 | Z report contents (gross/discounts/refunds/net/tax-by-rate/sales-by-category/sales-by-tender/sales-by-order-type/txn-count/AOV/void-comp-summary/cash-reconciliation/variance) | PARTIAL_BACKEND | Treasury | (same route as FR-FIN-020) | `day-close.service.ts`, self-documenting `SCOPE_BLOCK` in every response | as FR-FIN-020 | as above | as above | as above | as FR-FIN-020 | **Implemented:** gross sales, discounts (structurally zero — no discount mechanism), refunds, net sales, tax total (component sum only, not by rate), sales by order type, transaction count, AOV, void summary (comp half structurally zero — no code path writes `state:'comped'`), cash reconciliation, variance summary. **NOT implemented** (explicit, self-disclosed in every API response's `scope` field, per DC-R1 clause 3): tax by rate (only the component sum is persisted, per-rate/base not derivable), sales by category (no category snapshot on `sales.order_lines`, category placement is many-to-many mutable master data), comp half of void/comp summary, sales by tender beyond cash+manual_external_card. **This is a Must-have requirement explicitly, deliberately, and by governance ratification (DC-R1) shipped PARTIAL, never described as complete or waived.** | Full per-rate tax breakdown, category snapshot mechanism, comp code path, remaining tender families | P1_MAJOR_GAP |
| FR-FIN-023 [M] | §16.5 | Z reports sequentially numbered per branch, immutable, retrievable for any historical date | COMPLETE_BACKEND | Treasury | `GET /branches/:branchId/day-closes/:businessDay` (`day-close.controller.ts:359-388`) | `zNumber = MAX(z_number)+1` inside the transaction with `UNIQUE(tenant,branch,z_number)` structural backstop and bounded retry on collision (`day-close.service.ts:550-554`, `259-278`); `DayClose` insert-once, no UPDATE/DELETE grant | as above | as above | Read permission deliberately distinct from write: `report.view.financial` for GET (DC-R3), `cash.day.close` for POST | as above | `test/day-close.e2e-spec.ts` "IMMUTABILITY (mechanical, ros_app grants…)", "GET reads persisted snapshot only", "no persisted DayClose → 404", "GET never creates a DayClose as a side effect" | None identified | N/A |
| FR-FIN-024 [M] | §16.5 | Configurable business-day boundary per branch (e.g. 04:00) | COMPLETE_BACKEND | Organisation/Sales (shared resolver, reused by Treasury) | n/a (Treasury consumes, does not own) | `DailyTradingSalesQuery.currentBusinessDay` is the shared authoritative resolver Order-creation, Reporting, and DayClose all consume (`day-close.service.ts:349-353`) | n/a (out of this audit's file scope to re-verify the configuration surface itself) | n/a | n/a | n/a | not independently re-verified in this session (owned by a sibling module) | Ratified DC-R1: "FR-FIN-024 is already COMPLETE and is reused, never reimplemented." Recommend the Organisation/Sales section of this audit independently confirm the per-branch boundary *configuration* surface. | Cross-domain confirmation recommended | N/A |
| FR-FIN-025 [S] | §16.5 | Automatic day close at boundary, open sessions force-closed and flagged | MISSING_BACKEND | Treasury | none | none | none | n/a | n/a | n/a | none | Explicitly listed absent: `day-close.service.ts:219` "FR-FIN-025: automatic close (no scheduler)." No cron/scheduler code found. Ratified DC-R1 clause 5: "remains NOT IMPLEMENTED. No scheduler, no per-branch enablement flag, and no force-close-and-flag semantics are authorised or built." | DEFERRED |
| FR-FIN-026 [M] | §16.5 | Day close triggers: fiscal finalisation, inventory day-end snapshot, report pre-aggregation, accounting export | MISSING_BACKEND | Treasury (trigger point) / Fiscal, Inventory, Reporting, Platform (owning capabilities) | (same route as FR-FIN-020 publishes `day.closed` as the sole integration point) | none of the four trigger limbs implemented | n/a | n/a | n/a | `day.closed` event published with **zero registered subscribers** currently — establishes the future integration point only | none | All four sub-clauses explicitly self-declared NOT IMPLEMENTED (`day-close.service.ts:215-218`, ratified DC-R1 clause 4): (1) fiscal document finalisation — no fiscal-finalisation capability, no fiscal documents exist to finalise, "an empty set is NOT compliance"; (2) inventory day-end snapshot — `inventory.stock_levels` is a live projection with no date dimension; (3) report pre-aggregation — explicitly forbidden by RPT-R2 (no rollup persistence/`fact_*`/`dim_*` tables/analytics warehouse authorized); (4) accounting export — no transactional outbox exists, and §5.5.3 makes one mandatory (`FR-PLT-041`) for exactly this class of effect; "no fire-and-forget external call may be substituted." **This is a Must-have requirement whose entire enabling infrastructure (the transactional outbox) does not exist in the repository at all.** Governance (DC-R1) explicitly authorized shipping Day Close's core aggregate despite this — sequencing, not a waiver. | Requires: fiscal document generation capability, dated inventory snapshot mechanism, a transactional outbox (FR-PLT-041), and — separately, per RPT-R2 — report pre-aggregation remains out of scope entirely, not merely unbuilt | P1_MAJOR_GAP |

---

## 7. Treasury — Special Checks (consolidated, per audit-spec §6)

| Check | Classification | Evidence | Severity |
|---|---|---|---|
| **Drawer provisioning** | COMPLETE_BACKEND | `POST/GET /branches/:branchId/drawers` (`drawers.controller.ts`), permission `settings.branch.manage` (reused, no invented drawer-admin permission, documented rationale in `treasury.module.ts`). `DrawersService.create` validates branch + optional terminal-branch match (`drawers.service.ts:63-101`). Cashier-facing counterpart `GET /cash-sessions/drawers` resolves branch from the caller's own terminal, never a supplied `branchId` (`treasury.controller.ts:346-363`, `drawers.service.ts:171-185`). Added 2026-09-06 (`4d7a387`); the 403-authorization-target question raised the same day was investigated and **disproven** as a backend defect — `test/drawers-provisioning.e2e-spec.ts` includes the exact byte-identical-403 proof test added by that investigation (commit referenced in the task brief as the "prove authorization target is correct" commit). | N/A |
| **Cash session open** | COMPLETE_BACKEND | See FR-FIN-001/002 above. | N/A |
| **Employee custody** | COMPLETE_BACKEND | Employee/terminal identity always derived from the trusted, server-side-verified POS session (`requirePosIdentity`), never from the request body, on every mutating Treasury route. | N/A |
| **Variance / tolerance / approval enforcement** | COMPLETE_BACKEND | Real, configurable, per-branch, versioned, **immutable, effective-dated** cash-close policy (`CashClosePolicyService`/`CashClosePolicyResolver`/`treasury.cash_close_policies`), resolved deterministically by `(tenant, branch, asOf = session.openedAt)` per ratified R-3(a) — governs the version in effect at session OPEN time, not at close time, preventing a tolerance from being widened after a drawer is already open. Approval is enforced through Governance's real Approval Runtime (self-approval structurally excluded via `excludedApproverUserId`), not a stub or a client-trusted flag. `GET /branches/:branchId/cash-close-policy` (added 2026-09-07, `bf91d66`) lets an admin UI check current state; `POST` (pre-existing) creates a new version. Fail-closed: no valid policy version at session-open time ⇒ close cannot complete (R-5, acknowledged consequence). | N/A |
| **Close attempts / approval workflow transactionality** | COMPLETE_BACKEND | `declareClose`/`finalizeClose` both use `pg_advisory_xact_lock` keyed on the session id; permanent-id replay logic guards double-declaration; state machine (`open→closing→closed`) is DB-CHECK/RLS-enforced (cannot jump straight `open→closed` without an anchoring close-attempt row). Cannot be closed twice — idempotency-key + `closeAttemptId` permanence + `session.status !== 'open'` guard. | N/A |
| **Close-other (manager closing another employee's session)** | COMPLETE_BACKEND | `assertCloseAuthority` (`cash-session-close.service.ts:726-749`) selects `cash.session.close` vs `cash.session.close_other` based on `session.employeeId === actor.employeeId`, re-checked against branch-scoped authority **inside the same transaction as the write**, not merely at the route guard. `TREASURY_PERMISSIONS.CASH_SESSION_CLOSE_OTHER = 'cash.session.close_other'` (`treasury.permissions.ts:72`). Canonical role templates deliberately withhold `close_other` from Cashier (`canonical-role-templates.spec.ts:24`, re-confirmed by the 2026-09-07 golden-path-closure report). FR-FIN-006's non-owner-approver rule is enforced independently via `excludedApproverUserId`, so "close-other" and "variance-approver-not-owner" are two separately-enforced controls, not conflated. | N/A |
| **Business-day / day-close interaction** | COMPLETE_BACKEND | See FR-FIN-021. Additionally enforces a shared advisory-lock fence with Order creation (`test/day-close-cutover-race.e2e-spec.ts`) preventing new orders landing in an already-closed day and vice versa — order creation checks Treasury's public Day Close state after acquiring that fence and before its `INSERT`; Day Close acquires the same fence before its own write (per DC-R1's "Concurrency and the business-day cutover" note). | N/A |
| **Audit / event publication** | COMPLETE_BACKEND for what the SRS actually names | Audit actions (`audit.constants.ts`): `CASH_SESSION_OPENED`, `SHIFT_OPENED`, `CASH_VARIANCE_DECLARED`, `CASH_SESSION_CLOSED`, `CASH_CLOSE_POLICY_VERSION_CREATED`, `DAY_CLOSE_ACTIVATED`, `DAY_CLOSED`, `CASH_MOVEMENT_RECORDED` — all written inside the same transaction as their triggering mutation. Domain events: exactly `cash.variance.detected` and `day.closed` published (`treasury/contract/events.ts`), matching SRS §5.5.4's catalogue precisely for Treasury's publisher role — no invented events (e.g. no `cash.session.opened` domain event exists, correctly, since the SRS catalogue does not list one; only an audit entry, which the SRS does not preclude). | N/A |

---

## 8. Cross-references to prior-report evidence (re-verified, not trusted)

- **`2026-09-07_GOLDEN-PATH-BACKEND-CLOSURE_kds-refund-cashclosepolicy.md`** — its KDS fire-atomicity trace and the new multi-line rollback regression test were re-confirmed present in current source (`test/sales-fire.e2e-spec.ts`) and independently re-traced by this session's own fork investigation (§4 above), reaching the same conclusion via an independent read. Its `GET /branches/{branchId}/cash-close-policy` addition was re-confirmed present and correctly guarded (§7 above).
- **`2026-09-06_DEMO-OPS-HOTFIX-2_cashier-shift-and-kds-stations.md`** — its claim that Station CRUD (FR-KDS-001) was already correct and only lacked a frontend page is consistent with this session's independent finding that Station administration lives in Organisation, not Kitchen, and is complete. Its noted "no public HTTP administration route" gap for Drawer creation was **superseded** by the next report (HOTFIX-3) and is reflected as such above — not still true at current HEAD.
- **`2026-09-06_DEMO-OPS-HOTFIX-3_station-persistence-and-drawer-provisioning.md`** — its Drawer administration surface (`DrawersController`, `GET /cash-sessions/drawers`) was independently re-confirmed present, correctly permissioned, and tested in this session (§7 above).
- **`2026-09-06_DEMO-DRAWER-AUTHZ-HOTFIX-4_cashier-drawer-list-403.md`** — its finding that the reported 403 was not a backend defect (the `@AuthorizationTarget` was already correct) is consistent with this session's independent authorization trace of `GET /cash-sessions/drawers` (§4/§7); no contradicting evidence found.
- **`2026-08-30_KDS_operator-lifecycle-implementation.md`** — used as historical baseline only. Two of its "NOT IMPLEMENTED" classifications have since been **superseded** by later work found in current source: FR-KDS-029 (cancellation persistence — now PARTIAL_BACKEND via POS-FIN-1's `order-line-voided-postfire.handler.ts`, not present at that report's date) and the KDS-side reporting overview (FR-KDS-041/042 — a narrow RPT-DEMO-1 slice now exists, still far short of the full requirement, reclassified PARTIAL_BACKEND rather than that report's blanket NOT_IMPLEMENTED). All other classifications in that report were reconfirmed unchanged by this session's independent re-read.

---

## 9. Totals and classification counts

Total requirements/checks assessed in this report: **61**

KDS/Kitchen domain: 35 rows (5 Stations & Routing [§1] + 12 Display & Interaction [§2] + 10 Timing & Performance [§3] + 8 cross-cutting special checks [§4]).
Treasury domain: 26 rows (11 Cash Session Model [§5] + 7 Day Close [§6] + 8 special checks [§7]).

Where a row's classification is a compound (e.g. "COMPLETE_BACKEND (data layer); NOT_BACKEND_OWNED (rendering)"), it is counted once under its primary backend-facing classification (the first-named one), consistent with not over-crediting rendering-only completeness.

| Classification | KDS/Kitchen | Treasury | Total |
|---|---|---|---|
| COMPLETE_BACKEND | 15 | 17 | 32 |
| PARTIAL_BACKEND | 8 | 3 | 11 |
| MISSING_BACKEND | 8 | 6 | 14 |
| NOT_BACKEND_OWNED | 3 | 0 | 3 |
| NEEDS_VERIFICATION | 1 | 0 | 1 |
| **Total** | **35** | **26** | **61** |

Severity breakdown (34 rows are fully COMPLETE_BACKEND or NOT_BACKEND_OWNED with no remaining backend work and carry no severity; 27 rows carry a severity below — 26 from the PARTIAL_BACKEND/MISSING_BACKEND/NEEDS_VERIFICATION rows, plus one exception: FR-FIN-004 is COMPLETE_BACKEND overall but carries a P3_HARDENING note for its structurally-zero cash-tips term):

| Severity | Count |
|---|---|
| P1_MAJOR_GAP | 3 (FR-FIN-010, FR-FIN-022, FR-FIN-026) |
| P2_COMPLETENESS | 8 (FR-KDS-022, FR-KDS-029, FR-KDS-040, FR-KDS-041, FR-KDS-042, FR-KDS-044, NFR-REL-002, FR-FIN-007) |
| P3_HARDENING | 1 (FR-FIN-004's structurally-zero cash-tips term — the one COMPLETE_BACKEND row carrying a severity) |
| DEFERRED / DEFERRED_BY_GOVERNANCE | 14 (FR-KDS-012, FR-KDS-013, FR-KDS-023, FR-KDS-027, FR-KDS-030, FR-KDS-043, FR-KDS-045, NFR-REL-003, UC-KDS-01 steps 7-9, FR-FIN-003, FR-FIN-011, FR-FIN-012, FR-FIN-015..018 [one combined row], FR-FIN-025) |
| NEEDS_VERIFICATION | 1 (NFR-PERF-004) |
| **Total** | **27** |

No P0_PRODUCTION_BLOCKER was identified in either domain at current HEAD: KDS fire atomicity is proven correct and race-tested; cash-session close/variance/approval/close-other/day-close-blocking are all real, transactional, and tested; the significant Must-have gaps that remain (FR-FIN-010 full tender-family coverage, FR-FIN-022 full Z-report content, FR-FIN-026's fiscal/inventory-snapshot/pre-aggregation/accounting-export triggers) are each explicitly, deliberately, and governance-ratified as sequenced-not-blocking (DC-R1), not silently missing or misrepresented as complete anywhere in the current codebase's own contract responses.
