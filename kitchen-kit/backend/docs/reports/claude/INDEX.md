# Claude Code Reports — Index

Navigation only. Not governance, not SRS authority — see each report's own
authority statement. Entries are appended chronologically; never rewritten.

| Date | Slice | Report | Status | HEAD | Branch | Migrations | Tests | Purpose |
|---|---|---|---|---|---|---|---|---|
| 2026-08-30 | KDS operator lifecycle — implementation | [2026-08-30_KDS_operator-lifecycle-implementation.md](2026-08-30_KDS_operator-lifecycle-implementation.md) | COMPLETE — full operator lifecycle implemented and tested; no commit | `121b889` (unchanged) | `feat/production-spec` | 34 (unchanged — no migration created; local dev DB brought current via `prisma migrate deploy` of 8 pre-existing, already-committed migrations) | Unit 777/777 (57 suites, incl. module-boundaries 41/41); KDS e2e: authorization 14/14, first-viewed 6/6, operator-lifecycle 12/12, concurrency (real 2-transaction Postgres races) 10/10; full e2e 1004/1007 (2 pre-existing unrelated failures independently verified: `organisation.e2e-spec.ts` orphaned-location rows dated 2026-08-23..28 predating this session, and `approval-runtime.e2e-spec.ts` column-GRANT drift traced to the live DB's prior grant history, not the migration SQL, which is correct) | Implemented KDS-R11 (`kds.operate`, one permission, all 6 routes) and KDS-R12 (`ticket.recalled`) end to end: `KitchenController` (Kitchen's first controller) — `GET queue`, `POST view`, `POST start`, `POST bump`, `POST bump-all`, `POST recall`; `KdsStationGuard` enforcing terminal-active+`kds`-type (via new Identity `TerminalFactsQuery` contract) and exactly-one-station binding (via new Organisation `StationDisplayBindingQuery`/`KdsBranchConfigQuery` contracts, 0/>1 both 403); `KdsOperationsService` implementing first-viewed (write-once, audit derived from `RETURNING`), start, bump item/all (pure `projectTicketStatus` ticket-aggregate projection, unit-tested), and recall (window from existing `branch_kds_config` default, `recall_count` cumulative); **`UnitOfWork.execute` gained an opt-in bounded whole-transaction retry** (`PrismaService.withAuthContext` now accepts `isolationLevel`) applied to bump/bump-all/recall at `Serializable`, classifying `P2034`/raw `40001`/`40P01` as retryable and nothing else, proven against the real write-skew the acceptance correction identified (two-station same-order-line concurrent bump, real barrier-synchronized two-transaction test, Sales ends `ready` exactly once with no duplicate audit despite the internal retry) plus 9 further real-Postgres races (same-line, same-ticket-different-lines, bump-vs-bump-all, 3-station fan-out, Sales-subscriber-failure rollback, recall-vs-bump, recall Sales reversion, recall-subscriber-failure rollback); widened `ticket.bumped` to its v1 payload (`stationId`/`bumpedAt`/`orderLineIds`/`readyOrderLineIds`, the latter computed by a Kitchen-only cross-station SQL read) and added `ticket.recalled`, both with real Sales subscribers (`TicketBumpedHandler`/`TicketRecalledHandler`, state-guarded so `served`/`voided`/`comped` are never regressed, same-transaction rollback proven); zero new migrations, zero new business-domain module-boundary deviations (only the same category-(a) HTTP/audit plumbing every other controller-bearing module already carries); OpenAPI regenerated to the exact 6-route surface. Verdict **A — implemented, ready for acceptance review** |
| 2026-08-30 | KDS operator lifecycle — user ratification recorded | [2026-08-30_KDS_operator-lifecycle-user-ratification.md](2026-08-30_KDS_operator-lifecycle-user-ratification.md) | COMPLETE — governance recording only; register updated; no product code, schema, migration, test, OpenAPI or commit | `121b889` (verified; no source/schema/migration drift) | `feat/production-spec` | 34 (unchanged — no migration created or authorized) | n/a — no code changed, no test executed | Recorded the two user-ratified decisions unblocking the KDS MVP operator lifecycle, as a new register section `## KDS MVP Operator Lifecycle Ratification — 2026-08-30` (**+299 lines, −0** — `git diff` shows zero removed lines, so no existing register content was rewritten, reformatted or renumbered; no D-21 created, 20-decision tally unchanged). Limb ids continue the **P1E-2 `KDS-R<n>` series** at **KDS-R11/KDS-R12** after verifying two collisions: `KDS-R1…KDS-R10` are taken (and `KDS-R9` is cited live in `prisma/schema.prisma`'s `StationRoutingRule` docblock), and `R-7` is already a **D-20 option label** ("Defer to Appendix C") in the very same subject area. **KDS-R11** authorises **`kds.operate`** ("Operate a kitchen display station") over station queue read · first-viewed ack · item start · bump item · bump all · recall — the **third** explicit user-authorized exception to the zero-invented-codes discipline after `pos.order.fire` and `pos.payment.capture`; `kds.view`/`kds.ticket.*`/`kds.expedite` explicitly NOT authorized; reads sit behind it on the `pos.order.create` precedent; **it does not represent station authorization**, which is separately enforced fail-closed by active KDS-terminal binding resolving to **exactly one** station (0 ⇒ denied, >1 ⇒ denied as misconfigured), leaving **D-2's branch-RBAC defer untouched**; code-driven not migration-driven, seeded only by the slice creating the routes. **KDS-R12** authorises **`ticket.recalled`** (Kitchen Ops → Sales) as an explicit **EXTENSION of §5.5.4's self-declared "Core Subset"**, stating plainly that the SRS does not define it: Sales reverts exactly the affected lines `ready → fired` and clears `ready_at` **in the same UnitOfWork transaction**, never regressing `served`/`voided`/`comped`, with no cross-module table query and no schema change. Future standard-role intent recorded (Kitchen Staff · Head Chef · Branch Manager · Shift Supervisor · Owner; **not Auditor** — write authority) **without authorizing FR-SEC-010 seeding or modifying any role row**. **SERIALIZABLE+retry and the `TICKET_VIEWED` audit entry were deliberately NOT ratified** — the first an engineering correctness mechanism (§24.6.4 untouched), the second already decided by FR-AUD-001. Conflict check across `kds`/`ticket.bumped`/`ticket.recalled`/`recall`/`Appendix C`/`pos.order.fire`/`pos.payment.capture` found none; **D-20 is not reopened** (its deferral concerned a Governance approval-read code for a surface D-14 A-1 had removed, where deferral cost nothing). Verdict **A — governance-unblocked for implementation** |
| 2026-08-30 | KDS operator lifecycle — design-gate acceptance correction | [2026-08-30_KDS_operator-lifecycle-design-gate-acceptance-correction.md](2026-08-30_KDS_operator-lifecycle-design-gate-acceptance-correction.md) | COMPLETE — analysis/design only; corrects 3 external-review blockers; no code, migration, test, OpenAPI or commit | `121b889` (re-verified unchanged) | `feat/production-spec` | 34 (unchanged — analysis-only task) | n/a — no code changed, no test executed | **Blocker 1 (multi-station readiness):** conceded the prior gate's §13 claim was FALSE — worked the write-skew through statement-by-statement under READ COMMITTED (both transactions compute `readyOrderLineIds=[]`, both commit, Sales line stays `fired`, no repair event ever follows) and **proved `tickets.version` CAS structurally cannot serialize two Ticket aggregates** (`uq_tickets_order_station` guarantees A≠B, so two CASes hit two different rows); reported that `AuditService`'s per-tenant `pg_advisory_xact_lock` currently *masks* the race and rejected relying on it (incidental, statement-order-dependent, tenant-wide, pessimistic per §24.6.4); analysed `sales.orders.version` as instructed and rejected it on module-boundary (`kitchen->sales` deviation) **and** POS ETag/If-Match grounds (a cook's bump would 409 a waiter's unrelated line add); specified the migration-bearing Kitchen readiness-counter option to schema level and rejected it on merit (five consistency paths incl. an untestable cancellation path); **corrected mechanism = SERIALIZABLE for the KDS bump/recall UoW with bounded retry (3) on P2034/40001, audit ordered LAST so correctness cannot depend on the advisory lock** — feasibility verified (Prisma 7.9.1 `TransactionIsolationLevel.Serializable`, `withAuthContext` passes no options today, `correlationId` computed outside the tx so it is retry-stable); six-test deterministic barrier-based race suite incl. a READ COMMITTED guard test. **Blocker 2:** withdrew the "display progress, not a state change" exemption — FR-AUD-001 admits none; one `TICKET_VIEWED` entry per newly-first-viewed Ticket (write-once ⇒ one per ticket lifetime), lines in metadata, audit set derived from the stamping UPDATE's `RETURNING` so a zero-row acknowledgement writes zero entries. **Blocker 3:** withdrew one-terminal→N-stations; ACT-09 "One station" enforced fail-closed (0 ⇒ 403, >1 ⇒ 403 unsupported, path `stationId` must equal the single terminal-derived station); ACT-10/FR-KDS-013 breadth explicitly deferred. **Secondary auth:** added terminal-exists + `status='active'` + **`terminalType='kds'`** + employee-present checks via an Identity-owned guard/contract (Identity already publishes `contract/`), so a POS or dashboard session holding `kds.operate` is refused. Ratification count unchanged at two; **MIGRATION: NO** (re-argued, not preserved); verdict **A — acceptance correction clean** |
| 2026-08-30 | KDS operator lifecycle — final rebase + design + governance gate | [2026-08-30_KDS_operator-lifecycle-final-design-gate.md](2026-08-30_KDS_operator-lifecycle-final-design-gate.md) | COMPLETE — analysis/design/governance packet only; no code, migration, test, OpenAPI or commit | `121b889` | `feat/production-spec` | 34 (unchanged — analysis-only task) | n/a — no code changed, no test executed | Verified the baseline actually is `121b889` with P1G-1 CashSession Close present (not the `0f10afe` pre-close commit) and no shared-infrastructure drift; produced the 37-row current-implementation matrix proving the FR-KDS-040 substrate is COMPLETE while every lifecycle writer is NOT IMPLEMENTED (`first_viewed_at`/`started_at`/`ready_at`/`bumped_at`/`served_at`/`started_by`/`bumped_by`/`recalled_at`/`recall_count`/`cancelled_at` all have columns and no writer; `ticket.bumped` typed but never published; no controller); resolved station authorization as DESIGN-DECIDABLE from `Session.terminalId` + ratified D-16 `org.stations.display_terminal_id` with no new RBAC and no migration; **closed the multi-station Sales-readiness problem P1E-4 left explicitly "out of scope"** by having Kitchen compute `readyOrderLineIds` from its own `ticket_lines` (index `[tenantId, orderLineId, businessDay]` already exists), so no cross-module query and no contract query is needed; finalised the `ticket.bumped` v1 payload and revalidated (not copied) its aggregate-only trigger; established that `order.line.voided` **does not exist in any form** and post-fire void is structurally unreachable, leaving FR-KDS-029 BLOCKED upstream rather than fabricating a trigger; recommended deferring `served` with FR-KDS-013 `[S]` and FIFO-only sorting, classifying FR-KDS-040/023/022 honestly PARTIAL; confirmed **MIGRATION REQUIRED: NO** (permissions are application-seeded, not DDL); verdict **B — implementation ready after two narrow ratifications** (`kds.operate` permission vocabulary + role mapping, since SRS §15.2 has no KDS code and its designated Appendix C is absent; and the recall→Sales cross-module consequence, since `ticket.recalled` is absent from §5.5.4's self-declared "Core Subset") |
| 2026-08-31 | KDS operator lifecycle — final implementation acceptance correction (6 blockers) | [2026-08-31_KDS_operator-lifecycle-acceptance-correction.md](2026-08-31_KDS_operator-lifecycle-acceptance-correction.md) | **A. CLEAN — READY FOR FINAL ACCEPTANCE** — narrow correction of 6 external-review blockers against the accepted implementation report (not modified); no migration, no schema change, no governance edit, no commit, no push | `121b889` (unchanged) | `feat/production-spec` | 34 (unchanged — no migration created; scratch DB applied all 34 cleanly from zero, dropped after verification) | Unit **789/789** (58/58 suites); module-boundaries **42/42** (38→42, zero new `KNOWN_DEVIATIONS` entries); full e2e on a **fresh from-zero scratch DB** (`ros_scratch_test`, same Postgres instance, dropped after use, persistent `ros` DB untouched) **1014/1014, 51/51 suites, run twice** with **no pre-existing-dirty-DB exclusions** — `organisation.e2e-spec.ts`/`approval-runtime.e2e-spec.ts` both included and clean, confirming the Phase-1 report's dirty-DB attribution was correct; KDS suites within that total: authorization, first-viewed 10/10 (+4 amendment cases), concurrency 11/11 (rebuilt test H + new D0 guard), amendment (new) 2/2; `tsc`/lint clean for every file this task touched (1 pre-existing unrelated `tsc` error and pre-existing lint debt in untouched treasury/cash-session files reported, not fixed); OpenAPI regeneration idempotent (byte-identical), recall route present | **Blocker A** (KNOWN_DEVIATIONS growth risk): published `identity/contract/http.ts` and `governance/contract/audit.ts` as genuine thin public contracts so Kitchen never needs a `kitchen->identity`/`kitchen->governance` allow-list entry; strengthened the boundary test to assert Kitchen's violation list is structurally empty, not just three enumerated modules. **Blocker B** (audit ordering invalidated the SERIALIZABLE proof): reordered `bumpLineTx`/`bumpAllTx`/`recallTx` so the readiness-critical read and event publish run before `AuditService.record`; went further and reran the two-station race with the audit advisory lock removed entirely (no-op stub), which exposed and fixed a genuine third Prisma-7 driver-adapter serialization-error shape (`DriverAdapterError`/`.cause.originalCode`) the retry classifier had never handled — a real production defect found only because this task built a stronger proof than the minimum asked. **Blocker C** (amendment Fire into an already-bumped Ticket never reactivated it): extracted the ticket-aggregate projection into a shared `TicketProjectionService` and wired `OrderLineFiredHandler` to call it, gated strictly on `wasCreated` (idempotency-safe — a replay does not re-trigger it); also found and fixed a related latent bug where `readyAt`/`bumpedAt` were pinned to the *first* bump forever instead of refreshing on genuine re-transitions (recall-then-rebump and amendment-reactivate-then-rebump both affected). **Blocker D** (first-viewed didn't cover amendment lines on an already-viewed ticket): decoupled line-level from ticket-level stamping in `acknowledgeViewed`, scoping line stamps to every authorized ticket, not just newly-viewed ones; chose one audit entry per ticket with any newly-stamped fact (Option A), metadata distinguishing first-view from amendment-view. **Blocker E** (clean-DB verification): see Tests column — closed with zero exclusions. **Blocker F** (queue-index claim): `EXPLAIN (ANALYZE, BUFFERS)` proved the original code comment named the wrong index — both composite indexes share the same 3-column leading prefix and `status NOT IN (...)` is never sargable as an Index Cond under either, so the corrected comment states which index is actually used and why, while confirming the `branchId` fix itself (query-planning-level, not merely data-level, necessity) remains correct; classified CORRECTED, not VERIFIED. No deferred KDS feature touched, no new permission or event type, KDS-R11/KDS-R12 not reopened |
| 2026-08-31 | KDS operator lifecycle — final acceptance closure + source-control commit | [2026-08-31_KDS_operator-lifecycle-final-acceptance-closure.md](2026-08-31_KDS_operator-lifecycle-final-acceptance-closure.md) | **A. KDS FINAL ACCEPTANCE CLOSED — COMMITTED** (pending post-commit verification recorded in the same commit's chat response) — closure/source-control hygiene only; no redesign, no new product behaviour, no migration, no schema change, no governance edit | `121b889` (parent) | `feat/production-spec` | 34 (unchanged — no migration created or staged) | Relied on accepted evidence (unit 789/789, module-boundaries 42/42, e2e 1014/1014 ×2 on a from-zero scratch DB) plus fresh confirmation this session: `git diff --check` clean, `prisma validate` clean, module-boundaries 42/42, unit 789/789 — zero drift since the acceptance-correction report | Classified all 60 dirty working-tree paths A–H (zero Unknown): 35 KDS source, 12 KDS test, 2 KDS OpenAPI, 1 KDS ratified-governance-register section, 6 KDS report/INDEX (INDEX.md staged by constructed blob at exactly the 6 KDS rows, not whole-file, since its working-tree diff also carries 4 unrelated pre-existing report rows), 4 pre-existing unrelated reports correctly excluded and left untouched/unstaged. Re-verified all `§6` acceptance invariants (single `kds.operate` permission, zero `KNOWN_DEVIATIONS` growth, KDS auth/first-viewed/bump/serialization/multi-station/recall/amendment semantics, deferred scope still absent) directly against current code before staging. Staged explicitly by path (no `git add .`/`-A`/`-a`); ran the full staged-content hard-check list (no schema/migration, no `.env`, no credentials, no debug `console.*`, no `.skip`/`.only`, no weakened assertion, no unrelated formatting sweep) — all clean. Created exactly one normal commit, subject `feat: complete KDS operator lifecycle`, no amend/rebase/reset/force. Marks the KDS MVP Operator Lifecycle **FINAL ACCEPTED, SOURCE-CONTROL CLOSED** for Fire→queue→view→optional start→bump→Sales readiness→recall→Sales reversion including multi-station correctness and amendment reactivation; served/Expediter, post-fire cancellation, extra sort modes, colour engine, analytics, offline, peer discovery, 8h TTL and standard-role seeding remain explicitly deferred |
| 2026-08-27 | RENDER empty-DB demo provisioning check | [2026-08-27_RENDER_empty-db-demo-provisioning-check.md](2026-08-27_RENDER_empty-db-demo-provisioning-check.md) | COMPLETE — audit only, no code/migration/commit/execution | `9aa7a88` | `feat/production-spec` | 27 (unchanged — read-only task) | n/a — no code changed, no test run | Determined `src/scripts/seed-dev-data.ts` is additive-only (no DELETE/TRUNCATE), safe to re-run against the empty Render DB, and dials only `APP_DATABASE_URL` at runtime (`DATABASE_URL` only satisfies env validation); confirmed `identity.roles = 0` on Render is expected (no migration seeds roles; only the seed script's `RolesService.createTenantRole` call does, and it has never run there); gave the exact one-time provisioning command, read-only verification SQL, and expected `POST /auth/login` request — did not execute it |
| 2026-08-21 | P1D-1 / P1E gate | [2026-08-21_P1D1_acceptance-and-fire-kds-gate.md](2026-08-21_P1D1_acceptance-and-fire-kds-gate.md) | COMPLETE (Phase A corrected & verified; Phase B analysis-only, no implementation) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | 1153 passing, 0 skipped/todo | Corrected 4 P1D-1 acceptance defects (CashSession read authority, Workforce contract boundary, FR-SEC-028 classification, P1D-G consistency check) and produced the Fire/KDS source & architecture gate (routing matrix, Kitchen data model audit, event/module architecture gate, Fire API design, Payment readiness reassessment) |
| 2026-08-21 | P1E-1 domain-event/UoW foundation | [2026-08-21_P1E1_transactional-domain-events-uow-foundation.md](2026-08-21_P1E1_transactional-domain-events-uow-foundation.md) | COMPLETE (infrastructure + contracts + verification; no business event published) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | 1183 passing, 0 skipped/todo | Built the SRS §5.5.2 transaction-aware in-process domain-event mechanism (collector, dispatcher, Unit of Work) proven against real PostgreSQL, plus `contract/` publishing `order.line.fired` (Sales) and `ticket.bumped` (Kitchen) — no Fire, no Kitchen persistence, no Payment, no outbox |
| 2026-08-21 | P1E-1A registration/context correction | [2026-08-21_P1E1A_event-registration-context-correction.md](2026-08-21_P1E1A_event-registration-context-correction.md) | COMPLETE (P1E-1 acceptance corrected; system-wide §5.5.2 adoption still NOT claimed) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | 1204 passing, 0 skipped/todo | Corrected 2 P1E-1 acceptance defects: added a production Nest-DI handler registration path (`@DomainEventHandler` + `DomainEventHandlerRegistry`, proven via real PostgreSQL) and bound event `tenantId` to the trusted Unit-of-Work `AuthScope` (impossible to override, proven); also brought the envelope's `occurredAt`/`recordedAt`/`businessDay` into line with SRS §5.1 driver 7's network-ready-contract requirement |
| 2026-08-21 | P1E-1B causal context — final correction | [2026-08-21_P1E1B_causal-event-context-final-correction.md](2026-08-21_P1E1B_causal-event-context-final-correction.md) | COMPLETE — P1E-1 FINAL ACCEPTANCE claimed YES here, superseded by P1E-1C's own bypass finding below (system-wide §5.5.2 adoption still NOT claimed) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | 1221 passing, 0 skipped/todo | Corrected 3 remaining P1E-1/1A defects: moved the low-level `createDomainEvent` constructor to `common/domain-events/internal/` and added a mechanically-enforced architecture test barring `src/modules/**` from importing it; made `correlationId` bind once per `UnitOfWork.execute()` call instead of once per event (ties the causal chain, not the individual event); made `causationId` required/non-null, defaulting to the operation's own command identity for root events and overridable to a parent event's `eventId` for an explicit child event; withdrew an unsupported claim that event and HTTP idempotency keys must differ |
| 2026-08-21 | P1E-1C trusted publication boundary — actual final correction | [2026-08-21_P1E1C_trusted-event-publication-boundary.md](2026-08-21_P1E1C_trusted-event-publication-boundary.md) | COMPLETE — **P1E-1 FINAL ACCEPTANCE: YES** (system-wide §5.5.2 adoption still NOT claimed) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | 1230 passing, 0 skipped/todo | Closed the one bypass P1E-1B left open: `UnitOfWorkContext` still exposed the raw `DomainEventCollector` as `ctx.events`, whose `.record()` accepts any object shaped like the (legitimately public) `DomainEventEnvelope` type — business code could hand-build a fake envelope and queue it directly, skipping all trust binding. Replaced the two-step `createEvent()`+`events.record()` API with one `ctx.publishEvent(...)` operation that constructs and enqueues atomically; removed `events`/`createEvent` from the public `UnitOfWorkContext` entirely (moved to an `internal/`-only `InternalUnitOfWorkContext`); extended `trusted-construction-boundary.spec.ts` to mechanically bar `src/modules/**` from importing `DomainEventCollector` or using the raw `.events.record(` pattern, each with a self-test proving the detector fires |
| 2026-08-21 | P1E-2 KDS routing design closure (analysis only) | [2026-08-21_P1E2_kds-routing-design-closure.md](2026-08-21_P1E2_kds-routing-design-closure.md) | COMPLETE — semantics/persistence/tenancy design CLOSED; **MIGRATION-READY: NO** (2 schema prerequisites + 2 ratification points) | `e5648fb` | `feat/production-spec` | 20 (unchanged) | n/a — no code changed, no test run | Closed FR-KDS-001/010/011: validated 10 engineering directions (9 VALID, 1 NEEDS REVISION — "Kitchen-owned" branch KDS config conflicts with ratified ADR 0008 D-07); fixed five-tier first-match-wins semantics with distinct-union-within-tier; identified the modifier selector as `catalogue.Modifier` and proved a tenant-safe FK to it is currently impossible; specified migration-ready shapes, FKs, CHECK/UNIQUE constraints, indexes and RLS for `station_routing_rules`, a new `branch_kds_config`, and a new `order_line_station_overrides`; established routing resolves at Fire time (BR-POS-004 excludes it from the snapshot list) with runtime-only provenance (no FR-KDS analogue to FR-POS-042); recorded the governance D-xx namespace collision between the register and ADR 0008 |
| 2026-08-21 | P1E-3 KDS routing persistence + resolver | [2026-08-21_P1E3_kds-routing-persistence-and-resolver.md](2026-08-21_P1E3_kds-routing-persistence-and-resolver.md) | COMPLETE (persistence + contract + resolver layers); **FR-KDS-010 system-wide: PARTIAL** (no Fire caller, no Ticket state yet); FR-KDS-001/011 (persistence layer) COMPLETE — **superseded by P1E-3A's contract/implementation correction below** | `e5648fb` | `feat/production-spec` | 23 (+3 new, all applied from zero on a fresh scratch DB) | 690 unit + 567 e2e, all passing (0 new failures; local dev DB confirmed untouched) | Implemented P1E-2's accepted design: `catalogue.modifiers` tenancy + tenant-safe FK (Catalogue prerequisite); Sales K-1 additive indexes + composite FK to Catalogue modifiers; `org.stations.display_colour`; hardened `kitchen.station_routing_rules` (tenant_id, modifier_id, exactly-one-selector CHECK, `NULLS NOT DISTINCT` unique, tenant-safe FKs); new `kitchen.branch_kds_config` (Organisation-owned, ADR 0008 D-07); new `sales.order_line_station_overrides` (K-1 partition-safe FK chain, FR-KDS-011); new `organisation/contract` transaction-aware `RoutingConfigQuery`; new private Kitchen `RoutingResolverService` implementing FR-KDS-010's R1–R7 (5-tier precedence, multi-station union, multi-category conflict detection, typed `ROUTING_CONFIGURATION_CONFLICT`/`ROUTING_NO_DESTINATION` errors, runtime-only provenance); 3 new module-boundary architecture-test assertions (zero new deviations); no Fire, no Ticket, no Payment, no Outbox — next slice recommended: Ticket/TicketLine persistence design |
| 2026-08-22 | P1E-3A routing contract/implementation boundary correction | [2026-08-22_P1E3A_routing-contract-implementation-boundary.md](2026-08-22_P1E3A_routing-contract-implementation-boundary.md) | COMPLETE — **P1E-3 FINAL ACCEPTANCE: YES** | `e5648fb` | `feat/production-spec` | 23 (unchanged — no new/edited migration, schema.prisma untouched) | 693 unit + 574 e2e, all passing (+3 unit, +7 e2e vs P1E-3; local dev DB confirmed untouched) | Confirmed acceptance review's defect: `organisation/contract/routing-config.query.ts` contained the concrete `@Injectable()` Prisma-backed query service, not just its interface — a public `contract/` directory acting as a private implementation, violating SRS §5.4. Split it: the public contract is now interface + `ROUTING_CONFIG_QUERY` token + DTOs only; the Prisma implementation moved to a new private `organisation/routing-config/routing-config.query.service.ts`, bound via `useExisting` in `OrganisationModule`; Kitchen's resolver now injects the token (`@Inject(ROUTING_CONFIG_QUERY)`) instead of the concrete class — R1–R7 routing semantics byte-for-byte unchanged (all 15 pre-existing resolver unit tests pass unmodified). Added a behavioural (not textual) "persistence implementation in contract/" detector to `module-boundaries.spec.ts`, self-tested against a fabricated bad example, plus assertions that the private implementation exists outside `contract/` and Kitchen never imports it; new e2e suite proves the split preserves the same-transaction guarantee (exactly one `$transaction` call), RLS enforcement independent of the WHERE clause, fail-closed missing-context behaviour, and an unchanged DTO result shape |
| 2026-08-22 | P1E-4 Ticket/TicketLine architecture + persistence design closure (analysis only) | [2026-08-22_P1E4_ticket-ticketline-design-closure.md](2026-08-22_P1E4_ticket-ticketline-design-closure.md) | COMPLETE — design CLOSED (all 30 DoD questions answered); **MIGRATION-READY: NO** (one blocking upstream prerequisite, GAP-K1) | `e5648fb` | `feat/production-spec` | 23 (unchanged — no migration, no schema edit, no code) | n/a — no code changed; design gate only | Closed the Ticket/TicketLine architecture: cardinality **one Ticket per (tenant, order, business_day, station)** with one TicketLine per station Ticket (SOURCE-DECIDED from the Glossary + FR-KDS-028, reconciling FR-POS-038's "amendment ticket" with FR-KDS-028's "never as a new ticket"); amendment persistence via a `kitchen.ticket_fire_batches` child table (per-ticket `sequence_no`, Sales-minted `fire_batch_id` as the idempotency key), rejecting boolean and column-only alternatives; **self-contained snapshots — explicitly SUPERSEDING P1D-1's "a ticket needs no snapshot, it references" sketch** on SRS §24.8/§5.2.3/NFR-PERF-004/NFR-REL-003 grounds; relational `kitchen.ticket_line_modifiers` with a `kind` enum for FR-KDS-021; **final `order.line.fired` v1 payload** carrying routing inputs + full ticket header on every line event to eliminate event-order dependence; Kitchen mints Ticket ids (closing a P1D-1 open item); exact 4-table migration-ready design (columns classified SOURCE-REQUIRED / ENGINEERING-REQUIRED / DEFERRED, composite FKs, uniques, CHECKs, indexes, direct-anchor RLS); **the K-1 chain proving "Order X's Ticket + Order Y's OrderLine" unrepresentable needs no new Sales index — P1E-3's two additive uniques are exactly the targets**; state machine + all 7 FR-KDS-040 timestamps (bump sets `ready_at`+`bumped_at`, SOURCE-DECIDED from UC-KDS-01 step 5); bump/recall/cancellation semantics; per-column-class mutability replacing a blanket append-only claim (ADR-010 does not name Ticket); 51-test matrix. **Found GAP-K1: `catalogue.modifiers` has no FR-POS-021 [M] semantic kind, so FR-KDS-021 [M] is not implementable — a 3-statement prerequisite migration.** Also recorded: `sales.order_lines.item_name_snapshot` captures the customer-facing name, not `kitchen_names` (FR-MNU-005). Nothing implemented; resolves P1D-1 CONFLICT 1–5 |
| 2026-08-23 | P1E-5 Ticket/TicketLine persistence + Kitchen handler | [2026-08-23_P1E5_ticket-persistence-and-kitchen-handler.md](2026-08-23_P1E5_ticket-persistence-and-kitchen-handler.md) | COMPLETE — all 28 DoD items satisfied; **FR-POS-021/FR-KDS-021: PARTIAL** (18 pre-existing modifiers remain unclassified, by design — no fabricated backfill); FR-KDS-010/011/020/024/025/028/029/040: PARTIAL/NOT IMPLEMENTED per explicit non-narrowing classification | `e5648fb` | `feat/production-spec` | 26 (+3 new, all applied from zero on a fresh scratch DB) | 702 unit + 605 e2e, all passing (local dev DB confirmed untouched — 11 unapplied migrations, sentinels unchanged) | Implemented P1E-4's design with 4 acceptance corrections: (A) `catalogue.modifiers.kind`/`sales.order_line_modifiers.kind_snapshot` added NULLABLE with ZERO semantic backfill — investigated all 18 pre-existing rows (fixture residue, all literally "Chicken instead of Beef"), found no non-heuristic source for any, reported exact ids rather than inferring from name/price/recipe; new writes require `kind` (DTO `@IsEnum`, no `@IsOptional`); (B) `kitchen.ticket_fire_batches` has NO `sequence_no` allocator — `fire_batch_id` (Sales-minted) is the sole idempotency key, `ORDER BY fired_at, id` for display; (C) `kitchen.tickets.order_type_snapshot` is a Kitchen-owned `VARCHAR(32)`, not `sales."OrderType"`; (D) `branch_kds_config.recall_window_seconds DEFAULT 1800` (source-supported) vs `cancelled_line_visibility_seconds` nullable with NO invented default; new required correction: `kitchen.ticket_line_modifiers`'s idempotency key is `source_order_line_modifier_id` (the stable Sales row id), not `modifierId` alone (FR-MNU-011 allow-repeat). Built full `kitchen.tickets`/`ticket_fire_batches`/`ticket_lines`/`ticket_line_modifiers` schema (composite FKs prove "Order X's Ticket + Order Y's Line" unrepresentable; needs no new Sales index — P1E-3's K-1 uniques are exactly the targets), the final v1 `order.line.fired` contract (ticket header repeated on every line event to eliminate event-order dependence), a private transactional `OrderLineFiredHandler` (registered in `AppModule` for the first time; uses the accepted `RoutingResolverService`; check-then-insert idempotency proven safe by the dispatcher's sequential-processing guarantee; immutable-header mismatch fails closed via a typed error, never silently overwrites), and a self-contained `TicketReaderService` (functionally proven to touch zero Sales/Catalogue tables by revoking `ros_app`'s grants on both schemas and confirming the read still succeeds). 5 new module-boundary architecture-test assertions (zero new deviations). 31 new e2e tests. Reported two open Fire-producer prerequisites (Catalogue `categoryIds`/`kitchen_names` facts; no `serviceReference` source for non-dine-in orders) without silently solving either via a new private-table import. No Fire HTTP, no bump/recall routes, no Payment/Completion/Outbox, no governance edit — next slice: Fire authorization decision + Sales Fire command implementation |
| 2026-08-23 | P1E-5A Postgres conflict-safe Kitchen idempotency correction | [2026-08-23_P1E5A_postgres-conflict-safe-idempotency.md](2026-08-23_P1E5A_postgres-conflict-safe-idempotency.md) | COMPLETE — **P1E-5 FINAL ACCEPTANCE: YES** | `e5648fb` | `feat/production-spec` | 26 (unchanged — no new/edited migration, schema.prisma untouched) | 702 unit + 613 e2e, all passing (+0 unit, +8 e2e vs P1E-5; local dev DB confirmed untouched) | Confirmed acceptance review's defect: `TicketPersistenceService`'s four idempotent creation methods caught Prisma's `P2002` unique-violation exception and then issued a further query on the SAME PostgreSQL transaction to fetch the winning row — but Postgres marks a transaction ABORTED the instant any statement raises a real DB error, and Prisma's interactive `$transaction` does not auto-wrap queries in `SAVEPOINT`s, so the "recovery" query would itself fail with `25P02 current transaction is aborted`. Proven mechanically with a minimal two-transaction psql harness (with vs without an explicit `SAVEPOINT`). P1E-5's own sequential tests never caught it because each replay opened its own fresh transaction, always hitting the early `if (existing) return` branch instead of the broken catch path. Fixed all four methods (`kitchen.tickets`, `ticket_fire_batches`, `ticket_lines`, `ticket_line_modifiers`) to use parameterized `INSERT ... ON CONFLICT (<natural key>) DO NOTHING RETURNING ...` (Prisma tagged-template `$queryRaw`/`$executeRaw`, explicit Postgres type casts, no string interpolation) — the expected race resolves inside the statement itself with zero exception raised, so the losing transaction is never poisoned; added the narrowest symmetric immutable-snapshot check to `TicketLine` (mirroring the existing Ticket-header check, no schema change). New `kitchen-ticket-concurrency.e2e-spec.ts` (8 tests) proves genuine two-connection, barrier-synchronized PostgreSQL races for all four natural keys, that a losing transaction remains fully usable afterward (a follow-up statement succeeds), that a genuine header mismatch still fails closed with `TicketHeaderMismatchError`, and that a genuine FK violation still aborts/rolls back rather than being mistaken for idempotency. No schema redesign, no new migration, no retry framework around `UnitOfWork`, zero new module-boundary deviations, no Fire, no Payment, no governance edit, no commit — next: repository checkpoint / commit / push (not performed) |
| 2026-08-23 | Repository checkpoint through P1E-5A | [2026-08-23_REPOSITORY_checkpoint-through-P1E5A.md](2026-08-23_REPOSITORY_checkpoint-through-P1E5A.md) | COMPLETE — committed `2e21aeb`, pushed to `origin/feat/production-spec`, remote hash confirmed equal to local HEAD | `e5648fb` → `2e21aeb` | `feat/production-spec` | 26 (unchanged — audit-only, no schema/migration edit) | Cited from P1E-5A: 702 unit + 613 e2e passing, 8/8 real concurrency tests; re-verified in this task: `git diff --check` clean, `prisma validate` clean, `module-boundaries`+`trusted-construction-boundary` 27/27, `kitchen-ticket-concurrency` 8/8 | Full working-tree audit before the first commit since `e5648fb`: classified every one of 45 modified + 166 untracked paths (INTENDED PROJECT CHANGE / INTENDED REPORT-GOVERNANCE EVIDENCE / PROJECT REFERENCE MATERIAL — no UNKNOWN, no LOCAL/GENERATED/SECRET file); confirmed `.env`/`node_modules`/`dist`/`src/generated`/Claude local-settings are all `.gitignore`d and excluded from `git add -A`; content-scanned every intended file for credential patterns — found only pre-existing e2e test-fixture placeholder passwords (`'s3cure-passphrase'`, `'hunter2'`), no real secret; confirmed zero file deletions in the diff. One consolidated checkpoint commit (211 files: 45 modified, 166 added) pushed without force to the existing `origin/feat/production-spec` upstream; remote hash verified to match local HEAD via `git ls-remote` (both `2e21aeb36ccad480ec026108113c1126390edc07`). Follow-up documentation-only commit filled in this report's post-push evidence section (was shipped as a forward-looking placeholder in the main commit) and recorded that `ROS_SRS_v1.0.pdf`/`PHASE-15.md` — flagged post-push as reference material that should have been excluded — could not be retroactively removed without amend/force-push, both explicitly forbidden; the exclusion decision was handed to the user rather than resolved unilaterally. No feature work, no refactor, no Swagger/OpenAPI, no Fire, no Payment performed |
| 2026-08-23 | API — Swagger/OpenAPI frontend contract | [2026-08-23_API_swagger-openapi-frontend-contract.md](2026-08-23_API_swagger-openapi-frontend-contract.md) | COMPLETE — 131/131 live operations documented, 0 fabricated endpoints | `ff589fa` (unchanged) | `feat/production-spec` | 26 (unchanged — audit-only, `prisma migrate deploy` applied 11 pre-existing/already-committed migrations to catch up the local scratch DB, zero new) | 702 unit + 630 e2e passing (+0 unit, +17 e2e — new `openapi.e2e-spec.ts`) | Generated `docs/api/openapi.json`/`.yaml` (93 paths, 131 operations, 71 component schemas) solely from `@nestjs/swagger` metadata via a new `src/scripts/generate-openapi.ts` (`npm run openapi:generate`, requires `nest build` first for the newly-configured CLI plugin) and shared `src/swagger.config.ts`; audited and fixed the one zero-decoration route (`POST /auth/pin`); added inline response-body JSON-Schema (`src/common/openapi/schema-helpers.ts`) to all 12 controllers, verified field-by-field against actual view/factory functions (never Prisma/SRS) — 119/131 operations carry a body schema (100% of routes that return one; the other 12 are genuine 204s); documented the 3 real `Idempotency-Key` and 2 real `If-Match` routes with their actual header semantics, catching and fixing a real duplicate-parameter bug (case-mismatch between `@Headers()` and `@ApiHeader`); proved generation is deterministic (3 runs, identical SHA-256); new `test/openapi.e2e-spec.ts` mechanically checks $ref resolution, zero duplicate `operationId`s, security-metadata completeness, required-header presence, required-DTO-field preservation, and bidirectional live-route/document drift via Express-router introspection. Confirmed zero Fire/Payment/Completion/refund/bump/recall/Kitchen routes exist or were fabricated. No business logic, DTO validation, schema, or permission changed; no commit, no push — next: Fire authorization decision + Sales Fire command |
| 2026-08-23 | API-1A — OpenAPI 3.1 + external base path + error contract verification | [2026-08-23_API1A_openapi31-basepath-error-contract.md](2026-08-23_API1A_openapi31-basepath-error-contract.md) | COMPLETE — narrow correction on the accepted Swagger slice | `ff589fa` (unchanged) | `feat/production-spec` | 26 (unchanged — read-only `prisma migrate status` only, no migration applied) | 702 unit passing (unchanged); `openapi.e2e-spec.ts` grew 17→31, all passing; full e2e not re-run (no runtime behaviour changed, only Swagger metadata/generation) | Corrected document to real OpenAPI **3.1.0** (`DocumentBuilder.setOpenAPIVersion`, native `@nestjs/swagger@11` support) and structurally transformed all 330 `nullable:true` occurrences to JSON Schema 2020-12 (`type:[X,"null"]`, enum+null, $ref→anyOf-ready) via a new `src/common/openapi/oas31.util.ts` post-process shared by `main.ts` and the generator — zero `nullable` keyword remains, verified valid 3.1 against the official meta-schema (`@seriousme/openapi-schema-validator`, new dev-only dep) and parseable by real frontend tooling (`openapi-typescript`, not installed/committed). Added a reusable `ErrorResponse` component (`{statusCode, message: string\|string[], error?}`, truthfully matching Nest's actual default `HttpException` envelope — confirmed via live HTTP probes, not RFC 7807) auto-filled onto all 392 documented 400/401/403/404/409/422/429 responses with no prior body schema. Investigated and classified SRS §26.1 `/v1` versioning as NOT IMPLEMENTED (app) / NOT REPOSITORY-DECIDABLE (deployment — no proxy/infra config exists) rather than guessing; added a truthful `servers: ["/"]` entry instead. Classified FR-API-001/002 NOT IMPLEMENTED, FR-API-020 PARTIAL (3/72 mutating routes), FR-API-021/022/023 IMPLEMENTED (for those 3), X-Correlation-Id NOT IMPLEMENTED, rate-limit headers IMPLEMENTED-but-undocumented — report only, nothing fixed. Corrected two prior-report evidence errors: confirmed `git stash` WAS used in the prior session (contradicting that report's own "no destructive command" claim) with no residual stash and no data loss; confirmed the prior session's `prisma migrate deploy` targeted the single persistent local-dev DB, not a separate "scratch" DB (no second DB target exists in this repo). Zero controller files touched; 131/131 operations, all security/header/wire-format documentation unchanged. No Fire, no Payment, no migration, no `git stash`/`reset`/`checkout`/`restore`/`clean`/`rebase`, no commit, no push |
| 2026-08-24 | P1E-6 — Sales Fire command + transactional Kitchen integration | [2026-08-24_P1E6_sales-fire-command.md](2026-08-24_P1E6_sales-fire-command.md) | COMPLETE — first real Sales producer of `order.line.fired`, explicit Fire only | `01c0b0f` (unchanged) | `main` (renamed from `feat/production-spec` outside this session; content identical, `origin/main` == `origin/feat/production-spec`) | 26 (unchanged — permission is code/upsert-driven, not migration-driven; confirmed repository-wide, not just Sales) | 708 unit passing (+6, module-boundaries extension); e2e 668/669 (+39 net incl. 25 new Fire tests; the 1 failure is `organisation.e2e-spec.ts`'s location-registry check, proven pre-existing/unrelated — orphan branches from untouched `kitchen-ticket-*.e2e-spec.ts` fixtures dated before this slice began) | Ratified `pos.order.fire` recorded as an unnumbered "Fire Authorization Ratification — 2026-08-24" entry (no D-21); implemented the permission code (no migration — no module's permission catalog has ever had a production seed path, a pre-existing repo-wide gap, not fixed here); built `SalesFireService` (`POST /orders/{businessDay}/{id}/fire`, Idempotency-Key + If-Match required, `pos.order.fire`-guarded) reusing `order-state.ts`'s existing `assertMayFire`/`assertTransition`/`assertVersion` verbatim; first Fire DRAFT→OPEN + `order.opened` (new, narrowest-possible Sales event contract — "opened" is source-confirmed to mean the state-machine transition, not order creation), every Fire publishes one already-fully-specified P1E-5 `order.line.fired` per newly-fired line inside ONE `UnitOfWork` transaction Kitchen's existing handler consumes synchronously (zero Kitchen/routing/ticket-persistence files touched); added two narrow public Fire-facts contracts mirroring the P1E-3A pattern — `catalogue/contract` (categoryIds + Kitchen/KDS name, batched) and `organisation/contract` (dine-in Table `label`, the first-ever `sales->organisation` edge, public from the start) — with zero expansion of the one pre-existing `sales->catalogue` deviation; found and fixed a REAL lost-update race (the repo-wide `assertVersion`-then-plain-`UPDATE` convention has no atomic version guard) scoped narrowly to Fire's own write via `updateMany({where:{...,version}})`, proven safe under a genuine concurrent HTTP race; discovered and honestly documented (not fixed, out of scope) that Fire's mandated bodyless request means the existing idempotency fingerprint mechanism can't distinguish two different orders sharing one Idempotency-Key; proved all 5 FR-KDS-010 routing tiers + multi-station + conflict-rollback + no-destination-rollback + amendment-Fire-reuses-Ticket through the REAL producer, not the resolver in isolation; OpenAPI regenerated to 132 operations, 3.1 valid, exactly one documented Fire route. No Payment, no Completion, no auto-Fire, no schema/migration change, no commit, no push — next: Payment MVP (cash + manual external card capture) |
| 2026-08-24 | P1E-6A — Fire acceptance correction | [2026-08-24_P1E6A_fire-acceptance-correction.md](2026-08-24_P1E6A_fire-acceptance-correction.md) | COMPLETE — 7 acceptance defects corrected; P1E-6 core Fire architecture NOT reopened | `01c0b0f` (unchanged, no commit) | `main` (unchanged, unresolved discrepancy recorded read-only, no branch operation performed) | 26 (unchanged) | 708 unit passing (unchanged); e2e 679/679 (+10 net vs P1E-6) on a clean-from-zero scratch DB (`ros_p1e6a_scratch`, dropped after verification) — 678/679 on the persistent dev DB, the 1 failure being the pre-existing historical orphan-location pollution Defect/§6 fixes going forward (10 rows predating this session, not deleted per instruction) | Fixed the shared idempotency interceptor's fingerprint to hash the resolved request path instead of the registered Express route pattern (one line), closing a real cross-order replay collision on Fire/line-add with zero idempotency-service signature change and a documented fail-closed stored-key compatibility assessment; added a narrow `IllegalFireSourceStateError` guard so Fire is refused (422, full rollback) from any state other than draft/open (held/parked previously silently proceeded) without touching `order-state.ts`'s general state machine; added `UnresolvedServiceReferenceError` so a dine-in order with an unresolvable table fails closed instead of silently defaulting `serviceReference` to null — and discovered, via the real `orders_branch_id_table_id_fkey ON DELETE RESTRICT` constraint, that this scenario is actually unreachable through any real code path today, so the guard is verified defense-in-depth via a test-only DI seam on the existing `TABLE_DISPLAY_QUERY` contract, not a fix for a reachable bug; corrected the P1E-6 report's standard-role framing (permission+enforcement YES, shipped standard-role grant NO, pre-existing FR-SEC-010 gap) and gave the dev seed script's Cashier role `pos.order.fire` as a local-only convenience, explicitly not claimed as satisfying FR-SEC-010; built a new deterministic two-independent-transaction concurrency proof (`sales-fire-concurrency.e2e-spec.ts`) using a barrier injected through the existing `CATALOGUE_FIRE_FACTS_QUERY` seam (no new production hook), run 5/5 clean; fixed both Kitchen concurrency/persistence e2e files' branch fixtures to also create the matching `org.locations` row, verified complete via a 100%-passing full suite (679/679, 32/32 suites) on a from-zero migrated scratch database — which also surfaced and fixed a real test-harness mistake (the app's own `PrismaService` reads `APP_DATABASE_URL`, not `DATABASE_URL`, so both must be overridden together for a clean-DB run to be valid). No Payment, no Completion, no auto-Fire, no schema/migration change, no commit, no push, no branch operation — next: branch/checkpoint resolution, then Payment MVP |
| 2026-08-24 | Repository checkpoint — Fire branch resolution, commit, push | [2026-08-24_P1E6_CHECKPOINT_fire.md](2026-08-24_P1E6_CHECKPOINT_fire.md) | COMPLETE — committed `1ed3521`, pushed to `origin/feat/production-spec`; `origin/main` untouched | `01c0b0f` → `1ed3521` | `feat/production-spec` (recreated locally this session via `git switch -c --track`, since the prior local branch of that name had been externally renamed to `main`; `origin/main`/`origin/feat/production-spec` verified identical, `01c0b0f`, before any operation) | 26 (unchanged) | Cited from P1E-6A: 708 unit + 679 e2e (32/32 suites, 100%) on a clean scratch DB; re-verified in this task: 204/204 focused e2e (Fire, concurrency proof, idempotency-touching suites, OpenAPI) + 23/23 module-boundary unit tests, `prisma validate` clean, `openapi:check` zero drift (132 operations, 3.1.0) | Verified `origin/main` and `origin/feat/production-spec` both still pointed at the accepted Fire baseline before touching anything; recreated the local feature branch (no `checkout`, no `reset`) with the working tree proven byte-identical before/after; explicitly staged only the accepted P1E-6/P1E-6A scope (30 files) while deliberately leaving three pre-existing, Fire-unrelated local changes untouched (`.gitignore`, `src/main.ts`'s user-authored CORS change, and `src/scripts/seed-dev-data.ts` — the latter excluded in full rather than hunk-isolated, since it has zero prior git history and true hunk isolation was not mechanically possible); one commit (`1ed3521`, no amend), pushed with a plain `git push` (no force) to `origin/feat/production-spec` only; confirmed `origin/main` remained at the old hash after push. No Payment, no new feature, no destructive git command, no PR |
| 2026-08-24 | P1F-1 — Payment MVP: partial CASH + manual/external card capture | [2026-08-24_P1F1_payment-mvp-partial-capture.md](2026-08-24_P1F1_payment-mvp-partial-capture.md) | COMPLETE for explicit MVP scope — full/over-settlement deliberately gated pending Completion | `a095bb1` (unchanged, no commit) | `feat/production-spec` (unchanged; HEAD one commit ahead of the task's stated `1ed3521` baseline — confirmed to be only the prior task's own already-reported docs-only follow-up commit, not drift) | 27 (+1: `20260824100000_sales_order_payment_capture`) | 718 unit passing (+10 vs P1E-6A); e2e 726/726 (+47: 46 new `sales-payment.e2e-spec.ts` + 1 new `sales-payment-concurrency.e2e-spec.ts`) on a clean-from-zero scratch DB (`ros_p1f1_scratch`, dropped after verification, run twice for confirmation — 100% both times; one earlier run showed 8 transient unrelated `pin.e2e-spec.ts` hook-timeout flakes, ruled out via isolated re-run) | Built the first callable Payment slice per the ratified P1D-B..G carried items: append-only `sales.order_payments` (RLS enable+force, `ros_app` SELECT+INSERT only, UPDATE/DELETE revoked and proven via the real non-bypass `ros_app` connection with positive controls) with tenant-safe composite FKs to Order/Employee and a deliberately-tenant-only (not branch-safe, to avoid an unrequested Treasury-owned migration) FK to CashSession; CASH and MANUAL_EXTERNAL_CARD tenders only (FR-POS-060 partial), no PaymentAttempt for either (P1D-C); resolved SRS §24.2.4's own reference settlement pseudocode plus the finding that no comp/discount mechanism exists anywhere in the codebase to ground the §14 full/over-settlement safety gate as `paid_total >= grand_total` (not a guess) with `rounding_adjustment` correctly excluded from the threshold; cash rounding via exact-bigint `divideRounded`, persisted per-payment (P1D-D), never touching `paid_total`; a new first-ever `sales -> treasury` public contract (`CASH_SESSION_FACTS_QUERY`) validating P1D-G attribution (branch/employee/terminal/currency/open-status) entirely at the service layer; reused Sales' existing unchanged `sales -> localisation` deviation for the order's PINNED country pack rather than building a redundant new contract; atomic Order CAS (`updateMany` + `version` predicate) sharing one transaction with the append-only Payment insert (fixed a real `RETURNING *` snake_case-vs-camelCase bug found via a failing test, corrected to explicit column aliases matching `TicketPersistenceService`'s established pattern) and with audit (`PAYMENT_CAPTURED`); permanent-Payment-identity idempotency (FR-OFF-015) layered under the existing P1E-6A-corrected HTTP Idempotency-Key mechanism, unmodified; a new deterministic two-independent-transaction concurrency proof (barrier injected through the `CASH_SESSION_FACTS_QUERY` seam, zero production hook) proving the settlement-race case specifically (both sides individually partial, together over-settling, exactly one CAS winner), run 9/9 clean; updated three pre-existing "Payment does not exist" test assertions (`sales.e2e-spec.ts`, `cash-session.e2e-spec.ts`, `openapi.e2e-spec.ts`) to the new truthful reality, the same pattern P1E-6 itself established for Fire; a real test-authoring arity bug caught only by `tsc --noEmit` (not `eslint`), fixed. No Completion, no inventory depletion, no COGS, no receipt, no integrated-terminal lifecycle, no refunds/voids, no commit, no push |
| 2026-08-25 | P1F-1A — Payment acceptance correction | [2026-08-25_P1F1A_payment-acceptance-correction.md](2026-08-25_P1F1A_payment-acceptance-correction.md) | COMPLETE — 2 acceptance defects corrected; P1F-1 Payment MVP core NOT reopened/redesigned | `a095bb1` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27 (unchanged — `20260824100000_sales_order_payment_capture` corrected in place, confirmed still uncommitted before editing, no migration 28) | 722 unit passing (+4, module-boundaries extension); e2e 731/731 (+5: `sales-payment.e2e-spec.ts` 46→51 structural FK tests) on a clean-from-zero scratch DB (`ros_p1f1a_scratch`, recreated fresh and re-verified a second time, dropped after verification); concurrency 3/3 clean | Corrected Defect A: built Localisation's first public `contract/` (`PINNED_PAYMENT_POLICY_QUERY`, 4 payment-relevant facts only, deliberately synchronous to match the real in-memory `CountryPackService.requirePinned()` implementation) so `SalesPaymentService` no longer imports the private `country-pack/country-pack.service` path — rejecting P1F-1's own "zero new deviation" reasoning (an existing private-import deviation is debt, not a public API; a new consumer relying on it expands the real violation even with an unchanged `KNOWN_DEVIATIONS` shape) — proven via 4 new module-boundary tests, zero `KNOWN_DEVIATIONS` growth, `OrderLinesService`'s own pre-existing debt deliberately left unrepaired. Corrected Defect B: both `order_payments` FKs strengthened to EXISTING composite targets with no new indexes — Order FK now branch-inclusive via the P1E-3 `uq_orders_tenant_id_business_day_branch` target, Terminal FK now branch-safe via the ADR 0008 D-16 `identity.terminals(branch_id, id)` target `treasury.drawers` already uses — corrected in place in the still-uncommitted P1F-1 migration (not a new migration), verified via direct `pg_constraint` inspection plus 5 new structural FK tests using the migrator/admin client (permitted only for this; the existing RLS proof using the real `ros_app` connection was left completely unweakened/unmocked/unskipped). CashSession FK deliberately left unchanged (no branch-safe target exists on `treasury.cash_sessions`; a new one would be an unrequested Treasury migration) with explicit reasoning recorded. Found and honestly fixed a genuine regression the FK hardening surfaced: a pre-existing test had silently relied on the old weak Terminal FK to let a branch-A token pay a branch-U order, now correctly rejected. Full regression sweep 303/303, `tsc --noEmit` clean (only the known pre-existing baseline error), eslint clean, `prisma validate` clean, `git diff --check` clean, OpenAPI unchanged at 133 operations/3.1.0/zero drift, persistent dev DB proven untouched. No P1F-2, no Completion, no schema/migration beyond the two named FK corrections, no commit, no push |
| 2026-08-25 | Repository checkpoint — Payment commit, push | [2026-08-25_P1F1_CHECKPOINT_payment.md](2026-08-25_P1F1_CHECKPOINT_payment.md) | COMPLETE — committed `4b240b3`, pushed to `origin/feat/production-spec`; `origin/main` untouched | `a095bb1` → `4b240b3` | `feat/production-spec` (unchanged, no divergence before push) | 27 (unchanged) | Cited from P1F-1A: 722 unit + 731 e2e (34/34 suites, 100%) on a clean scratch DB; re-verified in this task: 57/57 focused unit (module-boundaries, order-state) + 83/83 focused e2e (Payment, Payment concurrency, OpenAPI) on a fresh from-zero scratch DB (`ros_p1f1_checkpoint_scratch`, dropped after verification), `git diff --check`/`prisma validate` clean, `openapi:check` zero drift (133 operations, 3.1.0), persistent dev DB reconfirmed untouched | Staged only the accepted P1F-1+P1F-1A scope (31 files, explicitly enumerated, no `git add -A`) while leaving three pre-existing, Payment-unrelated local changes untouched (`.gitignore`, `src/main.ts`, `src/scripts/seed-dev-data.ts`); confirmed the migration `20260824100000_sales_order_payment_capture` (P1F-1A-corrected) was still genuinely uncommitted before staging, migration count remained 27; one commit (`4b240b3`, no amend), pushed with a plain `git push` (no force) to `origin/feat/production-spec` only; confirmed `origin/main` remained at `01c0b0f` after push, local HEAD matched remote exactly. No P1F-2, no implementation change, no destructive git command, no PR |
| 2026-08-25 | P1F-2 — Final Payment + Order Completion atomic orchestration (DESIGN GATE) | [2026-08-25_P1F2_completion-architecture-gate.md](2026-08-25_P1F2_completion-architecture-gate.md) | **BLOCKED — DESIGN/GOVERNANCE REQUIRED**; audit/design only, no implementation | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged; local HEAD == `origin/feat/production-spec`, `origin/main` untouched at `01c0b0f`) | 27 (unchanged — read-only gate, no migration created; 2 migrations *proposed* for a future slice) | No test run — architecture/design gate only; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e, 34/34 suites) | Audited all 15 completed-sale consequences against SRS §1.2's nine mandated atomic effects, UC-POS-01 §11–15, §5.5.2/§5.5.3, §24.2.4's `Order.complete()` reference implementation, §25.1 ownership and the ratified governance register. **Source-decided and settled:** completion is owned by **Sales** (§25.1 + §24.2.4 + §5.5.4 publisher); the API should be **Option A — the existing `POST …/payments` route completes the order atomically when the tender settles it** (ENGINEERING RECOMMENDATION, matching UC-POS-01 §11→§12; OpenAPI stays **133**, no new route); settlement is `paid_total >= grand_total − comp_total`, collapsing to `paid_total >= grand_total` since no comp mechanism exists, with rounding/`changeGiven` proven NOT to participate (§24.2.4 has no rounding term) and overpayment permitted (`>=`, BR-POS-002 forbids only under-settlement); **both** `open → completed` and `partially_paid → completed` are required verbatim by §24.2.4, with no intermediate state; `completed_at` + `closed_by` (FR-POS-007 [M]) set in the single CAS; **NO Treasury mutation** at completion — proven from §16.2's *"Cash Sales (system-computed)"* vs *"Pay-ins (recorded)"*, FR-FIN-004's derivability from existing Payment columns, P1D-B/G, and the decisive fact that **`treasury.cash_movements` does not exist** (treasury holds only Shift/Drawer/CashSession); tax is **frozen, preserve-only** (BR-POS-004 snapshots, no tax ledger); fiscal and receipt are **correctly deferred** (§5.5.3 places both outside the transaction; `fiscal` schema holds only `TaxClass`; **no outbox exists repo-wide**); Kitchen has **no new action** (Fire already produced instructions; Kitchen is not an `order.completed` subscriber); customer/loyalty is **unreachable** (no `Customer` model, no `Order.customer_id`, no CRM module) so §1.2 effect 9 is vacuously satisfied; table release is deferred (`BranchTable.status` deliberately absent per D-05, FR-POS-081 is [S]); `order.completed` is **REQUIRED** with a §24.2.4-specified payload; one PostgreSQL transaction is **structurally guaranteed** (`withAuthContext` is a single `$transaction` and forbids nesting; `UnitOfWork` drains handlers before COMMIT). **Blocked by two ratified governance defers standing across two [M] SRS obligations:** (B-1) the D-17-05 §4.1 amendment and P1C-2 both explicitly withhold *"the completion-time COGS posting workflow"* while §1.2 effect 4 / FR-CST-001 [M] / §5.5.2 mandate COGS recognition at completion — and `orders.cogs_total` was proven to be a **sale-time estimate, not posted COGS**; (B-2) D-17-07 keeps `modifiers.recipe_delta` opaque and defers FR-MNU-013, making FR-POS-024 [M] (*"a 'no cheese' burger SHALL NOT deplete cheese"*) unimplementable. Plus (B-3) a hard structural finding: `stock_movements` is RANGE-partitioned on `occurred_at`, so **no unique constraint omitting the partition key is expressible** and `(reference_type, reference_id)` is only a plain index — there is currently **no natural key preventing duplicate sale depletion**. Also found and reported, not fixed: `orders.cogs_total` **never multiplies `unit_cost_snapshot` by line quantity** (understated for every line with qty ≠ 1); a **lost-update race on `stock_levels`** (no lock/CAS/version); a per-movement recipe-cost cascade threatening NFR-PERF-006; partitions ending 2027-09 with no DEFAULT partition and manual per-partition RLS; and `computed_cost` documentation drift that ships a false statement in the public OpenAPI. §AG **NOT GENERATED**. No code changed, no migration, no commit, no push |
| 2026-08-25 | P1F-2A — Completion economics, modifier depletion & Inventory idempotency/concurrency (RESOLUTION GATE) | [2026-08-25_P1F2A_completion-resolution-gate.md](2026-08-25_P1F2A_completion-resolution-gate.md) | **IMPLEMENTATION READY** — all P1F-2 blockers resolved; design/governance only, no production code | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged; local HEAD == `origin/feat/production-spec`, `origin/main` untouched at `01c0b0f`) | 27 today (unchanged — read-only gate); **3 module-owned migrations designed for P1F-2 → 30** (28 Sales, 29 Production, 30 Inventory) | No test run — design gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e, 34/34 suites) | Recorded the user's ratified package in the governance register as the **unnumbered** entry "P1F-2 Completion Economics & Depletion Resolution — 2026-08-25" (**no D-21 created**; tally unchanged; original D-17-05/D-17-07 text preserved verbatim in `PRODUCTION_SPEC_DESIGN_GATE.md` §4, history not rewritten): **D-17-05 narrowly reopened for completion-time COGS only**, **D-17-07 narrowly reopened for typed ADD/REMOVE_ALL modifier depletion only**, plus line-scoped depletion provenance, a non-partitioned Inventory effect registry, Inventory concurrency hardening as a precondition, and Option A (existing Payment route + `pos.payment.capture`) as the completion trigger — with D-16 OPEN / D-12 BLOCKED / D-3 IN PART / P-1 and all of P1D-B..G untouched, and no new permission code. **Resolved the feared fourth blocker by proof, not assertion:** no valuation gap is reachable at completion — `valuationUnitCost` is a *total* function (standard guaranteed by `ck_standard_cost_present`, weighted-average reads the maintained `average_cost`, FIFO values consumed layers and prices any unbacked remainder at that same FIFO-derived mean) and `stock_movements.unit_cost` is NOT NULL — so posted COGS is always fully determined, **no provisional-COGS or post-completion adjustment machinery is created, and BR-POS-001's completed-order immutability stays intact**. Designed: posted COGS as `order_lines.posted_cogs_total` (Option A — exact bigint sum of actually-valued movement `total_cost`, quantity-correct by construction, sale-time `unit_cost_snapshot` never rewritten, no derivable `posted_unit_cogs`/`posted_cogs_at` columns per the P1C-5 item-7 precedent, **exactly one rounding point** at the movement); a typed relational `production.modifier_recipe_effects` master (ADD/REMOVE_ALL only, XOR CHECKs, composite tenant-safe FKs, reusing the existing `RecipeComponentType` enum and building on the already-snapshotted `ModifierKind`) with deterministic **expand → aggregate → remove → add → re-aggregate** evaluation so removal strictly precedes addition and substitution is correct; an immutable normalized Sales snapshot `sales.order_line_modifier_effects` **plus** `sales.order_line_recipe_versions` — the latter a flagged extension closing a genuine late-binding hole where a republished **sub-recipe** would otherwise change a completed order's depletion (BR-POS-004 pins only the top-level version, and D-17-08 provides no temporal resolution); Production's first `contract/` (`RECIPE_CONSUMPTION_QUERY`, quantities only, no money) and Inventory's first `contract/` (`SALE_DEPLETION_COMMAND`); and **B-3 resolved architecturally** by a NON-partitioned `inventory.sale_depletion_effects` registry keyed `(tenant_id, order_line_id, stock_item_id, location_id)` — deliberately excluding `occurred_at` so partition mechanics never leak into business identity — append-only with RLS ENABLE+FORCE, SELECT/INSERT-only grants, a partition-aware composite FK to the ledger, and a real composite FK into `sales.order_lines` making "an effect cannot point at the wrong OrderLine" structurally true. Concurrency: replaced the lost-update read-then-upsert with an **atomic additive `ON CONFLICT DO UPDATE ... RETURNING`**, from which `balance_after` is now derived (repairing BR-INV-003 rather than racing it), and specified FIFO `ORDER BY created_at, id FOR UPDATE` with **no SKIP LOCKED** and a global `(stock_item_id, location_id)` lock ordering to prevent deadlock inversion. Performance: recipe-cost recomputation reduced from once-per-movement to **once per distinct FIFO item only** — proven from the code that weighted-average and standard valuations are unchanged by an outbound sale — with NFR-PERF-006 classified **PARTIAL until measured** and a mandated p50/p95 benchmark. §1.2 and UC-POS-01 explicitly **not** claimed COMPLETE (fiscal document, receipt, table release absent; customer/loyalty unreachable). Residual risks reported, not hidden: FIFO-with-zero-layers `average_cost` fallback, `MovementsService.post`'s lost update surviving for transfers/counts/waste, the 2027-09 partition horizon, and float arithmetic persisting in `costing.ts` helpers. §AA contains a complete self-contained Sonnet 5 implementation prompt. OpenAPI stays 3.1.0 / **133** operations (no new route). No production code, no migration created, no commit, no push |
| 2026-08-25 | P1F-2B — Completion semantics & repository-shape correction (CORRECTION GATE) | [2026-08-25_P1F2B_completion-correction-gate.md](2026-08-25_P1F2B_completion-correction-gate.md) | **BLOCKED — FIFO ZERO-LAYER VALUATION NOT SOURCE-DECIDABLE**; P1F-2A's "IMPLEMENTATION READY" verdict rejected; audit/design only, no production code | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged; local HEAD == `origin/feat/production-spec`, `origin/main` untouched at `01c0b0f`) | 27 today (unchanged — read-only gate); 3 module-owned migrations designed → 30, with migration 28's table set and privileges corrected | No test run — correction gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e) | Preserved the ratified P1F-2 governance package unamended and ran a 15-point regression audit (§14) returning PRESERVED for the effect registry, its natural key, 1-effect=1-movement, line-scoped provenance, within-line aggregation, the conflict-safe INSERT, deterministic lock ordering, FIFO-without-SKIP-LOCKED, module ownership and the one-UnitOfWork/Order-CAS-last shape. **Resolved nine of ten corrections and found four places where P1F-2A was mechanically wrong.** (C-6) P1F-2A's projection-first write order is **illegal**: `stock_levels.last_movement_id` is a real IMMEDIATE composite FK to the partitioned ledger and there is **not one `DEFERRABLE` constraint in the repo**, so the upsert would violate the FK; `balance_after` is also `NOT NULL` and `stock_movements` has `REVOKE UPDATE`, so it cannot be patched later — replaced with a proven three-statement sequence (atomic `+=` without the pointer columns → RETURNING → movement insert → pointer UPDATE) that is race-free because `ON CONFLICT DO UPDATE` holds the row lock to COMMIT, and which **repairs** BR-INV-003 by deriving `balance_after` from the atomic result. (C-2) P1F-2A's "conversion gap → zero depletion" is **wrong** — FR-INV-004 says the System **"SHALL reject"**; corrected to fail closed, with the ratified STRUCTURAL-vs-VALUATION gap split (BR-MNU-012 vs P1C-5 item 4) governing each of the six audited cases. (C-3) P1F-2A missed a **reachable** mutation: `conversionToStockBaseUnit` resolves via `StockItem.baseUnitId`, whose FR-INV-002 guard is application-only, unlocked and **vacuous today** because no sale writes movements — so recipe-version pinning alone is insufficient; corrected by snapshotting the resolved NET base-unit consumption at capture (`sales.order_line_consumption_components`), which also makes conversion gaps structurally unreachable at completion, makes NFR-PERF-006 trivial, costs nothing extra (the capture traversal already computes and discards those quantities), and lets P1F-2A's `order_line_modifier_effects` table be **dropped**. (C-4) P1F-2A granted full DML to tables it called immutable — corrected to the `order_payments` append-only recipe (SELECT+INSERT only, no UPDATE/DELETE policy), with the honest limit that a child-only REVOKE cannot stop a cascade from a deletable parent (RI triggers run as table owner under `SECURITY_NOFORCE_RLS`). (C-5) P1F-2A claimed OpenAPI stays 133 while providing **no write path at all** for modifier effects — audit proved Production has zero modifier awareness and Catalogue has **no modifier-mutation route whatsoever** (`MODIFIER_UPDATED` is declared and unused), so nothing can be extended: added one Production route `PUT /modifiers/{id}/recipe-effects` under the existing `recipe.edit` (no invented permission, D-17-06 intact), FK-validated exactly as `RecipesService` already validates Catalogue ids → **OpenAPI corrected 133 → 134**. (C-7) `total_cost` proven POSITIVE MAGNITUDE by an explicit test (`totalCost(-3,250n)===750n`), a deliberate test-locked deviation from literal §7.4.3 — preserved, not rewritten, so posted COGS sums directly with no sign flip. (C-8) FR-CST-002 corrected from "COMPLETE with documented deviation" to **PARTIAL** — documented is not satisfied. (C-9) `order.completed` version = **1**: the SRS's `eventVersion: 2` sits in an envelope whose every sibling field is a placeholder, so it is illustrative, not normative; all three existing repo events are v1. (C-10) the synchronous shape is **required, not merely preferred** — handlers return `void`, `drain` discards results and `UnitOfWorkContext` exposes only `{tx, publishEvent}`, so there is no return channel for the valued depletion Sales needs; §5.5.1 authorises it and §5.5.2's atomicity requirement is met, but with **zero subscribers** the literal subscriber framing is honestly classified NOT implemented. **The one blocker: FIFO with zero remaining layers.** FR-INV-012 defines FIFO only as "the cost of the oldest **remaining** batch", FR-INV-013 presupposes batch-tracked draw-down, §24.5.2's `drawDown` is undefined for empty input, and the Inventory design gate's own "cannot be resolved" list omits the case entirely — so the existing `averageCost` fallback was never ratified, and it is the cross-method fallback P1C-2 forbids verbatim; the case is reachable (concurrent completions of a near-zero FIFO item) and `unit_cost` is NOT NULL, so every depletion must produce a cost. Smallest decision offered with three narrow options (recommendation: value the unbacked remainder at the most recently exhausted layer, narrowing P1C-2's "latest purchase cost" clause) — §S **NOT GENERATED**. No production code, no migration, no commit, no push |
| 2026-08-25 | P1F-2C — FIFO Exhaustion Carry-Forward ratification & final implementation gate | [2026-08-25_P1F2C_fifo-ratification-final-gate.md](2026-08-25_P1F2C_fifo-ratification-final-gate.md) | **IMPLEMENTATION READY** — the single P1F-2B blocker closed; governance recorded; replacement Sonnet prompt generated; no production code | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged; local HEAD == `origin/feat/production-spec`, `origin/main` untouched at `01c0b0f`) | 27 today (unchanged — design gate); **3 module-owned migrations → 30**, unchanged from P1F-2B and with **no schema added for FIFO** | No test run — design/governance gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e) | Recorded the user's ratification as the **unnumbered** register entry "FIFO Exhaustion Carry-Forward Ratification — 2026-08-25" (**no D-21**; tally unchanged; original P1C-2 and D-17-05 text preserved verbatim), narrowing **P1C-2's no-fallback clause for the empty-FIFO-queue case only** — it now forbids substituting a *different costing method* or a *purchase-price lookup* for an **available** layer, while permitting an item to continue **its own FIFO cost lineage** at its last actual layer cost when the SRS defines no value (FR-INV-012 speaks only of the "oldest **remaining**" batch; §24.5.2's `drawDown` is undefined for empty input). Resolved the three FIFO cases precisely: **available layers** always consumed first, each backed quantity valued at **its own** layer cost; **partial coverage** values only the unbacked remainder at the last layer of *our own consumption plan* — **no historical query needed and no blended mean**, so layer-level FIFO truth survives; **zero coverage** carries forward from the most recently **exhausted** layer via a query that is the exact reverse of `selectBatches`' own ordering (`created_at DESC, id DESC` for FIFO; `expiry_date DESC NULLS FIRST, …` for FEFO), which is **exact rather than approximate** because every `quantity_remaining` reduction in the codebase flows through `selectBatches`, so exhaustion order always follows the configured strategy — and which distinguishes "most recently exhausted" from "most recently purchased" exactly as the ratification demands. Proved **no `exhausted_at` column and no new index are needed** (`stock_batches` has only `created_at`, but the strategy inference plus the existing `(tenant_id, stock_item_id, location_id)` index suffice), so migrations stay at **30**. Proved the terminal no-historical-layer case is **unreachable through the sale path** (batch history is protected by `ON DELETE RESTRICT` on the tenant/item/location FKs *and* by `stock_movements.batch_id → stock_batches` RESTRICT, with no `stockBatch.delete` path anywhere; and the only layer-less configuration — FIFO costing with `is_batch_tracked=false` — is refused at line capture by P1C-5), and specified **fail-closed** behaviour for it regardless, never substituting another costing method. Fixed the exact arithmetic with **one rounding point**: `total_cost = round_half_up(Σ(layerQty × layerCost) + unbackedQty × carryCost)` in exact Rational/bigint, positive magnitude per the test-locked repo convention, with `unit_cost = round_half_up(total_cost / |quantity|)` explicitly **representative only** — posted COGS derives from `total_cost` alone and must never be recomputed as `unit_cost × quantity`. All 20 P1F-2B corrections carried forward unchanged (fail-closed conversion gaps, capture-time consumption snapshot, append-only Sales snapshots, ADD/REMOVE_ALL modifiers, the `recipe.edit` PUT route, the FK-safe three-statement stock-level/movement/pointer order, deterministic FIFO locks without SKIP LOCKED, one UnitOfWork, Order CAS last, `order.completed` v1, zero subscribers reported honestly). OpenAPI stays **3.1.0 / 134**. Classifications preserved and not relaxed: FR-CST-002 **PARTIAL**, NFR-PERF-006 **PARTIAL until measured**, §1.2 and UC-POS-01 **PARTIAL**. §N contains the complete replacement Sonnet 5 prompt, which **supersedes P1F-2A §AA in full**. No production code, no migration, no commit, no push |
| 2026-08-25 | P1F-2D — Final implementation sanity gate (C-11 / C-12 / C-13) | [2026-08-25_P1F2D_final-sanity-gate.md](2026-08-25_P1F2D_final-sanity-gate.md) | **IMPLEMENTATION READY** — three residual issues resolved; no governance reopened; final Sonnet prompt generated; no production code | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged; local HEAD == `origin/feat/production-spec`) | 27 today; **3 module-owned migrations → 30** (unchanged count; migration 28's Sales table set corrected) | No test run — design gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e) | **C-11 (FIFO costing vs FIFO/FEFO physical consumption)** — resolved WITHOUT a new cost-layer table by proving a source-compatible interpretation: the SRS models FIFO valuation entirely in terms of *batches* (FR-INV-012 "the cost of the oldest remaining **batch**"; FR-INV-013 "record which **batch** was consumed"; §24.5.2 reads `batches.availableOrdered`) and **never posits an accounting layer distinct from the physical batch**, so for a batch-tracked item the cost layer *is* the batch and the two axes differ only in selection order. Confirmed the current schema **cannot** represent independent physical and accounting flow (`stock_batches` has a single `quantity_remaining`), and that inventing one would contradict Inventory's ratified §29 non-invention of the fefo+fifo cost basis — so P1F-2 must not create it. Produced the full combination matrix: `fifo`+`fifo` **COMPLETE** (receipt order satisfied literally), `fefo`+`fifo` **DOCUMENTED OPEN ITEM** (behaviour preserved, never relabelled as FIFO compliance), weighted-average/standard unaffected, and `is_batch_tracked=false` unreachable via the sale path. **Corrected the carry-forward query** from P1F-2C's `batch_strategy`-branched ordering to **receipt order always** (`created_at DESC, id DESC`) — because FR-INV-013 ties FIFO *valuation* to *receipt* order and carry-forward is a valuation rule; identical for fifo-strategy items, and for fefo it keeps the one new rule P1F-2 introduces receipt-order-faithful. Also collapsed P1F-2C's separate partial-coverage special case: since an unbacked remainder means the queue is now empty, **one query run after the decrements serves both partial- and zero-coverage**. **C-12 (aggregated snapshot provenance)** — the contradiction was real (`cheese = 20g recipe + 10g modifier A + 5g modifier B` has no truthful single `provenance`/`order_line_modifier_id`) and is **dissolved by construction** under C-13's Option A: the aggregated row is never persisted, so `sales.order_line_consumption_components` and the `ConsumptionProvenance` enum are both **removed** from the design and no column can become false after aggregation. **C-13 (expansion location)** — adopted **Option A**: Completion performs the real recursive expansion inside the UnitOfWork, so NFR-PERF-006's "SHALL execute within the order's transaction" is honoured **literally with no deviation claimed**. Proved it is clean because published recipe structure is already **DB-frozen** (every `recipe_lines` write policy requires `EXISTS(... v.status='draft')`, and `recipe_versions` has `REVOKE UPDATE` bar a column-level `UPDATE(status)`), so only the two genuinely mutable inputs need pinning — modifier effects (restored `order_line_modifier_effects`) and the conversion basis (new `order_line_component_conversions`, since `uom_conversions` is fully writable with no RLS and `base_unit_id` has a live route whose FR-INV-002 guard is vacuous today). Valuation is still **not** snapshotted; it remains completion-time current valuation. **Production contract lifecycle disambiguated** to exactly two methods at two distinct moments — `resolveConsumptionBasis` (line capture; returns closure + effects + factors) and `planConsumption` (completion; performs genuine recursive expansion from pinned inputs only, never reading `uom_conversions` or `base_unit_id`) — so no method merely returns data Sales already owns. **Modifier configuration read-back**: proved no existing route can expose the new Production-owned table (Catalogue's modifier view covers only Catalogue columns), so a full-replace PUT alone would not be operable; added `GET /modifiers/{modifierId}/recipe-effects` under the existing `recipe.view` alongside the `recipe.edit` PUT — no permission invented — taking **OpenAPI to 3.1.0 / 135** (superseding P1F-2B/C's 134). Migrations stay at **30**. All P1F-2C/B decisions in §8 preserved untouched, including the FIFO Exhaustion Carry-Forward ratification. §N contains the complete replacement Sonnet 5 prompt, which **supersedes both P1F-2A §AA and P1F-2C §N in full**. No production code, no migration, no commit, no push |
| 2026-08-25 | P1F-2E — Dual-axis batch accounting & multi-batch traceability gate (C-14 / C-15) | [2026-08-25_P1F2E_dual-axis-inventory-gate.md](2026-08-25_P1F2E_dual-axis-inventory-gate.md) | **IMPLEMENTATION READY** — both residual source-compliance issues resolved; no governance reopened; true-final Sonnet prompt generated; no production code | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27 today; **3 module-owned migrations → 30** (count unchanged; migration 30 expanded within Inventory) | No test run — design gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e) | **C-14 (FIFO costing under FEFO physical strategy)** — P1F-2D's "documented open item" is **rejected**: FR-INV-013 [M] conditions receipt-order FIFO valuation only on the item being *batch-tracked*, never on `batch_strategy`, and the `fifo`+`fefo` combination is normal for any expiry-tracked FIFO item. Proved **by counterexample** that the accounting position is *not derivable* from `quantity_remaining` (two batches, FEFO consuming the newer one first, causes the older cost layer to be double-charged on the second sale while the newer layer is never charged), so a second independently-advancing counter is **necessary**. Chose the **minimal** model after comparing three: a single additive column `stock_batches.fifo_cost_quantity_consumed` (DECIMAL(18,6) NOT NULL DEFAULT 0, cost-remaining derived as `quantity_received − consumed`), keeping the **batch as the SRS cost layer** exactly as FR-INV-012/013 and §24.5.2 frame it — rejecting a separate `fifo_cost_layers` table because it would duplicate the layer set, need a receipt-side writer that **does not exist anywhere in `src/`**, double the lock surface, and reify an entity the SRS never names. Chose *consumed* rather than *remaining* so `DEFAULT 0` gives new batches full cost quantity and **no batch-creation writer has to change**; backfill `= quantity_received − quantity_remaining` is exact and vacuous from zero. Physical flow still follows `batch_strategy` (expiry order for FEFO); accounting flow always follows **receipt order**; the ratified **FIFO Exhaustion Carry-Forward** rule is not reopened but is correctly re-pointed at the *accounting* axis (most recently exhausted **cost** layer, receipt order), with one query serving partial- and zero-coverage. The two plans are merged by a deterministic exact-Decimal **two-pointer zipper** producing truthful `(physical batch, cost basis, qty)` triples that sum exactly to the depletion. Verified `costing_method` has **no update path** (`StockItemsService` exposes create/list/findOne/changeBaseUnit/setReorderConfig/createReasonCode/listReasonCodes), so an item can never switch axes mid-life; `MovementsService.post` is extended with **counter maintenance only** (receipt-order advance for FIFO batch-tracked items) so the accounting queue cannot drift, explicitly **without** touching `valuationUnitCost` or how transfers/waste/counts are valued. **C-15 (multi-batch traceability)** — populating `stock_movements.batch_id` only when exactly one batch was consumed fails FR-INV-013 [M]'s "record which batch was consumed" and destroys the FR-INV-027 [S] substrate; corrected to **1 logical effect = N allocations = N stock movements**, preserving the accepted business identity `(tenant_id, order_line_id, stock_item_id, location_id)` on an unchanged parent while moving cost and movement columns to a new append-only child `inventory.sale_depletion_allocations` carrying `physical_batch_id`, `cost_basis_batch_id`, quantity, `unit_cost`, `total_cost` and the partition-aware movement FK. Every movement now carries its **actual** cost-basis unit cost rather than a blended representative — so P1F-2D's "representative unit_cost" caveat disappears and the rounding point moves to **per allocation**, which is strictly more truthful. All four FR-INV-027 traceability queries (batch→orders, order→batches, line→cost layers, effect→movements) become mechanically possible and are test-mandated. Locking simplified by the model choice: because both counters live on the **same batch row**, a single `FOR UPDATE` over the union of physically- and accounting-available layers, ordered `created_at ASC, id ASC`, covers both axes and eliminates cross-axis lock-order inversion; the FK-safe three-statement stock-level/movement/pointer sequence is repeated per allocation so `balance_after` stays truthful and BR-INV-003 is not traded for fewer statements. Also corrected two P1F-2D wording defects: "all four Sales snapshot tables" → **three**, and `resolveConsumptionBasis` returns "no *resolved/net consumption* quantities and no money" (configured modifier ADD quantities and pinned conversion factors are legitimately included). Migrations stay **30**, OpenAPI stays **3.1.0 / 135**. FR-INV-013 is now **COMPLETE for the completion path** and explicitly not claimed globally; no mandatory requirement remains a "documented open item" on the Completion path. §P contains the true-final Sonnet 5 prompt, superseding P1F-2A §AA, P1F-2C §N and P1F-2D §N. No production code, no migration, no commit, no push |
| 2026-08-25 | P1F-2E-A — Dual-axis inventory acceptance correction (C-16 … C-20) | [2026-08-25_P1F2E-A_inventory-acceptance-correction.md](2026-08-25_P1F2E-A_inventory-acceptance-correction.md) | **IMPLEMENTATION READY** — five acceptance corrections resolved; P1F-2E core architecture preserved; **no governance created or amended**; final Sonnet prompt generated | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27 today; **3 module-owned migrations → 30** (count unchanged; migration 30 expanded and re-ordered) | No test run — design gate; latest accepted evidence cited from P1F-1A (722 unit / 731 e2e) | **C-16 (FIFO counter backfill)** — P1F-2E's `fifo_cost_quantity_consumed = quantity_received − quantity_remaining` was **provably wrong for the very configuration the slice exists to support**: replaying P1F-2E's own A/B counterexample shows it seeds `A=0, B=5` where receipt-order truth is `A=5, B=0`, mis-charging every later COGS. Corrected to derive the **total** physically consumed per (tenant, item, location) and re-allocate it through layers in `created_at ASC, id ASC` via exact DECIMAL window SQL, FIFO batch-tracked items only, non-FIFO left at DEFAULT 0. Audit also found the derivation's premise is **not structurally guaranteed** — only `ck_batch_qty_nonneg` exists and **`quantity_remaining <= quantity_received` is enforced nowhere** — so the migration now (a) RAISEs an explicit exception rather than inventing state, and (b) adds the missing CHECK permanently; verified there is exactly **one** batch mutation site in `src/` (`movements.service.ts:218`, decrement only — no increment, no refill, so batch quantities can only ever have decreased) and that the ledger cannot substitute because `batch_id` is populated only for single-batch consumption. A **migration-UPGRADE test is now mandatory** (migrate through 29, seed FEFO/receipt-divergent history, apply 30, assert receipt-order state) — clean-from-zero testing is explicitly declared insufficient. **C-17 (shared counter locking)** — the counter becomes shared state written by both Completion and `MovementsService.post`, so a single **private Inventory kernel** (`fifo-cost-ledger`, deliberately not a cross-module contract) now owns deterministic `FOR UPDATE` locking (`created_at ASC, id ASC`, no SKIP LOCKED), receipt-order planning, counter increment and carry-forward lookup; `MovementsService.post` — which today takes **no batch lock at all** — routes through it, an additive locking-and-counter change that explicitly does **not** touch `valuationUnitCost` or how transfers/waste/counts are valued. New mandatory race test: Completion vs existing outbound on the same FIFO item/location, asserting the final counter equals the serial result. **C-18 (reservation order)** — the effect `INSERT … ON CONFLICT DO NOTHING RETURNING` is now normatively **step 1**, before any batch lock, counter mutation, projection write, movement or allocation; Inventory is never mutated before the business identity is reserved, which is also what makes the zero-side-effect idempotency test meaningful. **C-19 (carry-forward provenance)** — the carry-forward query now returns `id, unit_cost` and the allocation persists `cost_basis_batch_id = <the actual exhausted FIFO layer>` while `physical_batch_id` stays NULL, since physical backing and accounting provenance are independent axes; yields the clean invariant that for `costing_method='fifo'` **every** valued allocation carries a non-null cost basis (the terminal case fails closed rather than emitting a basis-less row), so a later receipt can never change how an old carry-forward COGS is explained. **C-20 (structural consistency)** — audited that `stock_batches` has only `(tenant_id, id)`, so P1F-2E's FKs proved tenant membership but **not** that a referenced batch shares the effect's item/location; added an additive `UNIQUE (tenant_id, id, stock_item_id, location_id)` on `stock_batches`, a matching unique target on `sale_depletion_effects`, and `stock_item_id`/`location_id` on allocations so three composite FKs make the wrong-reference state unrepresentable — relying on MATCH SIMPLE so a NULL batch id correctly disables its FK for unbacked/non-FIFO rows. The allocation↔movement item/location binding is **deliberately left service-enforced** with the cost rationale stated (it would add a fourth unique index to the highest-volume RANGE-partitioned table, propagated to every partition, for a marginal gain), and the prompt explicitly forbids claiming a PostgreSQL test proves an invariant the FKs do not encode. Migrations stay **30**, OpenAPI stays **3.1.0 / 135**, FR-CST-002 stays PARTIAL, and the `stock_levels` lost update plus receipt-order valuation of transfers/counts/waste are recorded as out-of-scope Inventory debt. §L contains the final Sonnet 5 prompt, superseding P1F-2E §P and all earlier P1F-2 prompts. No production code, no migration, no governance change, no commit, no push |
| 2026-08-25 | RENDER — deploy-blocker fix, `ALTER DEFAULT PRIVILEGES` removed repo-wide (42501/P3018) | [2026-08-25_RENDER_identity-rls-default-privileges-unblock.md](2026-08-25_RENDER_identity-rls-default-privileges-unblock.md) | COMPLETE — 6 already-committed, not-yet-applied-on-Render migrations edited, scope extended from 1→6 with explicit user approval mid-task | `cf04e00` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27 (unchanged — 6 existing, not-yet-applied-on-Render migrations edited in place; no migration added/removed) | No repo test suite run; validated twice via a disposable throwaway Docker Postgres container (full 27-migration `prisma migrate deploy` replay, clean both times) plus a full `has_table_privilege` matrix over all 70 tables in the 6 edited schemas confirming zero table actually depended on the removed mechanism — existing 9-day local dev DB untouched throughout | Root-caused Render's `P3018`/`42501 permission denied to change default privileges` to `ALTER DEFAULT PRIVILEGES FOR ROLE ros_migrator IN SCHEMA <x> ...` in `20260812145207_identity_rls/migration.sql` — requires the connecting role to be, or `SET ROLE` to, `ros_migrator`, which Render's migration role cannot. `grep` found the identical executable statement, unmodified, in 5 further committed migrations (`20260812175712_governance_audit_entries`, `20260816110000_organisation_foundation` ×2, `20260816150000_catalogue_foundation`, `20260816210000_inventory_foundation`) — meaning an `identity_rls`-only fix would only move the Render failure to the next migration, not resolve it. Surfaced this to the user (not applied silently); user explicitly chose to extend the same narrow removal to all 6. Removed exactly the 6 `ALTER DEFAULT PRIVILEGES` statements (one comment-only replacement each); preserved every `GRANT`/`REVOKE`, all RLS `ENABLE`/`FORCE`, and all `CREATE POLICY` statements verbatim across all 6 files, confirmed by diff. Privilege-matrix validation proved every table in `identity`/`governance`/`org`/`kitchen`/`catalogue`/`inventory` already carries its own explicit `GRANT` (project convention, confirmed via 3 later migrations whose comments cite the removed mechanism but do not actually rely on it), so no runtime access regression exists for any current table — only a forward-looking reminder that a *future* migration adding a new table to these 6 schemas must grant `ros_app` explicitly. `npx prisma generate` and `npm run build` both succeeded before and after. No commit, no push, no Render deploy performed |
| 2026-08-26 | MVP — current-state audit, remaining work, next-slice gate | [2026-08-26_MVP_current-state-and-next-slice.md](2026-08-26_MVP_current-state-and-next-slice.md) | **AUDIT ONLY — READY TO CONTINUE**; no code/migration/governance changed | `9aa7a88` (unchanged) | `feat/production-spec` (unchanged) | 27 (unchanged — audit only, confirmed nothing exists past `20260824100000_sales_order_payment_capture`) | No test suite run destructively; 5 parallel read-only code audits (Sales/Treasury, Production/Inventory, Kitchen/Catalogue/Receipt, Identity/Org/Governance/Sync/Workforce+RLS, API/OpenAPI+build) each independently grepped/read current code rather than trusting prior reports; `npm run build`/`tsc --noEmit`(1 known baseline error, unchanged)/`npm run openapi:check`(zero drift)/`git diff --check` all clean | Full protected-MVP-path audit (PIN→Shift/CashSession→Order→Fire→Kitchen→Payment→COMPLETED→depletion→COGS→Receipt→close→reports), one table per node, each classified COMPLETE/PARTIAL/DESIGNED ONLY/BLOCKED/NOT IMPLEMENTED against actual code, not report claims. **Key finding: PIN through partial Payment is real and COMPLETE; the Order→COMPLETED transition does NOT EXIST anywhere in code** — `order-state.ts`'s `TRANSITIONS` map has no `open`/`partially_paid`→`completed` entry, and `SalesPaymentService` explicitly throws `FullPaymentRequiresCompletionError` rather than complete an order, by design, pending Completion. **Verified, not assumed, that P1F-2 (Completion/recipe-expansion/depletion/COGS) is DESIGNED ONLY**: zero occurrences of `resolveConsumptionBasis`/`planConsumption`/`modifier_recipe_effects`/`fifo_cost_quantity_consumed`/`sale_depletion_effects`/`sale_depletion_allocations`/`posted_cogs_total`/`order.completed` anywhere in `src/` or `prisma/`, and no migration exists after Payment (27 total, unchanged) despite P1F2E-A's ratified design targeting 30. Confirmed OpenAPI is still exactly 133 operations (not 134/135 as later P1F-2 gates projected), independently corroborating no P1F-2 route was ever added. Found and confirmed **Receipt is 0% implemented** (repo's own code comment calls it a documented gap) and **KDS operator lifecycle (display/start/ready/bump/recall) is 0% implemented beyond schema vocabulary** — `KitchenModule` has no controller at all; Fire-time ticket persistence is real but distinct from operator-facing KDS. Confirmed **Treasury has only one route** (`CashSession` open) — no close, no variance, no X report, no day close anywhere. Confirmed **branch-scoped RBAC is NOT IMPLEMENTED** (`MembershipRole.branchId` is an inert schema column, `TenantContextService`/`PermissionGuard` use a flat tenant-wide permission set) — a real, current, unresolved FR-SEC-010 gap, not merely historical. Confirmed the 2026-08-26 Country Pack unblock artifacts are genuinely committed (`git log` shows `9aa7a88`) and verified as real/tested, explicitly scoped as demo-only, not production fiscal certification. Cross-schema RLS survey found every tenant-scoped table `ENABLE`+`FORCE`d, `ros_app` confirmed `NOBYPASSRLS` everywhere. Re-flagged (not fixed) the still-uncommitted local `prisma.config.ts` DB-role anomaly from the prior session. Selected the single next slice — **P1F-2 Completion** — as the sole hard blocker on the rest of the MVP path, with full WHY/DEPENDENCIES/RISKS/VERIFICATION-GATES writeup, explicitly not implemented in this task. No code, migration, or governance changed; no commit, no push |
| 2026-08-26 | RENDER — Country Pack runtime provisioning for order-create unblock | [2026-08-26_RENDER_country-pack-order-create-unblock.md](2026-08-26_RENDER_country-pack-order-create-unblock.md) | **READY TO CONFIGURE RENDER** — one signed demo EG/EGP pack + trust manifest operationalized through the existing, unmodified P1C-3 design; no redesign, no runtime signing capability added | `18a155f` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27 (unchanged — no migration touched) | New: `country-pack.deployment-artifacts.spec.ts` (6/6) + `country-pack.service.spec.ts` (3/3), full localisation suite 160/160; scratch-DB e2e (`ros_country_pack_smoke_scratch`, dropped after use) — 27/27 migrations, `sales.e2e-spec.ts` 56/56, `tax-class-rls`/`sales-lines`/`catalogue` e2e 122/122; persistent local dev DB untouched | Root-caused the `ConfiguredCountryPackTrustStore`/`CountryPackService` "no pack active" warnings to `COUNTRY_PACK_DIR`/`COUNTRY_PACK_TRUST_MANIFEST` never being set (correct fail-closed behaviour, not a bug). Inspected the real trust-manifest schema (`{keys:[{keyId,algorithm:"Ed25519",publicKey:base64url-32-bytes,status}]}`, file path, `parseTrustManifest`) and pack parser (`country-pack.parser.ts`/`.model.ts`) directly from source — nothing inferred. Read `seed-dev-data.ts` directly to confirm the demo branch's actual, already-committed jurisdiction is **EG/EGP** (not assumed/defaulted despite coinciding with the SRS §22.2 example). Used that exact SRS §22.2 sample content (byte-identical to the existing `country-pack.fixture.ts`) as the pack payload — sourced, not invented. Built a narrow offline signing CLI (`src/scripts/sign-country-pack.ts`, no Nest/DB/HTTP) that reuses the production `stripSignature`/`canonicalCountryPackBytes` functions so signed bytes are guaranteed byte-identical to what the verifier re-derives; generated the Ed25519 key via plain `openssl genpkey`, stored only under a newly `.gitignore`d `/secrets/` (verified ignored, repo-wide grep for `BEGIN PRIVATE KEY` clean); committed only the signed pack (`config/country-packs/EG-2026.1.pack.json`) and public trust manifest (`config/country-packs/trust-manifest.json`) — no private key anywhere. Traced TaxClass provisioning (`TenantsService.create` → `TaxClassProvisioningService.provisionForTenant`, idempotent `ensureFromPack`) and established that since `seed-dev-data.ts` creates a fresh timestamped tenant every run, activating the pack before (re-)running the existing seed script auto-provisions tax classes correctly with no new script/API needed. Added 9 new focused unit tests (no existing test weakened) proving the exact committed artefacts activate, resolve as effective, and reject tampered/unknown/revoked signatures, plus branch currency-mismatch refusal (a previously untested pure method). Full verification run against a disposable scratch DB, never the persistent local dev DB. **Residual, unrelated, flagged not fixed:** `prisma.config.ts` was found already locally modified (uncommitted, predates this task) to use `APP_DATABASE_URL` (`ros_app`) instead of `DATABASE_URL` (`ros_migrator`) for the Prisma CLI — architecturally wrong (`ros_app` has no DDL rights) and would break `prisma migrate deploy` again if ever committed/pushed; worked around locally for this task's own verification only, recommended the user revert it. Gave exact Render values (`COUNTRY_PACK_DIR=config/country-packs`, `COUNTRY_PACK_TRUST_MANIFEST=config/country-packs/trust-manifest.json`) and confirmed a redeploy is required. No commit, no push, no Render deploy performed, no historical migration touched, no destructive git command |
| 2026-08-26 | P1F-2 — Final Payment + Order Completion atomic orchestration | [2026-08-26_P1F2_order-completion.md](2026-08-26_P1F2_order-completion.md) | **IMPLEMENTATION COMPLETE FOR THE COMPLETION PATH, WITH ONE HONEST GAP** — all 30 migrations verified clean-from-zero AND via the mandatory upgrade path; full atomic Completion (recipe expansion → dual-axis depletion → FIFO carry-forward → COGS → Order CAS → `order.completed`) implemented and green; NFR-PERF-006 measured PARTIAL (p95 2120ms vs 200ms target), reported honestly not claimed | `9aa7a88` (unchanged, no commit) | `feat/production-spec` (unchanged) | 27→**30** (3 new migrations: `20260826010000_sales_completion_snapshots`, `20260826020000_production_modifier_recipe_effects`, `20260826030000_inventory_sale_depletion_fifo_dual_axis`) | Unit 732/732 (53/53 suites), E2E 747/747 (37/37 suites) — zero regressions; 3 new e2e files (`order-completion` 9, `order-completion-concurrency` 6 = 2 real-Postgres-barrier scenarios × 3 clean runs, `order-completion-performance` 1); `module-boundaries.spec.ts` 31/31 (`KNOWN_DEVIATIONS` unchanged); `tsc`/`eslint`/`prisma validate`/`nest build`/`git diff --check` all clean; OpenAPI 3.1.0, exactly 135 operations, no `/complete` route; persistent `ros` dev DB confirmed untouched throughout (still 26 `_prisma_migrations` rows) | Implemented the P1F2E-A-controlling final Sonnet prompt literally: 3 module-owned migrations (Sales snapshot tables, Production `modifier_recipe_effects`, Inventory dual-axis `sale_depletion_effects`/`allocations` + `fifo_cost_quantity_consumed` counter); Production's `resolveConsumptionBasis`/`planConsumption` public contract (recipe-closure walk, in-memory BOM-style quantity expansion, modifier REMOVE_ALL-then-ADD semantics, VALUATION-gap fail-closed / STRUCTURAL-gap tolerate); Inventory's private `fifo-cost-ledger` kernel (`FOR UPDATE`, one lock order for both physical and cost axes, receipt-order planning, carry-forward lookup) and `SaleDepletionCommand` (reserve-first effect, dual-axis zipper, three-statement-per-allocation write, `MovementsService.post` routed through the same kernel for counter maintenance only); `SalesPaymentService` migrated to `UnitOfWork.execute`, the §14 full-payment block replaced by a real settling-completion branch, `order.completed` payload transcribed verbatim from the SRS's own `Order.complete()` §24.2.4 pseudocode (read directly from the PDF). **Verified, not assumed**: migration-upgrade backfill is receipt-order-correct (proven against the exact FIFO+FEFO-divergence counterexample P1F2E-A names, not a per-batch physical copy); dual-axis depletion genuinely charges FIFO receipt-order cost while consuming FEFO nearest-expiry physical stock, with no weighted-average fallback; FIFO exhaustion carry-forward retains the true exhausted batch as cost basis even with a null physical batch, unchanged by a later receipt; "no cheese" (REMOVE_ALL) depletes no cheese and a later ADD scales by both modifier and line quantity; two settling Payments on one Order/version converge to exactly one winner that genuinely completes the order; two Orders racing the same FIFO batch land on the deterministic serial-equivalent result on both axes, no double consumption. **Found and fixed a real, generalizable pre-existing bug** (not this task's own code, previously flagged-but-not-fixed in the prior session's audit report): `prisma.config.ts`'s CLI datasource pointed at `APP_DATABASE_URL` (the RLS-constrained, `NOBYPASSRLS` `ros_app` role) instead of `DATABASE_URL` (`ros_migrator`), which silently no-ops ANY tenant-scoped DML statement inside a migration file with zero error and a false "successfully applied" result — discovered while isolating why the upgrade backfill appeared to do nothing, root-caused by direct reproduction (identical SQL correct via `psql -f` as migrator, silently inert via `prisma migrate deploy` under the broken config), fixed, and both the clean-from-zero and upgrade-path evidence re-verified after the fix. Honestly reported gaps: **NFR-PERF-006 measured PARTIAL** — p50=1195ms/p95=2120ms for a 30-line/nested-recipe/mixed-costing/multi-batch-FIFO/modifier-bearing Completion against a 200ms p95 target, root-caused to the controlling design's own mandated per-allocation three-sequential-statement write discipline (batching explicitly forbidden for `BR-INV-003` truthfulness), with an identified-but-deliberately-unimplemented lock-grouping optimization; only 2 of the 5 named concurrency scenarios were built (both with 3 real-barrier clean runs); the full modifier/pinning/gap test matrix's mechanisms are all exercised but not every named clause has its own dedicated assertion. `KNOWN_DEVIATIONS` did not grow — a new `sales→production` need for `RECIPE_COST_RECOMPUTER` was satisfied by re-exporting the existing symbol through `production/contract/` rather than adding a new private import path. FR-INV-012/013 (completion path)/022/023/030, BR-INV-003 (completion path), FR-CST-001, FR-POS-024 all COMPLETE; FR-INV-027 substrate-only (reporting NOT IMPLEMENTED); FR-CST-002 PARTIAL (unchanged); NFR-PERF-006/§1.2/UC-POS-01 PARTIAL. No commit, no push, three preserved user files untouched |
| 2026-08-28 | P1F-2 — Acceptance closure (concurrency matrix, structural FKs, pinning/gaps, RLS, sub-recipe gap finding) | [2026-08-28_P1F2_acceptance-closure.md](2026-08-28_P1F2_acceptance-closure.md) | **IMPLEMENTATION DEFECT FOUND** — one genuine defect found and fixed (§I: a modifier ADD effect targeting an unpublished sub-recipe was silently and completely forgotten; fixed via an extra `resolveConsumptionBasis` return field + `ORDER_LINE_ADDED` audit metadata, no migration/new persistence/governance change); every other mandatory verification this pass covers is complete and green | `9aa7a88` (unchanged, no commit) | `feat/production-spec` (unchanged) | 30 (unchanged — no new migration; clean-from-zero re-verified on a fresh scratch DB; persistent local `ros` DB confirmed still untouched, 26 rows) | Unit 732/732 (53/53 suites, unchanged); E2E **793/793** (41/41 suites — 747 baseline + 46 new: `order-completion-concurrency-2` 9 = 3 scenarios × 3 clean runs [weighted-average BR-INV-003, Completion-vs-MovementsService via a genuine cross-connection `pg_stat_activity` lock wait, lock-order-inversion deadlock-freedom], `order-completion-structural` 6 [5 real-FK negative proofs + 1 explicitly-labelled service-enforced test], `order-completion-pinning` 10 [named clauses A–I + the §5 finding test], `order-completion-rls` 21 [own/cross-tenant SELECT, INSERT positive-control, UPDATE/DELETE rejection + row-survival, `information_schema.role_table_grants`, across all 5 P1F-2 tables]); `module-boundaries.spec.ts` 31/31 (`KNOWN_DEVIATIONS` unchanged); `tsc`/`eslint`/`prisma validate`/`nest build`/`git diff --check`/`openapi:check` all clean, OpenAPI byte-identical, 3.1.0/135, zero drift | Closed all 5 gaps the prior P1F-2 report itself listed as missing. Built the 3 remaining concurrency scenarios (found and fixed a real test-authoring defect along the way: a blocked `SELECT...FOR UPDATE` waiter shows up in `pg_locks` as a relation-less `transactionid` wait, not a `tuple`/`relation` row — verified with 3 real concurrent `psql` sessions before correcting the poll to `pg_stat_activity.wait_event_type='Lock'`). Built all 5 structural FK negative proofs (item/location mismatch × physical/cost-basis batch, plus cross-tenant) and the one explicitly-labelled service-enforced (not DB-structural) allocation↔movement binding test. Built all 9 named pinning/gap/modifier-composition tests A–I (modifier-master-data-after-capture, base-unit-change-after-capture via `standard` costing to route around FR-INV-002, live-conversion-factor-change-after-capture, VALUATION-gap-rolls-back-everything via a modifier-ADD-only gap since the pre-existing capture-time cost check already blocks any base-recipe gap, STRUCTURAL-gap-partial-depletion-retained-in-audit, nested-duplicate-path-aggregation, same-item-two-lines-independent-attribution, substitution, double-modifier-scaling). Built full RLS/append-only/grants verification (21 tests) across all 5 P1F-2 tables via the real `ros_app` connection, including the direct `information_schema.role_table_grants` query the prior report had only inferred functionally. **Investigated and resolved the one open finding**: traced the modifier-sub-recipe-gap through `resolveModifierEffects`/`planConsumption` by direct code reading, confirmed the effect was silently and completely forgotten (no structural-gap record anywhere, `no_published_version` cannot fire for this specific case since the effect never reaches `planConsumption` at all), classified it IMPLEMENTATION DEFECT FOUND, confirmed the schema genuinely cannot persist an "unresolved" sub-recipe pin (`ck_olme_component_xor` requires a non-null version), and fixed it narrowly — extended the `ResolveConsumptionBasisResult` contract with a `droppedModifierEffects` field and recorded it in the existing `ORDER_LINE_ADDED` audit metadata (no migration, no new table/column, no governance change) — proven with a dedicated test. NFR-PERF-006 deliberately not re-measured (no Completion-path code changed). No commit, no push, three preserved user files untouched |
| 2026-08-28 | P1F-2 — Modifier structural gap, final narrow design gate | [2026-08-28_P1F2_modifier-structural-gap-final-gate.md](2026-08-28_P1F2_modifier-structural-gap-final-gate.md) | **A. CURRENT FIX ACCEPTED — NO FURTHER IMPLEMENTATION** — analysis/design only; no product code, no migration, no governance change, no D-21+, no test authored or run | `9aa7a88` (unchanged, no commit) | `feat/production-spec` (unchanged) | 30 (unchanged — **migration 31 explicitly NOT required** and not written) | None run — analysis-only gate; the 2026-08-28 acceptance evidence (unit 732/732, E2E 793/793, module-boundaries 31/31, OpenAPI 3.1.0/135 zero drift, migrations 30, persistent `ros` DB untouched) is confirmed to remain valid in full and needs no rerun, since no `src/`/`prisma/`/`test/` file was modified | Adjudicated whether the acceptance-closure fix for the modifier-ADD-targeting-unpublished-sub-recipe condition (recorded only in `ORDER_LINE_ADDED` audit metadata via a new `droppedModifierEffects` contract field) conforms to authority, answering each layer separately from primary sources. **Literal SRS: silent** — extracted `ROS_SRS_v1.0.pdf` via `pdftotext` and found **zero** occurrences of any completion/audit/depletion "gap" requirement (`ORDER_COMPLETED.gaps` is wholly a P1F2E-A construct, absent from §24.2.4's own `Order.complete()` field list), BR-MNU-012 read verbatim (line 2752) imposes only permit-sale + zero/partial cost + list-the-item obligations and speaks of *an item's recipe*, and FR-MNU-013 is **[S]** with its wider surface governance-deferred. **Ratified governance: silent and satisfied** — register grep returns zero hits for `modifier_recipe_effects`/`no_published_version`/`ORDER_COMPLETED`; the two binding clauses (D-17-07 reopened, ratified points 6 and 7) are met because the dropped effect contributes nothing and cannot be retroactively resurrected by a later publish (Completion resolves only against pinned snapshot rows), and Completion never re-reads mutable modifier master data — verified structurally, `modifierRecipeEffect` appears nowhere in `src/` outside `src/modules/production/`. **P1F2E-A: literally conformant** — §L defines `ORDER_COMPLETED.gaps` as `planConsumption`'s gaps, which is exactly what `sales-payment.service.ts` writes, and the design never addresses this capture-time condition. Concluded durable `ORDER_LINE_ADDED` evidence is sufficient, verifying its durability rather than assuming it (audit_entries `GRANT SELECT, INSERT` / `REVOKE UPDATE, DELETE, TRUNCATE`, hash-chained, RLS-scoped, and `sanitizeMetadata`'s `FORBIDDEN_KEY` matching none of `modifierId`/`sequence`/`reason`). Classified `droppedModifierEffects` a **valid implementation-level extension**, not requiring a design amendment: P1F2E-A's constraints on that return are stated as prohibitions ("NO resolved/net quantities, NO money") which it respects, the three listed members are a persistence-mapping onto the three snapshot tables which it leaves intact, and it crosses no fence (no persistence, migration, governance, permission, or OpenAPI change) — recorded, not ratified. Evaluated all four persistence options against the rejection criteria: **A** (current) viable and accepted; **B** (new append-only Sales table) viable but disproportionate; **C** (relax `ck_olme_component_xor` + discriminator, reproducing the base-recipe path — the smallest design *if* co-location were mandatory) viable but weakens the absolute "a `sub_recipe` snapshot row always names a real pinned version" invariant for all future readers; **D none genuinely fits** — `order_line_recipe_versions` rejected twice over (FK RESTRICT has no version to name; pinning a draft would invent a sentinel *and* make `planConsumption` expand an unpublished recipe, changing depletion), reading `audit_entries` at Completion rejected as making audit evidence load-bearing domain state plus a cross-module private read, `recipe_delta` rejected as permanently opaque per D-17-07 point 1, a zero-quantity `sale_depletion_effects` row rejected as false. **Verified from code (not assumed) that BR-MNU-012's "recipes requiring completion" report does NOT surface this**: `RecipeCompletenessService.structuralGaps` queries `tx.recipeLine.findMany` only, never `modifier_recipe_effects`, and is keyed variant→recipe→recipe-line graph from which a modifier's target is unreachable — but is **not required to**, since BR-MNU-012 governs an item's own recipe and modifier recipe effects are a P1F-2 invention postdating it; recorded as future-slice Production-reporting debt (no migration), not a P1F-2 acceptance defect. **One residual recorded honestly and left unfixed**: `ORDER_COMPLETED.gaps` is asymmetric — the identical `no_published_version` reason reaches it for a base-recipe sub-recipe (because `planConsumption` re-reads DB-frozen live structure and can notice the unresolvable line) but not for a modifier-target sub-recipe (which exists at Completion only as a pinned row that was never written); classified a legibility inconsistency, explicitly **not** a conformance, correctness, or immutability failure, and explicitly not a recurrence of the original defect (which was total loss with zero trace). **Migration 31 NOT required.** No Sonnet prompt issued (verdict A). Two open items handed to the user unfixed and undecided: the §F asymmetry (adopt Option C only if co-location is later preferred over migration stability) and the §E completeness-report blind spot. No commit, no push, three preserved user files untouched |
| 2026-08-28 | POST-P1F-2 — MVP audit rebase & next-slice selection | [2026-08-28_POST-P1F2_MVP_next-slice-rebase.md](2026-08-28_POST-P1F2_MVP_next-slice-rebase.md) | **ANALYSIS ONLY — NEXT SLICE SELECTED: P1G-1 CashSession/Shift Close, Cash Count & Variance**; no product code, no migration, no governance change, no D-21+, no commit, no push | `bfe7e69` (unchanged) | `feat/production-spec` (unchanged) | 30 (unchanged — audit only) | None run — analysis gate; classification driven by the actual implemented controller/service surface, never by schema or tests alone | Rebased the MVP audit onto the post-P1F-2 baseline rather than repeating the 2026-08-26 audit. **Reconstructed the 18-step protected path from repository evidence**: PIN → Shift open (PARTIAL — `ShiftsService` exposes only `openShift` and is referenced by **no controller**) → CashSession open (COMPLETE — `treasury.controller.ts` has exactly **one** route) → Order create → pricing/tax → Fire → routing/ticket persistence (all COMPLETE) → **KDS operator lifecycle NOT IMPLEMENTED** (enumerated all 12 controllers; **none under `kitchen/`**, so nothing can move a ticket out of `queued`) → Payment → full settlement → Completion → depletion → posted COGS (all COMPLETE via P1F-2) → **Receipt, CashSession close, Shift close, Day close, minimum reports all NOT IMPLEMENTED**. **Re-verified P1F-2's unlocks without overclaiming**: it made expected cash computable for the first time (`openingFloat` + Σ cash-tender `amount`), since before `bfe7e69` no Order could reach `completed`; explicitly recorded that P1F-2 **did not** unlock the KDS lifecycle, which depends only on Fire/ticket persistence and was equally implementable beforehand. **Selected P1G-1 (CashSession/Shift close) on four pieces of hard evidence**: it is the only candidate unblocking a strict downstream chain (FR-FIN-021 [M] blocks Day close while any session is open → Day close gates the FR-FIN-022 Z report → gates management totals); it is the genuine P1F-2 unlock; **every permission it needs is named verbatim in the SRS** (`cash.session.close`, `cash.session.close_other`, `cash.variance.approve`, `cash.day.close`), satisfying the zero-invented-codes discipline with **no governance ratification** — whereas a full-SRS grep returns **no `kds.*` code at all**, so a KDS slice cannot begin without ratifying invented codes exactly as `pos.order.fire` required; and it carries no fiscal dependency. **Rejected Receipt as next on governance evidence**: P1C-1's fiscal exclusion (*"no tax documents, invoice templates, fiscal submissions"*) **stands**, re-affirmed verbatim in the 2026-08-25 P1F-2 register entry, yet FR-POS-100/101 [M] demand invoice sequence, mandated QR and template-driven layout — so the [M] obligations sit largely on the blocked side; separated ordinary vs fiscal vs reprint vs snapshot vs offline vs country-pack concerns and recommended a governance decision on non-fiscal receipt scope as the correct first step instead of an implementation slice. **KDS scope discipline**: mapped FR-KDS-020…042 against substrate, finding it complete and unusually well-prepared (**all seven FR-KDS-040 timestamps present on BOTH `Ticket` and `TicketLine`**, both lifecycle enums, `recall_window_seconds` default 1800) so a KDS slice would likely need **no migration** — recorded as the strong runner-up. **Treasury discipline**: verified `CashSessionsService` = `open`/`findOne`/`findOpenForDrawer` only, `CashSession` has `status`+`closedAt` but **no counted/expected/variance columns**, **no denomination/count model** (FR-POS-097 [M]), **no `Approval` model at all** against FR-FIN-006 [M]'s approval requirement and governance's repeated *"No approval schema is changed"*, and **no pay-in/pay-out/safe-drop model** despite SRS-named `cash.payin`/`cash.payout`/`cash.safedrop` — handing six open questions to a design gate rather than inventing close semantics. **NFR-PERF-006 classified a PRE-PILOT BLOCKER** (not immediate next, not deferrable to pre-production): it is a latency not correctness defect, but is 10× target with a **structural** root cause that scales with order size and converts directly into `FOR UPDATE` lock contention across terminals — a throughput cliff single-terminal testing cannot reveal. Recommended sequence: **P1G-1 → NFR-PERF-006 remediation → KDS → Receipt**. Design gate **required** before implementation; migration **likely** (one Treasury-owned migration 31). Folded the X report and per-session tender totals into P1G-1 to prevent the report and the close disagreeing. No code, migration, or governance changed |
| 2026-08-28 | P1G-1 — CashSession/Shift close design gate | [2026-08-28_P1G1_cash-close-design-gate.md](2026-08-28_P1G1_cash-close-design-gate.md) | **C. BLOCKED — APPROVAL/GOVERNANCE RESOLUTION REQUIRED** — analysis/design only; no product code, no migration, no governance change, no D-21+, no commit, no push | `bfe7e69` (unchanged) | `feat/production-spec` (unchanged) | 30 (unchanged — **migration 31 NOT created**; contingent shape recorded only) | None run — design gate; substrate verified by direct schema/source/migration inspection | Determined P1G-1 is **not** implementation-ready and corrected the prior next-slice report's "no governance blocker" reading. **Proved the approval blocker rather than asserting it**: FR-SEC-030 [M] mandates a *general* approval mechanism, FR-SEC-031/032/033 [M] mandate request+decision entities with expiry and immutability, FR-FIN-006 [M] requires approval by a non-owner holding `cash.variance.approve`, and FR-SEC-016 [M] requires the system to **block, not warn**, on approving one's own cash variance *regardless of role configuration* — with §15's SoD matrix independently naming `cash.session.close` + `cash.variance.approve` as "Self-approved shortage". That mechanism **does not exist in any form** (verified exhaustively: no `Approval*` model, no approval tables in any migration, no approval source files — only FK-less recorded-UUID columns `audit_entries.approval_id` and `waste_records.approval_request_id`, NULL in this phase, plus a caller-supplied `requires_approval` boolean that is not a workflow). **Standing it up is not available to an implementation slice**: the register classifies "Create approval request" as **Class C + E on D-16** (*"the `request_type` contract is deliberately open"*, *"must not be resolved here"*) and "Approve/reject" as **C + E on parent-linkage**, and records **"NO SOURCE-SUPPORTED RECOMMENDATION between P-1 and P-2"** requiring an **architectural ratification**. **Corrected the brief's own premise** that "P-1 remains: approval_decisions references approval_requests directly" — P-1 is one of two unresolved candidates whose approved-design basis the register records as **"NONE — a deviation"**, not a ratified position; this is the gate's most consequential finding. **Audited all eight FR-FIN-004 [M] expected-cash terms individually**: Opening Float IMPLEMENTED; Cash Sales and Rounding Adjustments IMPLEMENTABLE (the latter two only because of P1F-2); **Pay-ins/Pay-outs/Safe Drops NOT IMPLEMENTED though mandatory under FR-POS-091 [M]**; Cash Tips (FR-POS-056/057 are **[S]**) and Cash Refunds (explicit non-goal) OUTSIDE CURRENT SUPPORTED OPERATION — so only 3 of 8 terms exist, and **rejected "scoping out" the mandatory ones**, concluding a **P1G-0 Treasury CashMovement slice is a genuine prerequisite** (unblocked; `cash.payin`/`cash.payout`/`cash.safedrop` are SRS-named) while FR-FIN-004 stays PARTIAL regardless since tips/refunds have no operations. Established **repository cardinality facts** governing shift coupling: `cash_sessions.shift_id` is indexed **non-uniquely** (one Shift owns many sessions) and `uq_one_open_session_per_drawer` constrains **drawers not shifts** (sibling sessions can be open concurrently), so closing one session **cannot** close the Shift; the Shift-close trigger/actor/timing is **NOT SOURCE-DECIDABLE** and no standalone Shift-close route is proposed. Found **FR-PLT-025/026 [M] hierarchical settings entirely absent** (no model, no module, no resolver — only opaque `org.settings` JSONB), so blind-count mode and variance tolerance have no conformant home; blind-as-default is safe because FR-POS-095 [M] states it, but **no SRS variance-tolerance default exists anywhere**, making it a user/governance decision that compounds the approval blocker. Designed the **within-tolerance close path only** (single-command close computing expected cash inside the closing transaction under a session row lock, so a session can never close from stale expected cash), explicitly **declining** to invent `closing`/`awaiting_variance_approval` states for a path that cannot be built, and defining the count-invalidation race and a lock order (session → order → inventory layers) that matches P1F-2 and introduces no deadlock inversion. Specified a **server-enforced blind-count protocol** (declaration precedes disclosure; expected cash returned only in the close response; immutable declaration; idempotent retry) after auditing that no current endpoint leaks expected cash. Chose an **immutable `cash_counts` parent + denomination rows** model (integer minor units, no floating point), while recording that **valid denomination values are NOT SOURCE-DECIDABLE** — the country pack carries only currency code/exponent/cashRounding and **no denomination catalogue** — so no Egyptian catalogue may be invented. Justified storing expected/counted/variance as **immutable posted facts rather than projections** on FR-FIN-007 [M] + CR-04 grounds, mirroring P1F-2's `posted_cogs_total` reasoning. Concluded **FR-FIN-007 stays PARTIAL** (immutability met, adjusting entries unmet) because an adjusting entry's authorization code is **not named by the SRS**. Ruled tender totals should be **derived from immutable Payments, not snapshotted** (historically stable by construction once the session is closed), covering only `cash` and `manual_external_card`, so **global FR-FIN-010 cannot be claimed**. Found the **X report authorization NOT SOURCE-DECIDABLE** — no `cash.x_report` exists and `report.view.<category>`'s vocabulary is unenumerated — and confirmed it does **not** block the close calculation. Classified all 19 requirements: **only FR-POS-095 would reach COMPLETE**. Listed five minimum governance decisions and four NOT-SOURCE-DECIDABLE items; recommended **P1G-0 proceed now in parallel with the Governance approval ratification**. No Sonnet prompt issued (verdict is not A). No code, migration, or governance changed |
| 2026-08-28 | P1G-0 — Mid-shift Treasury cash movements design gate | [2026-08-28_P1G0_cash-movements-design-gate.md](2026-08-28_P1G0_cash-movements-design-gate.md) | **B. P1G-0 DESIGN READY FOR FR-POS-091; FR-POS-092 REMAINS PARTIAL** — analysis/design only; no product code, no migration, no governance change, no D-21+, no commit, no push. **Contains a material self-correction: the 2026-08-28 P1G-1 gate was WRONG on P-1** | `bfe7e69` (unchanged) | `feat/production-spec` (unchanged) | 30 (unchanged — **migration 31 NOT created**; exact shape specified only) | None run — design gate; substrate verified by direct schema/source/migration/SRS inspection | Defined the smallest correct prerequisite slice for FR-POS-091 [M] (PAY_IN / PAY_OUT / SAFE_DROP) and found it **fully source-decidable**: the three operations, their mandatory `reason` and `amount`, and the SRS-named permissions `cash.payin`/`cash.payout`/`cash.safedrop` are literal SRS text, and every attribution fact is already a trusted repository fact via the **existing** `CASH_SESSION_FACTS_QUERY` contract. **Separated literal SRS facts from design implications** field by field. **Settled money semantics**: store **positive amount + type** (rejecting signed, and rejecting both-as-redundant) on the repository's own positive-magnitude convention (`OrderPayment.amount`, P1F-2's test-locked positive `total_cost`), with the type supplying the sign exactly once in the totals contract; clients may never send a negative value; BIGINT minor units, no floating point. **Established that a client-generated ULID is MANDATORY, not optional** — §21.3's local data model lists *"Shifts, cash sessions, drawer events — Up — Continuous"* and FR-OFF-015 [M] requires device-created entities to take a client ULID as their **permanent primary key** the server SHALL NOT reassign — so Idempotency-Key alone is insufficient, and the OrderPayment permanent-id-replay-first precedent applies. **Derived the own-session-only rule from a permission-catalogue asymmetry**: §15.2 supplies `cash.session.close_other` for close but **no `_other` variant for any movement**, so cross-session authorization is NOT SOURCE-DEFINED and is not invented — adopted as the narrowest safe reading and classified explicitly as an interpretation. **Proved FR-POS-092 undecidable on all four parameters** — `treasury.drawers` has no limit column, the country pack carries only currency/cashRounding, FR-PLT-025's resolver does not exist, and `tenant.settings`/`defaultSettings` are **inert JSONB never read by any service** (grep: zero hits); no monetary threshold exists anywhere in the SRS; "a prompt **or** a block" never says which; and auto-creating a SafeDrop is unsupported and would fabricate a financial record no human declared. Concluded FR-POS-091 and FR-POS-092 are cleanly separable, so P1G-0 ships FR-POS-091 **COMPLETE** while FR-POS-092 is **NOT IMPLEMENTED (substrate enabled)** — stating plainly that zero of its clauses are met rather than overclaiming "PARTIAL". Chose **query the immutable ledger directly, no projection** (append-only rows make totals historically stable by construction), exposing a `tx`-first `CASH_MOVEMENT_TOTALS_QUERY` module contract with **no HTTP read route**, since no source-backed read permission exists. Specified the exact migration-31 shape (tenant/branch-safe composite FKs, `amount > 0` and non-blank-`reason` CHECKs, append-only grants, RLS ENABLE+FORCE with SELECT/INSERT policies only) while **deliberately declining to denormalise** `drawer_id`/`shift_id` (immutably reachable via the session). Chose **three routes over one `/movements`** on hard repository evidence: `@RequirePermission` is route-level static and must not inspect the request body to pick a permission. Designed the locking protocol (all writers `SELECT … FOR UPDATE` the session row and assert `status='open'`), proving **no inversion is possible** since P1G-0 takes only the first lock of P1F-2's `CashSession → Order → Inventory` chain, and flagging honestly that Payment currently takes no session lock — a gap belonging to P1G-1. Followed the `STOCK_MOVEMENT_RECORDED` precedent with **one** `CASH_MOVEMENT_RECORDED` audit action carrying `movementType` in metadata, and **published no domain event** because the SRS event catalogue defines none for cash movements. Recorded **corrections/reversals as NOT SOURCE-DECIDABLE**, explicitly refusing to let a PAY_IN cancel a PAY_OUT since that would falsify both type and reason. **§17 — P-1 GOVERNANCE CORRECTION: classified P-1 RATIFIED / RETAINED**, quoting the register verbatim (heading *"PL — approval_decisions → Parent Linkage (carried item — RATIFIED)"*; *"STATUS: RATIFIED 2026-08-18"*; *"RATIFIED — P-1: `approval_decisions` SHALL REFERENCE `approval_requests` DIRECTLY"*; and the later *"Parent linkage P-1 is RATIFIED and not reopened"*). **The prior P1G-1 gate mistook a retained analysis line (*"NO SOURCE-SUPPORTED RECOMMENDATION between P-1 and P-2"*) for the register's conclusion — it is the problem statement, and the ratification follows 44 lines later.** The register is **not** internally contradictory. P1G-1's verdict C survives on **narrower** grounds (D-16 still OPEN post-ratification, the approval mechanism absent entirely, endpoints Class C+E, SB-1/SB-3 unresolved with DP-1…DP-4 unselected), but its parent-linkage ground is **falsified**; a narrow correction pass on that report is recommended and **not performed here**. Issued a complete Sonnet implementation prompt (verdict B). Four user/governance decisions listed for FR-POS-092; none blocks P1G-0. No code, migration, or governance changed |
| 2026-08-28 | P1G-0 — Mid-shift Treasury cash movements implementation | [2026-08-28_P1G0_cash-movements.md](2026-08-28_P1G0_cash-movements.md) | **IMPLEMENTATION COMPLETE — FR-POS-091 COMPLETE, FR-POS-092 NOT IMPLEMENTED (substrate enabled)**; contains one binding production-code correction to the design gate's own locking mechanism (§F: advisory lock replaces `FOR UPDATE`, which is inoperable under `ros_app`'s grants) and one test-harness-only fix (§G: a genuine ~35-minute hang traced to a `supertest` concurrent-HTTP-dispatch artifact, not a production deadlock — resolved by switching the CONCURRENCY block to direct service calls). No commit, no push | `bfe7e69` (unchanged) | `feat/production-spec` (unchanged) | **31** (+1 new: `20260828010000_treasury_cash_movements`; applied cleanly from zero on a fresh scratch DB; persistent local `ros` DB confirmed still untouched, 26 rows) | Unit **732/732** (53/53 suites, unchanged); E2E **828/828** (42/42 suites — 793 baseline + 35 new: 8 domain, 1 totals-contract, 6 AUTH, 4 idempotency, 4 RLS, 12 concurrency [4 scenarios × 3 clean isolated runs, real Postgres advisory-lock contention proven via `pg_stat_activity` polling, never sleeps]); `module-boundaries.spec.ts` 31/31 (`KNOWN_DEVIATIONS` unchanged); `tsc`/`eslint`/`prisma validate`/`nest build`/`git diff --check` all clean; OpenAPI **3.1.0/138** (135+3, exactly as the design gate predicted) | Implemented the design gate's three routes (`POST /cash-sessions/{id}/pay-in\|pay-out\|safe-drop`) with the user's two binding pre-execution corrections applied exactly: (1) client-generated `id` REQUIRED on all three DTOs, no server fallback, OpenAPI marks it required, tested missing→400/malformed→400/preserved-exactly; (2) exactly ONE actual `INSERT ... ON CONFLICT ("id") DO NOTHING RETURNING ...` per call, permanent-id replay/conflict check performed **logically first** (before any CashSession-dependent mutation), and on a lost race the winner is `SELECT`ed in the still-healthy transaction with no duplicate audit on replay — proven under a real two-connection Postgres race (12 concurrency tests, 3 clean runs each). **Found and fixed two genuine implementation issues during testing, both documented as corrections rather than silently patched**: (§F) the design gate's literal `SELECT ... FOR UPDATE` session-lock mechanism fails with Postgres `42501 permission denied` because `ros_app` deliberately holds no UPDATE grant on `cash_sessions` (append-only-until-P1G-1 posture) — replaced with a transaction-scoped `pg_advisory_xact_lock(hashtext('ros_cash_session'), hashtext(sessionId))`, matching the existing `AuditService.record()` precedent exactly, verified via three real concurrent `psql` sessions before relying on it, and recorded as a **binding contract P1G-1 must reuse the identical lock to actually serialize against**; (§C) `assertIdentical`'s replay-detection excludes the server-defaulted `occurredAt` field from the identity comparison (mirroring `OrderPayment`'s own `processedAt` exclusion precedent), since two legitimate retries of an identical body each get a different server-stamped instant otherwise. **User-flagged ~35-minute test hang, diagnosed live per the user's explicit no-discard/no-DB-touch/diagnose-with-pg_stat_activity-before-fixing protocol**: proved via targeted instrumentation that the stall was a `supertest`/single-in-process-HTTP-server concurrent-dispatch artifact (the second racing HTTP request never reached the controller at all; `pg_stat_activity` showed exactly one `ros_app` backend, `idle in transaction`/`wait_event_type='Client'`, no blocked query, no second connection) — not a Postgres lock wait and not a production deadlock, since production code was never reached by the second call; fixed test-only by switching the CONCURRENCY block to direct `CashMovementsService` calls, verified clean across 3 consecutive isolated runs before resuming. Updated 3 pre-existing slice-boundary "tripwire" e2e assertions (`cash-session.e2e-spec.ts` ×2, `catalogue.e2e-spec.ts` ×1, `inventory.e2e-spec.ts` ×1) to admit the 3 new routes and 1 new table — a deliberate, reviewed widening, not a weakening (`close`/`count`/`variance`/`drawer` remain asserted absent, since P1G-1 and FR-POS-092 are genuinely unimplemented). Classified precisely: FR-POS-091 COMPLETE; **FR-POS-092 NOT IMPLEMENTED (substrate enabled)** — not overclaimed PARTIAL; **FR-FIN-004 PARTIAL — 6/8 terms** (tips/refunds still have no operations, not claimed COMPLETE); FR-API-020 PARTIAL (system-wide); FR-API-021/022/023 IMPLEMENTED for covered routes; NFR-REL-011 HELD for this slice's operations only. Corrections/reversals recorded as NOT SOURCE-DECIDABLE — no UPDATE/DELETE, no compensating-movement invented. **Recorded, per the user's explicit instruction and without editing the P1G-1 report itself, that P-1 (`approval_decisions` → `approval_requests` direct parent linkage) is RATIFIED/RETAINED** (per the 2026-08-28 P1G-0 design gate's own §17 correction) **and that the earlier 2026-08-28 P1G-1 report's reading of P-1/P-2 as unresolved was incorrect**. No commit, no push, three preserved user files untouched |
| 2026-08-28 | P1G-0 — Acceptance closure (performed_by FK audit, missing concurrency scenarios) | [2026-08-28_P1G0_acceptance-closure.md](2026-08-28_P1G0_acceptance-closure.md) | **P1G-0 FINAL ACCEPTED** — no production code changed; the `performed_by` tenant-safety concern investigated and found to be a correct restatement of an already-shipped pattern, not a defect; two new real-Postgres concurrency tests added and 3× re-verified clean; no migration 32, no commit, no push | `bfe7e69` (unchanged) | `feat/production-spec` (unchanged) | **31** (unchanged — no migration 32; re-applied cleanly from zero on 3 separate fresh scratch DBs across this pass; persistent local `ros` DB confirmed still untouched, 26 rows, throughout) | Unit **732/732** (53/53 suites, unchanged); E2E **837/837** (43/43 suites — 828 baseline + 9 new: 3 SAFE_DROP-vs-Payment interleaving runs + 6 movement-vs-future-close lock-contract runs [3 required-case + 3 opposite-direction], each independently re-verified 3 consecutive clean times against fresh scratch DBs — 27 individual clean passes total); `module-boundaries.spec.ts` 31/31 unchanged; `tsc`/`eslint`/`prisma validate`/`nest build`/`git diff --check` all clean; OpenAPI **byte-identical** regeneration confirmed via true before/after snapshot diff (not just `git diff` vs HEAD) — 3.1.0/138, zero drift | Investigated the acceptance-closure prompt's claim that `cash_movements.performed_by` should carry a tenant-safe composite FK and found the premise factually incorrect by reading the controlling design gate's own migration-31 spec verbatim: `performed_by` was **never** listed among "ALL tenant-safe composites" (only `cash_session_id`/`branch_id`/`employee_id` are), and is specified from the start as a **plain** FK mirroring the already-shipped `inventory.stock_movements.performed_by` precedent (byte-identical FK shape, confirmed by direct migration-SQL comparison). Proved `identity.users` structurally **cannot** expose a `(tenant_id, id)` composite target — it has no `tenant_id` column at all, because tenancy is a genuine many-to-many via `Membership` (one User can hold active memberships in several tenants), so adding one would require an unauthorized Identity-table redesign this pass has no gate for. Traced the ACTUAL tenant-safety guarantee instead of assuming none exists: `performed_by` is never client-supplied (grep-verified — no such DTO field), is always `context.userId`, and `TenantContextService.resolve()` DB-verifies (not merely JWT-trusts) an **active** `Membership(userId, tenantId)` before any request reaches the service — a per-request, database-checked guarantee strictly stronger than a raw structural FK could express for a multi-tenant User in the first place. Declined to fabricate the requested "real FK violation" test once this was established, since no such structural violation is constructible or honest to claim (documented explicitly rather than silently skipped). **No migration 32, no correction made — disposition: NO DEFECT.** Re-confirmed `identity.employees` (not `workforce.employees`) is the sole live Employee table; the `workforce.employees` string is dead prose surviving only in two OLDER migrations' comments, documenting a historical deviation, never a real table — no table move performed. Built both previously-missing concurrency scenarios as new tests in `test/cash-movements-close-and-payment-concurrency.e2e-spec.ts`: **§D** SAFE_DROP vs a settling Payment on the same CashSession — explicitly framed and proven as an INTERLEAVING/INTEGRITY proof rather than a mutual-lock proof, since `SalesPaymentService.capture` reads CashSession facts via a plain SELECT and takes no lock at all (verified by direct code reading); a real settling Payment (menu item with **no recipe**, exploiting the verified `0 depletion, no gap` code path for a `recipeVersionId === null` line) is proven to complete correctly WHILE a SAFE_DROP holds the `ros_cash_session` advisory lock open and uncommitted, with two independent-connection checks proving the SAFE_DROP row genuinely was not yet visible both before and after the Payment resolved — real temporal overlap, not sequencing. **§E** Movement vs a future CashSession close — a TEST-ONLY raw-migrator-connection "close writer" (no production service, no `ros_app` grant change) exercising the IDENTICAL advisory-lock namespace/key P1G-1 is contractually bound to reuse; proved both the required close-wins case (close-writer locks + updates to `closed` + pauses uncommitted → a real movement attempt is proven genuinely BLOCKED via `pg_stat_activity` polling → close commits → movement unblocks, sees `closed`, returns a deterministic 409, zero rows written) and, as a bonus, the opposite movement-wins-first direction (movement holds the lock, close-writer proven genuinely blocked, movement commits, close then proceeds normally) — 3 clean runs each, 6 total. Re-verified and restated (not re-derived) the advisory-lock contract: `ros_app` genuinely lacks the UPDATE privilege `FOR UPDATE` would require; the lock is transaction-scoped with no leaked waiters observed across any of the 15 new test executions; no grant was widened anywhere in this pass; explicitly restated that Payment still does NOT serialize with CashSession today (§D's own passing assertions are direct evidence FOR this fact, not against it). Softened the original implementation report's §G wording from a categorical "supertest limitation" claim to "a test-harness HTTP dispatch artifact; the exact lower-level dispatch cause was not further isolated, but production code and PostgreSQL lock contention were excluded by direct evidence" — corrected in place in the same report, the hard-evidenced claims (single debug-log fire, single `ros_app` backend, `Client` not `Lock` wait) retained verbatim since they were directly observed and remain true. Confirmed via `git diff --stat` that zero RLS/grant statements were touched anywhere in this pass. One genuine defect was found and fixed — in the NEW TEST'S OWN fixture setup (a missing branch `Location` row `SaleDepletionService.depleteForCompletedSale` requires even for a zero-depletion line), diagnosed from the real Postgres error and fixed by matching the already-accepted P1F-2 concurrency fixture pattern, not a production defect. All required/accepted P1G-0 semantics (PAY_IN +, PAY_OUT −, SAFE_DROP −, positive-magnitude BIGINT storage, required client id, one actual INSERT, append-only ledger, own-session-only, three SRS-named permissions only, no read route, no correction/reversal, no drawer limit, no `expected_cash` storage, no new domain event, FR-POS-092 still NOT IMPLEMENTED, FR-FIN-004 still PARTIAL 6/8) verified unchanged — zero production files touched in this pass. No commit, no push |
| 2026-08-29 | APPROVAL — Governance/runtime resolution gate (FR-SEC-030…033; unblocking P1G-1 cash-variance approval) | [2026-08-29_APPROVAL_governance-runtime-resolution-gate.md](2026-08-29_APPROVAL_governance-runtime-resolution-gate.md) | **B. USER RATIFICATION REQUIRED — MINIMUM PROPOSAL PROVIDED** (6 items) — analysis/architecture/governance-resolution only; no product code, no migration, no register edit, no D-21+, no commit, no push. Register verified **NOT internally contradictory** | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | 31 (unchanged — **migration 32 NOT created**; contingent shape recorded only) | None run — analysis gate; every claim traced to SRS text, register ratification blocks, or direct repository inspection at HEAD | Resolved the minimum architecture/governance surface for the SRS general Approval mechanism. **Found far more already ratified than the brief assumed**: the core request/decision model is essentially fully specified (D-1 fields, D-4 lifecycle `pending→approved\|rejected`, D-5 SINGLE-STEP, D-6 immutable-except-`status` via Production GAP-2, D-7 M2 self-approval via RLS `WITH CHECK` traversal, D-8 decisions fully append-only, D-9 S1+N1+U4 RLS as amended by D-7/D-10, D-10 E2 decision-time expiry with no scheduler/no `expired` status, P-1 direct parent linkage). **Verified P-1 RATIFIED and cited verbatim** (register lines 4378, 4606 + 18 binding clauses, ratification log 5893); did **not** reopen or re-compare P-1/P-2. **Ran the mandated register-contradiction check and cleared it**: the trailing "Design Gate Readiness" section (which still lists D-14/D-15/D-17/D-18/D-19/D-20 as unratified) is *superseded historical text*, not a contradiction — the register operates by forward supersession with historical text deliberately left unedited, and says so at line 5904; recorded explicitly because **this is the same class of reading error the P1G-1 gate made on P-1**, and the rule ("a Final Decision Matrix row and a dated ratification block outrank earlier prose") is written down so it is not made a third time. **THE DECISIVE FINDING: building the approval runtime exactly as ratified would NOT unblock P1G-1** — D-14 A-1 (no HTTP surface) + D-2 (a) (synchronous manager PIN for *approvals* out of scope, explicitly NOT lifted by the 2026-08-19 amendment, which covered PIN *authentication* only) + D-11 N-B (strict no notifications, async knowingly unmet) + D-20 (no read surface) jointly leave **no path by which any human can approve anything**; D-14 records this in its own words — the phase *"delivers the tables and internal service exercisable only by tests."* **Two new findings, neither previously recorded: (F-1)** D-7's ratified `requester ≠ approver` traversal does **not** satisfy FR-FIN-006's `approver ≠ session owner` under `cash.session.close_other` (requester = closing supervisor, so the *session owner* remains free to approve their own variance — exactly the combination FR-SEC-016 [M] requires to be *blocked, regardless of role configuration*); corroborated by D-7 cl. 10's own disclaimer that no claim is made for the **Finance** FR-SEC-016 combination, which was out of Phase 1 scope — bringing cash variance in-scope is what exposes it. Three candidate resolutions given, R-c (service-level enforcement) rejected on D-7 cl. 5's own text. **(F-2)** synchronous PIN approval requires **Identity to publish its first `contract/` directory** — it has none (publishes via Nest `exports` only), and `module-boundaries.spec.ts` permits no module to import `employees/pin.service`, with `KNOWN_DEVIATIONS` asserted exactly so debt cannot grow silently; `PinService.authenticate` is already a pure verification returning `{employeeId,userId,branchId,terminalId,membershipId}` and minting **no** session, so the capability exists and only its publication is missing. **D-16**: established that only the *constraint form* is open — D-16's own finding 5 already settles the type as `VARCHAR(32)` via the directly-on-point `stock_movements.reference_type` precedent — and that **cash variance is one of the NINE approval requirements outside FR-SEC-030's seven** (D-16's own evidence table, first row), so options (a)/(c) would silently make FR-FIN-006 **unsatisfiable**; only (d)/(e) leave P1G-1 reachable. Confirmed `cash.variance` as a literal value is **NOT SOURCE-DECIDABLE** and did not invent it. **SB-1**: representation already settled (permission **code**, not id); only FK posture open — RP-1 favoured on a live, concrete hazard (`cash.variance.approve` is deliberately unseeded per `treasury.permissions.ts:18`, so RP-2's FK would couple Governance writes to Treasury's seeding order). **SB-3**: genuinely blocks — the migration is literally unwritable without the FK's `ON DELETE` clause, which P-1 coupled to the D-8 cl. 6 cascade question; DP-3 already rejected, DP-1 favoured as the only option needing no V2 verification. **Also resolved/classified**: decision cardinality is NOT SOURCE-DECIDABLE but **mandatory before schema design**, because D-15 forecloses locking (§24.6.4 confines pessimistic locking to two named cases, approval not among them) leaving a UNIQUE constraint as the *entire* concurrency mechanism — so the report **refuses to claim the concurrency strategy is settled**; D-4 cl. 5 resolved as BOTH-with-distinct-roles (immutable `decision` fact vs single mutable `status` projection, not duplicate truth); subject model already settled as `entity_type`+`entity_id` (approved SQL + D-17 cl. 4) with **no fake cross-module FK** (the `audit_entries` un-FK'd precedent is directly on point); **no request fingerprint/hash proposed** (D-6 already makes the row immutable at the DB layer, and D-19 ratified no additional hash coverage); expiry semantics fully ratified but **default duration NOT SOURCE-DECIDABLE** and no figure invented; **no generic `approval.*` permission invented** — the approver's authority is the request's own `required_permission`, and because D-14 A-1 leaves no route, the prior Class C/E create-endpoint problem **dissolves** rather than being hidden; FR-API-020 correctly stated as not attaching (D-14 cl. 12), with the consuming Treasury close route's own `Idempotency-Key` supplying at-most-once. **D-12 NOT reopened** — confirmed the runtime can proceed single-step without it (D-5 + D-4 cl. 6 + P-1 cl. 2); **no `approval_steps` proposed**. Ownership confirmed Governance-owned by two independent SRS statements (§7.3 row 36; §25.1 names both tables in the `governance` schema) — **not** Treasury. Repository state verified at HEAD: **zero** approval models/tables/source files; only four vestigial artifacts (two un-FK'd audit columns, `waste_records.approval_request_id` never written, two `requires_approval` refusal gates) plus a negative test at `test/inventory.e2e-spec.ts:644-649` asserting the table was NOT created — flagged as a tripwire any future migration must update. **§24 P1G-1 impact — an unexpected conclusion recorded plainly**: the approval mechanism is *not* P1G-1's nearest blocker; the **variance-tolerance/settings source is**, since FR-FIN-006 says *"beyond a **configurable** tolerance"* and FR-PLT-025/026 hierarchical settings are entirely absent, blocking even the within-tolerance close the P1G-1 gate designed. Minimum ratification surface deliberately minimised to **six** items, with SB-2, the expiry default, the D-17-extension question and F-1's resolution **excluded** from it and justified individually. Single cohesive proposal provided using the existing carried-item/amendment mechanism — no D-21, P-1 unamended, D-12 left BLOCKED, D-16's enumeration left OPEN, tally unaltered. **No Sonnet implementation prompt issued** (architecture not fully source/governance-decided). No commit, no push, unrelated working-tree reports untouched |
| 2026-08-29 | APPROVAL — Ratification proposal narrow correction (SB-2 blocking; F-1 requires ratification) | [2026-08-29_APPROVAL_ratification-proposal-correction.md](2026-08-29_APPROVAL_ratification-proposal-correction.md) | **B. CORRECTED RATIFICATION PROPOSAL REQUIRED — PROVIDED** — the prior gate's six-item claim **withdrawn**; corrected total **EIGHT**. Analysis only; no product code, no migration, no register edit, no D-21+, no commit, no push | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | 31 (unchanged — **migration 32 NOT created**; corrected contingent shape recorded only) | None run — analysis/correction gate; every claim traced to SRS text, register ratification blocks, or the prior report's own text | Upheld the user's challenge on **both** counts, against the prior gate's own reasoning. **SB-2 IS SCHEMA-BLOCKING**: `approval_requests.value` is mandatory (`FR-SEC-031` [M] enumerates it; **D-1 RATIFIED option (a) "ADD ALL THREE FIELDS"**, having explicitly rejected its own options (b)/(c) as leaving a mandatory requirement *"unmet by construction"*), and **no authority types it** (SB-2: *"the SRS does NOT mandate `DECIMAL(18,6)`, `JSONB`, a dimension enum, or any other specific persistence type"*; NO SOURCE-SUPPORTED RECOMMENDATION among VT-1/2/3/5, only VT-4 excluded). **The prior §21 table is the proof of its own error** — it printed `**SB-2 UNRESOLVED**` in the *type* column and `—` in the *nullability* column of a mandatory field while §22 claimed the migration could proceed; a column with no type cannot be written. Diagnosed the precise reasoning failure: prior §14.3 concluded *"Governance need carry only what `FR-SEC-031` enumerates"* **without noticing that `FR-SEC-031` enumerates the value** — the premise refutes the conclusion. Same failure mode the prior gate had correctly identified for SB-3's `ON DELETE` clause and then inconsistently failed to apply to SB-2. **Recommended VT-3 (`JSONB NOT NULL`)** on three ranked grounds, without inventing any new option: **(i)** D-13's RATIFIED language *"Governance is a generic carrier"* is a near-verbatim endorsement of the opaque-carrier posture the brief says to prefer *"if and only if authority/governance supports it"* — and VT-3 is the only genuinely opaque candidate, VT-1/VT-2 both requiring Governance to hold a numeric (and for VT-2, semantic) opinion about values it does not own; **(ii)** it is the **only** live option carrying `FR-POS-047`'s **boolean** dimension — VT-1/VT-2 are numeric columns and cannot represent *"discount permitted after payment started"* at all, so under either the column must be nullable and `FR-SEC-031` [M] goes partly unmet for a whole class of approvals — and this **resolves as a side effect the nullability sub-question SB-2 expressly left open**; **(iii)** VT-2 would require ratifying a Governance-owned descriptor vocabulary that SB-2 itself warns *"must not become a proxy for D-16's enumeration"*, enlarging rather than minimising the ratification surface, whereas VT-3 invents no vocabulary at all. Rejected VT-1 (no boolean; a stored `15.000000` is permanently ambiguous between 15%, 15 units and 15 minor units), VT-2, VT-5 (register's own words: leaves *"SHALL specify the value"* **only partly served** — the same defect D-1 rejected), VT-4 (already excluded). **Made the storage/semantics separation binding rather than asserted**, with two enforceable clauses: money carried as **exact decimal strings of minor units, never JSON numbers** (JSON numbers are IEEE-754 doubles — the precise float hazard the BIGINT discipline exists to prevent; the string convention is already shipped in `CashMovementDto.amountMinor` and in `audit_entries` JSONB metadata), and **no Governance RLS policy, CHECK, index or predicate may read into `value`**. **F-1 CANNOT REMAIN A DESIGN DETAIL**: its only fully DB-enforced resolution (R-b) amends **two** ratified decisions — D-1's exhaustively-enumerated column set, and D-9's INSERT predicate — and the register **fixes the precedent verbatim at line 1112**: *"M2 would add a second conjunct to that ratified policy. That extension **must be ratified explicitly, not applied silently**"*, a rule already applied twice (D-7 cl. 6, D-10 cl. 9), making a fourth conjunct the third instance under the same rule. **New analysis withdrew R-a as incorrect, not merely inconvenient**: reassigning `requested_by` to the session owner would (a) record a **false immutable fact** in an append-only, D-6-frozen row against `FR-SEC-031`'s plain *"the requesting user"*, and (b) **silently drop** the genuine requester≠approver protection, letting the supervisor who actually requested approve their own request — defeating SRS §7.3 row 36's aggregate invariant. Recommended a **generic excluded-party column + fourth RLS conjunct**, Governance attaching no domain meaning (D-13-consistent), D-7's existing conjunct left unchanged so §7.3 #36 and `FR-FIN-006` hold simultaneously and independently. Recorded **R-d** (restrict beyond-tolerance close to the session owner, making requester ≡ owner so D-7's *existing* predicate definitionally yields `FR-FIN-006`) as the only no-amendment path, with a formal proof **and** its disclosed cost — `cash.session.close_other` unusable for that path, and the guarantee made conditional on a service-layer restriction that a later slice could silently relax, precisely the fragility D-7 cl. 5 exists to prevent. **Found a cross-item interaction neither the prior gate nor the challenge anticipated**: items 7 and 8 are **mutually reinforcing and must be ratified together** — item 8's typed column is exactly what keeps item 7's opaque payload out of every security predicate; choosing VT-3 *without* item 8 would force the RLS predicate to parse domain-supplied JSONB and collapse the D-13 boundary. **Six-item review**: items 1, 3, 4, 5, 6 **KEEP AS WRITTEN** (each re-verified against the brief's specific tests — D-16 enumeration stays OPEN while the type is authorised; DP-1 + RESTRICT internally consistent; `decision`/`status` are an append-only event and a single-transition projection, not duplicate truth; exactly-one-final-decision **entailed by** D-4's terminal lifecycle and D-6's single mutable column, and not a D-15 amendment since a UNIQUE constraint is declarative schema rather than a concurrency mechanism; the D-2 amendment lifts the synchronous half only); item 2 **AMENDED** with one clarifying clause after confirming RP-1 does **not** impede decision-time permission validation — the check is a membership test over the already-resolved permission-code set (`TenantContextService` → `PermissionGuard`), in which no foreign key participates — and recording the **deliberate layer asymmetry**: permission-holding is validated service-side like all other permissions, while self-approval is DB-enforced **because** `FR-SEC-016` demands it hold *"regardless of role configuration"* and therefore cannot itself be a role/permission check. Residual disclosed honestly: under RP-1 an unseeded/typo'd code yields a permanently un-approvable request, which fails **closed**. **Corrected total: EIGHT ratifications**; corrected §21 deltas given (`value JSONB NOT NULL`; new nullable excluded-party UUID; decision-INSERT predicate goes from three conjuncts to four), with the migration writable at that point and **not before**. Verdicts C and D explicitly not selected — neither item needs a *separate* gate, both being resolvable inside the same narrow carried-item block. All other prior-gate findings stand unamended (P-1 verification, register internal consistency, the D-14/D-2/D-11/D-20 "no human can approve anything" finding, F-2 Identity-has-no-`contract/`, and §24's conclusion that the variance-tolerance/settings source is P1G-1's nearest blocker). No D-21, P-1 unchanged, D-12 BLOCKED, async `FR-SEC-032` deferred, no Governance HTTP surface. No commit, no push, unrelated working-tree reports untouched |
| 2026-08-29 | APPROVAL — Ratification RECORDED in the Governance Register (items 1–8; item 8 = R-b) | [2026-08-29_APPROVAL_ratification-recorded.md](2026-08-29_APPROVAL_ratification-recorded.md) | **RECORDED — EIGHT RESOLUTIONS BINDING** — governance recording only; **the Governance Decision Register is the binding record, this report is evidence**. No product code, no migration, no test change, no implementation, no D-21+, no commit, no push | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | 31 (unchanged — **no migration written or authorised**) | None run — governance recording; verification was mechanical (`git diff --numstat`, phrase-level checks against the new register text) | Recorded the user's explicit ratification of the corrected Approval Runtime Minimum Resolution items 1–8 (item 8 = **R-b**) plus two binding clarifications, as an **unnumbered carried-item/amendment entry** in the register's established forward-supersession style — a new `## Approval Runtime Minimum Resolution — 2026-08-29` section placed chronologically after the FIFO Exhaustion Carry-Forward Ratification and before the Final Decision Matrix, matching the P1A/P1C/P1D · Fire Authorization · P1F-2 Completion Economics · FIFO convention, plus a condensed authoritative bullet appended to the Ratification log. **Register diff is `263 insertions, 0 deletions`** — mechanical proof that no historical text was rewritten, no decision renumbered and no D-21+ created; the SB status table of 2026-08-19, D-15's clauses 4/10/11 and D-2's 2026-08-17 text are all byte-identical and superseded forward only. **Eight resolutions recorded**: (1) `request_type` = `VARCHAR(32) NOT NULL` with no CHECK this phase, **D-16's enumeration explicitly left OPEN and D-16 NOT closed**, literal cash-variance code undecided; (2) **SB-1 RESOLVED** — immutable §15.2 code, no FK, authority checked at decision time against the approver's resolved permission-code set; (3) **SB-3 RESOLVED** — no DELETE capability, no DELETE policy, decisions FK `ON DELETE RESTRICT`, CASCADE still rejected, and the **D-8 clause 6 cascade verification thereby dissolved** with **V2 not required**; (4) **D-4 clause 5 RESOLVED** — `decision` the immutable fact, `status` the current-state projection and still the sole D-6-updatable column; (5) exactly one final decision per request via `UNIQUE (tenant_id, approval_request_id)`; (6) **D-2 amended in part** — synchronous manager-PIN approval only, with async `FR-SEC-032`, D-11, D-12, broader branch RBAC, D-14 A-1 and D-20 all expressly preserved; (7) **SB-2 RESOLVED** — `value` = `JSONB NOT NULL` as an opaque carrier Governance never parses, indexes into or reads in any RLS/CHECK/security rule, money as base-10 integer strings of minor units, money-only `BIGINT` exclusion preserved and the open nullability sub-question resolved by `NOT NULL`; (8) **F-1 = R-b** — one nullable excluded-approver column plus a DB-enforced fourth conjunct on the decisions INSERT policy, amending D-1's column set and D-9's policy in the manner of D-7 cl. 6 and D-10 cl. 9. **One substantive correction was required during recording and was made rather than glossed**: the correction report's claim that item 5 *"does not amend D-15"* is **false** — **D-15 clause 4 prohibits the one-decision-per-request UNIQUE constraint by name**, and the ratification log repeats it verbatim; the ratified **substance was recorded unchanged** while its governance characterisation was corrected to a **narrow amendment of D-15 clause 4 via the route D-15 clause 14 itself provides** (*"must be handled by an explicit future decision/amendment … It must not be inferred from D-15"*), superseding D-15 clauses 10 and 11 while **preserving clauses 3, 5 and 9's C-3 prohibition**. Recording the claim as written would have placed a demonstrably false statement into the register and left item 5 resting on an inference D-15 expressly forbids. **A second correction**: the prior reports' IEEE-754 attribution for JSON numbers was **withdrawn** per the user's binding instruction and is **not asserted in the register** — PostgreSQL `jsonb` stores numbers as `numeric`, so the claim was wrong as applied to storage; the recorded rationale is application-layer numeric precision. **A third latent conflict was surfaced and resolved without amending governance**: item 8's fourth conjunct sits against D-15 clause 9's *"preserve D-9 exactly"*, so the entry supersedes that **general** preservation only to the extent of the new conjunct while leaving its **specific** C-3 pending-status prohibition in force — conflating them would have either blocked item 8 wrongly or silently voided a live prohibition. **Binding clarification B proved load-bearing**: `approval_decisions.approver_id` resolves to `identity.users` while `cash_sessions.employee_id` resolves to `identity.employees`, so without the User-not-Employee clarification the comparison would have been type-mismatched and the conjunct would never have fired. **One Design-Gate consequence recorded as a note and explicitly NOT ratified**: `identity.employees.user_id` is **nullable** (verified in `prisma/schema.prisma`; SRS §7.3 #25 *"May link to at most one User"*), so the Employee→User mapping can yield NULL and leave the conjunct inert — the Design Gate must address it, though PIN authentication already requires a linked User. Register entry carries an explicit **"Not decided by this entry"** block naming all sixteen items the brief listed (literal cash-variance `request_type` code, exact column name/FK/RLS SQL, service interfaces, Identity contract shape, migration number, close API shape, variance tolerance, denomination catalogue, X-report permission, Shift close, Day Close, D-12, async approval, notifications) so none can later be read as settled by implication. **Verified after editing**: P-1 binding text unmodified and restated as RATIFIED/UNCHANGED · 0 new `## D-nn` headings · D-12 BLOCKED · D-16 enumeration OPEN · async `FR-SEC-032` deferred · no Governance HTTP surface · no Governance read surface · all eight items present exactly once · both clarifications present · D-15 clause 4's original text confirmed intact · `git diff --check` clean. **20-decision tally unaltered (17 RATIFIED · 1 IN PART · 1 BLOCKED · 1 OPEN)**, D-16 remaining the OPEN one. The approval schema is now fully typed and posture-complete so a migration is **writable for the first time**, but **is not authorised** — a runtime Design Gate must first settle column name, FK shape, RLS SQL, the Identity `contract/` shape for PIN verification and the concurrency matrix; and P1G-1 remains separately blocked on the **variance-tolerance/settings source**, which can proceed in parallel. No commit, no push, unrelated working-tree reports untouched |
| 2026-08-29 | APPROVAL — Runtime final design gate (schema, RLS, transaction, Identity PIN contract) | [2026-08-29_APPROVAL_runtime-final-design-gate.md](2026-08-29_APPROVAL_runtime-final-design-gate.md) | **A. APPROVAL RUNTIME IMPLEMENTATION READY** — design only; **no USER RATIFICATION REQUIRED items remain**; no product implementation, no migration, no commit, no push, no D-21+ | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | 31 (unchanged — **migration 32 planned but NOT created**; exact shape specified only) | None run — design gate; every claim traced to SRS text, the current working-tree register (incl. the uncommitted 2026-08-29 ratification), or direct code inspection at HEAD | Produced the implementation-writable design for the shared Governance approval runtime, treating the twenty ratified facts as binding and using the **working-tree register as authority over both earlier analysis reports**. **Three findings from current code, not report prose, shaped the design.** (1) **The expiry inequality is already decided** — D-10's ratified option E2 reads *"a decision may not be inserted once `now() > expires_at`"*, and clause 3 says *"has passed"*, so a decision **at exactly** `expires_at` is permitted; the policy uses `expires_at < now()` as the violating condition and **no ratification was requested**, correcting the brief's premise that this might be NOT SOURCE-DECIDABLE. (2) **The pending-status guard already exists as a DB predicate in the correct place** — **D-9 U4** ratifies the *request UPDATE* policy as `USING (T AND status='pending')` + `WITH CHECK (T AND status IN ('approved','rejected'))`, while **D-15 clause 9 forbids** a pending predicate on the *decisions INSERT* policy (a prohibition the 2026-08-29 ratification expressly preserved) — so the decisions policy keeps exactly **four** conjuncts, the compare-and-set lives on the UPDATE policy where it was ratified, and **no new predicate is invented**; the application additionally asserts affected-rows=1 to convert an RLS-filtered no-op into an error. (3) **`PinService.authenticate` opens its own transaction and nested `withAuthContext` is explicitly unsupported**, so PIN verification **cannot** sit inside the approval transaction — dictating the whole command ordering (verify outside → then one transaction for INSERT → CAS UPDATE → audit), a constraint a naive design would hit only at runtime; it is also **security-load-bearing**, since `recordFailure` runs outside any transaction so lockout counters survive a caller rollback (otherwise an attacker gets unlimited PIN attempts by forcing rollbacks). **§15 / F-1 nullable-User question resolved without ratification**: `PinService.authenticate` refuses any employee with `userId = NULL` and `CashSession.employeeId` comes only from a PIN-issued claim, so a POS-reachable session owner always has a linked User **at open time** — but this is **not structurally guaranteed** (`open` never selects `userId`, `Employee.userId` is nullable with `onDelete: SetNull`, `ros_app` holds schema-wide DELETE on `identity`, and nothing re-verifies at close), so the design **fails closed**, refusing the beyond-tolerance close exactly as `counts.service.ts:237` / `waste.service.ts:46` already refuse an approval-requiring post; all four prohibited escapes are rejected explicitly, including the observation that substituting `employee_id` would compare an Employee id to a User id and **never match**, silently voiding the conjunct. **§4 answered A** — the runtime is fully generic and needs **no** cash-variance `request_type` value; because the column is unconstrained, tests may use an explicitly test-only literal carrying no authority (materially unlike P1G-0, where a closed enum forced real values), so **`cash.variance` was not invented** and the literal stays with P1G-1. **§5 schema** given column-by-column for both tables with types, nullability, defaults, CHECKs, UNIQUEs, FKs, indexes, grants and RLS; excluded field named **`excluded_approver_user_id`** (generic, no cash vocabulary); **no FK on any user column**, decided on reasoning rather than nearest-file copying — `governance.audit_entries` ships `actor_id`/`approver_id`/`approval_id` FK-less while `cash_movements.performed_by` has an FK, and the governance precedent governs because (a) `identity.users` is **global** so an FK would give **zero tenant safety** (which instead comes from the row's `tenant_id` under FORCE RLS plus membership-validated provenance of every user id), (b) **no `ON DELETE` action is safe** — `SET NULL` would silently void the exclusion and weaken the ratified conjunct, `CASCADE` would delete a never-deletable request, and `RESTRICT` would permanently block deleting any user ever named on any request since requests are never deleted, and (c) consistency with the one existing Governance table; the *DB FK ⟺ Prisma relation* rule is preserved so **`User` and `Tenant` models are not modified at all**. **§9 transaction** proves no divergence window (both writes in one transaction; affected-row assertion converts a filtered CAS into a rollback), and resolves the two-manager race with the ratified **`UNIQUE (tenant_id, approval_request_id)`** alone — no advisory lock, no `SELECT FOR UPDATE`, no version column — using **`ON CONFLICT DO NOTHING`** rather than catching `23505` because a raised violation aborts the transaction and makes the recovery read fail with `25P02` (the P1E-5A lesson); constraint kept **IMMEDIATE** since deferring would move the violation to COMMIT and defeat `ON CONFLICT`; noted that this **closes precisely the residual D-15 recorded** (*"a decision can be inserted against an already-decided request"*), and that the only extra lock is `AuditService`'s pre-existing per-tenant chain lock, acquired last and identically system-wide, so **D-15 clause 5 is untouched**. **§10 RLS** gives the exact four-conjunct `NOT EXISTS` policy honouring D-7's ratified wording (noting the repo currently has **zero** `NOT EXISTS` policies — every existing one uses `EXISTS` — so the ratified wording was followed over the local idiom and the divergence recorded), proves NULL-safety of the excluded-user disjunct without an `IS NOT NULL` guard, proves fail-closed on missing tenant context, and notes the tenant conjunct must stay top-level because the subquery would otherwise evaluate TRUE with no context. **§3 Identity contract** classified a **verification contract** (`identity/contract/pin-verification.contract.ts`, token `TERMINAL_PIN_VERIFIER`, `useExisting: PinService`) — Identity's **first** public contract — with two documented departures: **not `tx`-first** (security-motivated, per above) and **returns the effective permission set**, justified because no service answers "does user X hold code Y" for a non-principal user and the alternatives (a general permission-query API, or a private `TenantContextService` import) are strictly larger surface or a boundary violation. **§11** records the deliberate layer asymmetry — permission-holding checked service-side like all other permissions, self-approval DB-enforced **because** `FR-SEC-016` demands it hold *"regardless of role configuration"* and therefore cannot itself be a role check — with all three failure modes failing closed. **§13** specifies two audit actions (one decision verb + outcome in metadata, per the `CASH_MOVEMENT_RECORDED` precedent), **no expiry event** (expiry mutates nothing), keeps `value` out of audit metadata to preserve the opacity boundary, notes `stableStringify` **throws on a `bigint` leaf**, recommends extending `AuditEvent` with optional `approvalId`/`approverId` to populate the existing columns, and **explicitly does not claim GAP-11 is fixed**. **§17** gives all 14 concurrency scenarios with mechanisms, barriers and ≥3 clean runs. **§18** plans **migration 32** (Governance-owned), enumerates every touchable file, notes `KNOWN_DEVIATIONS` must **not** grow since both new imports are public-surface, expects OpenAPI to stay at **138** (drift would signal an accidental endpoint), and flags `test/inventory.e2e-spec.ts:644-649` — the **only** such assertion in the suite — for deliberate inversion. **§19** classifies honestly and separates substrate from consumer: FR-SEC-031/033 COMPLETE (substrate), **FR-SEC-030 PARTIAL** (mechanism exists but **zero consumers wired**), **FR-SEC-032 PARTIAL** (async deferred, so COMPLETE cannot be claimed), FR-SEC-016 PARTIAL (one of four combinations), **FR-FIN-006 NOT IMPLEMENTED — substrate enabled**. **§20 classifies every item; NONE is USER RATIFICATION REQUIRED**, so a Sonnet implementation prompt is permissible — deliberately not issued here since this is a design gate. Reiterates that the variance-tolerance/settings source, not the approval mechanism, remains P1G-1's nearest blocker and can proceed in parallel. P-1 unopened, D-12 BLOCKED, D-16 enumeration OPEN, async deferred, no Governance HTTP or read surface. No commit, no push, unrelated working-tree reports untouched |
| 2026-08-29 | APPROVAL — Runtime design acceptance closure (expiry clock, decided_at, PIN trust boundary) | [2026-08-29_APPROVAL_runtime-design-acceptance-closure.md](2026-08-29_APPROVAL_runtime-design-acceptance-closure.md) | **B. DESIGN CORRECTION REQUIRED — PROVIDED, NO NEW RATIFICATION** — 7 corrections; design only; no product code, no migration, **no register edit**, no commit, no push, no D-21+ | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | 31 (unchanged — migration 32 still planned, **NOT created**) | None run — design correction; every claim traced to ratified register clauses, PostgreSQL semantics, or direct migration/schema inspection at HEAD | Upheld the challenge on every count. **The expiry defect was real and would have VIOLATED ratified D-10, not merely been stylistically weak**: PostgreSQL `now()`/`CURRENT_TIMESTAMP`/`transaction_timestamp()` are all **transaction-start** time, so a transaction beginning before `expires_at` could execute the decision INSERT after genuine wall-clock expiry and be **admitted** — and the enclosing transaction here is Treasury's close, which in the P1F-2 pattern also runs consumption planning, depletion and COGS posting, so the window is wide. Established that **D-10 clauses 2, 8 and 9 independently fix evaluation at the decision INSERT boundary** (*"evaluated when an approval decision is inserted"*, *"at the database approval-decision INSERT boundary"*, *"unexpired **at decision time**"*), which transaction-start time does not satisfy. **Separated the brief's A/B question decisively**: conceptual current-time-at-INSERT is **RATIFIED** (four clauses), while a literal mandate to call SQL `now()` is **NOT** — the string occurs exactly once in D-10, inside the **retained options-comparison table** (row E2) whose own adjacent column reads *"lazy, at the point of action"*, and **none of D-10's fifteen ratified clauses names any SQL function**; reading it as a literal mandate would make the ratification self-contradictory against clause 8. This is the same retained-analysis-vs-binding-text distinction the P1G-1 gate got wrong on P-1. Corrected to **`statement_timestamp()`** — exactly the brief's stated property (stable for the statement, never frozen at transaction start); rejected `clock_timestamp()` because it can advance *within* one statement, making a security predicate non-deterministic across rows for no gain. **Verified the repository is clean**: across all 31 migrations `now()` appears in **zero RLS policies** (only 3 data backfills and 1 DEFAULT), so the correction breaks no precedent and no analogous defect exists elsewhere; `statement_timestamp()` would be the first use, introduced because the ratified text compels it. **`decided_at` bound to the same basis structurally, not by convention**: `DEFAULT statement_timestamp()` (expressible in Prisma via `dbgenerated`, already used at `schema.prisma:692`) **plus a column-level `GRANT INSERT` that omits `decided_at`/`created_at`**, so `ros_app` cannot supply them at all — the first column-level INSERT grant in the repo, with the mechanism precedented by the ratified GAP-2 column-level `GRANT UPDATE ("status")`. **Proved both obligations**: since RLS `WITH CHECK` is evaluated on the resulting row within the same INSERT statement, the predicate's `statement_timestamp()` returns the identical value stored in `decided_at`, so a row with `decided_at > expires_at` **cannot be admitted**, and `decided_at = expires_at` **is** admitted per the ratified boundary — making the forbidden "policy says unexpired while stored `decided_at` says expired" divergence **unrepresentable**; noted the predicate could equivalently read `expires_at < decided_at` but kept `statement_timestamp()` so the security boundary survives a future grant widening. **Added mandatory concurrency scenario 15** (BEGIN before expiry → hold open across the boundary → INSERT after → must be rejected), which is the **discriminator between `transaction_timestamp()` and `statement_timestamp()`** and which the prior matrix could not have caught since scenarios 5/6 used short transactions where the two clocks coincide; every correctness assertion is a **DB timestamp comparison** (`transaction_timestamp() < T_exp`, `statement_timestamp() > T_exp`, transaction timestamp provably unchanged), with a bounded cross-connection poll used only as cadence, never as proof; scenario 5 retained and strengthened to assert the **stored** `decided_at`. **PIN trust boundary ACCEPTED on evidence rather than hand-waved**, answering all four sub-questions: (A) in a modular monolith the module edge is a design-discipline boundary, not a defence against a hostile in-process caller — which could bypass Governance entirely via `$executeRaw` — while the **ratified DB conjuncts trust nothing passed in**, so self-approval, exclusion, tenant and expiry all hold regardless; (B) **YES, and Governance itself already does it** — the shipped `AuditService.record` accepts `actorId`/`tenantId` as plain trusted values from every module and writes them into the **hash-chained** trail, so rejecting this pattern would make the shipped tamper-evidence mechanism unsound, and `VerifiedTerminalPrincipal` is strictly stronger than the bare strings `SalesPaymentService`/`CashMovementsService`/`CashSessionsService` already accept; (C) **NO** — FR-SEC-032 requires approvals be *obtainable* by manager PIN and assigns no module, while the SRS context map places authentication in Identity *"upstream, conformist ──▶ every context"*, so Identity-verifies/Governance-records is the SRS's own layering; (D) **NOT SAFELY** — calling the verifier inside Treasury's open transaction would take a second pooled connection (pool-exhaustion/self-deadlock anti-pattern, against `prisma.service.ts`'s explicit guidance), and making the verifier `tx`-first would put `recordFailure` inside the caller's transaction, letting an attacker obtain **unlimited PIN attempts by forcing rollbacks** — a real regression in an FR-SEC-022 control; passing the raw PIN through Treasury is worse still. Accepted with the trust assumption **stated explicitly** and **mechanically fenced**: a **branded type** using a non-exported `declare const … unique symbol` so no module outside Identity can construct a conforming literal and fabrication requires a greppable `as`-cast; a **boundary-test detector** (with self-test, following the existing `containsPersistenceImplementation`/`containsForeignPrismaQuery` precedent) asserting that cast appears only under `identity/`; and Governance still asserting `approver_id === approver.userId` and the permission membership. No new token, signed proof, session or HTTP endpoint invented. **Zero-row algorithm specified branch by branch** with ordering justified: same-id checked **first** (checking the per-request UNIQUE first would misreport a legitimate replay as a conflict), identical→replay / differing→409; a **different** id for the same request is **always** a conflict and never a replay even when the outcome matches, since a distinct id is a distinct business act; plus a defensive no-visible-winner branch. Guarantees enumerated: no duplicate audit (written only on the created path), no CAS UPDATE on replay (the winner already transitioned status), and no `23505` recovery in an aborted transaction (`ON CONFLICT` raises nothing, so both reads run in a healthy transaction — the P1E-5A lesson); `assertIdenticalDecision` excludes the DB-generated `decided_at`/`created_at`, mirroring the `OrderPayment.processedAt` / `CashMovement.occurredAt` precedent. **FR-SEC-016 corrected from PARTIAL to NOT IMPLEMENTED — enforcement substrate enabled**, since **zero of the four named combinations is operational** after migration 32 alone (cash variance awaits P1G-1, discounts have no Sales consumer, requisitions have no Procurement domain, strict SoD does not exist), adopting the P1G-0 "substrate enabled" phrasing because PARTIAL would imply one combination is partly working; the other five classifications re-checked for the same error and sharpened (FR-SEC-030 PARTIAL with zero consumers wired; FR-SEC-031/033 COMPLETE-substrate, 033 strengthened by the now-unforgeable `decided_at`; FR-SEC-032 PARTIAL with async knowingly unmet; FR-FIN-006 NOT IMPLEMENTED — substrate enabled). **Seven-item delta listed exhaustively** with each mapped to the artifact it touches; **§7's entire preserved list left untouched** — migration 32, two tables, P-1 composite FK, no user FKs, `excluded_approver_user_id`, opaque JSONB value, one-final-decision UNIQUE, no HTTP/read surface, Identity PIN contract, D-12, async deferred, four conjuncts, D-9 U4 CAS, no pessimistic lock, OpenAPI 138, full matrix (now 15 scenarios). **No governance impact and no register edit**: the expiry fix *implements* D-10 rather than amending it, the SQL primitive is expressly a Design-Gate detail the 2026-08-29 ratification reserved to this gate, the column-level grant only narrows what `ros_app` may write, and the branded type has no runtime or DB effect; P-1 unopened, D-12 BLOCKED, D-16 enumeration OPEN, async deferred, D-15 clauses 3/5 and the C-3 prohibition preserved. No commit, no push, unrelated working-tree reports untouched |
| 2026-08-29 | APPROVAL — Runtime implementation (migration 32, Governance Approval mechanism) | [2026-08-29_APPROVAL_runtime.md](2026-08-29_APPROVAL_runtime.md) | **A. APPROVAL RUNTIME IMPLEMENTATION COMPLETE** — reported as COMPLETE, not FINAL ACCEPTED (that determination is made outside this session after report review, per the executing prompt's own instruction). No commit, no push; the register's uncommitted 2026-08-29 ratification was read as authority and left byte-identical | `55e4ae8` (unchanged) | `feat/production-spec` (unchanged) | **32** (+1: `20260829010000_governance_approval_runtime`, Governance-owned only; applied cleanly from zero on 6 independent fresh scratch DBs across this task; persistent local `ros` DB confirmed untouched throughout — still 26 rows, newest `20260823030000_kitchen_ticket_persistence`, `governance.approval_requests` confirmed absent there) | Unit **746/746** (54/54 suites — baseline 732 + 7 new module-boundary + 7 new `approvals.service.spec.ts`); E2E **877/877** (44/44 suites — baseline 837 + 40 new `approval-runtime.e2e-spec.ts`, run twice full-suite against independent fresh scratch DBs, both clean); targeted PIN/auth regression check 48/48 (`auth.service.spec.ts`+`auth.service.refresh.spec.ts` 7/7, `pin.e2e-spec.ts`+`auth.e2e-spec.ts` 41/41) confirming `PinService.authenticate` byte-unchanged; `module-boundaries.spec.ts` 38/38, `KNOWN_DEVIATIONS` unchanged (zero growth — both new contracts are pure public-surface imports); `tsc`/`eslint`/`prisma validate`/`prisma format`/`nest build`/`git diff --check` all clean; OpenAPI **byte-identical** regeneration (true before/after snapshot diff, not just git diff) — 3.1.0/138, confirming zero accidental Governance route | Implemented the shared Governance Approval runtime (FR-SEC-030..033) to the corrected design (final design gate + acceptance closure), building `governance.approval_requests`/`governance.approval_decisions` exactly as specified: D-6 Model B + Mechanism 1 three-line grant form for requests (`status` the sole mutable column); a **column-level `GRANT INSERT`** on decisions that deliberately omits `decided_at`/`created_at`, **empirically verified** before any application code was written that `ros_app` genuinely cannot supply `decided_at` explicitly; the four-conjunct `NOT EXISTS` RLS `WITH CHECK` using `statement_timestamp()` (not `now()`) as the expiry clock per the ratified D-10 correction; D-9 U4 reproduced verbatim on the requests UPDATE policy; **no `status='pending'` fifth conjunct on the decisions INSERT policy** (D-15 cl. 9 preserved). Built Identity's **first public contract** (`identity/contract/pin-verification.contract.ts`, token `TERMINAL_PIN_VERIFIER`) implementing `verifyTerminalPin` by calling the **existing, byte-unchanged** `PinService.authenticate` for 100% of the verification path (zero duplication of hashing/lockout/branch-validation) plus one additional membership-permission-projection query reusing `TenantContextService`'s exact query shape; `VerifiedTerminalPrincipal` is **branded** via a non-exported ambient `unique symbol` so no plain object literal can satisfy it, confined to `identity/` by two new self-tested `module-boundaries.spec.ts` detectors. Built Governance's public contract (`APPROVAL_COMMANDS`, `tx`-first `createRequest`/`decide`, `tenantId`/actor id as trusted positional args never payload fields, `value` typed as the generic `Prisma.InputJsonValue`/`JsonValue` never a Governance-owned shape) and its **private** `ApprovalsService` implementing the full permanent-id/zero-row/RLS-rejection protocol, wired through a new `GovernanceModule` with **no controller and no HTTP route of any kind**. **Found and fixed one genuine implementation defect via the mandatory real-Postgres concurrency tests — exactly the discipline they exist to enforce**: `approval_decisions` carries **two** unique constraints (the `id` PK and the one-final-decision `UNIQUE(tenant_id, approval_request_id)`), and the first implementation's `ON CONFLICT ("id") DO NOTHING` — a *targeted* conflict clause — suppresses a violation of only the named constraint; two managers racing the same request with two different fresh decision ids conflict on the *per-request* constraint instead, which a targeted clause does not catch, producing a raised, unhandled `23505` instead of the silent zero-row outcome the recovery algorithm requires. Scenario 1 of the concurrency matrix failed on its very first real-Postgres run with exactly this shape (`Received constructor: PrismaClientKnownRequestError`); fixed by switching `decide()`'s INSERT to a **bare** `ON CONFLICT DO NOTHING` (no target list, catching either constraint), documented extensively in the service's own docblock including the exact race shape that exposed it; re-verified clean across all subsequent runs. Also found and fixed two smaller gaps during verification, both honestly distinguished from the concurrency defect: a **test-fixture** completeness gap (the new e2e file's fixture created a branch without the paired `org.locations` registry row a pre-existing, unrelated repository-wide invariant test in `organisation.e2e-spec.ts` requires of every branch — fixed by adding the missing `admin.location.create`, not a production defect) and a **test-metadata** correction (an RLS/grants assertion queried `information_schema.role_table_grants`, which does not surface a column-level-only grant in this Postgres version, fixed to query `role_column_grants` instead — the underlying grant was correct throughout, already proven by a separate functional UPDATE-attempt test). Implemented the full 15-scenario concurrency/acceptance matrix plus an RLS/grants matrix in `test/approval-runtime.e2e-spec.ts` (40 tests total): two-manager same-request race, approve-vs-reject race, requester self-approval racing a valid manager, excluded-approver racing a valid manager, exact expiry-boundary semantics (`decided_at <= expires_at` proven via the real DB-supplied value), decision-after-expiry rejection, duplicate-decision-id replay, second-different-decision-id-always-conflicts, a genuine transactional-atomicity proof for the INSERT+CAS pair (using the identical raw-SQL statement shapes deliberately aborted mid-sequence, since decide()'s own CAS-failure branch is provably unreachable under the constraint discipline in normal operation — documented as such rather than faked), cross-tenant structural unrepresentability, missing-tenant-context fail-closed, missing-required-permission refusal, wrong/locked PIN (proving lockout persists independently of the caller's own transaction/rollback), a valid-PIN happy path asserting the returned principal's identity facts and permission set, and — the acceptance-closure's mandatory discriminator — a long-transaction-held-open-across-the-expiry-boundary scenario proving `statement_timestamp()` (not `transaction_timestamp()`) governs the RLS predicate, using only DB-timestamp comparisons as proof (a bounded cross-connection poll for cadence only). Extended `AuditEvent` with optional `approverId`/`approvalId` mapped to the **pre-existing, previously-unpopulated** `audit_entries.approver_id`/`.approval_id` columns, **explicitly excluded from `computeEntryHash`'s input** so D-19's ratified hash-coverage decision and GAP-11 remain exactly as before — documented as not a hash-coverage fix. Chose **no FK on any user column** (`requested_by`, `approver_id`, `excluded_approver_user_id`), following the existing `governance.audit_entries` precedent over the `treasury.cash_movements.performed_by` one, since `identity.users` is global (no `tenant_id`) so an FK would supply zero tenant safety, and no `ON DELETE` action is safe given requests are never deletable — `User`/`Tenant` Prisma models are consequently **not modified at all**. Classified requirements precisely, keeping runtime-substrate and future-consumer classifications separate throughout: FR-SEC-031/033 COMPLETE for the substrate; **FR-SEC-030/032 PARTIAL** (zero consumers wired; async knowingly unmet); **FR-SEC-016 NOT IMPLEMENTED — enforcement substrate enabled** (zero of four named combinations operationally wired, not overclaimed as PARTIAL); **FR-FIN-004/FR-FIN-006 untouched, remaining P1G-1's**. Confirmed the full scope fence held (no P1G-1, no cash-variance consumer, no literal `request_type`/`entity_type` values invented, no variance tolerance, no Day Close, no X/Z reports, no drawer limits, no denomination catalogue, no async approval, no notifications, no D-12, no `approval_steps`, no Governance HTTP surface of any kind, no NFR-PERF-006 work) and that the register was read as authority but never edited by this task. Final verdict reported as **COMPLETE**, deliberately not self-declared **FINAL ACCEPTED** per the executing prompt's own instruction that acceptance is determined outside this session. No commit, no push, unrelated working-tree reports untouched |
| 2026-08-30 | P1G-1 — Variance tolerance / settings substrate FINAL design gate | [2026-08-30_P1G1_variance-settings-final-design-gate.md](2026-08-30_P1G1_variance-settings-final-design-gate.md) | **C. USER RATIFICATION REQUIRED — NARROW ITEMS PROVIDED** — design/analysis only; four narrow items (R-1 tolerance representation *migration-critical*, R-2 comparison semantics, R-3 which effective version governs a session, R-4 approval-expiry source *migration-critical*) plus acknowledgement R-5; **no Sonnet implementation prompt issued** per the gate's own §32 rule. No product code, no migration, no test change, no commit, no push, no D-21+ | `1f9ea1f` (unchanged) | `feat/production-spec` (unchanged) | 32 (unchanged — **migration 33 planned only, NOT created**; migration 34 not planned) | n/a — design gate, no code changed and no test executed in this session; 746 unit / 877 e2e / 38 module-boundaries and OpenAPI 3.1.0 / 138 operations cited from the accepted Approval Runtime report, not re-run | Selected **S-3** — a narrow, Treasury-owned, branch-scoped, effective-dated, immutable-versioned `treasury.cash_close_policies` (rejecting the inert `tenants.settings` / `brands.default_settings` JSONB, the deferred `org.settings`, and both generic settings-core options, which still yield FR-PLT-025 PARTIAL at platform cost and require minting setting-key literals). Established without invention: **write authorization is SOURCE-DECIDED** (`settings.branch.manage`, §15.2, already seeded — correcting the brief's premise); **no tolerance default is needed** (fail-closed when unconfigured is derived, removing it from the migration-critical set); **blind default is SOURCE-DECIDED** (FR-POS-095, DB `DEFAULT 'blind'`); `entity_type = 'cash_session'` already exists in `AUDIT_ENTITY`; `request_type = 'cash.variance'` is implementation-owned (D-16 stays OPEN). FR-PLT-025/026 recorded as remaining NOT IMPLEMENTED; FR-FIN-006 explicitly not claimed complete before P1G-1 |
| 2026-08-30 | P1G-1 — Cash-close policy ratification RECORDED in the Governance Register (R-1(a), R-2(a), R-3(a), R-4(a), R-5 + constraints C-1/C-2) | [2026-08-30_P1G1_variance-settings-ratification-recorded.md](2026-08-30_P1G1_variance-settings-ratification-recorded.md) | **RECORDED — FIVE RESOLUTIONS BINDING, TWO DESIGN CONSTRAINTS RECORDED** — governance recording only; **the Governance Decision Register is the binding record, this report is evidence**. No product code, no migration, no test change, no implementation, no D-21+, no commit, no push | `1f9ea1f` (unchanged) | `feat/production-spec` (unchanged) | 32 (unchanged — **migration 33 NOT created**) | n/a — governance recording, no code changed and no test executed | Register edited additively only: **284 insertions, 0 deletions** (new unnumbered section `## P1G-1 Cash-Close Policy Ratification — 2026-08-30` placed after the 2026-08-29 Approval entry and before the Final Decision Matrix, plus one Ratification-log bullet). Binds: **R-1(a)** absolute money tolerance (`BIGINT` minor units + ISO 4217, no float, no percentage this phase) recorded explicitly as **user ratification resolving source silence, NOT an SRS finding**; **R-2(a)** `abs(counted − expected) > tolerance` with the **source-backed strict `>`** and the **user-ratified absolute-value framing kept textually separate**, `FR-FIN-005`'s signed variance unchanged; **R-3(a)** policy version effective at **session OPEN**, resolved lazily at close via `opened_at`, **no snapshot column and no change to the accepted P1D-1 open path**; **R-4(a)** approval expiry from a **configured positive duration on the policy version**, no DB/app default and **no `5m`/`15m`/`30m` constant**, D-10 E2 unchanged; **R-5** fail-closed unconfigured branch acknowledged, **no default tolerance invented**, `settings.branch.manage` (existing, seeded) required, `blind` still source-defaulted under FR-POS-095. Constraints **C-1** (no isolated `/v1` retrofit; global `/v1` gap stays open; no route URL ratified) and **C-2** (no backdating; rollout case for already-open sessions left explicitly open). **FR-PLT-025 six-level hierarchy and FR-PLT-026 locks recorded as remaining NOT IMPLEMENTED**; P-1, D-12 BLOCKED, D-16 OPEN, D-13, async FR-SEC-032, D-14 A-1, D-20, opaque `value`, Identity-USER excluded approver and `expires_at` semantics all preserved; **0 new `## D-nn` headings** |
| 2026-08-30 | P1G-1 — Cash-close policy substrate implementation (migration 33, Treasury) | [2026-08-30_P1G1_cash-close-policy-substrate.md](2026-08-30_P1G1_cash-close-policy-substrate.md) | **A. CASH-CLOSE POLICY SUBSTRATE IMPLEMENTATION COMPLETE** — reported as COMPLETE, not FINAL ACCEPTED (external review determines that). CashSession Close itself NOT implemented — settings substrate only, per scope fence. No commit, no push; the uncommitted "P1G-1 Cash-Close Policy Ratification — 2026-08-30" register entry was read as binding authority and left byte-identical | `1f9ea1f` (unchanged) | `feat/production-spec` (unchanged) | **33** (new — `treasury.cash_close_policies` + `treasury."CashCountMode"` enum; migration 34 NOT created) | 746/746 unit (unchanged) · 38/38 module-boundaries (0 new deviations) · 904/904 e2e (877 baseline + 27 new `cash-close-policy.e2e-spec.ts`, reproduced clean across 3 independent fresh-scratch-DB runs including the >=3x concurrency race) · scratch 1→33 migration verified 3x clean; persistent `ros` DB reconfirmed untouched (still 26 migrations, table absent) · OpenAPI 3.1.0, 138→139 operations, zero deletions in JSON/YAML | Implements R-1(a) (absolute-money `BIGINT` tolerance, no default), R-2(a) (comparison semantics documented, not yet consumed), R-3(a) (branch-scoped effective-dated immutable versions; NO `cash_sessions` column added), R-4(a) (configured positive approval-expiry seconds, no invented duration), R-5 (fail-closed: absence of a row, not a column default) and constraints C-1 (route `POST /branches/{branchId}/cash-close-policy`, no `/v1` retrofit) and C-2 (DB-enforced anti-backdating via `statement_timestamp()` evaluated once per statement — "effective immediately" satisfies the CHECK by equality using DB time only). `settings.branch.manage` reused as a local string literal (no duplicate `PermissionDef`, no new `treasury->organisation` import). Currency derived from `tx.branch.baseCurrency` via the existing `CashSessionsService.open` precedent (direct Prisma access, not a module-boundary deviation). Column-level INSERT grant excludes `created_at` (empirically verified un-forgeable), mirroring migration 32's `approval_decisions` pattern exactly. FR-PLT-025/026 explicitly proven and recorded as remaining NOT IMPLEMENTED (tests 5/6, labelled DOCUMENTED GAP). Four pre-existing e2e tests with hard-coded table/route inventories required one-line maintenance additions (not defects) to acknowledge the new authorised table/route — same class of update every prior migration in this repo has required of its own tests |
| 2026-08-30 | P1G-1 — Cash-close policy substrate acceptance closure (SRS §5.2.3 module-boundary correction) | [2026-08-30_P1G1_cash-close-policy-acceptance-closure.md](2026-08-30_P1G1_cash-close-policy-acceptance-closure.md) | **A. CASH-CLOSE POLICY SUBSTRATE CORRECTED — FINAL-ACCEPTANCE READY** — corrects a false compliance claim in the prior implementation report (direct `tx.branch.findUnique` query from Treasury, which passed `module-boundaries.spec.ts` only because that suite checks import paths, not table ownership). No new migration, no governance change, no commit, no push | `1f9ea1f` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — no migration 34, byte-identical DDL, confirmed via scratch `prisma migrate deploy` still showing exactly 33 rows) | 751/751 unit (746 + 5 new architecture-test cases) · 38/38 module-boundaries (0 new `KNOWN_DEVIATIONS`) · 43/43 including the new targeted db-ownership spec · `cash-close-policy.e2e-spec.ts` 27/27 reproduced clean across 3 fresh-scratch-DB runs (incl. the ≥3x concurrency race through the new contract) · full e2e 904/904 (one transient 271s/46-failure run recorded as an isolated environmental flake confined entirely to the untouched `catalogue.e2e-spec.ts`, refuted by an immediate clean 84.5s/904-pass re-run) · persistent `ros` DB reconfirmed untouched (26 migrations) · OpenAPI 3.1.0/139 unchanged, zero route change | Added Organisation `contract/branch-currency.query.ts` (`BRANCH_CURRENCY_QUERY`, `tx`-first, mirrors `TableDisplayQuery`/`RoutingConfigQuery`) + its private `branches/branch-currency.query.service.ts` implementation; `CashClosePolicyService` now consumes it inside the SAME transaction instead of querying `tx.branch` directly; `TreasuryModule` imports `OrganisationModule` (the module-class exemption, no deviation entry needed). New targeted architecture test (`cash-close-policy.db-ownership.spec.ts`) proves no direct Branch-model access remains, every Organisation import is contract-only, the query moved to Organisation's own private path, and `KNOWN_DEVIATIONS` gained no `treasury->organisation` entry — and itself caught a genuine false positive (call-shaped code literal inside this task's own docblock prose) before merge. Explicitly records, without fixing (out of fence), a matching pre-existing direct-query instance in the accepted `CashSessionsService.open` (P1D-1). Corrects authorization-language overclaim: `settings.branch.manage` is enforced under the current TENANT-WIDE resolver, not branch-scoped RBAC (FR-SEC-002/003/004 remains a separate, deferred gap per ADR 0008 D-02) — no new permission code. States honestly that repo-wide SRS §5.2.3 DB-table-ownership enforcement remains PARTIAL; only this one edge is closed |
| 2026-08-30 | P1G-1 — CashSession Close FINAL design / rebase gate | [2026-08-30_P1G1_cashsession-close-final-design-gate.md](2026-08-30_P1G1_cashsession-close-final-design-gate.md) | **B. P1G-1 IMPLEMENTATION READY AFTER NARROW USER RATIFICATION** — exactly one item (R-6, approval rejection/expiry recovery); design/analysis only; **no Sonnet implementation prompt issued** per §49. No product code, no migration, no test change, no governance change, no D-21+, no commit, no push | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — **migration 34 planned only, NOT created**) | n/a — design gate, nothing executed; 751 unit / 904 e2e / 38 module-boundaries and OpenAPI 3.1.0/139 cited from the accepted acceptance-closure report, not re-run | Selected **C-3** close protocol — durable immutable count attempt with a same-transaction fast path: one request closes within tolerance, two only when approval is needed. **Rejected the one-shot protocol on security grounds**: telling a cashier "approval required" before committing anything is a variance oracle that can be binary-searched to recover expected cash, defeating FR-POS-095's stated control. **Two load-bearing repository findings, both proven not asserted**: (1) `SalesPaymentService` takes **no** advisory lock and reads CashSession status through an unlocked SELECT (`:183`) under READ COMMITTED, so a payment can commit after a close computed expected cash — **accepted Payment code must be narrowly modified** (4-line `ros_cash_session` advisory-lock acquisition before the Order read, preserving the canonical CashSession→Order→Inventory order); classified in-slice, not a prior blocker, since it needs no migration; (2) `ros_app` holds **`GRANT SELECT, INSERT` only** on `cash_sessions` with no UPDATE grant or policy, so closing is structurally impossible today — migration 34 must add a **column-level UPDATE + compare-and-set RLS policy** (`USING status IN ('open','closing')`), reusing migration 32's ratified `approval_requests` pattern verbatim, which also makes FR-FIN-007 immutability DB-enforced rather than service-enforced. **Expected cash audited term-by-term**: six of eight live (incl. `rounding_adjustment`, which P1F-1 added — correcting the 2026-08-28 gate's stale "6/8 missing" framing); **tips and refunds proven structurally unrecordable** (`tipTotal` written by zero code paths; `refunded`/`partially_refunded` have no inbound transition in `TRANSITIONS`), so their contribution is *necessarily* zero, not conveniently zero — FR-FIN-004 stays **PARTIAL**. Freeze implemented by adding `closing` to `CashSessionStatus`, which freezes Payment and all three movement types with **no change to either consumer's existing `!== 'open'` check**. New smallest-possible **Sales** public `CASH_SESSION_TENDER_TOTALS_QUERY` (tx-first) since Sales publishes no query contract today and Treasury must not query `order_payments` directly. Recommends folding in the carried `CashSessionsService.open` §5.2.3 fix. FR-POS-094/095/096/097, FR-FIN-005/006, FR-SEC-016/030/033 predicted COMPLETE; FR-FIN-004/007/010 and FR-SEC-032 explicitly **PARTIAL**; branch-scoped RBAC honestly stated as still NOT IMPLEMENTED |
| 2026-08-30 | P1G-1 — CashSession Close design acceptance closure (3 defects corrected) | [2026-08-30_P1G1_cashsession-close-design-acceptance-closure.md](2026-08-30_P1G1_cashsession-close-design-acceptance-closure.md) | **A. P1G-1 DESIGN CORRECTED — R-6 ONLY REMAINS** — design correction only; no new ratification discovered; **no Sonnet implementation prompt issued** (R-6 is P1G-1-critical and unratified). No product code, no migration, no test change, **no governance recording**, no D-21+, no commit, no push | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — **migration 34 re-issued as a plan only, NOT created**) | n/a — design correction, nothing executed; evidence gathered by direct source/migration inspection at HEAD | **Defect 1 (above-tolerance one-request approval): REMOVED** — `managerPin` and `reason` deleted from the `POST /close` DTO entirely, because a manager PINning before the authoritative variance is disclosed cannot have read the `ApprovalRequest.value` they approve, making the FR-SEC-031 record hollow and FR-SEC-033 immutably so; reason is now collected at finalisation, where blind-mode cashiers first learn the variance. **Defect 2 (RLS did not freeze core facts): CORRECTED by removing the mutable duplicate rather than policing it** — PostgreSQL RLS cannot compare OLD to NEW, so the prior `USING/WITH CHECK` pair could not stop a `closing→closing` rewrite of `counted_cash`; the corrected design writes core facts **only at the CLOSED transition** and adds `ck_cs_core_facts_only_when_closed`, making such an UPDATE a **CHECK violation**. **No trigger needed** — verified the repo contains **zero** triggers/functions/rules across all 33 migrations while using CHECK constraints pervasively (39+). The retryable-reason exception dissolved entirely: no Treasury reason column is mutable during CLOSING; each finalize carries its own reason into its own immutable `ApprovalRequest.value`. **Defect 3 (FR-OFF-015): RESOLVED AS APPLYING** — the close attempt is created by the cashier on the POS device and §21.3 lists drawer events as device-originated, so `closeAttemptId` is a **mandatory client ULID and the table PK** (contrasted against the accepted cash-close *policy*, correctly server-generated as an administrative write); denomination rows take a **composite PK** `(tenant_id, close_attempt_id, denomination_minor_units)`, no synthetic ids. **Additional correction proven from the accepted Approval implementation**: approval **expiry is structurally unreachable** in the synchronous same-transaction path (`expires_at = now + N`, N≥1s, tested microseconds later) and, if it ever fires, `decide()` throws on the RLS violation and **rolls back the request too**, so **no expired request survives** — the prior gate's "expiry recovery" wording implied a stored expired row and is **retracted**; R-6 narrows to **rejection only**. Rejection must **COMMIT** (never throw after the decision INSERT) and returns **200 with an outcome discriminator**, not 4xx, since a 4xx would make the idempotency interceptor release the reservation and let a replay create a second request. Structural zeros for tips/refunds now **DB-enforced** by phase-marked CHECKs a future tips/refunds slice must explicitly drop. Payment advisory-lock correction and the `CashSessionsService.open` §5.2.3 fix preserved unchanged |
| 2026-08-30 | P1G-1 — CashSession Close design FINAL acceptance closure (expiry base, attempt anchor, BIGINT abs) | [2026-08-30_P1G1_cashsession-close-final-acceptance-closure.md](2026-08-30_P1G1_cashsession-close-final-acceptance-closure.md) | **A. P1G-1 DESIGN FINAL-ACCEPTANCE READY — R-6 ONLY** — design correction only; no new ratification discovered; **no Sonnet implementation prompt issued** (R-6 is P1G-1-critical and unratified). No product code, no migration, no test change, **no governance recording**, no D-21+, no commit, no push | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — **migration 34 re-issued as a plan only, NOT created**) | n/a — design correction, nothing executed; evidence from direct source/migration inspection at HEAD | **Defect A (expiry base): CORRECTED, and my own prior "structurally unreachable" claim retracted** — `transaction_timestamp()` is fixed at BEGIN while the finalisation transaction's *first* statement waits on the `ros_cash_session` advisory lock, so a lock wait ages the TTL and can produce a request already expired at creation, most likely exactly under load; the base becomes the **DB statement instant read immediately before `createRequest`** (`SELECT statement_timestamp()`), which is the *same primitive the decision RLS policy already evaluates*. Verified the contract **requires a JS `Date`** (`instanceof Date` guard, bound as `${expiresAt}::timestamptz`), so no SQL expression could be passed. **D-10, the decision policy, and the R-4(a) duration are all untouched; no default duration introduced.** Expiry recharacterised honestly as *uncommon but reachable*, needing **no business ratification** because a decision-time rejection rolls the request back leaving no state — but it is a real retry path and is now tested (60, 61). **Defect B (attempt anchor): CORRECTED relationally** — the prior plan let raw `ros_app` set `status='closing'` with no attempt row at all and satisfy every CHECK; `cash_sessions` now carries `close_attempt_id` bound by `ck_cs_attempt_anchor` and a **three-column composite FK** `(tenant_id, id, close_attempt_id) → cash_session_close_attempts(tenant_id, cash_session_id, id)` whose **middle column is the session's own id**, proving exact session ownership rather than mere same-tenancy. Combined with `UNIQUE (tenant_id, cash_session_id)` this yields a formal proof that **exactly one legal value** for the column can ever exist, making attempt substitution impossible — **no trigger**, preserving the repo's zero-trigger precedent, and relying on the existing nullable-composite-FK (MATCH SIMPLE) precedent `drawers_terminal_fkey`. Circular FK needs no deferral: the attempt is inserted before the session UPDATE populates the nullable anchor. **Defect C (BIGINT abs): CORRECTED** — `abs(-9223372036854775808::bigint)` raises `bigint out of range`, so the CHECK is restated as `variance > tol OR variance < -tol` (exact for `tol >= 0`, never overflows) and mirrored verbatim in TypeScript so SQL and app cannot drift; recorded that the DTO's `^\d{1,18}$` cap is load-bearing for the 8-term formula CHECK. R-6(a) confirmed as the **only** user ratification; `close_attempt_id`, FK shape, timestamp primitive and overflow-safe arithmetic all explicitly classified as mechanics, not ratifications. Test matrix 52 → 62 |
| 2026-08-30 | P1G-1 — CashSession Close migration-compatibility closure (legacy CLOSED rows, enum sequencing, ownership FKs) | [2026-08-30_P1G1_cashsession-close-migration-compatibility-closure.md](2026-08-30_P1G1_cashsession-close-migration-compatibility-closure.md) | **A. P1G-1 MIGRATION DESIGN READY — R-6 ONLY** — design/migration review; no new business gap found; **no Sonnet implementation prompt issued** (R-6 unratified). No product code, **no migration file created**, no test modification, **no governance recording**, no D-21+, no commit, no push | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — migration 34 planned only) | n/a — review task; every design claim **executed against real PostgreSQL 16.15** on throwaway DBs (all dropped) and a throwaway Prisma project **outside the repo**; persistent `ros` read-only and re-verified unchanged (26 migrations, 196 open / 33 closed sessions) | **Blocker A proven REAL, not theoretical** — the persistent `ros` DB holds **33 CLOSED cash sessions today** (creatable because `ros_app` has table-level INSERT and the INSERT policy checks only tenant, even though no close *transition* path exists). The prior plan's `ck_cs_closed_is_complete` was executed against a faithful reproduction and returned **`ERROR: check constraint … is violated by some row`** — **migration 34 as previously planned would have aborted on the live development database.** Replaced with four legacy-tolerant CHECKs (anchor / core-facts-require-anchor / anchored-close-complete / closed_at-implies-closed) which applied cleanly over the same data, deliberately excluding `closed_at` from the NULL set because all 33 legacy rows have it set, and never treating unknown historical facts as zero. New writes are gated by adding **`close_attempt_id IS NOT NULL` to the RLS UPDATE `WITH CHECK`**; executed as an unprivileged `NOBYPASSRLS` role, all three §11 attacks were blocked (`OPEN→CLOSED` unanchored → RLS violation; `OPEN→CLOSING` unanchored → RLS violation; legacy CLOSED row → `UPDATE 0`). **No legacy state, no marker column, no backfill** — `close_attempt_id IS NULL` on a closed row is itself the relational discriminator. **Enum sequencing resolved the OPPOSITE way to the prior report's caution**: raw `BEGIN; ALTER TYPE … ADD VALUE; …use…; COMMIT;` fails with `unsafe use of new value`, **but Prisma Migrate 7.9.1 executes migration statements non-transactionally** — proven by a migration whose later statement failed on `1/0` while the enum value, an RLS policy using it, and a marker table all persisted; add-then-use in one file applied cleanly through the real CLI. **Decision: one migration 34, no migration 35**, with the recorded corollary that a mid-file failure leaves a partial apply needing `migrate resolve`, making the from-zero scratch proof mandatory. **Blocker B closed relationally** by two executed three-column FKs — `(tenant_id, branch_id, cash_session_id) → cash_sessions(tenant_id, branch_id, id)` and `(tenant_id, branch_id, policy_version_id) → cash_close_policies(tenant_id, branch_id, id)` (new UNIQUE targets added) — proven to reject a cross-branch session and a cross-branch policy while admitting the consistent row; the direct `org.branches` FK dropped as transitively redundant, no column duplicated. Added `declared_by_user_id → identity.users(id)` and branch-safe `(branch_id, terminal_id) → identity.terminals(branch_id, id)` (existing ADR 0008 D-16 target). **`created_at` moved to a column-level INSERT grant** so `ros_app` cannot forge DB persistence time, keeping `declared_at` (device event) and `created_at` (server provenance) distinct. Test matrix 62 → 70 |
| 2026-08-30 | P1G-1 — R-6(a) cash variance approval rejection recovery ratification RECORDED in the Governance Register | [2026-08-30_P1G1_R6-ratification-recorded.md](2026-08-30_P1G1_R6-ratification-recorded.md) | **A. R-6(a) RATIFICATION RECORDED CLEANLY** — governance recording only; **the Governance Decision Register is the binding record, this report is evidence**. No product code, no migration, no test change, no OpenAPI change, no implementation, no D-21+, no commit, no push | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 33 (unchanged — migration 34 remains a plan only, NOT created) | n/a — governance recording, no code changed and no test executed | Register edited additively only: **156 insertions, 0 deletions** (new unnumbered section `## R-6 — Cash Variance Approval Rejection Recovery — RATIFIED 2026-08-30` placed after the P1G-1 Cash-Close Policy Ratification and before the Final Decision Matrix, plus one Ratification-log bullet). Binds **R-6(a)** verbatim (14 clauses): on explicit manager rejection the session **stays CLOSING**, the **physical count and closeAttemptId stay immutable/unchanged**, **payments and all cash movements stay blocked**, **no reopen/recount**, retry permitted with a **NEW** ApprovalRequest/ApprovalDecision id pair, **possibly another manager and a different reason**, every rejected request/decision **remains immutable and auditable**, and the **final** variance_reason is the one on the **APPROVED** close. **No supervisor override, no new permission, D-12 remains BLOCKED.** **Expiry explicitly excluded from R-6** and recorded as a technical rollback/retry path, not a business state — decision-time expiry rolls the whole finalize transaction back leaving no expired request, session stays CLOSING, attempt untouched; **D-10 is NOT amended**. Recorded as **user-ratified business behaviour, NOT SRS-derived** — compatible with but not mandated by FR-POS-095/FR-FIN-006/FR-SEC-016/FR-SEC-033. R-1(a)…R-5, all of D-1…D-20, P-1, D-12 BLOCKED, D-16 OPEN, D-15's amendment, and D-14 A-1 all preserved unchanged; tally unchanged at 17 RATIFIED · 1 IN PART · 1 BLOCKED · 1 OPEN. This closes the CashSession Close design track's sole outstanding ratification item — migration 34 and all implementation remain unauthorised and not self-declared complete |
| 2026-08-30 | P1G-1 — CashSession Close final implementation slice (migration 34) | [2026-08-30_P1G1_cashsession-close.md](2026-08-30_P1G1_cashsession-close.md) | **A. COMPLETE — ACCEPTANCE READY** — implementation report, not FINAL ACCEPTED (external review determines that). No commit, no push, no deployment, no production/persistent-`ros` migration | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | **34** (new — `cash_session_close_attempts`, `cash_count_denominations`, 8 new `cash_sessions` columns, `closing` enum value; legacy-compatibility proven 2x clean on real pre-existing CLOSED/OPEN data from fresh scratch DBs; persistent `ros` reconfirmed untouched at 26) | 760/760 unit · 52/52 module-boundaries + both db-ownership specs (0 new deviations) · 926/937 e2e, 43/46 suites (the 3 failing suites are pre-existing, `git diff`-confirmed-untouched files failing on hardcoded jest timeouts unrelated to this slice, reproduced even in full isolation) · this slice's own `cash-session-close.e2e-spec.ts` 29/29 and rewritten `cash-movements-close-and-payment-concurrency.e2e-spec.ts` 13/13, each reproduced clean across multiple runs · OpenAPI 3.1.0, 139→142 operations, 100→103 paths, drift-detection passing | Implements the full accepted design: `declareClose` (one-request fast close within tolerance, freeze-to-`closing` above it), `finalizeClose` (Approval Runtime consumption, R-6(a) explicit-rejection commit + fresh-id retry, self-approval blocking, owner-unlinked-User fail-closed), `getCloseContext` (FR-POS-095 blind-mode fields structurally absent, not null). Closed the Payment/Close advisory-lock gap the final design gate identified (`sales-payment.service.ts` step 1.5) — proven via real concurrent races (Payment vs. Movement, Payment vs. Close, Movement vs. Close, both tenders) after discovering and working around a genuine Prisma-interactive-transaction-timeout-vs-lock-hold test-harness fragility (independently corroborated by 3 unrelated pre-existing e2e files exhibiting the same class of failure). Closed the carried `CashSessionsService.open` SRS §5.2.3 defect via `BRANCH_CURRENCY_QUERY`. Published Sales' first `contract/` QUERY (`CASH_SESSION_TENDER_TOTALS_QUERY`); resolved the resulting Treasury↔Sales circular **module** import (not a circular provider) via `forwardRef()` on both sides — a mechanical DI fix, not a business-flow redesign. Requirement classifications (FR-FIN-004/007 PARTIAL, FR-SEC-032 PARTIAL, branch-scoped RBAC NOT IMPLEMENTED, all else COMPLETE) match exactly what the task specified in advance as the expected honest outcome |
| 2026-08-30 | P1G-1 — CashSession Close implementation acceptance closure | [2026-08-30_P1G1_cashsession-close-acceptance-closure.md](2026-08-30_P1G1_cashsession-close-acceptance-closure.md) | **A. P1G-1 CASHSESSION CLOSE — ACCEPTANCE CLOSURE CLEAN** — acceptance-closure report, not FINAL ACCEPTED (external review determines that). Corrects the prior implementation report's environmental classification (below) with direct evidence. No commit, no push, no deployment, no production/persistent-`ros` migration | `0f10afe` (unchanged) | `feat/production-spec` (unchanged) | 34 (unchanged — migration 34's SQL file untouched by this closure task; not re-run, since nothing its legacy-compatibility proof depends on changed) | 760/760 unit (unchanged) · 52/52 module-boundaries + both db-ownership specs (0 new deviations) · 963/964 e2e post-fix, 45/46 suites (one isolated non-reproducing flake, 3/3 clean immediate re-run) · +6 new audit/event tests + 21 new deterministic concurrency tests, all reproduced clean across multiple runs · OpenAPI 3.1.0, 142 operations/103 paths unchanged, drift-detection passing · persistent `ros` reconfirmed untouched at 26 | **Blocker A (FR-AUD-001/006) closed**: added `AUDIT_ACTION.CASH_VARIANCE_DECLARED`, written once per newly-created close attempt (both tolerance paths, never on replay) — the prior slice audited only the final `CASH_SESSION_CLOSED` fact, leaving an above-tolerance `closing` session's variance itself unaudited for however long it stayed unresolved. **Blocker B (SRS §5.5.4 `cash.variance.detected`) closed**: Treasury's first published domain event, via the pre-existing `UnitOfWork`/`ctx.publishEvent` mechanism (`declareClose` switched from `PrismaService.withAuthContext` directly to `UnitOfWork.execute` — a mechanical substitution, not a redesign); dispatches to zero handlers without error, exactly like Sales' own pre-Fire `order.line.fired`. **Blocker C (deterministic concurrency) closed**: replaced the statistical-only proof requirement with a NEW raw session-level `pg_advisory_lock` harness (the `pg` package directly, no Prisma transaction, so immune to any interactive-transaction timeout) proving exact ordering for Payment-vs-Close (both tenders, both directions, ≥3 runs each) and all three movement types vs. Close (both directions) — 21 new tests, faster AND more reliable than the retired gate technique. **Blocker E (baseline attribution) found a GENUINE INDIRECT REGRESSION**: a real `git archive 0f10afe` baseline, migrated and run fresh, passed the 3 suites the prior report called environmental (16/16) — proving they were NOT pre-existing flakes. Root cause: those three files' own two-party test barrier races two `SalesPaymentService.capture()` calls on the SAME cash session, which the NEW Payment/Close advisory lock now serializes BEFORE the barrier's own seam, deadlocking the test's own synchronization against a 5000ms Prisma transaction timeout. Fixed by giving each pair's second call an INDEPENDENT cash session (orthogonal to what each test actually verifies); re-verified against baseline-equivalent freshness, 16/16, faster than baseline. **Blind-mode contract fixed**: `toleranceMinorUnits` (a POLICY fact) was incorrectly omitted from blind-mode `getCloseContext` responses — the accepted design table requires it present in BOTH modes, only `expectedCash`/breakdown are blind-omitted; corrected. **Requirement-classification bases corrected** against SRS verbatim text: FR-POS-096 (variance+reason+manager-acknowledgement, not merely "declare"), FR-FIN-007 (PARTIAL because adjusting entries are unimplemented, not because of Day Close), FR-SEC-016 (newly classified: global PARTIAL — requisition/discount domains and inventory strict-SoD don't exist — with the cash-variance limb COMPLETE). `tsc` baseline-compared directly (identical single pre-existing error, not merely asserted). 100-item coverage matrix built from actual executable proof (the original ~70-case prompt text was not recoverable verbatim in this session; stated explicitly, not glossed over) |
| 2026-08-31 | POST-KDS — Internal MVP final rebase + next-slice selection gate | [2026-08-31_POST-KDS_MVP-final-rebase-and-next-slice.md](2026-08-31_POST-KDS_MVP-final-rebase-and-next-slice.md) | **A. INTERNAL MVP REBASE CLEAN — NEXT SLICE SELECTED** — analysis/rebase only; no product code, no migration, no schema, no route, no permission, no governance edit, no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — none created, none planned by this task) | **No test run executed in this session**; test files cited as structural evidence only, no prior run's results restated as newly executed | Rebased the Internal-MVP assessment against the FINAL-ACCEPTED, source-control-closed KDS Operator Lifecycle at `38e007b`. Baseline verified exactly (HEAD/parent/branch/dirty-tree all matched; zero source/schema/test/migration/OpenAPI drift; the four unrelated dirty reports untouched). Built a 33-row protected-path capability matrix from executable evidence only — schema existence never counted as implementation, reports never counted as evidence: **26 COMPLETE · 2 PARTIAL · 5 NOT IMPLEMENTED**. Protected-path walkthrough proves **exactly two missing executable edges** in the sale-to-close cycle: **Receipt** and **daily report**. **RECEIPT: BLOCKED** (not merely absent) — exhaustive grep found no model, no generator, no route, no country-pack `invoice` section (`country-pack.model.ts:34`), and the approved SQL's `fiscal.tax_documents`/`invoice_templates` are unmigrated; **CARRIED ITEM P1C-1's fiscal exclusion stands, reaffirmed verbatim by the 2026-08-25 P1F-2 ratification and untouched by every later Preservation clause**. **BRANCH AUTHORIZATION: BLOCKED by D-2** (defer reconfirmed in force by the KDS ratification at this very HEAD) — negative proof that `TenantContext.branchId` is never populated, `membershipRoles` carry no branch predicate, and `GET /orders?branchId=` is an optional client-supplied filter on a tenant-scoped permission. **DAY CLOSE: NOT IMPLEMENTED** and correctly sequenced *after* reporting — FR-FIN-022's Z-report content is not computable at this HEAD (discounts/refunds/voids/comps structurally zero; **tax-by-rate not directly derivable** — new finding: `orders.country_pack_version` pins a version string with no pack code and no FK) and FR-FIN-026's fiscal limb is P1C-1-blocked; Day Close is explicitly *"not decided"* in BOTH the 2026-08-29 Approval Runtime and 2026-08-30 P1G-1 ratifications. **Post-fire void / FR-KDS-029: DEFERRED MVP GAP**, not a hard blocker (depletion fires at completion, not fire, so no orphaned stock; UC-POS-01's main flow has no cancellation). **`served`/Expediter: safely deferred**; the accepted KDS slice is NOT reopened. **Corrected the prior P1G-1 rebase downward**: the X report was *not* "folded in" — what shipped is `GET /cash-sessions/{id}/close-context`, a close pre-flight, and the register lists the X-report permission as undecided. **SELECTED NEXT SLICE: MINIMUM OPERATIONAL REPORTING** — the only remaining protected-path gap that is real, upstream-complete AND not behind a ratified exclusion; it completes the already-PARTIAL FR-FIN-010 [M] per-day half, needs at most one additive index (`order_payments (tenant_id, branch_id, business_day)`) and possibly zero schema change, and converts Day Close from unbuildable to buildable. Query-time aggregation recommended with an explicit FR-RPT-001/002/003/005 Internal-MVP carve-out and FR-RPT-004 satisfied in full — **no Analytics warehouse for an Internal MVP**. Two narrow user decisions isolated (§14.1 Receipt/P1C-1 reopening; §14.2 report route branch-scope shape), neither blocking the slice. Fresh NON-AUTHORITATIVE estimate replacing the old ~55-60%: **26/33 protected-path capabilities (79%)**, **13/15 readiness-PDF capabilities (~87%)**, **4 remaining blocking slices, exactly ONE startable today**. Next step: Minimum Reporting design gate (15 questions), then narrow ratification, then an authorised implementation task |
| 2026-08-31 | MINIMUM OPERATIONAL REPORTING — final design & governance gate | [2026-08-31_MINIMUM-reporting-final-design-gate.md](2026-08-31_MINIMUM-reporting-final-design-gate.md) | **B. MINIMUM REPORTING READY AFTER NARROW USER RATIFICATION** — design/governance packet only; no product code, no migration, no schema, no route, no permission, no OpenAPI, no governance edit, no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — **NO MIGRATION EXPECTED**; the anticipated `order_payments(tenant_id, branch_id, business_day)` index is explicitly NOT needed and NOT created) | **No test suite executed in this session**; test files cited as structural evidence only. Four read-only `EXPLAIN` statements run as `ros_app` inside `BEGIN … ROLLBACK` (no ANALYZE, no write, no DDL, no seed) | Final design + governance gate for the POST-KDS-selected slice: a one-branch, one-business-day daily-trading READ surface. Baseline verified exactly (HEAD/parent/branch/dirty tree; zero source/schema/test/migration/OpenAPI drift; the four unrelated dirty reports untouched). **Five corrections to the POST-KDS rebase, each source-backed** (§3): (1) **no migration** — an orders-first join is fully index-driven and, via the branch-inclusive `order_payments -> orders (tenant_id, order_id, business_day, branch_id)` FK, **provably complete**, while the payments-first shape seq-scans (§20 EXPLAIN); (2) `treasury.cash_sessions` **carries no `business_day` column at all**, so sessions are attributed to a day by the day's payments, not by their own row; (3) `orders.rounding_adjustment` ≠ `order_payments.rounding_adjustment` — only the payments-side sum is the FR-FIN-004 term (all 4 live non-zero order-side rows sit on `partially_paid` orders); (4) a thin `reporting` orchestration module owning **zero tables** is chosen over Sales ownership (§5.2.4 names Reporting as extraction candidate #1); (5) tax **by rate is not derivable at all** — FR-FIN-032 multi-component tax means only the component SUM is persisted. **SALES POPULATION** = `state='completed'` only; **TENDER POPULATION** = ALL payments for the branch-day regardless of order state, because a completed-only tender total would contradict the ratified P1G-1 cash close (`CashSessionTenderTotalsQueryService` filters by session alone) — the gap is exposed as `unsettledCapturedTotal` and the identity `Σ tender = gross + unsettled` is a required test. **GROSS/NET decided by source, not convention**: FR-CST-003 `[M]` — `Net = Gross − Discounts − Refunds − Tax`; gross is tax-inclusive `Σ orders.grand_total`; `orders.subtotal` is banned from every formula (its meaning differs by pricing mode). Cash sales use `payment.amount` **only** — 90 due / 100 tendered / 10 change reports **90**. **Attempt cardinality closed at the database level**: `uq_csca_one_per_session` makes a second close attempt structurally impossible; the session ROW (`status='closed'`) is authoritative and the attempt table is not queried. **FR-RPT-004 SATISFIED IN FULL** via server-authoritative `dataAsOf` (`SELECT now()` inside one RepeatableRead transaction) plus a three-state `periodStatus` OPEN/UNSEALED/SETTLED derived from an exhaustive enumeration of what can still change a day's figures; **`SEALED` deliberately not emitted** (DayClose owns it). **Branch safety solved WITHOUT touching D-2** (§16 Option C): mandatory `branchId`, tenant-visible-branch 404, exactly-one-operative-branch or 403 — the KDS-R11 consequence-note pattern applied to a tenant-shape fact, never to a principal's scope; disclosed consequence: a multi-branch tenant gets 403 on the route (473 dev tenants have 1 active branch, 101 have more). **FR-RPT-001/002/003/005 remain NOT IMPLEMENTED** — never 'waived', never 'complete'; FR-RPT-042 drill-down **NOT IMPLEMENTED** (`GET /orders` has no `businessDay` or `state` filter and is gated by `pos.order.create`); FR-RPT-043/044 export **OUT OF SCOPE**, `report.export` not created; no audit entry on the GET. **THREE user ratifications only** (§26): (1) the concrete `report.view.<category>` token(s) — recommended **BOTH `report.view.sales` AND `report.view.financial` (AND)**, on the KDS-R11 source-silence precedent, with §15.2's Appendix C absent and `treasury.controller.ts` already recording the code as 'unenumerated'; (2) Internal-MVP sequencing authorisation to ship the read surface while the four FR-RPT architecture requirements stay unmet; (3) the `averageOrderValue` basis (net recommended) — the one genuinely source-silent business definition. Branch guard, module ownership, isolation level, index decision and gross/net are **engineering-resolved and deliberately NOT put to the user**. Receipt/P1C-1, fiscal, DayClose, Z and X report NOT reopened. Includes the four public contract designs, exact HTTP surface, error semantics, implementation DoD, and a full test plan. |
| 2026-08-31 | MINIMUM OPERATIONAL REPORTING — design-gate acceptance correction | [2026-08-31_MINIMUM-reporting-design-gate-acceptance-correction.md](2026-08-31_MINIMUM-reporting-design-gate-acceptance-correction.md) | **A. REPORTING DESIGN CORRECTION CLEAN — READY FOR USER RATIFICATION** — design correction only; no product code, no migration, no schema, no route, no permission, no OpenAPI, no governance edit, no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — **NO MIGRATION EXPECTED**, re-confirmed against three additional query shapes) | **No test suite executed in this session**; test files cited as structural evidence only. Three additional read-only `EXPLAIN` statements plus one `transaction_timestamp()` observation, run as `ros_app` inside `BEGIN … ROLLBACK` (no ANALYZE, no write, no DDL, no seed) | Corrects the 2026-08-31 Minimum Operational Reporting final design gate **before** ratification; the original is preserved unmodified and this correction governs where they differ. **A — Cash reconciliation:** the day-level `varianceTotal` is **REMOVED** (a session spanning two business days would have contributed its single whole-session variance to BOTH days' totals); `payIn/payOut/safeDrop/expected/counted` day-totals removed with it; `cashReconciliation.scope = "WHOLE_SESSION"` is now a literal wire field and each session carries `businessDayCount` / `spansMultipleBusinessDays`. Verified that `cash_sessions`, `shifts`, `cash_movements` and `cash_session_close_attempts` **all** lack a `business_day` column, and no anchor is invented — §19.3 Cash Reconciliation reclassified **DELIVERED → PARTIAL**, and the claim that this slice supplies FR-FIN-021's blocking-session list is **WITHDRAWN** (FR-FIN-021 NOT IMPLEMENTED; DayClose stays a separate slice). **B — periodStatus** blockers re-derived against the report's OWN day-scoped figures: exactly `openOrderCount` and the renamed `unclosedContributingSessionCount`; a **zero-payment session is proved NOT a blocker** (a new payment on a past day requires a non-terminal order of that day, and `business_day` is server-clock-derived at order creation). **C — Future days:** `businessDay > branchCurrentBusinessDay` ⇒ **400** *Future business days are not supported.*, replacing the invalid `>= ⇒ OPEN` rule; no `FUTURE` enum added. **D — Historical currency:** authority moves from today's **mutable** `org.branches.base_currency` to the **immutable** transaction snapshots (`orders.currency` ∪ `order_payments.currency`), with branch currency as fallback only on a financially empty day and session currencies validated (never merged) — a legitimate branch currency change no longer breaks a historically coherent past day, with that regression as a named test; **FR-RPT-005 explicitly NOT claimed solved**. **E — KNOWN_DEVIATIONS growth = ZERO** (not merely zero category-(b)): the `reporting->identity` allow-list option is **rejected**; Reporting consumes the already-published `identity/contract/http.ts` (verified to export every primitive the slice needs), on the exact Kitchen precedent the boundary spec already asserts. **F — TOCTOU closed:** the `ReportingBranchGuard` is **removed**; branch existence, the single-active-branch assertion, `currentBusinessDay` and `dataAsOf` all execute **inside the one RepeatableRead transaction** with the report facts (precedent: `orders.service.ts:225` throws inside `withAuthContext`). **D-2 untouched; no fourth user decision created.** Also: **`dataAsOf` overclaim withdrawn** — it is `transaction_timestamp()`, a stable transaction-scoped marker (measured this session: `now()` = txn_ts, stable across statements, and **1.66 ms earlier** than the first statement, proving it is not the MVCC snapshot instant) — sufficient for FR-RPT-004, no equivalence claimed; **AOV posture made coherent** — a true ratification with **no manufactured default**, the field being **omitted** if unanswered; and the forward-binding constraint on `orders.grand_total` **withdrawn**, replaced by a five-item Discount/Comp/Refund/Void/adjusting-entry revisit trigger so an Internal-MVP reporting decision cannot dictate the future Sales financial model. Corrects one precision point: `ros_app` **does** hold column-level UPDATE on the `cash_sessions` close columns, so a closed session's immutability is **service**-enforced (`cash-session-close.service.ts:251,460`), not grant-enforced. **Preserved unchanged:** thin zero-table Reporting module · one composite route · one branch/one day · dashboard-only · completed-only sales population · all branch-day payments as tender population · `payment.amount` never `tendered_amount` · rounding outside revenue · tax by class only, no by-rate · no COGS · RepeatableRead · no migration · orders-first indexed joins · no audit on GET · no export · FR-RPT-001/002/003/005 and FR-RPT-042 NOT IMPLEMENTED · Receipt/DayClose out of scope. **THREE user ratifications remain** with exact proposed text, alternatives, recommendation and explicit not-authorized lists: report.view.sales + report.view.financial (AND) · Internal-MVP sequencing · AOV = net (no default). Includes corrected DTO, error semantics with a specified 404→403→400→409 ordering, DoD deltas (4 amended, 6 added) and test deltas including the spanning-session double-count regression, the zero-payment-session SETTLED proof, the branch-currency-change regression and the TOCTOU test. |
| 2026-08-31 | MINIMUM OPERATIONAL REPORTING — final user ratification record | [2026-08-31_MINIMUM-reporting-user-ratification.md](2026-08-31_MINIMUM-reporting-user-ratification.md) | **A. REPORTING RATIFICATIONS RECORDED — IMPLEMENTATION GOVERNANCE-UNBLOCKED** — governance recording only; no product code, no migration, no schema, no route, no permission code in source, no test, no OpenAPI, no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — no migration created, modified or authorised) | **No test suite executed and no test file modified** — governance recording only | Records the user's explicit ratification (*«موافق، اعتمد القرارات 1 و2 و3.»* — "Agreed, ratify decisions 1, 2 and 3") of the three decisions tabled in the acceptance correction's §18. Baseline verified before and after (HEAD/branch/dirty tree/34 migrations; zero source/schema/test/OpenAPI drift). Written to `GOVERNANCE_DECISION_REGISTER.md` as **one unnumbered ratified entry — "Minimum Operational Reporting Ratification — 2026-08-31"** — placed before the Final Decision Matrix exactly where the Fire/P1F-2/FIFO/Approval-Runtime/P1G-1/R-6/KDS entries sit, plus one matching matrix bullet. **No `D-21`; the 20-decision tally is unchanged.** Limb ids open a **new `RPT-R<n>` series** (`RPT-R1 … RPT-R3`) after verifying `RPT` was unused in all 7,126 lines; the cash `R-<n>` series is not continued and **`R-7` is again NOT reused** (a D-20 option label in this very subject area — the same reason KDS-R11 refused it). **RPT-R1** — creates **`report.view.sales`** ("View sales reports") and **`report.view.financial`** ("View financial reports"), **BOTH required together (`mode:'all'`, AND, never OR)** on `GET /reports/branches/{branchId}/daily-trading/{businessDay}`, because the response spans two §19.3 categories; the **fourth and fifth** explicit user-authorized exceptions to the zero-invented-codes discipline after `pos.order.fire`, `pos.payment.capture` and `kds.operate` — but recorded as a **weaker** form of invention, since §15.2 supplies the template `report.view.<category>` and §19.3 the categories; `report.export`, `report.view.daily_trading` and every other `report.view.*` code **NOT authorized**; codes carry **no branch scope**; ADR 0008 D-01 remap route recorded without making the ratification provisional. **RPT-R2** — authorises **Internal-MVP sequencing only**: build the live read now over the transactional primary while **FR-RPT-001/002/003/005 remain NOT IMPLEMENTED — not waived, not reinterpreted, not complete**, with no artefact permitted to claim otherwise; no replica/star schema/fact_*/dim_*/Type-2/rollup/cache/export-pipeline/warehouse authorized; FR-RPT-004 may be implemented in full; FR-RPT-042 and FR-RPT-043/044 stay NOT IMPLEMENTED; FR-FIN-010 stays PARTIAL (per-scheme and nine tender families unsatisfied); §19.3 Cash Reconciliation PARTIAL (payment-contributing sessions only, WHOLE_SESSION facts, no day-level variance total, zero-payment/movement-only attribution not implemented and no anchor invented); FR-FIN-020..026/DayClose/X/Z stay NOT IMPLEMENTED and the slice does **not** supply FR-FIN-021's blocking-session list; FR-AUD-008 stays a known gap; no audit on ordinary GET. **RPT-R3** — fixes **averageOrderValue = netSales ÷ completedOrderCount, HALF_UP, null at zero count** as a user-ratified resolution of source silence (SRS names AOV five times, defines no numerator), compatible with but not mandated by §13's Net-Sales denominator and FR-CST-035; applies to AOV alone, authorises no other KPI, exposes no COGS. **Branch fail-closed posture recorded as an implementation consequence, NOT a fourth ratification** (explicit branchId · tenant-visible branch or tenant-safe 404 · exactly ONE active branch, 0 ⇒ denied, >1 ⇒ denied as unsupported · supplied id must equal it · verified inside the SAME RepeatableRead transaction as the report) — it grants nothing, reads a tenant-shape fact never a principal's scope, **D-2 UNCHANGED**, FR-SEC-002/003/004 still NOT IMPLEMENTED. Thin zero-table module, RepeatableRead, transaction_timestamp() dataAsOf, future-day 400, historical transaction-currency selection, orders-first joins, zero migration, WHOLE_SESSION scope, tax-by-class-only and zero KNOWN_DEVIATIONS growth are recorded as **accepted engineering consequences, deliberately not ratified**. **Conflict scan CLEAN** across report.view · report.export · Reporting · FR-RPT-001/002/003/005 · AOV/average order · D-2 · D-20 · KDS-R11 · Appendix C: FR-RPT and AOV had **zero** prior mentions; `report.view` appeared once as a §15.2 quotation inside D-20's evidence; D-20 is not reopened (its deferral was scoped to a Governance approval-read code for a surface D-14 A-1 removed — the distinction KDS-R11 already adjudicated). **Register diff = 451 insertions, 0 deletions, two contiguous additive hunks — nothing modified, superseded silently or deleted.** Both prior design reports byte-unchanged; nothing staged or committed. **Minimum Operational Reporting is GOVERNANCE-UNBLOCKED**; a separate explicitly authorised implementation task is still required. |
| 2026-08-31 | Minimum Operational Reporting — implementation | [2026-08-31_MINIMUM-reporting-implementation.md](2026-08-31_MINIMUM-reporting-implementation.md) | **A. IMPLEMENTED — READY FOR ACCEPTANCE REVIEW**; no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — no migration created; clean scratch DB built from zero, all 34 applied, dropped after use) | Unit 792/792 (58 suites, incl. module-boundaries 45/45); Reporting e2e 56/56 across 8 new files (authorization 15, sales 7, tender 5, tax 4, cash-reconciliation 8, period 10, currency 4, snapshot/concurrency 3); full e2e on a **clean, from-zero scratch database** (`ros_scratch_rpt_*`, dropped after use, persistent `ros` DB untouched): first run 1069/1070 (the one failure was the expected OpenAPI-drift detection, pre-regeneration), **final run 1070/1070, 59/59 suites** | Implemented RPT-R1/R2/R3 end to end: `GET /reports/branches/{branchId}/daily-trading/{businessDay}`, dashboard-only, `report.view.sales` + `report.view.financial` (AND), zero query parameters enforced by an empty DTO + the existing global `forbidNonWhitelisted` pipe; the entire response (dataAsOf, branch existence, single-active-branch fail-closed assertion, future-day check, every financial fact, periodStatus) composed inside **one RepeatableRead transaction** with no `ReportingBranchGuard` and no second transaction — proven by instrumentation (a paused, DI-substituted contract call plus an independent-connection concurrent write) rather than source inspection; four new additive public contracts (Sales `DAILY_TRADING_SALES_QUERY`, Treasury `DAILY_CASH_RECONCILIATION_QUERY`, Organisation `BRANCH_REPORTING_SCOPE_QUERY`, Localisation `TAX_CLASS_LABELS_QUERY`), each `tx`-first and consumed only by `reporting`; completed-only sales population with `orders.subtotal` proven unused; all-payment tender population using `payment.amount` only (90/100/10 case verified); gross/net/tax/AOV formulas (NET-basis AOV per RPT-R3, `null` at zero count); `unsettledCapturedTotal` and the `tenderGrandTotal = grossSales + unsettledCapturedTotal` identity; tax by class only with all three required identities and graceful unresolved-label handling; historical-currency resolution from observed transaction currency (**regression-tested**: a branch `baseCurrency` change after the fact does not corrupt a historical report) with `BRANCH_FALLBACK`/`409` cases; WHOLE_SESSION cash reconciliation with `businessDayCount`/`spansMultipleBusinessDays` and **no day-level variance/expected/counted/movement-total key of any kind**; `OPEN`/`UNSEALED`/`SETTLED` `periodStatus` including the zero-payment-session non-blocker proof; `dataAsOf` via `transaction_timestamp()`. **Zero `KNOWN_DEVIATIONS` growth** (module-boundaries 42→45, all new assertions passing); **no migration, no schema change** (`prisma validate` clean, 34 confirmed); `EXPLAIN` evidence shows index-driven access on every production query shape, specifically **no sequential scan on `sales.order_payments`**; OpenAPI regenerated as a pure addition of exactly one path. One extract-method refactor (`cutoverLookup` moved from a private `OrdersService` static into the shared, exported `business-day.ts`, used by both callers) so Reporting's `currentBusinessDay` and Order creation share the single FR-FIN-024 implementation — zero behaviour change, pre-existing tests re-verified. FR-RPT-001/002/003/005/042/043/044 remain explicitly NOT IMPLEMENTED; DayClose, refunds, comps, post-fire void, and card-scheme grouping remain out of scope; nowhere does any artefact claim "Reporting complete" |
| 2026-08-31 | Minimum Operational Reporting — narrow acceptance correction (completed overpayment) | [2026-08-31_MINIMUM-reporting-acceptance-correction.md](2026-08-31_MINIMUM-reporting-acceptance-correction.md) | **A. REPORTING ACCEPTANCE CORRECTION CLEAN — READY FOR FINAL ACCEPTANCE**; no commit | `38e007b` (unchanged) | `feat/production-spec` | 34 (unchanged — no migration created; a second clean, from-zero scratch DB verified all 34, dropped after use) | Reporting e2e 61/61 (9 files: the 56 pre-existing + 5 new overpayment cases); unit 792/792 (58 suites, incl. module-boundaries 45/45, unchanged); full e2e on a fresh from-zero scratch database: **1075/1075, 60/60 suites** | Corrected one false invariant found on acceptance review: the implementation report's `tenderGrandTotal === grossSales + unsettledCapturedTotal` is FALSE on a currently reachable P1F-2 state, verified directly against `sales-payment.service.ts` — completion requires `paidTotal >= grandTotal` (never equality) and `capture()` places no upper bound on a Payment's amount, tender-agnostically, so a completed order's `paidTotal` can legitimately exceed its `grandTotal`. Added ONE neutral, reconciliation-only field, `tenderTotals.completedExcessCapturedTotal = Σ max(paidTotal − grandTotal, 0)` over the completed population — computed from the SAME already-loaded order rows (`paidTotal` added to the existing `select`), zero new queries, zero new query shape, EXPLAIN re-verified identical. **No business/accounting disposition was invented** for the excess (not revenue, tax, tip, discount, refund, rounding, or variance) — no new governance sought or required, RPT-R1/R2/R3 not reopened. The corrected identity `tenderGrandTotal === grossSales + unsettledCapturedTotal + completedExcessCapturedTotal` is proven through the REAL `OrdersService`/`OrderLinesService`/`SalesPaymentService` Payment/Completion path (never a direct DB insert) in a new `reporting-overpayment.e2e-spec.ts` (5 cases: cash over-tender control distinguishing it from genuine overpayment, single-payment cash overpayment, tender-agnostic card overpayment, partial-then-overpaying-final settlement, and a mixed day combining exact/overpaid/partially-paid orders with the full identity asserted as bigint arithmetic); two pre-existing zero-excess tender tests extended to the corrected 3-term form. Zero `KNOWN_DEVIATIONS` growth (45/45, unchanged). `tsc`/`eslint`/`prisma validate`/`git diff --check`/`nest build` all clean (one pre-existing, unrelated `tsc` error confirmed untouched by this or the prior task); OpenAPI regenerated, still exactly one Reporting path, drift-detection e2e 32/32. Requirement classifications (FR-RPT-004 COMPLETE, FR-RPT-001/002/003/005/042/043/044 NOT IMPLEMENTED, FR-FIN-010/Cash Reconciliation PARTIAL, Day Close NOT IMPLEMENTED) unchanged |
| 2026-08-31 | Minimum Operational Reporting — final acceptance closure + source-control commit | [2026-08-31_MINIMUM-reporting-final-acceptance-closure.md](2026-08-31_MINIMUM-reporting-final-acceptance-closure.md) | **A. REPORTING FINAL ACCEPTANCE CLOSED — COMMITTED** — closure/source-control hygiene only; no redesign, no new product behaviour, no migration, no schema change, no governance edit | `38e007b` (parent) | `feat/production-spec` | 34 (unchanged — no migration created or staged) | Relied on accepted evidence (Reporting e2e 61/61, unit 792/792, module-boundaries 45/45, full e2e 1075/1075 on a from-zero scratch DB) plus fresh confirmation this session: `git diff --check` clean, `prisma validate` clean, module-boundaries 45/45 re-run live — zero drift since the acceptance-correction report | Classified every dirty working-tree path A–H (zero Unknown): 24 Reporting source, 9 Reporting test, 2 Reporting OpenAPI, 1 Reporting ratified-governance-register section (2 purely additive hunks), 7 Reporting report/INDEX paths (INDEX.md staged by constructed blob at exactly its 6 pre-existing Reporting rows plus this row, not whole-file, since its working-tree diff also carries 4 unrelated pre-existing report rows across 2 hunks), 4 pre-existing unrelated reports correctly excluded and left untouched/unstaged. Proved no post-acceptance semantic drift: the corrected three-term tender identity (`tenderGrandTotal === grossSales + unsettledCapturedTotal + completedExcessCapturedTotal`), exactly two permission codes with no `report.export`, zero `console.*`, zero `.skip`/`.only`, and a pure-addition one-path OpenAPI diff were all re-verified directly against current source. Staged explicitly by path (no `git add .`/`-A`/`-a`); no schema/migration/`.env`/credential change anywhere in the classified set. Created exactly one normal commit, subject `feat: add minimum operational reporting`, no amend/rebase/reset/force/push. Marks Minimum Operational Reporting (RPT-R1/R2/R3) **FINAL ACCEPTED, SOURCE-CONTROL CLOSED**; FR-RPT-001/002/003/005/042/043/044, DayClose, Receipt/fiscal and D-2 branch-scoped RBAC remain explicitly deferred |
| 2026-08-31 | POST-REPORTING — Internal MVP rebase + next-slice selection + DayClose readiness gate | [2026-08-31_POST-REPORTING_MVP-rebase-and-next-slice.md](2026-08-31_POST-REPORTING_MVP-rebase-and-next-slice.md) | **A. POST-REPORTING MVP REBASE CLEAN — NEXT SLICE SELECTED (DAY CLOSE)** — analysis/design-gate selection only; no product code, no migration, no schema change, no route, no permission, no governance edit, no OpenAPI regeneration, no commit, no push, no deploy | `7bc5d2c` (unchanged) | `feat/production-spec` | 34 (unchanged — none created, none planned into existence by this task) | **No test suite executed in this session**; test files and prior accepted results cited as structural/historical evidence only, none restated as newly executed | Rebased the Internal-MVP picture against the FINAL-ACCEPTED, source-control-closed Minimum Operational Reporting slice at `7bc5d2c`. Baseline verified exactly (HEAD/parent/branch/dirty tree; zero source/schema/test/migration/OpenAPI drift; the four unrelated dirty reports and their four INDEX rows untouched); OpenAPI 109→**110 paths**; migrations 34. Protected path re-walked: **exactly two missing executable edges remain — Receipt (12) and DayClose (15)**; edge 14 (Daily Trading Report) closed at this HEAD. **Two factual corrections to the POST-KDS rebase, both against source that existed at its own HEAD**: (1) **`FR-FIN-024` is NOT absent — it is COMPLETE** (`org.operating_hours.business_day_cutover` + `sales/orders/business-day.ts`'s `cutoverLookup`/`resolveBusinessDay`, used by both Order creation and Sales' `currentBusinessDay`; verified present at `38e007b` too), removing a claimed DayClose prerequisite; (2) **voids are NOT structurally absent** — `order-lines.service.ts:571` writes `state:'voided'` on the pre-fire path with a reason CHECK, so `FR-FIN-022`'s "void and comp summary" is half-derivable (comps remain genuinely structurally zero — nothing writes `comped`). **Receipt: BLOCKED (unchanged)** — P1C-1 reaffirmed and left explicitly unchanged by RPT-R2 cl. 13; **branch-scoped authorization: BLOCKED by D-2 (unchanged)** — reconfirmed by RPT-R2 cl. 13 and the 2026-08-31 fail-closed consequence note; **post-fire void and `served`/Expediter: DEFERRED, not hard blockers**; **no newly-emerged hard blocker found**. **Exhaustive DayClose audit (A–Y)**: aggregate is **Treasury** (SRS §7.3 #29, §25.1, §5.5.4 `day.closed`, §15.2 Cash family) — Reporting ownership **rejected** as contradicting RPT-R2 cl. 5's zero-tables ratification; it is **both** a state-changing command and an immutable document. **Decisive finding — `FR-FIN-021` needs NO business-day anchor**: its text ("blocked while **any** cash session remains open") and §7.3 #29's invariant ("All sessions closed before day close") are both unqualified by business day, so the blocking set is exactly `cash_sessions WHERE branch AND status <> 'closed'` (with `closing` blocking, since its variance is declared but unapproved) — which covers zero-payment and movement-only sessions that the payment-derived Reporting contract structurally cannot see. **Timestamp derivation proven unsound on four independent grounds** (`branches.timezone` is UPDATABLE via PATCH; the cutover is effectively mutable and non-deterministic — no unique constraint on `(branch_id, day_of_week)` and no `ORDER BY` in `cutoverLookup`; `closedAt` is NULL while open — precisely the blocking rows; sessions provably span days, as the accepted `businessDayCount`/`spansMultipleBusinessDays` fields exist for that reason). **Recommended attribution: Option E — attribute AT close into the approved SQL's own `treasury.session_summaries` with `UNIQUE (tenant_id, cash_session_id)`**, making the partition of sessions across day closes total and disjoint: no `cash_sessions` column, no legacy backfill, untouched session lifecycle (R-3(a) precedent), correct for spanning sessions, Treasury-owned; Options A/B/C/D compared and rejected on correctness, ownership or stability. **DayClose ≠ CashSession Close, and closing the last session does NOT close the day** (checked, not assumed: a precondition is not a trigger; separate aggregates; separate permissions; the only automatic close is `FR-FIN-025` [S] at the configured boundary). **Z content: Reporting now supplies 8 of `FR-FIN-022`'s 13 mandated fields; FIVE remain** — tax by rate (not derivable at all), sales by category, sales by order type, void-and-comp summary, and the variance summary (RPT-R2 cl. 9 forbids a day-level variance total today); cash reconciliation is PARTIAL. **`FR-FIN-026` is vacuous, not blocking** — all four limbs unreachable (fiscal via P1C-1; no inventory day-end snapshot exists; `FR-RPT-002/003` NOT IMPLEMENTED; export "where configured"); **Receipt/fiscal does NOT gate DayClose and no fiscal user decision is required**, so verdict C does not apply. **Permission `cash.day.close` classified SOURCE-DECIDED** (SRS §15.2 verbatim; already named in `treasury.permissions.ts` as "seeded by the slice that implements it") — the first slice since Fire needing **no permission ratification**. Sequential per-branch Z numbering and historical retrieval are **[M] and required now**; `FR-FIN-025` automation, X report, export/PDF and signature/hash are **deferrable** (Z ≠ PDF). Idempotency **required** (`FR-API-020`, financially significant; unanimous Treasury precedent). Concurrency: **unique constraint + `pg_advisory_xact_lock`**, not SERIALIZABLE (zero repository precedent; §24.6.4's OCC has no prior version to assert against on an INSERT); three replay layers (unique constraint · `@Idempotent` · append-only column-level INSERT grant). **Branch security analysed rather than copied**: the single-active-branch posture is **sufficient without lifting D-2** (cardinality-1 argument), but the multi-branch consequence is materially worse for a write (operations blocked, not merely a report unavailable) and Treasury's existing terminal-derived posture may be stronger — a gate question. **MIGRATION EXPECTATION: ONE OR MORE ADDITIVE** (conceptual only — `day_closes` + `UNIQUE (tenant, branch, business_day)`, per-branch Z number, immutable Z snapshot, `session_summaries` + uniqueness, RLS + append-only grants; **no `cash_sessions` column**). Matrix deltas: row 27 **N→C**, `FR-FIN-024` **→COMPLETE**, all else unchanged ⇒ **27/33 ≈ 82 %** protected-path, **≈ 90 %** on the readiness-PDF 15-capability definition, **3 remaining blocking slices, exactly ONE startable**. **SELECTED: DAY CLOSE. DESIGN GATE REQUIRED: YES**, with 17 enumerated gate questions — three of them user decisions (Z-content completeness on a permanently immutable document · tax-by-rate · the attribution model), none of them a permission and none reopening P1C-1 or D-2 |
| 2026-08-31 | DAY CLOSE — final design & governance gate | [2026-08-31_DAYCLOSE-final-design-gate.md](2026-08-31_DAYCLOSE-final-design-gate.md) | **B. DAYCLOSE READY AFTER NARROW USER RATIFICATION** (DC-R1 sequencing · DC-R2 spanning-session variance ownership · DC-R3 historical Z read authority) — design/governance gate only; no product code, no migration, no schema change, no route, no permission, no governance edit, no OpenAPI regeneration, no commit, no push, no deploy | `7bc5d2c` (unchanged) | `feat/production-spec` | 34 (unchanged — migration 35 specified **conceptually only**, NOT created) | **No test suite executed in this session**; test files cited as structural evidence only, no prior run's results restated as newly executed | Final implementation-writable design gate for `FR-FIN-020…026`. Baseline verified exactly (HEAD/parent/branch/dirty tree; zero source/schema/test/OpenAPI drift; 34 migrations; 110 OpenAPI paths). **FOUR CORRECTIONS, one to this model's own prior report.** **(1) SERIALIZABLE HAS PRECEDENT — the POST-REPORTING claim of "zero repository precedent" was FALSE**: `kds-operations.service.ts:40-43` defines `KDS_SERIALIZABLE_RETRY` (Serializable, maxAttempts 3) used on three paths, backed by a **deliberately generic, non-Kitchen-specific** `common/domain-events/serialization-retry.ts` (three empirically-discovered failure shapes: `P2034`, raw SQLSTATE `40001`/`40P01`, unwrapped `DriverAdapterError`), first-class `UnitOfWork` retry support (`unit-of-work.ts:43-46,158,191-208`, fresh event collector per attempt, stable `correlationId`), and a write-skew **guard** test (`kds-concurrency.e2e-spec.ts:347`). **(2) The POST-REPORTING `session_summaries` + `UNIQUE(tenant_id, cash_session_id)` recommendation is REJECTED** — it silently forbids a legitimate spanning-session relationship the accepted Reporting slice proves occurs. **(3) sales by category UPGRADED to NOT HONESTLY DERIVABLE** — `order_lines` carries **no category snapshot**, `MenuItem` deliberately has no `category_id`, placement is the **many-to-many** `menu_item_placements`; aggregating would join a sealed immutable Z to mutable master data (the exact `FR-RPT-005` restatement hazard RPT-R2 records as NOT IMPLEMENTED) and is ambiguous for a multi-placed item. **(4) advisory lock DEMOTED** — a lock on `(branch, businessDay)` does not protect the blocking-session read (different key space); SSI predicate locks do. **`FR-FIN-021` CONFIRMED to need NO session→businessDay attribution**: the text ("**any** cash session remains open") and SRS §7.3 #29's invariant ("All sessions closed before day close") are both unqualified, so the blocker set is `cash_sessions WHERE branch AND status <> 'closed'` (`closing` blocks — its variance is declared but unapproved), covering zero-payment, movement-only and spanning sessions, served by the **already-existing** `@@index([tenantId, branchId, status])` with **no new persistence and no new index**; Reporting's payment-derived contract is explicitly NOT used. **Attribution re-audited across six options**: A/B (stamp at open/close) rejected on correctness + legacy-backfill hazard; C (multi-DayClose linkage) viable but duplicates whole-session facts; D rejected as invention; E sound but timestamp-derived; **recommended OPTION F — a two-layer split**: LINKAGE `UNIQUE(tenant_id, day_close_id, cash_session_id)` (a spanning session legitimately appears twice) + exclusive OWNERSHIP via a **partial** `UNIQUE(tenant_id, cash_session_id) WHERE <owner>` (precedent: the one-open-session-per-drawer partial index), so nothing is duplicated and nothing legitimate is forbidden. **Approved SQL audited and found DIRECTIONALLY CORRECT but PHYSICALLY INCOMPLETE**: no `tenant_id` on any of the three tables (conflicts `FR-PLT-010`), no Z number column (conflicts `FR-FIN-023`), no Z content at all (conflicts `FR-FIN-022`), no immutability model, `session_summaries` summarises nothing and has no uniqueness; `closed_by → identity.users(id)` kept **verbatim** as the correct untenanted-global precedent; `variance_reports` (one FK + one BIGINT, 1:1) probably does not earn a table. **Ownership: TREASURY** on four authorities (§7.3 #29, §25.1, §5.5.4 publisher, §15.2 Cash family); Reporting rejected as contradicting **RPT-R2 clause 5**'s zero-tables ratification. **Z audited field-by-field into six classes** (not "8/13"): **A exactly available ×5** (gross, net, tender*, count, AOV) · **B structurally zero and truthful ×2 + the comp half** (refunds proven *structurally impossible* — `order-state.ts` gives `refunded`/`partially_refunded` **no inbound transition**) · **C new contract, no new semantics ×2** (sales by **order type** — an immutable NOT NULL enum on the order; the **void** half — pre-fire voids ARE written at `order-lines.service.ts:571` with `voided_by` + a reason CHECK) · **D source-silent ×1** (variance summary) · **E not derivable ×2** (**tax by rate**, **sales by category**) · **F blocked by an excluded capability ×0 — no Z field is P1C-1-blocked**. **Tax-by-rate framed as SEQUENCING, never a semantics vote** — components are not persisted, so no user choice can conjure the data. **`FR-FIN-026` re-audited limb-by-limb with proof, not assertion**: grammar resolved to "where configured" attaching to the export limb only, but **immaterial because all four limbs are unavailable regardless**; fiscal finalisation is a **trigger over an empty set** (zero fiscal models among 88, none of 34 migrations creates one) *and independently* P1C-1-excluded; no dated inventory snapshot table exists; report pre-aggregation is **forbidden by RPT-R2** and must not be built; accounting export needs the **mandatory `FR-PLT-041` outbox**, which is proven absent (`grep -c -i outbox prisma/schema.prisma` → **0**, plus five accepted module docblocks refusing to fake it). **RECEIPT INDEPENDENCE PROVEN on three independent legs** — `FR-FIN-022` enumerates no fiscal field, `FR-FIN-023`'s per-branch Z number is unrelated to §22.2's invoice `pre_allocated_block`, and limb 1 is vacuous (classified NOT IMPLEMENTED, never *complete*). **Concurrency: SERIALIZABLE + bounded retry as PRIMARY, `UNIQUE(tenant, branch, business_day)` as the structural backstop** — §24.6.4 restricts *pessimistic* locking to two named cases and SSI is *optimistic*, so SERIALIZABLE is **more** §24.6.4-consistent than the advisory lock previously proposed; one model answers all seven concurrency questions, and `SerializationRetryExhaustedError` maps to the existing 409 "lost a race" convention, never 422. **Both critical races designed explicitly**: the late-session race is impossible under SSI (a concurrent session INSERT falls inside the read predicate); the **late-sale race is real beyond the transaction window** because `order_payments.business_day` is *copied from the order*, and is closed by a **fourth precondition — zero open orders for the target business day** (payment capture requires a non-finalised order **and** an `open` session, and a new order can never be created into a past day since `business_day` is server-derived), making the sealed Z **immutable *and true***; its operational cost (a forgotten parked order blocks the close) is **disclosed** and classified an engineering consequence, not a ratification. Z persistence: **insert-once, no draft→finalise, no status, no `version`** (nothing to assert against on an INSERT), normalised BIGINT columns with DB CHECK identities rather than a JSON blob, immutability **DB-enforced** by the accepted `cash_close_policies` grant/RLS pattern. **Z number: `MAX+1` per `(tenant, branch)` starting at 1, never reset, no sequence object (non-transactional ⇒ guaranteed gaps) and no trigger (repo has zero), gaplessness a property not a promise, and `FR-OFF-016`/`OrderNumberBlock` block allocation explicitly NOT imported** (day close is never offline), nor is §22.2 fiscal invoice sequencing. **Branch security: the single-active-branch posture is sufficient for an irreversible write on a cardinality-1 argument**, D-2 untouched, `FR-SEC-002/003/004` still NOT IMPLEMENTED, check inside the write transaction, with the worse multi-branch consequence (operations blocked, not merely a report unavailable) disclosed. **Session type: POS allowed** (`@AllowPosSession`, matching every Treasury write route) — decided from actors and endpoint conventions, **not inferred from the permission name**. Idempotency and DB uniqueness kept **distinct** across seven enumerated scenarios (an already-closed day under a *different* key is catchable only by the unique constraint); **DayClose id is SERVER-generated** — `FR-OFF-015` binds device-originated facts, and this is the administrative-write class (`CashClosePolicy` precedent), not the `CashSessionCloseAttempt` one. `day.closed` published once inside the UoW with the §5.5.4 envelope, retry-safe by construction; **no synchronous external call, nothing faked**. **DC-R3 surfaced rather than hidden**: `cash.day.close` is a WRITE code and `treasury.controller.ts` has **already refused that exact reinterpretation** for `cash.session.open`; `report.view.financial` is a deliberately narrow RPT-R1 code whose clause 8 forbids broadening; §15.2's Cash family has no read code and **Appendix C is absent from the SRS** (D-20 precedent). **THREE user ratifications only** — DC-R1 sequencing (scope acceptance, never a waiver, with §10's binding permitted/forbidden wording so no artefact may say "`FR-FIN-022` waived/satisfied by subset/complete"), DC-R2 spanning-session variance ownership (six candidate semantics enumerated; A/B/C shown structurally unsound; **no business answer recommended**), DC-R3 historical Z read authority (four options, reuse of `cash.day.close` explicitly refused). Permission, ownership, event, blocker check, naming, RLS, indexes, audit literal, idempotency, transaction mechanics and session type **deliberately NOT put to the user**. Includes exact API surface with 400/401/403/404/409 semantics and 404→403→400→409 ordering, conceptual migration 35 object list (**no `cash_sessions.business_day` column; unconditional `UNIQUE(tenant_id, cash_session_id)` FORBIDDEN**), a 21-point implementation DoD with deterministic-barrier concurrency tests and no sleeps, and the full `FR-FIN-026` sequencing packet naming every unmet limb and the future slice that reopens it |
| 2026-08-31 | DAY CLOSE — design-gate acceptance correction (pre-ratification) | [2026-08-31_DAYCLOSE-design-gate-acceptance-correction.md](2026-08-31_DAYCLOSE-design-gate-acceptance-correction.md) | **A. DAYCLOSE DESIGN CORRECTION CLEAN — READY FOR USER RATIFICATION** — design correction only; the original gate is preserved byte-unmodified and this correction governs where they differ; no product code, no migration, no schema change, no route, no permission, no governance edit, no OpenAPI change, no commit, no push, no agents/forks | `7bc5d2c` (unchanged) | `feat/production-spec` | 34 (unchanged — migration 35 still conceptual only) | **No test suite executed in this session**; test files cited as structural evidence only | Corrects the DayClose final design gate **before** ratification. **CORRECTION A — the SSI late-session proof is WITHDRAWN.** The gate's claim that a concurrent CashSession INSERT "falls inside the SERIALIZABLE read predicate, therefore cannot commit" is **not a sufficient SSI proof**: PostgreSQL SSI aborts only on a **dangerous structure** (a pivot with both inbound and outbound rw-antidependencies forming a cycle). DayClose→SessionOpen is a **single unidirectional** antidependency — the opener reads nothing the closer writes — so it serializes validly as *close-then-open* and **both commit**. The KDS Serializable precedent does **not** transfer because its proof rests on a real mutual cycle (documented by its own `D0 [GUARD]` write-skew test). Replaced by a **structural close boundary: a manual DayClose may close ONLY a STRICTLY PAST business day** (`target < branchCurrentBusinessDay`; current ⇒ **409** transient-conflict, future ⇒ **400** per Reporting's precedent), resting on four structural legs — new orders are server-clock-derived into the CURRENT day and cannot enter a past day; a session opened after the close belongs operationally to the later day and can never acquire the sealed day's `closedBusinessDay`; `openOrderCount == 0` closes the late-completion path for orders already in the target day; and FR-FIN-021 still checks **all** open/closing branch sessions at the close instant. **Source checked for any requirement to early-close the CURRENT day — none found** (FR-FIN-025's "at the configured boundary" is consistent, not contradictory), so no STOP condition. **No lock was added to preserve the previous design.** **CONCURRENCY RE-DERIVED CASE BY CASE, claiming SERIALIZABLE only where a genuine cycle exists**: (A) same branch/day ⇒ `UNIQUE(tenant,branch,business_day)`, a **terminal** 409 that must **not** be retried (`isSerializationFailure` correctly excludes P2002); (B) **different past days competing for `zNumber` ⇒ the one genuinely load-bearing SERIALIZABLE case** — both transactions read `MAX(z_number)` over the same range and both write into it, a **mutual** rw-antidependency and therefore a real cycle ⇒ SSI abort ⇒ bounded whole-UoW retry recomputes; **new refinement: the two unique constraints carry different meanings** — day-uniqueness is a terminal business conflict (409) while **z-number uniqueness is a retryable allocation collision**, and collapsing them would turn a transient collision into a spurious permanent failure; (C) past-day order completion ⇒ closed by the **already-accepted** shared `ros_cash_session` advisory-lock chain (Payment `:180`, Close `:226`/`:451`, Movements `:183`) plus FR-FIN-021 plus zero-open-orders — DayClose adds **no** lock; (D) new order creation ⇒ structurally impossible in a past day; (E) session opening ⇒ **race is harmless**, both outcomes correct; (F) session finalisation ⇒ FR-FIN-021 blocker plus a proof that a session can never acquire `closedBusinessDay = D` after `DayClose(D)` commits. **CORRECTION B — DC-R2 now has a recommendation: CLOSE-BUSINESS-DAY OWNERSHIP.** A session's whole-session close facts, above all variance, belong exactly once to the business day in which it became CLOSED, recorded as an **immutable `closedBusinessDay`** derived at final-close time from the **same** authoritative resolver Sales uses, persisted in the same single UPDATE that already writes the close facts exactly once at the CLOSED transition, **NULLABLE** for legacy tolerance (real pre-existing closed rows exist; `NULL` = a pre-DayClose legacy close owned by no DayClose — the P1G-1 `close_attempt_id` discriminator pattern), and **never re-derived** from the mutable branch timezone or cutover. **Distinguished from the previously-rejected `cash_sessions.business_day`**: that meant "the day this session's trading belongs to" (lossy for spanning sessions); this means "the day it became closed" — a single-instant fact making no trading-attribution claim. A spanning session **may still be linked to two DayCloses** for its day-scoped tender contribution, so **unconditional `UNIQUE(tenant_id, cash_session_id)` remains FORBIDDEN**; linkage uniqueness is `UNIQUE(tenant_id, day_close_id, cash_session_id)`. **Re-evaluated the partial owner UNIQUE as instructed: it is NOT the mechanism** — ownership is functionally determined by immutable `closedBusinessDay` plus the one-DayClose-per-branch-day constraint, making uniqueness a **theorem rather than a constraint**; the partial form is kept only as **defence in depth** if the flag is materialised for snapshot self-explanation. **Zero-payment and movement-only sessions explained**: they contribute no tender to any day but their variance (genuinely non-zero via float miscounts and pay-in/pay-out/safe-drop) is owned exactly once by their closing day — **the first model in which they become attributable at all**, since accepted Reporting's payment-derived set structurally cannot see them. **CORRECTION C — DC-R3 now recommends a NARROW SCOPE EXTENSION of the existing `report.view.financial`** to `GET /branches/{branchId}/day-closes/{businessDay}`, **creating no new code**: SRS §19.3 lists "Z Report — Statutory day close" under **Financial Reports**, the same category RPT-R1 drew the code from, so RPT-R1 clause 6's NOT-authorized list is not breached; the widening of RPT-R1's single-route gating is surfaced explicitly as the thing DC-R3 ratifies. **`cash.day.close` is REFUSED as read authority** — a write permission is not a read-history permission, and `treasury.controller.ts` already refused this exact reinterpretation for `cash.session.open`; `cash.day.read`/`cash.z.read`/`report.view.z`/`report.view.day_close` are **not created**. **CORRECTION D — exact DC-R1 ratification wording drafted (nine binding clauses)** authorising the Internal-MVP operational DayClose now while `FR-FIN-022` stays **PARTIAL** (tax by rate, sales by category, the comp half, and PARTIAL sales-by-tender all named with reasons), `FR-FIN-026` stays **PARTIAL with all four limbs unmet**, and `FR-FIN-025` **[S]** stays NOT IMPLEMENTED — with "waived", "satisfied with a subset", "Z fully compliant" and "FR-FIN-020..026 COMPLETE" **expressly forbidden in every artefact**, and the state-changing aggregate explicitly distinguished from Z-content compliance. **FR-FIN-026 wording corrected**: the gate's "vacuous, not blocking" framing is replaced — an empty fiscal-document set is **never** to be written as "satisfied"; the required form states that no documents exist to finalise, **no fiscal-finalisation capability is implemented**, P1C-1 remains untouched, and the limb is therefore **NOT IMPLEMENTED**. Receipt independence is unchanged and still proven. **§24.6.4 aggregate-version resolved explicitly as OPTION B (no column)** rather than silently ignored: the rule's operative clause governs **UPDATEs**, DayClose is insert-once with no UPDATE grant, and repository precedent is unambiguous — **exactly three** models carry `version` (`Order`, `Ticket`, `RecipeVersion`, all mutable with real `assertVersion` paths) while **six** immutable append-only tables omit it; Option A (immutable `version = 1`) recorded as acceptable but inferior. **Z session/variance persistence model corrected** into four strictly separated concerns (day-scoped tender · linkage · WHOLE_SESSION close facts · variance ownership) with exact conceptual uniqueness and four binding anti-double-counting rules — `openingFloat`/`expectedCash`/`countedCash`/movement totals are **never** emitted as day totals and must be wire-labelled `WHOLE_SESSION`. **Closeable order set audited exhaustively from current source**: blocking states are `draft`/`open`/`held`/`parked`/`partially_paid` taken verbatim from the accepted `OPEN_ORDER_STATES`; `completed` and `cancelled` are proven **non-blockers** (`TRANSITIONS` gives both `[]`, `assertMayCapturePayment` admits only `open`/`partially_paid`, `assertOrderMutable` refuses finalised) and `partially_refunded`/`refunded` are **structurally unreachable**; the precondition is recorded as an **engineering safety invariant**, not an invented SRS requirement, with its operational cost disclosed. **Z number re-confirmed**: sequential per `(tenant, branch)` from 1, **no reset**, no fiscal invoice sequencing and no offline block allocation imported, `MAX+1` acceptable **because** case B's proof is sound, a failed transaction persists no number (hence no SEQUENCE object — non-transactional, and the repo has zero), and **gaplessness is a property of the mechanism, not a claimed requirement**. **Still exactly THREE ratifications** (DC-R1 sequencing recommended YES · DC-R2 close-business-day ownership · DC-R3 `report.view.financial` extension); **no fourth decision created** for the strictly-past-day rule, SERIALIZABLE, schema names, version omission, the zero-open-orders precondition, or the branch posture. **Corrected DoD adds 26 numbered items** including the three close-boundary tests, the *concurrent session open may serialize after the close and both commit without corrupting the sealed Z* test that would have failed the withdrawn claim, spanning-session double-linkage, `closedBusinessDay` persistence at final close, variance-in-exactly-one-Z, zero-payment ownership, legacy-NULL ownership, different-day zNumber retry, and event/audit exactly-once under serialization retry — all with deterministic barriers, no sleeps |
| 2026-08-31 | DAY CLOSE — pre-ratification final correction (legacy attribution · resolver ownership · cutover race) | [2026-08-31_DAYCLOSE-pre-ratification-final-correction.md](2026-08-31_DAYCLOSE-pre-ratification-final-correction.md) | **A. DAYCLOSE PRE-RATIFICATION CORRECTION CLEAN — RATIFICATIONS READY** — design correction only; both prior DayClose reports preserved byte-unmodified and this report governs where they differ; DC-R1/DC-R2/DC-R3 not reopened; no product code, no migration, no schema/source change, no route, no permission, no governance edit, no commit, no push, no agents/forks | `7bc5d2c` (unchanged) | `feat/production-spec` | 34 (unchanged — migration 35 still conceptual only) | **No test suite executed in this session**; every claim traced to a source path and line | Resolves the two implementation-safety points left open before ratification, and withdraws one further prior claim. **§1 LEGACY ATTRIBUTION — NO SAFE BACKFILL, PROVEN.** Every candidate anchor enumerated and refuted: `closed_at` + timezone/cutover is forbidden (branch timezone is updatable via PATCH; operating-hours rows are creatable with **no** unique constraint on `(branch_id, day_of_week)` and `cutoverLookup` applies **no `ORDER BY`**, so the effective cutover is mutable and can be non-deterministic); `MAX(order_payments.business_day)` yields the last **trading** day, not the **closing** day, and is **absent entirely** for zero-payment/movement-only sessions — precisely the sessions DC-R2 exists to reach; `cash_movements`, `cash_session_close_attempts` and `workforce.shifts` carry no `business_day` at all. Production DBs are **not** assumed empty — P1G-1's migration-compatibility closure recorded real pre-existing open and closed rows. Resolved by the **smallest durable fail-closed mechanism: an immutable Treasury `day_close_activations` row per (tenant, branch)** created **lazily and idempotently on the first DayClose request**, recording `activation_business_day = branchCurrentBusinessDay` at that **live** instant (a live-clock derivation, never a historical one), written by the **application not the migration** (a SQL backfill would require a second business-day algorithm — forbidden). Eligibility becomes `activation < target < branchCurrentBusinessDay`; the activation day itself is excluded because it provably straddles the migration and may hold both NULL and populated closes. **The first request for a branch always 409s**, activating it and naming the first closable day — deliberate and self-documenting. **Legacy NULL rows are retained honestly** (the P1G-1 `close_attempt_id` legacy-discriminator pattern), can **never** enter a variance summary (`closed_business_day = :day` never matches NULL), and — decisively — **no Z is ever produced for any day they could belong to**, so this is **not silent variance loss**: the variance is not omitted from a Z claiming completeness, no such Z exists. **`FR-FIN-023` historical retrieval is retrieval of PERSISTED records only** — a pre-activation day returns **404** and the system **never retroactively manufactures a Z**, which would be an unauditable fabrication of a statutory record. Organisation is untouched. **§2 RESOLVER — one implementation, zero deviation growth.** `resolveBusinessDay`/`cutoverLookup` stay the single copy; `closedBusinessDay` is exactly the existing `currentBusinessDay` computation (`resolveBusinessDay(new Date(), branch.timezone, cutoverLookup(...))`, verified at `daily-trading-sales.query.service.ts:48-74`). The Sales↔Treasury module edge **already exists in BOTH directions** with `forwardRef()` on both sides (`sales.module.ts:68-70,97-98`; `treasury.module.ts:7`) and **both directions already consume public contracts** (Treasury→Sales `CASH_SESSION_TENDER_TOTALS_QUERY` at `cash-session-close.service.ts:65-66`; Sales→Treasury `CASH_SESSION_FACTS_QUERY` at `sales-payment.service.ts:20-21`) with **no `KNOWN_DEVIATIONS` entries**. Recommended: a small additive **public Sales** business-day contract delegating to the same functions (reusing `DAILY_TRADING_SALES_QUERY` recorded as an acceptable fallback — a naming judgement, not a correctness one); Organisation recorded as the correct long-term owner but **not moved**, since it would touch accepted Sales and Reporting code for no functional gain. **§3 THE CUTOVER RACE IS REAL — BOTH TRANSACTIONS CAN COMMIT TODAY.** Traced line by line: `at = input.at ?? new Date()` is captured **before** the transaction opens (`:175`) and is **not client-suppliable** (the controller never passes it, `:341-356`); the transaction is `withAuthContext` with **no isolationLevel ⇒ READ COMMITTED**; `businessDay` is derived inside the tx from that pre-tx instant (`:224-228`); `pg_advisory_xact_lock('ros_order_number', "branchId:businessDay")` is taken inside `allocateOrderNumber` before the INSERT and held to commit (`:112-116`); the Order INSERT is in the same tx (`:239`); **no CashSession is required or read**. Hence the `ros_cash_session` lock **does not apply** — the two transactions share no serialization mechanism through it — and **SERIALIZABLE would not abort**: T1 reads orders-of-D and T2 writes into that predicate, but T2 reads nothing T1 writes, so there is **one unidirectional rw-antidependency, no pivot, no cycle**. **FURTHER CORRECTION: SERIALIZABLE is WITHDRAWN as DayClose's isolation level** — beyond not helping, it is **unsafe with a blocking lock**, because `withAuthContext`'s **first statement** is a snapshot-acquiring `SELECT set_config(...)` (`prisma.service.ts:62-66`), so the transaction snapshot is fixed **before** any lock wait and a DayClose that blocked would resume blind to the very order it waited for — the same defect class P1G-1 found when `transaction_timestamp()` aged across a lock wait. **DayClose therefore runs at READ COMMITTED**, matching every other Treasury/Sales write path; coherence comes from the fence, not the isolation level. **§4 SOLUTION — Option C + Option B, both required, neither sufficient alone.** **C:** DayClose **participates in the already-existing** `ros_order_number` `(branch, business-day)` advisory lock — no lock invented, no new key space, no new lock ordering, and §24.6.4 already sanctions pessimistic locking for exactly this key's purpose (stated plainly that DayClose is nonetheless a **second consumer**, with a documentation-only note owed to `allocateOrderNumber`). C alone fails because the loser simply proceeds after release. **B:** Order creation, **after** that lock and **before** the INSERT, checks an additive **public Treasury `DAY_CLOSE_STATE_QUERY.isClosed(tx, …)`** and refuses with 409 — on the **already-precedented** Sales→Treasury public edge, so no new cycle, no private query, **zero `KNOWN_DEVIATIONS` growth**; B alone would be a TOCTOU without C. Both interleavings proved: lock-holder = order-create ⇒ close refused as an open order exists; lock-holder = DayClose ⇒ order refused as the day is closed. **Only Order creation needs the check** — payment, line-add and pre-fire void all require a non-finalised target-day order, which cannot exist after the close. **Z-number collisions across two different past days** (different lock keys, so no mutual exclusion) are handled by a **small bounded retry local to the DayClose service**, keyed on the constraint name and distinguished from the terminal day-uniqueness violation — **no new advisory lock, no change to `UnitOfWork`, no new isolation level**. **§5 re-derives late payment/completion** without overclaiming: no new order can enter closed D; no target-day open order exists at commit (read under the fence, so not stale); `completed`/`cancelled` are terminal and `partially_refunded`/`refunded` structurally unreachable; a later current-day CashSession cannot create tender for D because no payable D-order can ever exist again — with **Refund, Comp, post-fire Void, adjusting entries and offline sync recorded as explicit revisit triggers** that would invalidate that step. **§6** clarifies DC-R2 under the activation rule: new closes get `closed_business_day` in the existing single close-facts UPDATE (ownership deterministic, no flag needed); legacy NULLs are owned by no DayClose, with **no silent variance loss**. **§7: still exactly THREE ratifications, unchanged — no fourth created**, since no business semantic was discovered; the activation epoch, resolver contract, lock participation, order-create check, READ COMMITTED correction and local Z-number retry are all engineering mechanics. **§8: still ONE additive Treasury migration 35**, gaining `treasury.day_close_activations` (immutable, `UNIQUE(tenant_id, branch_id)`) rather than a separate migration, and **no `sales.*` schema change** — the Order-create fix is application code only. **§9 adds 15 deterministic DoD tests, no sleeps**, including the exact cutover interleaving asserting the end state is **never** committed-DayClose(D)-plus-subsequently-committed-Order(D), both lock-ordering directions, activation idempotence/immutability, pre-activation 409 and 404-not-manufactured, a guard test that the production path does **not** silently acquire Serializable, and different-day Z-number retry without a spurious terminal 409 |
| 2026-08-31 | DAY CLOSE — activation mechanic final correction (`day_close_activations` lifecycle / transaction semantics) | [2026-08-31_DAYCLOSE-activation-mechanic-final-correction.md](2026-08-31_DAYCLOSE-activation-mechanic-final-correction.md) | **A. DAYCLOSE ACTIVATION MECHANIC CLEAN — RATIFICATIONS READY** — design correction only; all previous DayClose reports preserved byte-unmodified and this report governs where they differ; DC-R1/DC-R2/DC-R3 not reopened; no product code, no migration, no schema/source change, no governance edit, no commit, no push, no agents | `7bc5d2c` (unchanged) | `feat/production-spec` | 34 (unchanged — migration 35 still conceptual only, and this task seeds no data) | **No test suite executed in this session**; every claim traced to a source path and line | Corrects the one remaining implementation-correctness defect before ratification. **THE DEFECT IS REAL AND WORSE THAN STATED — confirmed by TWO independent mechanisms.** The prior design's *"the first request always returns 409 … the activation nevertheless persists"* is **self-contradictory and WITHDRAWN**: (1) the activation INSERT and the ConflictException would sit in the **same** `withAuthContext` transaction, so the throw rolls the INSERT back; and (2) verified this session in `idempotency.interceptor.ts`, a throw calls `idempotency.release()` (`:137-141`) so **nothing is stored either**, while success calls `complete()` storing status+body (`:129-136`) and a replay restores it without running the handler (`:97-101`, `:118-123`). **Consequence stated explicitly: every request would be "the first request" forever, `firstEligibleBusinessDay` would never stabilise, and `FR-FIN-020` would be permanently unreachable.** **Resolved by OPTION A — an explicit committed activation result — using a RATIFIED precedent already implemented in accepted Treasury code**: R-6(a)'s rule that a durable state change must **COMMIT and return an outcome discriminator, never throw** (*"a 4xx would make the idempotency interceptor release the reservation"*), realised at `cash-session-close.service.ts:802-806` as `buildFinalizeResult(session, outcome: 'closed' | 'rejected')` (with a second discriminator precedent, `created: justCreated`, at `:797`). The generalisable invariant is recorded: **throw only when nothing durable was written; commit and report the outcome when something was.** One transaction now yields `ACTIVATED` **or** `CLOSED`; on the activation path it creates the row, writes the audit entry, **commits, and does not throw**, returning `200 { outcome:'ACTIVATED', activationBusinessDay, firstEligibleBusinessDay }`. **Proof that the two outcomes can never collide**: on the first request `A := currentBusinessDay`, and eligibility requires `A < target < current`, i.e. an empty interval — so `ACTIVATED` is always the first outcome. **Options B and C rejected with reasons**: B (migration seeding) would need timezone + per-weekday cutover maths in SQL — **a second business-day algorithm**, forbidden — and the only SQL-expressible safe bound is a `utc_date + 1` fence (a UTC+14 branch can be a calendar day ahead), excluding ~2 extra days per branch, while **failing entirely for branches created after migration 35**, where absence-of-row is ambiguous between "new branch" and "seed missed"; data-seeding migrations are precedented exactly once (`20260816180000_org_location_registry`) but that seeds from **existing relational facts**, never a derived temporal computation. C (separate bootstrap) invents an operator workflow, route and permission (§15.2 supplies none) for something A obtains as a by-product. **All eleven required properties discharged**, including immutability via the accepted `cash_close_policies` grant pattern (SELECT + column-level INSERT, REVOKE UPDATE/DELETE/TRUNCATE, RLS ENABLE+FORCE, no UPDATE/DELETE policy) and `UNIQUE(tenant_id, branch_id)` with the ADR 0008 D-09 composite FK. **Idempotency fully resolved**: the committed `ACTIVATED` 200 is stored, a **same-key** replay reproduces it byte-identically with `Idempotent-Replay: true` (correct — it is the true permanent outcome of *that* command under `FR-API-022`, not a stale conflict), and a **new key** later performs a real close once eligible; the "activation-era conflict replayed forever" hazard is shown to be an artefact of the withdrawn throwing design, which stored nothing at all. **Audit required and specified**: activation is a durable state change from a user POST, so `FR-AUD-001` applies — a new `DAY_CLOSE_ACTIVATED` literal distinct from `DAY_CLOSED`, in the **same transaction** as the INSERT, `before` null, actor recorded as both user and employee (P1D-E); **no user governance decision** is created for the literal (P1G-1 records audit literals as Design-Gate detail). **Terminology corrected to remove the prior contradiction**: `activationBusinessDay` (**stored**, immutable) vs `firstEligibleBusinessDay` = `A + 1` (**derived**, single source of truth, reported in every response); **`A` itself is NEVER closeable and must never be called "the first closable day"**, because migration 35 lands *during* some business day so that day provably mixes NULL and attributed closes — only a day lying entirely after `A` is trustworthy. Worked example recorded: activation on `D` ⇒ first eligible `D+1`, closeable once current reaches `D+2`. **The accepted cutover solution is preserved and proven undisturbed**: order creation never touches `day_close_activations`; the activation read/insert sits **inside** the existing `ros_order_number(branch, businessDay)` critical section, adding no new lock; two concurrent first-requests for the **same** day serialise on the same advisory key, and for **different** days contend only on the `(tenant, branch)` unique index — one wins, the loser is handled by the **same local bounded retry** already designed for the `z_number` collision and then takes the normal ineligible-day 409; **no deadlock is possible** since neither transaction ever waits on the other's advisory lock. **Still exactly THREE ratifications, unchanged — no fourth**, since everything here is transaction semantics, idempotency mechanics, an audit literal and a field name. Adds 9 DoD items including a guard test that the activation survives the first request (which the withdrawn design would have failed), same-key replay creating no second row or audit entry, two-concurrent-first-requests yielding exactly one row under a deterministic barrier with no sleeps, and a post-migration-created branch activating on first use with no special-casing |
| 2026-08-31 | DAY CLOSE — final user ratification RECORDED in the Governance Register (DC-R1, DC-R2, DC-R3) | [2026-08-31_DAYCLOSE-user-ratification.md](2026-08-31_DAYCLOSE-user-ratification.md) | **A. DAYCLOSE RATIFICATIONS RECORDED — IMPLEMENTATION GOVERNANCE-UNBLOCKED** — governance recording only; **the Governance Decision Register is the binding record, this report is evidence**. No product code, no migration, no schema change, no route, no permission in source, no test, no OpenAPI change, no D-21, no commit, no push | `7bc5d2c` (unchanged, verified before and after) | `feat/production-spec` (unchanged) | 34 (unchanged — conceptual migration 35 remains conceptual; nothing authorised by this recording) | **No test suite executed and no test file modified** — governance recording only | Records the user's explicit ratification (*«موافق، اعتمد DC-R1 وDC-R2 وDC-R3.»* — "Agreed, ratify DC-R1, DC-R2 and DC-R3") of the three decisions tabled by the corrected Day Close gate. Baseline verified before and after (HEAD/branch/`git log -3`/dirty tree documentation-only/34 migrations; zero source/schema/test/OpenAPI drift). Written to `GOVERNANCE_DECISION_REGISTER.md` as **one unnumbered ratified entry — "Day Close Ratification — 2026-08-31"** — placed immediately before the Final Decision Matrix exactly where the Fire/P1F-2/FIFO/Approval-Runtime/P1G-1/R-6/KDS/Reporting entries sit, plus one matching matrix bullet. **No `D-21`; the 20-decision tally is unchanged (17 RATIFIED · 1 IN PART · 1 BLOCKED · 1 OPEN).** Limb ids open a **new `DC-R<n>` series**, **verified unused** (`DC-R` and bare `DC-` both returned **zero** occurrences); the cash `R-1(a) … R-6` (whose bare `R-<n>` labels collide with D-20's own `R-1 … R-7` option labels), `KDS-R1 … KDS-R12` and `RPT-R1 … RPT-R3` series are deliberately not continued. **DC-R1** authorises the **Internal-MVP operational Day Close now** — the state-changing Treasury aggregate plus its minimum persisted Z/historical surface — implementing `FR-FIN-020`, **`FR-FIN-021` IN FULL (block *and* blocking-session list, over every branch session with `status <> 'closed'`, unqualified by business day)** and `FR-FIN-023`, reusing the already-COMPLETE `FR-FIN-024`; **simultaneously `FR-FIN-022` remains PARTIAL** (tax by rate not derivable — only the `FR-FIN-032` component *sum* is persisted; sales by category has no `order_lines` snapshot and would bind a sealed document to mutable many-to-many master data; the comp half is structurally zero while the **void half IS implemented**; sales by tender PARTIAL per RPT-R2 cl. 8) and **`FR-FIN-026` remains PARTIAL with all four limbs unmet** — with the register binding that an empty fiscal-document set is **never** to be called *"satisfied"*, *"vacuously satisfied"* or *"complete by absence"*, that report pre-aggregation **must not** be built because RPT-R2 excludes it, and that accounting export is blocked by the absent `FR-PLT-041` outbox — plus **`FR-FIN-025` [S] NOT IMPLEMENTED**; recorded as **sequencing only: NOT a waiver, NOT a reinterpretation, NOT a claim that `FR-FIN-020 … 026` are complete or that the Z report is fully compliant**, with no artefact permitted to claim otherwise and the Day Close **aggregate** required to be distinguished from Z-content **compliance** wherever either is described. **DC-R2** fixes **CLOSE-BUSINESS-DAY OWNERSHIP** — whole-session close facts, above all variance, owned **exactly once** by the business day in which the CashSession becomes CLOSED, via an immutable `closedBusinessDay` derived at final close from the **same authoritative resolver Sales already uses** and **never historically re-derived** from mutable timezone/cutover; a spanning session's day-scoped tender **may contribute to BOTH** days while its variance is owned **only by the closing day**, so the same session **MAY** appear in multiple DayClose snapshots and an **unconditional `UNIQUE (tenant_id, cash_session_id)` on the linkage is PROHIBITED**; whole-session figures are labelled `WHOLE_SESSION` and never emitted as day totals so variance cannot double-count; **legacy `closed_business_day IS NULL` sessions remain unknown honestly — never backfilled, inferred, assigned or included**, with no speculative backfill authorised; zero-payment and movement-only sessions are reached for the first time, RPT-R2 cl. 9 unchanged. **DC-R3** extends the existing **`report.view.financial`** narrowly to `GET /branches/{branchId}/day-closes/{businessDay}`, **creating NO new permission code** — recorded as an **explicit user-ratified extension of RPT-R1 clause 3** (*"No other route is authorised by these codes"*) **by exactly one route and nothing else**, category-consistent because §19.3 lists *"Z Report — Statutory day close"* under Financial Reports; `report.view.sales` not required and not extended; **`cash.day.close` remains the separate source-decided WRITE authority and MUST NOT serve as historical-read authority**; `cash.day.read`/`cash.z.read`/`report.view.z`/`report.view.day_close` and any other token **NOT authorized**; code carries no branch scope; ADR 0008 D-01 remap route recorded without making the ratification provisional. **Activation mechanic and cutover fence recorded as CONSEQUENCES, explicitly NOT a `DC-R4`**: the first command may create the immutable activation epoch and return `outcome = ACTIVATED` on a transaction that **COMMITS and does not throw**; `activationBusinessDay` is never closeable; `firstEligibleBusinessDay = activationBusinessDay + 1`; closeable only when `activation < target < branchCurrentBusinessDay`; historical Z is **never** retroactively manufactured (pre-activation day ⇒ 404); Order creation and Day Close share the **existing** `ros_order_number(branchId, businessDay)` fence with the public Day Close state checked after the fence and before INSERT — **SERIALIZABLE is NOT claimed to solve that race** and **advisory-lock mechanics are NOT ratified as business policy**. **Consistency scan CLEAN** across DC-R · DayClose · `cash.day.close` (zero prior occurrences) · `report.view.financial` · RPT-R1/RPT-R2 · FR-FIN-022/026 · variance · CashSession · D-2 · P1C-1: RPT-R2 cl. 10 explicitly anticipated a separate DayClose slice with its own gate so DC-R1 does not contradict it, and RPT-R2 cl. 9's no-anchor statement remains true of the Reporting slice because DC-R2 introduces `closedBusinessDay` **prospectively at close time**; the single scope interaction — **RPT-R1 clause 3** — is handled by **explicit recorded extension, never silent supersession**. **P-1, D-12 BLOCKED, D-16 OPEN, D-13, D-14 A-1, D-20, R-1(a) … R-6, KDS-R1 … KDS-R12, RPT-R2, RPT-R3 all preserved unchanged; D-2's branch-scoped RBAC defer remains IN FORCE with `FR-SEC-002/003/004` NOT IMPLEMENTED; CARRIED ITEM P1C-1 untouched; `FR-SEC-010` role seeding NOT authorized.** **Register diff = 401 insertions, 0 deletions, two contiguous additive hunks, zero `## D-` headings added** — nothing modified, superseded silently or deleted. All four prior DayClose design/correction reports preserved byte-unmodified; nothing staged or committed. **Day Close is GOVERNANCE-UNBLOCKED**; a separate explicitly authorised implementation task is still required |
| 2026-09-01 | Internal MVP current state — post-reporting / DayClose check (audit only) | [2026-09-01_INTERNAL-MVP-current-state.md](2026-09-01_INTERNAL-MVP-current-state.md) | **AUDIT ONLY — not implementation, no changes made.** HEAD unchanged at `7bc5d2c` (accepted baseline); working tree carries a substantial **uncommitted** DayClose implementation (13 modified + ~17 untracked files, incl. migration 35). DayClose classified **IMPLEMENTED — NEEDS ACCEPTANCE**: schema/migration/controller/service/queries/audit/events/permissions/cutover-fence all present and internally consistent with ratified DC-R1/R2/R3; zero dedicated tests; OpenAPI not regenerated; not committed. Receipt remains **BLOCKED BY P1C-1**, MVP-exit necessity still an open/unresolved user decision. D-2 (branch RBAC) confirmed **RATIFIED — core-only deferral**, non-blocking for Internal-MVP's single-branch carve-out. Read-only verification performed: `tsc --noEmit` clean (1 pre-existing unrelated error), `prisma validate` clean, `module-boundaries.spec.ts` 45/45, treasury+sales suites 83/83 — no regressions found. | `7bc5d2c` (unchanged) | `feat/production-spec` | 35 present in working tree, uncommitted (34 at HEAD) | module-boundaries 45/45; treasury+sales 83/83; zero DayClose-specific tests exist | **1 hard Internal-MVP blocker: DayClose acceptance** (test coverage + commit + formal acceptance pass — not further design/implementation). Next action selected: **RUN DAYCLOSE ACCEPTANCE**. Receipt's MVP-exit necessity remains an open decision, tracked separately, not counted as a second hard blocker |
| 2026-09-01 | DayClose — acceptance completion + verification (authorized test/verification/narrow-correction task) | [2026-09-01_DAYCLOSE-acceptance-completion.md](2026-09-01_DAYCLOSE-acceptance-completion.md) | **A. VERIFICATION COMPLETE — READY FOR FINAL ACCEPTANCE REVIEW.** No correctness defect found in DayClose service/controller/DTO/migration #35 after full code review against DC-R1/R2/R3 (no fix made — nothing was defective); migration #35 applied 35/35 from a genuinely disposable scratch DB; OpenAPI regenerated (289 additive insertions, both routes present, drift test 32/32); ESLint 100% clean on all DayClose production source (test files carry disclosed, unfixed cosmetic `res.body` typing debt, precedented). Not committed, not pushed. | `7bc5d2c` (unchanged) | `feat/production-spec` | 35 present in working tree, uncommitted (34 at HEAD); applied cleanly from zero on scratch DB | 49 new dedicated DayClose tests (35 e2e + 5 cutover-race e2e + 4 Z-number-concurrency e2e + 5 unit), all passing; full unit suite 797/797 (59 suites, +5 from baseline 792); full e2e suite run TWICE against the scratch DB, 63/63 suites, 1119/1119 tests both runs (baseline 1075 + 44 new e2e) — 4 legitimate pre-existing boundary-guard test expectations (table-list/route-list snapshots in `inventory.e2e-spec.ts`/`catalogue.e2e-spec.ts`/`cash-session.e2e-spec.ts`) narrowly corrected for migration 35's additive schema/routes, as explicitly authorized; no DayClose source/schema/migration touched to make them pass | Cutover race (Order-create vs DayClose, real + 2 deterministic orderings) proven safe on every run; Z-number concurrency (barrier at the existing `DAY_CLOSE_SALES_FACTS_QUERY` seam, no new lock) proven distinct/sequential with no duplicate rows/audit; immutability proven mechanically (`ros_app` UPDATE/DELETE rejected by Postgres on all 5 new tables); two prior-report corrections applied (Reporting exports = NOT IMPLEMENTED per FR-RPT-043/044, not "present"; Post-fire void = DEFERRED NON-BLOCKER, not "present"). Requirement classification: FR-FIN-020/021/023/024 COMPLETE, 022/026 PARTIAL, 025 NOT IMPLEMENTED [S]. D-2 and P1C-1 untouched. Receipt MVP-exit decision remains open, not decided here |
| 2026-09-01 | DayClose — final acceptance cleanup (narrow acceptance-correction: new-test lint debt + closed_business_day immutability proof) | [2026-09-01_DAYCLOSE-final-acceptance-cleanup.md](2026-09-01_DAYCLOSE-final-acceptance-cleanup.md) | **A. ACCEPTANCE CLEAN — READY FOR FINAL ACCEPTANCE.** Gap A: 76 errors/2 warnings in `test/day-close.e2e-spec.ts` fixed via typed local response interfaces sourced from the production `DayClosePostResult`/`DayCloseView` types (no rule weakened, no eslint-disable, no blanket `any` cast) — full DayClose changeset now 0 new errors/0 new warnings (1 pre-existing, out-of-diff `cash-session-close.service.ts:610` recorded separately, not fixed). Gap B: new dedicated e2e test proves `treasury.cash_sessions.closed_business_day` (and every other close fact) is unwritable via raw SQL once `status='closed'` — both raw UPDATE attempts as `ros_app` affected 0 rows, threw no error (standard RLS `USING`-clause behaviour of migration 34's pre-existing `cash_sessions_update` policy); persisted value verified byte-identical before/after. No DayClose production/schema/migration file changed — only `test/day-close.e2e-spec.ts` edited. Not committed, not pushed. | `7bc5d2c` (unchanged) | `feat/production-spec` | 35 present, uncommitted; applied 35/35 from zero on two fresh scratch DBs this task | Dedicated DayClose tests 50/50 (49 baseline + 1 new Gap B immutability test) — 45 e2e + 5 unit; full unit suite 797/797; full e2e suite run once, no exclusions, 63/63 suites, 1120/1120 tests (baseline 1119 + 1 new e2e); module-boundaries 45/45, zero KNOWN_DEVIATIONS growth; static checks (prisma validate/nest build/tsc/git diff --check) all clean, TSC wording corrected to "1 PRE-EXISTING ERROR, ZERO NEW ERRORS" | All prior-accepted DayClose results (activation, idempotency, strictly-past rule, FR-FIN-021, open-order blocker, cutover fence, DAY_CLOSE_STATE_QUERY, closed-business-day ownership, spanning session, zero-payment/movement-only variance, Z-number concurrency, immutability, historical GET, permissions, audit, event, rollback, OpenAPI, FR-FIN-022/026 PARTIAL, Reporting-export correction, post-fire-void correction) preserved, re-verified, not reopened |
| 2026-09-01 | DayClose — final acceptance closure (source-control commit) | [2026-09-01_DAYCLOSE-final-acceptance-closure.md](2026-09-01_DAYCLOSE-final-acceptance-closure.md) | **A. DAYCLOSE FINAL ACCEPTED — SOURCE-CONTROL CLOSED.** Externally accepted DayClose slice (implementation, migration #35, schema, tests, OpenAPI, DC-R1/DC-R2/DC-R3 governance recording, evidence-chain reports) committed in exactly one normal commit; no code/test/schema/migration/governance-semantics change made in this task. `INDEX.md` staged via exact-blob construction (`git hash-object` + `git update-index --cacheinfo`) to include only the 9 DayClose-related rows plus this one, never the 4 unrelated pre-existing dirty rows physically interleaved earlier in the table; `GOVERNANCE_DECISION_REGISTER.md` staged whole-file after confirming its diff is 401 insertions/0 deletions containing only DC-R1/DC-R2/DC-R3 (zero new `## D-` headings). Not pushed, not deployed. | `7bc5d2c` (parent) | `feat/production-spec` | 35, now committed | Carried forward from the accepted verification, not re-run in full: dedicated DayClose tests 50/50, full unit 797/797, full e2e 1120/1120 (63/63 suites), module-boundaries 45/45 with zero KNOWN_DEVIATIONS growth, ESLint 0 new errors/warnings, TSC 1 pre-existing/0 new. This task's own cheap post-stage checks: `git diff --cached --check` clean, `prisma validate` clean, module-boundaries re-run 45/45 | Requirement classification carried forward unchanged: FR-FIN-020/021/023/024 COMPLETE, FR-FIN-022/026 PARTIAL, FR-FIN-025 NOT IMPLEMENTED [S]. P1C-1 and D-2 untouched, not reopened. Internal-MVP exit is NOT claimed by this report. Remaining `git status` dirt after this commit is exactly the 4 explicitly-excluded unrelated historical reports and their corresponding INDEX rows |
| 2026-09-01 | Full API surface — OpenAPI/DTO/schema audit and correction (all 151 current HTTP endpoints) | [2026-09-01_FULL-API-openapi-schema-audit-and-correction.md](2026-09-01_FULL-API-openapi-schema-audit-and-correction.md) | **B. FULL API AUDIT CLEAN — DEFECTS FOUND AND FIXED.** Documentation-only: 9 real MISSING_SUCCESS_RESPONSE_SCHEMA defects (KDS ×6, DayClose ×2 incl. one undocumented ACTIVATED/CLOSED outcome, Reporting daily-trading ×1) plus a systemic path-parameter format gap (104 UUID/businessDay path params missing `format`, 2 `version` params typed `number` not `integer`, across virtually the whole API) found and fixed; zero runtime wire change, zero DB/migration/governance change. | `02fd05a` (unchanged) | `feat/production-spec` | 35 (unchanged — no migration touched; scratch DB `ros_scratch_openapi_audit` applied 35/35 from zero, dropped after use; persistent `ros` DB untouched) | Mechanical source-vs-OpenAPI route reconciliation 151/151 identical (zero mismatches); OpenAPI suite 36/36 (32 baseline + 4 new schema-completeness tests, each proven to fail against the pre-fix document with exactly the 9 real defects and pass after correction); full unit suite 797/797 (59 suites); module-boundaries 45/45 (zero `KNOWN_DEVIATIONS` growth); full e2e on the from-zero scratch DB 1124/1124, 63/63 suites (baseline 1120 + 4 new); `tsc`/`prisma validate`/`git diff --check`/ESLint on all 5 changed files all clean (1 pre-existing unrelated Identity `tsc` error, zero new) | Built the endpoint inventory mechanically from all 16 `*.controller.ts` files (151 routes) and cross-checked byte-for-byte against generated `docs/api/openapi.json` (151 operations, identical set). Found 9 endpoints (`GET`/`POST /branches/{branchId}/day-closes/{businessDay}`, all 6 `/kds/*` operator-lifecycle routes, `GET /reports/branches/{branchId}/daily-trading/{businessDay}`) whose `@ApiOkResponse` carried a `description` only, no `schema` — despite the description text naming rich structured fields the handler genuinely returns (verified against `TicketCardDto`/`StationQueueDto`/`DayCloseView`/`DayClosePostResult`/`DailyTradingReportView` and each service's real return type). Added 9 new inline response schema objects to `kitchen.controller.ts`/`day-close.controller.ts`/`reporting.controller.ts`, following the repository's own established inline-`schema:`-object convention (`schema-helpers.ts`'s `uuidSchema`/`moneyStringSchema`/`businessDaySchema`/`nullable`) rather than introducing a competing DTO-class pattern; represented DayClose's ACTIVATED/CLOSED outcome the same flattened-with-presence-notes way Treasury's own `declareCloseResponseSchema`/`finalizeCloseResponseSchema` already do, for internal consistency rather than a new `oneOf`. Separately found every UUID-shaped/`businessDay` path parameter across the entire API (`branchId`, `itemId`, `sessionId`, `ticketId`, etc. — 22 distinct names) was CLI-plugin-inferred as bare `type: string` with no `format`, while the identical field already carries `format: uuid`/`format: date` in every response body (443 pre-existing `uuidSchema()` uses) — fixed with one new document-level finalization pass (`enrichPathParameterSchemas` in `src/common/openapi/oas31.util.ts`, alongside the existing `nullable`-rewrite/`ErrorResponse`-fill passes), driven by an exhaustive manually-verified name list, not a blind heuristic. Verified a large set of scanner-flagged "untyped object" hits (localized-name/address/theme/capacityConfig JSON columns in Catalogue/Organisation/Inventory) were FALSE POSITIVES — the repository's existing, deliberate, already-documented (`description`-carrying) convention for genuinely opaque JSON columns — left unchanged. Verified all 8 scanner-flagged "missing request body" POSTs (`/auth/logout`, `/inventory/counts/{sessionId}/post`, 4 KDS action routes, `/orders/.../fire`, `/recipes/.../publish`) and all 12 `204` responses are genuinely bodyless (`Promise<void>`, no `@Body()`) by reading the controller source directly — documented as an explicit allowlist, now asserted mechanically by a new test. Extended `test/openapi.e2e-spec.ts` with 4 new tests deriving their check set from `doc.paths` itself (not a hardcoded route list): full 2xx-response schema-completeness sweep against the verified bodyless allowlist, allowlist cross-check, request-body schema-completeness sweep, and error-schema-completeness sweep — each demonstrated to fail against the restored pre-fix `docs/api/openapi.json` (exactly the 9 known defects) and pass after correction. Route set unchanged 151→151 (byte-identical sorted list, no unrelated route disappearance); `components.schemas` count unchanged 82→82 (all new schemas are inline, not componentized). Zero runtime wire change: no field renamed/added/removed, no status code changed, no nullability/optionality behaviour changed — confirmed by full unit (797/797) and full e2e (1124/1124) regression on a from-zero scratch DB. Zero DB schema, migration, or Governance Register change; all existing requirement classifications (DayClose FR-FIN-020/021/023/024 COMPLETE, 022/026 PARTIAL, 025 NOT IMPLEMENTED `[S]`; Reporting/KDS/D-2/P1C-1) preserved unchanged. Committed as `fix: complete API response schemas` |
| 2026-09-01 | Full API OpenAPI — final contract acceptance correction (DayClose oneOf union, path-param mapping helper, opaque-object test hardening) | [2026-09-01_FULL-API-openapi-final-acceptance-correction.md](2026-09-01_FULL-API-openapi-final-acceptance-correction.md) | **A. FULL API CONTRACT ACCEPTANCE CLEAN — READY TO PUSH.** Narrow follow-up to the prior full audit: replaced DayClose POST's flattened 200 schema with a real `oneOf` union (ACTIVATED/CLOSED, each `additionalProperties:false` + `outcome` `const`, structurally rejecting every hybrid/unknown-outcome combination); exported a single shared `classifyPathParamName` helper from `oas31.util.ts` so the path-parameter format post-processor and its new exhaustive test can never drift into two lists; narrowed the schema-completeness sweep's opaque-object exemption to eliminate a top-level "any object with a description passes" loophole (zero real top-level opaque responses exist, now mechanically enforced via an explicit, currently-empty allowlist). Zero runtime wire change, zero DB/migration/governance change. | `803aa3d` (unchanged) | `feat/production-spec` | 35 (unchanged — no migration touched; scratch DB `ros_scratch_final_correction` applied 35/35 from zero twice this task, dropped after use; persistent `ros` DB untouched) | OpenAPI suite 46/46 (36 baseline + 10 new: 8 DayClose-union tests proven to fail against the restored pre-correction flattened schema and pass after correction, 2 exhaustive path-parameter format/type tests deriving their check set from `doc.paths` globally); full unit 797/797 (59 suites); module-boundaries 45/45 (zero `KNOWN_DEVIATIONS` growth); full e2e on a from-zero scratch DB 1134/1134, 63/63 suites (baseline 1124 + 10 new) — one parallel-worker-only flake (`order-completion-rls.e2e-spec.ts`, `42501` on `order_number_blocks`) investigated (grants verified correct, isolated re-run 21/21 clean) and resolved by a serial (`--runInBand`) full re-run on a fresh scratch DB, not by any code change; `tsc`/`prisma validate`/`git diff --check`/ESLint on all 3 changed files all clean (1 pre-existing unrelated Identity `tsc` error, zero new) | Confirmed `DayClosePostResult` in `day-close.service.ts` is a real two-branch TS discriminated union, not one object with optional fields; built `dayCloseActivatedResultSchema`/`dayCloseClosedResultSchema` (5 vs 6 required fields, `outcome: {const:'ACTIVATED'|'CLOSED'}`, `additionalProperties:false` on both) and replaced the flattened schema with `{oneOf:[...]}` — no controller/service output changed. Added 8 new tests: oneOf-with-2-concrete-variants, each variant's required/const/additionalProperties, and a small purpose-built structural validator (deliberately not the undeclared-transitive `ajv` sitting under `@seriousme/openapi-schema-validator` in `node_modules`) proving exactly the named valid ACTIVATED/CLOSED payloads match and the named invalid hybrid/unknown-outcome payloads match zero branches. Verified OpenAPI 3.1 meta-schema validation and full `$ref` dereferencing both still pass with `oneOf`/`const` present. Refactored `enrichPathParameterSchemas` to call a newly-exported pure `classifyPathParamName` instead of inlining its three name checks, and imported that same function into the test file for a new exhaustive path-parameter test (placeholder↔parameter 1:1 match + non-optional; uuid/businessDay/version format/type correctness) that derives from `doc.paths` globally rather than hardcoding the 106 previously-affected instances. Removed `isUnderspecifiedObject`'s `{type:'object', description:...}` exemption entirely for its only call sites (top-level response/request schemas — it was never recursed into nested properties, so the repository's real nested opaque-JSON convention in Catalogue/Organisation/Inventory/Kitchen is untouched and not re-flagged); added a currently-empty `TOP_LEVEL_OPAQUE_ALLOWLIST` for the narrow case, preserving the audited zero-top-level-opaque-response invariant mechanically. Route count reconfirmed 151→151 identical, source/OpenAPI mismatch 0, generation determinism reverified (byte-identical across two consecutive runs). Requirement classifications (DayClose FR-FIN-020/021/023/024 COMPLETE, 022/026 PARTIAL, 025 NOT IMPLEMENTED `[S]`; Reporting/KDS/D-2/P1C-1) preserved unchanged. Committed as `fix: tighten OpenAPI response contracts` |
| 2026-09-01 | INTERNAL-MVP RECEIPT — narrow non-fiscal receipt design + governance gate | [2026-09-01_INTERNAL-MVP-receipt-narrow-design-gate.md](2026-09-01_INTERNAL-MVP-receipt-narrow-design-gate.md) | **A. RECEIPT DESIGN ACCEPTED-READY — IMPLEMENTATION CAN START.** Analysis/design/governance gate only; no source, schema, migration, route, permission, governance edit, OpenAPI regeneration, test execution, commit or push | `1cc9ace` (verified; parent chain `803aa3d`→`02fd05a`→`7bc5d2c`→`38e007b`→`121b889` re-verified) | `feat/production-spec` | 35 (unchanged — **MIGRATION REQUIRED: NO**, re-argued from source, not preserved) | **No suite executed.** Test files cited as structural evidence only; the prompt's baseline (unit 797, boundaries 45, e2e 1134/63, migrations 35) is restated as the user's declared baseline, not as results produced here. Two figures independently re-counted this session: `prisma/migrations/` = **35**, `module-boundaries.spec.ts` `it(` = **45**; `docs/api/openapi.json` = 111 paths / **151 operations** | Made the user's ratified **non-fiscal itemized Internal-MVP receipt** implementation-ready as the smallest safe slice, and **proved every §27 stop condition NO**. Extracted the SRS (`pdftotext -layout`, 6,510 lines) and established that **§8.8 lines 2383–2399 is the complete receipt requirement set** (FR-LOC-022 read in full and **excluded** — it governs pack *signing*, not receipt content, correcting the readiness audit's pairing). **HISTORICAL STABILITY PROVEN, not asserted:** `order_lines.item_name_snapshot` (item **and** variant name maps, `order-lines.service.ts:320–324`) + `order_line_modifiers.name_snapshot` (L361) are BR-POS-004 `JSONB` snapshots with **no rewrite path**; all money is `BigInt` columns on the line row; tax is the **stored computed amount**, never a re-applied rate, doubly protected by the FR-LOC-021 per-order pack pin whose registry **refuses to re-register a version** (`country-pack.registry.ts:111–116`); `completed` has **no outbound transition** (`order-state.ts:87`). Established the **exact-`BigInt` Σ-lines ≡ totals invariant** by copying `recomputeOrderTotals`'s own `state NOT IN ('voided','comped')` filter verbatim — including `comped`, which no code writes today, so the invariant survives a future comp implementation instead of silently breaking. **ZERO cross-module dependencies:** all four source tables (`orders`, `order_lines`, `order_line_modifiers`, `order_payments`) are `@@schema("sales")` and **Sales-owned** — notably `OrderPayment` is **not** a Treasury table, so no boundary is crossed at read time. **Designed, costed and then REJECTED a per-taxClass breakdown** (technically free — `LocalisationModule` already exports `TAX_CLASS_LABELS_QUERY`, `SalesModule` already imports it, zero new `KNOWN_DEVIATIONS`) because FR-POS-100 names *"tax breakdown"* as a **country-pack-mandated fiscal element**, which is out of the ratified boundary — dropping it makes the slice wholly Sales-internal and keeps that element honestly NOT IMPLEMENTED. Also **rejected reading the pinned country pack** for tax-inclusive/exclusive mode: `requirePinned` needs `branch.countryCode`, obtainable only by repeating `order-lines.service.ts:212`'s pre-existing `org.branches` **table-ownership violation** on a read path, and `country_code` is **mutable**, so a read-time resolve would reintroduce the very drift §H exists to prevent — replaced with a **pure derivation from the frozen order row alone** (`taxPresentation`: INCLUSIVE/EXCLUSIVE/NOT_APPLICABLE/UNDETERMINED), required because **FR-FIN-031 [M]** supports both modes and a client given subtotal+tax+total with no discriminator would **print a double-counted total under inclusive pricing**. Contract: `GET /orders/{businessDay}/{id}/receipt`, GET-only (no POST justified), `pos.order.create` **reused** (grants zero new visibility — `GET /orders/{bd}/{id}` already returns a superset), **`pos.reprint.receipt` deliberately NOT adopted and reserved for FR-POS-104's duplicate-marking+logging**; `completed`-only, everything else **422** via `ReceiptNotAvailableError extends OrderStateError` (zero filter changes, the `fire.errors.ts` precedent) per the filter's own documented 409-vs-422 rule; cross-tenant **404 never 403** under RLS; **no `generatedAt`** so two GETs are **byte-identical** and reprint determinism becomes strict deep equality; **no ETag**; three-layer non-fiscal marker (`documentType` single-value enum + `fiscal:false` + `disclosureKey`) with **no `invoiceNumber`/`taxRegistrationNumber`/`qrCode`/`fiscalUuid` field existing to populate**, and `orderNumber`'s OpenAPI description stating it is **not** an invoice sequence; COGS (`unitCostSnapshot`/`postedCogsTotal`), merchant refs (`authorizationCode`/`paymentTerminalTxnRef`) and **branch name/logo** all excluded (the last as mutable master data **and** an FR-POS-101 template concern). **Verified the new path trips none of the 10 route-surface/forbidden-pattern assertions** at `openapi.e2e-spec.ts:275`, and that **nine existing assertions already auto-cover** the endpoint; recommends extending the existing `it` (suite stays **46**, not 47) to keep the "exactly one exact route" discipline. **Found 2 real PRE-EXISTING contract defects that survived the 2026-09-01 full-API audit** — `countryPackVersion` documented `integer` but emitted as a **string** (`VarChar(24)`), and `lines[].priceRule` documented `object` but emitted as **`string\|null`** (`VarChar(160)`) — **reported, not fixed** (out of scope), with the receipt schema designed not to repeat them. **NEW TABLES: NONE · NEW MIGRATIONS: NONE · NEW DOMAIN EVENTS: NONE · NEW AUDIT ACTIONS: NONE · NEW PERMISSIONS: NONE**; `AuditService` deliberately **not injected** so no future edit can add an audit write invisibly. 16-scenario e2e matrix (A–P, adding **P: no-DB-mutation-on-GET** for §19 cl.14) with unsupported capabilities (refund, post-fire void, comp, non-zero discount, tips, printing) **excluded rather than faked**. **P1C-1 checked exclusion-by-exclusion** (`register:5172–5173`): the projection creates **no tax document, no invoice template, no fiscal submission and no `fiscal.tax_rules` table** — so it is **not reopened, not amended, not narrowed**, **remains a blocker to the full fiscal receipt**, and is **NOT globally closed**. **FR-POS-100 [M] moves NOT IMPLEMENTED → PARTIAL and nothing else moves**; FR-POS-101/102/103/104/105/106 **all unchanged as NOT IMPLEMENTED**; **no requirement reaches COMPLETE**. Governance: **exactly one entry, RCPT-R1** (unnumbered, `DC-R`/`RPT-R`/`KDS-R` convention, collision-checked) — ceremonial entries for ownership/no-persistence explicitly declined as ordinary engineering. Receipt is stated to be **the final known feature slice before the Internal-MVP Exit Gate**, with Internal MVP explicitly **NOT** declared complete |
| 2026-09-01 | INTERNAL-MVP RECEIPT — implementation + final acceptance candidate | [2026-09-01_INTERNAL-MVP-receipt-implementation-and-acceptance.md](2026-09-01_INTERNAL-MVP-receipt-implementation-and-acceptance.md) | **A. RECEIPT ACCEPTANCE CLEAN — INTERNAL MVP READY FOR EXIT GATE.** Implementation of the already-accepted narrow design; commit follows this report | `1cc9ace` (baseline; commit follows) | `feat/production-spec` | 35 (unchanged — verified twice: `prisma migrate status` against dev DB, and a from-zero `prisma migrate deploy` of all 35 against a disposable scratch DB; no migration 36) | Unit **815/815** (60 suites, +18 new `receipt.views.spec.ts` cases); module boundaries **45/45** (zero `KNOWN_DEVIATIONS` growth); OpenAPI **49/49** (46 baseline + 3 new dedicated adjacent-contract assertions, existing route-surface `it` extended not duplicated); dedicated Receipt e2e **16/16** (A–P); full e2e against a **fresh, disposable scratch DB** (`ros_scratch_test`, dropped after use, dev DB never touched for the authoritative run) **1153/1153, 64/64 suites, Run 1** (default parallel workers) **and Run 3** (freshly recreated DB, `--runInBand`, fully sequential) **both 100%**; an intermediate **Run 2** (same DB reused, data-accumulated) showed **1 pre-existing failure** in `organisation.e2e-spec.ts`'s global whole-DB invariant check — investigated per instruction rather than dismissed, confirmed identical to a failure already observed against the persistent dev DB **before any Receipt code existed** and documented in a prior accepted report as dirty-DB/cross-suite-isolation debt, **not** caused by or touching any Receipt file (`git status` confirms `organisation.e2e-spec.ts` untouched this session) | Implemented exactly design-gate `2026-09-01_INTERNAL-MVP-receipt-narrow-design-gate.md` (verdict A), no new design invented. New: `receipt.errors.ts` (`ReceiptNotAvailableError extends OrderStateError`, zero filter changes), `receipt.views.ts` (pure `deriveTaxPresentation` + `toReceiptView`), `receipt.views.spec.ts` (18 unit tests), `receipt.service.ts` (`ReceiptService` — 3 reads inside `withAuthContext`, `AuditService` deliberately **not injected**), `receipt.openapi.ts` (fully typed schema), `test/receipt.e2e-spec.ts` (16 scenarios). Modified: `orders.controller.ts` (new `GET :businessDay/:id/receipt` handler + 3 authorized adjacent OpenAPI corrections), `sales.module.ts` (+`ReceiptService` provider, not exported), `test/openapi.e2e-spec.ts` (extended route-surface `it` with exact-one-route `receiptMatches` + 2 new forbidden patterns `receipts?/(email|print|reprint)` and `fiscal[-_]?receipts?`, +3 new dedicated `it`s for the adjacent corrections). **The three §2-authorized adjacent Order contract corrections, source-reverified before fixing:** `orderSchema.countryPackVersion` `integer`→**`string`** (`VarChar(24)`, verbatim from `toOrderView`); `orderLineSchema.priceRule` opaque `object`→**nullable string** (`VarChar(160)`, verbatim from `toOrderLineView`); `orderLineSchema.taxClassId` nullable→**non-nullable uuid** (`NOT NULL`, D-09) — **zero runtime wire change**, each proven by a dedicated new OpenAPI assertion, not merely inspected once. Route/paths verified in the twice-regenerated (byte-identical) OpenAPI doc: **111→112 paths, 151→152 operations, 0 mismatches**. **Found and fixed the one real regression the new route caused**: `test/sales.e2e-spec.ts` carries its own independent `/orders` route-surface exact-list assertion (separate file, separate test, from the one extended in `openapi.e2e-spec.ts`) that needed the receipt route added — fixed by adding the route to its expected array and extending its doc comment to name RCPT-R1, not weakened. **Historical stability proven live, not just analyzed**: e2e test G completes an order with a modifier, reads the receipt, renames the MenuItem/Variant/Modifier via direct admin writes (independently confirming the rename actually took effect, so the test isn't vacuously passing), re-reads, and asserts the `itemNameSnapshot`/`nameSnapshot` are byte-for-byte unchanged. Test E proves the **exact-BigInt Σ-lines ≡ totals invariant** live over a modifier-bearing line. Test P proves **zero DB mutation** across 3 GETs via `orders.version`/`updated_at`/tenant `audit_entries` count, all unchanged. **Recorded RCPT-R1** in `docs/governance/GOVERNANCE_DECISION_REGISTER.md`, inserted at the established pre-`## Final Decision Matrix` slot every prior named ratification (Fire, P1F-2, FIFO, Approval Runtime, P1G-1, R-6, KDS MVP, Minimum Reporting, Day Close) has used — **append-only, verified `git diff --numstat` = 87 insertions, 0 deletions** — collision-checked against every existing `-R` series; no new `D-*` decision, 20-decision tally unchanged; **FR-POS-100 → PARTIAL** (TRN/invoice-sequence/tax-breakdown/QR/country-pack-element-set/print all still named NOT IMPLEMENTED within it), **FR-POS-101…106 unchanged NOT IMPLEMENTED**, **P1C-1 unchanged globally / still blocks the full fiscal receipt / NOT globally closed**, **D-2 not reopened**. **Zero** new tables/migrations/domain events/audit actions/permissions/cross-module dependencies (module boundaries confirmed **45/45**, zero `KNOWN_DEVIATIONS` growth). Static gates: `prisma validate` clean (schema untouched), `nest build` clean, `tsc --noEmit` **1 PRE-EXISTING ERROR, ZERO NEW ERRORS**, `git diff --check` clean, ESLint **0/0** on every one of the 10 files this task touched or created (48 pre-existing issues elsewhere confirmed via `git status` to be in files never touched this session). Verdict: **A. RECEIPT ACCEPTANCE CLEAN — INTERNAL MVP READY FOR EXIT GATE.** Internal MVP explicitly **NOT** declared complete; commit authorized and follows |
| 2026-09-01 | Internal MVP — final exit gate (independent audit) | [2026-09-01_INTERNAL-MVP-final-exit-gate.md](2026-09-01_INTERNAL-MVP-final-exit-gate.md) | **A. INTERNAL MVP COMPLETE** — controlled, online, single-active-branch posture, non-fiscal receipt; explicitly NOT production-ready, NOT full-SRS-complete, NOT pilot-ready until hardening blockers addressed. Independent audit only — no implementation, redesign, migration, source/test/governance change, commit, push, or deploy | `ec616a0` (verified; parent `1cc9ace` re-verified; full 10-commit accepted chain re-verified via `git log`) | `feat/production-spec` | 35 (unchanged — verified from zero **three separate times** this session on disposable scratch DBs, always 35/35, never 36) | **Every baseline figure independently re-executed live, not copied from prior reports.** Full unit **815/815** (60 suites); module boundaries **45/45** (`KNOWN_DEVIATIONS` unchanged, 21 entries); Receipt dedicated **16/16 e2e (A–P) + 18/18 unit**; OpenAPI **49/49**; `prisma validate` clean; `nest build` clean; `tsc --noEmit` **1 PRE-EXISTING ERROR, ZERO NEW** (explicitly not called clean); `git diff --check` clean. **Full e2e run three times on genuinely fresh, disposable scratch DBs** (never the persistent `ros` dev DB): Run A (parallel workers) hit **100 failures across 4 suites** — investigated, found 15 stale DB sessions + leftover jest-worker processes still attached post-run, confirming resource contention (the same failure *class*, not instance, the accepted Full-API-audit report already documented and resolved identically); killed the stale processes. Run B (fresh DB #2, `--runInBand` serial) — **1 failure**: `organisation.e2e-spec.ts` *"leaves no org location entity without a registry row"*, a global whole-database invariant vulnerable to other suites' raw-admin fixture accumulation; **verified 62/62 clean in total isolation** on the same DB. Run C (fresh DB #3, `--runInBand` serial) — **1153/1153, 64/64 suites, 100%**, including that exact test. **Classified A — KNOWN NON-BLOCKING TEST-ISOLATION DEBT**, not a correctness blocker: non-deterministic, ordering-dependent, proven correct alone, confirmed via `git status`/`git diff` that Receipt (or any commit in this chain since `121b889`) never touches `organisation.e2e-spec.ts` or anything it depends on; explicitly recorded as unresolved harness debt, not swept aside. **Built and independently verified the 34-capability Internal-MVP matrix** (tenant/auth through OpenAPI completeness) — **34/34 COMPLETE, zero PARTIAL, zero BLOCKED** — by direct route-decorator grep against live source (`orders.controller.ts` 8 routes incl. the new receipt route at its expected position; `kitchen.controller.ts` exactly 6 KDS routes; `treasury.controller.ts` 7 CashSession routes + `day-close.controller.ts` 2 + `cash-close-policy.controller.ts` 1), not inferred from file existence. **Traced the full happy-path chain edge-by-edge** (tenant/branch/terminal → PIN → CashSession open → order → priced/taxed lines → fire → KDS queue → kitchen lifecycle → payment → completed → inventory/COGS → **non-fiscal receipt GET** → CashSession close → daily report → DayClose → historical DayClose GET) and confirmed each adjacent pair is linked by a real accepted e2e suite exercising the *same* order/session identity across the transition, not two isolated tests assumed to compose; one honest seam noted (Receipt's own fixtures build CashSession via raw-admin insert matching `sales-payment.e2e-spec.ts`'s own precedent — the real `POST /cash-sessions` route is independently proven by `cash-session.e2e-spec.ts`, not re-tested by Receipt). **Receipt final acceptance independently re-verified against source** (not merely cited): `GET /orders/{businessDay}/{id}/receipt` confirmed present at its exact line; Sales-owned confirmed by zero cross-module imports in `receipt.service.ts`; `pos.order.create` reuse confirmed, no new permission; non-fiscal discriminator, no persistence/migration/audit-write/domain-event all re-confirmed; historical-snapshot stability (test G), cross-tenant 404 (test L), deterministic repeat-GET (test H) all re-run live this session. **DayClose FR-FIN-020/021/023/024 COMPLETE, 022/026 PARTIAL, 025 NOT IMPLEMENTED [S]** cross-checked identical between the DayClose closure report and DC-R1's own register text — zero drift found. **Reporting**: `FR-RPT-004` COMPLETE; independently grepped the Reporting module to confirm **zero** CSV/XLSX/PDF export code exists and tax-by-rate is genuinely absent, rather than trusting the prior report's negative claim. **KDS**: 6-route surface and KDS-R11/KDS-R12 reconfirmed unaltered since 2026-08-30. **Auth/tenancy**: quoted D-2's exact ratified text (core-only, 2026-08-19 amendment scope) and independently read the single-active-branch fail-closed source in both `daily-trading-report.service.ts` and `day-close.service.ts` (identical guard pattern, identical throw message), then confirmed Sales order routes deliberately carry no such check and explained why that is consistent rather than a gap (terminal-bound branch has no cross-branch ambiguity to guard). **NFR-PERF-006**: re-cited the exact measured figure (p95=2120ms vs 200ms target, `2026-08-26_P1F2_order-completion.md` §C) and its root cause, and applied the task's own three-way test explicitly: does NOT block Internal-MVP feature completion, DOES block pilot readiness (multi-terminal lock contention invisible to single-terminal testing), trivially blocks full-SRS completion — all three classified separately, none conflated. **26-item deferred/non-blocking list** built with SRS status, why-not-a-blocker, and when-it-becomes-blocking for every category the task named, zero silent omissions. All 16 exit criteria (§18) individually checked and passed; all 9 forbidden full-SRS-completeness claims (§19) explicitly confirmed NOT made. Ranked 6-item pilot/production-hardening punch list for the next phase, with zero implementation begun |
