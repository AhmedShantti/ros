# SRS Completion Audit — POS Order Capture / Tables / Discounts-Comps-Voids / Payments / Refunds / Receipts

**Report type:** Backend-only SRS completion audit section (read-only investigation; no files modified anywhere).
**Authority statement:** This report is non-authoritative evidence. The SRS (`ROS_SRS_v1.0.pdf`) and the ratified entries in `docs/governance/GOVERNANCE_DECISION_REGISTER.md` remain the sole authority for requirements, permission semantics, and architecture decisions. Nothing in this document creates, amends, or ratifies governance. Every claim is traced to an exact file:line read in this session, or to a named prior report re-verified against current code.
**Date:** 2026-09-07
**HEAD (lane-d, primary/canonical):** `bf91d6671a1c39fd0b3c0e8b4ba6c66aa8132ed8`, branch `full-srs/lane-d4-reporting-demo`
**HEAD (lane-a, secondary):** `0ca3c4bcbe465420e78a16eac1802e83f27ff293`, branch `full-srs/lane-a3-pos-financial-corrections`
**Working tree summary (lane-d) at task start:** clean except 4 pre-existing untracked report files under `docs/reports/claude/` (not produced by this task; unrelated to this session's scope). Working tree summary (lane-a): clean.
**Task identifier:** Full-SRS backend completion audit — POS/SALES domain section (order capture, tables, discounts/comps/voids/corrections, payments, refunds, receipts).

---

## 0. CRITICAL PREMISE CORRECTION — read this before the rest of the report

The task brief that scoped this section stated lane-d "does NOT have: discounts, comps, post-fire void, or the append-only refunds/reporting-reconciliation work (verified: no `src/modules/sales/contract` or `src/modules/sales/orders` directory)" and that lane-a's POS-FIN-1 commits (`2bf0825`, `1783a6c`, `71b16f7`, `edf74ed`, `d4baad5`) "do not exist in lane-d."

**This is no longer true as of lane-d HEAD `bf91d66`.** Direct verification this session:

- `find kitchen-kit/backend/src/modules/sales -type f` in lane-d returns `discounts.service.ts`, `post-fire-void.service.ts`, `refunds.service.ts`, `sales.permissions.ts` (with `DISCOUNT_APPLY`/`DISCOUNT_APPROVE`/`DISCOUNT_UNLIMITED`/`COMP_APPLY`/`ORDER_VOID_LINE_POSTFIRE`/`REFUND_ISSUE`/`REFUND_DIFFERENT_TENDER`), etc. — the full POS-FIN-1 file set, byte-for-byte present.
- `diff -rq` of `src/modules/sales/`, `src/modules/kitchen/`, `prisma/migrations/` and `test/pos-financial-corrections.e2e-spec.ts` between lane-d and lane-a shows the POS-FIN-1 content is **byte-identical** between the two worktrees (the only diffs found are lane-d-only additions from later, unrelated work — SIGNUP-1 permission re-exports, HR-1 audit constants, an RPT-DEMO-1 KDS summary query — plus a whitespace-only `prisma format` realignment).
- `git log --oneline --follow -- src/modules/sales/orders/discounts.service.ts` in lane-d shows the file was introduced by commit `1c6402c` ("feat(sales): add governed discounts, comps, post-fire void and Kitchen signal (POS-FIN-1)"), a **different SHA from lane-a's `2bf0825`** because it was cherry-picked onto a different base — but the diffed content of the two commits (via `git show <sha>:<path>`) is identical.
- A prior report already documents this exact integration, done in a **third worktree** (`/Users/mac/projects/ros-worktrees/integration`, branch `full-srs/4day-integration`) and subsequently carried into lane-d: `docs/reports/claude/full-srs-4day/2026-09-04_MW1I_ci-dep-pos-hr1-integration.md`. It cherry-picked all 20 commits from `full-srs/lane-a3-pos-financial-corrections@0ca3c4b` (POS-FIN-1, 8 commits) and `full-srs/lane-b2-workforce-core@b8ac578` (HR-1, 3 commits) plus 6 CI/security and 2 dependency-remediation commits from lane-g3, onto integration-branch HEAD `1149be4`, resolving one real conflict (union of disjoint `AUDIT_ACTION`/`AUDIT_ENTITY` blocks in `governance/audit/audit.constants.ts`) and re-verifying with a from-zero Postgres migration run (44/44 migrations), generated RLS gates (FR-PLT-013/014, both PASS), 1150/1150 unit tests, and targeted POS-FIN-1/HR-1 e2e suites (44/44 and 35/35 respectively). Lane-d's own commit chain (`1c6402c` through `dc2fd41`, then continuing through workforce/reporting/registration work to `bf91d66`) is exactly this integration branch's continuation, confirmed by identical commit-message sequence and identical file content.

**Consequence for this report:** the INTEGRATION_GAP the task brief anticipated for this domain **does not currently exist** — lane-d (canonical) already contains the full POS-FIN-1 feature set, and lane-a is now a strict subset of lane-d for every file this report's scope touches (lane-a lacks lane-d's later SIGNUP-1/HR-1/RPT-DEMO-1/multi-tenant-hardening work, none of which is in this report's scope). The dedicated INTEGRATION_GAP section below is retained per the task's output-format instruction, but it reports "gap closed" with the reconciling evidence, not an open gap. All classifications below are therefore assessed directly against lane-d as deployed-as-is, which is also valid for lane-a for the shared subset (noted per-requirement only where lane-a genuinely diverges, which for this domain is nowhere found).

A second, independent piece of evidence for the same conclusion: two prior lane-d reports **already audited this exact domain 1 and 0 days before this session** and found concrete gaps, one of which was fixed same-day, this session:
- `docs/reports/claude/2026-09-06_GOLDEN-PATH-AUDIT_pos-create-fire-payment-receipt-refund.md` (HEAD `3e4f85c`) — found `pos.refund.issue` on **no** canonical role.
- `docs/reports/claude/2026-09-07_GOLDEN-PATH-BACKEND-CLOSURE_kds-refund-cashclosepolicy.md` (commit `bf91d66`, same-day) — added `pos.refund.issue` to the Cashier canonical role template, closing that specific gap. Verified directly this session: `src/modules/identity/authz/canonical-role-templates.ts:69-80` now lists `SALES_PERMISSIONS.REFUND_ISSUE` in `CASHIER_PERMISSION_CODES`.

This report treats those two as evidence (per the task's own priority-6 instruction) and independently re-verified their claims against current code rather than repeating their trace work verbatim.

---

## 1. POS Order Capture

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| FR-POS-001 | 8.2.1 | Order types: dine-in, takeaway, delivery, drive-through, pickup, aggregator-injected | COMPLETE_BACKEND | both (identical) | `OrderType` enum + `orders.service.ts:43,285` (`tableId`), `POST /orders` `orders.controller.ts:426-482`, `sales.dto.ts` `CreateOrderDto`. `pos.order.create` guard (`orders.controller.ts:430`, `sales.permissions.ts:31`). Test: `test/sales.e2e-spec.ts` (57 `it`s). | P3_HARDENING |
| FR-POS-002 | 8.2.1 | `<branch_code>-<business_day_seq>` order numbering, locally-issued block, 80%-consumed reissue, offline fallback `<terminal_code>-<local_seq>` | PARTIAL_BACKEND | both | `order-number.ts` implements the format and generation (`order-number.spec.ts` unit-tested). Block issuance/80%-threshold/offline terminal fallback semantics were **not located** in this session's read of `order-number.ts`/`orders.service.ts` — the SRS's own offline-first architecture (Ch. 21) is out of this report's scope to verify end-to-end, but the online-server-side numbering path is present and uniqueness is DB-enforced (`@@unique([branchId, businessDay, orderNumber])`, `schema.prisma:2080`). NEEDS_VERIFICATION against Ch. 21's sync/offline-terminal-block machinery, out of this section's domain. | P2_COMPLETENESS |
| FR-POS-003 | 8.2.1 | Dine-in requires table before firing | COMPLETE_BACKEND | both | `order-state.ts:206-223` (`assertMayFire` calls the table-assignment check with the literal FR-POS-003 message). Enforced at Fire (`sales-fire.service.ts`), not merely documented. Test: `test/sales-fire.e2e-spec.ts` (35 `it`s). Frontend defect (default `orderType: "dine_in"` with no table picker) is OUT OF SCOPE (frontend), documented separately in `2026-09-06_GOLDEN-PATH-AUDIT_pos-create-fire-payment-receipt-refund.md` step 16 — backend enforcement itself is correct. | P3_HARDENING |
| FR-POS-004 | 8.2.1 | Seat-number assignment on dine-in lines | PARTIAL_BACKEND | both | `OrderLine.seatNumber Int?` exists (`schema.prisma:2117`), but no seat-level SPLIT/service logic (bill-splitting by seat, FR-POS-062) was found — see FR-POS-062 below. Data model present, consuming feature absent. | P2_COMPLETENESS |
| FR-POS-005 | 8.2.1 | Multiple concurrently-open orders per terminal, switch without data loss | COMPLETE_BACKEND (server-side) | both | `GET /orders` list route (`orders.controller.ts:484`) is cursor-paginated and branch/terminal-scoped; each `Order` is an independent row (`draft`/`open` states persist server-side), so switching is a client-side navigation concern, not a server invariant that could be violated. NOT_BACKEND_OWNED for the "no data loss on switch" client guarantee itself. | P3_HARDENING |
| FR-POS-006 | 8.2.1 | Park and resume by same/another authorised user/terminal | NEEDS_VERIFICATION | both | No explicit "park" state/endpoint was found distinct from an ordinary open `draft`/`open` order that simply has no further activity; `OrderState` enum was not fully enumerated this session. Resume-by-another-terminal requires only that the order is tenant/branch-visible, which the existing read routes already provide. Flagged for a dedicated re-check against the full `OrderState` enum and any "park" verb. | P2_COMPLETENESS |
| FR-POS-007 | 8.2.1 | Record `opened_by`, `served_by`, `closed_by` | PARTIAL_BACKEND | both | `Order.openedBy` (`schema.prisma:2037`, required) and `Order.servedBy`/`Order.closedBy` (`:2038-2039`, both nullable) exist in the schema. `openedBy` is populated at creation (`orders.service.ts`). Write paths for `servedBy`/`closedBy` were not located in the files read this session (no explicit "serve" or "close" actor-stamping call site found in `orders.service.ts`/`sales-payment.service.ts`) — needs a dedicated grep across the module for `servedBy:`/`closedBy:` write sites before upgrading to COMPLETE. | P2_COMPLETENESS |
| Add line | 8.2 | `POST /orders/:businessDay/:id/lines` | COMPLETE_BACKEND | both | `orders.controller.ts:649-726` → `OrderLinesService.addLine` (`order-lines.service.ts:163-328`). Price resolution (FR-POS-040), modifier resolution, tax computation, variant handling all inline. Idempotent (`@Idempotent()`), If-Match/ETag CAS. Test: `test/sales-lines.e2e-spec.ts` (37 `it`s). | P3_HARDENING |
| Remove line (pre-fire) | 8.2 | `DELETE /orders/:businessDay/:id/lines/:lineId` | COMPLETE_BACKEND | both | `orders.controller.ts:935` → `OrderLinesService.voidLinePreFire` (`order-lines.service.ts:519-632`), reason-code required (FR-POS-013 constraint cross-reference in-file), `ck_order_line_void_reason` DB CHECK. | P3_HARDENING |
| Remove/void line (post-fire) | 8.5 (FR-POS-070/071) | `POST /orders/:businessDay/:id/lines/:lineId/void-postfire` | See §4 below (Voids/Corrections) | both | — | — |
| Amend line (change quantity in place) | 8.2 | No dedicated "amend quantity" endpoint | MISSING_BACKEND | both | No `PATCH`/update-quantity route exists on `orders.controller.ts` (`@Patch` grep returns zero hits in this controller) and `order-lines.service.ts` exposes only `addLine`/`voidLinePreFire` — no `updateLine`/`amendLine` method. The FR-POS-070 table's own "Line void (pre-fire)" + a fresh `addLine` is the only implemented path to change a pre-fire line's quantity/selection; this is a workable pattern but is NOT an explicit "amend" primitive as the master-audit-spec's "add/remove/amend lines" phrase implies. Recorded as a genuine gap, not assumed equivalent. | P2_COMPLETENESS |
| Fire | 8.2.5 (FR-POS-035/038) | `POST /orders/:businessDay/:id/fire` | COMPLETE_BACKEND | both | `orders.controller.ts:741-808` → `SalesFireService.fire` (`sales-fire.service.ts:92-376`), one `UnitOfWork.execute` transaction covering routing resolution + every Kitchen write (verified atomic end-to-end same-day by `2026-09-07_GOLDEN-PATH-BACKEND-CLOSURE_kds-refund-cashclosepolicy.md`, re-verified this session by reading `sales-fire.service.ts:61-100`'s own docblock and the `RoutingNoDestinationError` rollback-then-422 mapping). Amendment-ticket distinction (FR-POS-038, "clearly marked as an addition, not a reprint") confirmed via Kitchen's per-line write-once tracking: `kds-operations.service.ts:94-168` and `ticket-projection.service.ts:50-98` explicitly handle "amendment line" reactivation as a distinct code path from a fresh ticket. Idempotent, If-Match required. Test: `test/sales-fire.e2e-spec.ts`, `test/sales-fire-concurrency.e2e-spec.ts`. | P3_HARDENING |
| FR-POS-035 | 8.2.5 | Auto-fire (fast-casual) vs explicit-fire (table-service), configurable per branch/order-type | PARTIAL_BACKEND | both | Only **explicit** Fire is implemented ("explicit MVP Fire — no auto-Fire", `orders.controller.ts:757-758` docblock). No auto-fire-upon-line-entry mode exists. Recorded scoping narrowing, not an oversight (per the same docblock). | P2_COMPLETENESS |
| FR-POS-036/037 | 8.2.5 | Courses, hold-and-fire | MISSING_BACKEND | both | `OrderLine.course Int?` exists (`schema.prisma:2116`) as a data field, but no "fire this course only" / "hold and release" verb was found on `orders.controller.ts` or `sales-fire.service.ts` — Fire operates over all eligible pending lines, not a per-course subset. | P2_COMPLETENESS |

---

## 2. Tables / Dine-in Backend Support

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| Table CRUD | 8.6 | Create/list/update a branch table (label, section, seat capacity) | COMPLETE_BACKEND | both | `organisation.controller.ts:754-808` (`POST/GET branches/:branchId/tables`, `PATCH tables/:tableId`) → `TablesService` (`tables/tables.service.ts`). DTOs: `create-table.dto.ts` (label, section, seatCapacity only — `status` deliberately excluded per its own ADR-0008-D-05 comment, "live table state is order-driven ... not part of the Organisation configuration aggregate"). | P3_HARDENING |
| FR-POS-003 (table-required-to-fire) | 8.2.1/8.6 | cross-reference | COMPLETE_BACKEND | both | See §1 FR-POS-003. `TABLE_DISPLAY_QUERY` contract resolves the table's label for Fire-facts (`sales-fire.service.ts:98-99`, `table-display.query.service.ts:26-37`). | P3_HARDENING |
| FR-POS-080 [S] | 8.6 | Graphical floor-plan editor: areas, tables, shapes, capacities, positions | MISSING_BACKEND | both | `CreateTableDto`/`UpdateTableDto` carry only `label`, `section`, `seatCapacity` — no shape, no x/y position, no "area" entity distinct from `section` (a free string). No floor-plan-layout persistence found anywhere in `organisation/tables/`. | P2_COMPLETENESS |
| FR-POS-081 [S] | 8.6 | Live table state: available/seated/ordered/food-served/bill-requested/payment-in-progress/needs-cleaning | MISSING_BACKEND | both | `TableDisplayQueryService.find` (`table-display.query.service.ts:27-36`) returns only `{ label }` — no state field, no derivation from open orders against the table. `BranchTableSummary` (`branch-table.view.ts`) likewise carries only `id/branchId/label/section/seatCapacity`, no live state. The ADR-0008-D-05 comment on `CreateTableDto` explicitly defers "live table state" to Sales, but no Sales-side query or view producing that 7-value state machine was found in this session's read of `sales.views.ts`/`orders.controller.ts`. | P2_COMPLETENESS |
| FR-POS-082 [S] | 8.6 | Merge tables, split a table's order, transfer order between tables (each audited) | MISSING_BACKEND | both | No merge/split/transfer verb exists on `organisation.controller.ts` or `orders.controller.ts`. `Order.tableId` is a plain nullable scalar set once at creation (`orders.service.ts:285`) with no update path found. | P2_COMPLETENESS |
| FR-POS-083 [S] | 8.6 | Time-since-seated / time-since-last-course display | MISSING_BACKEND | both | No such derived field exists; would require the live-state tracking FR-POS-081 also lacks. | P3_HARDENING |
| FR-POS-084 [C] | 8.6 | Server section assignment restricting/highlighting tables | MISSING_BACKEND | both | `section: String?` exists on `BranchTable` as a free-text field only; no assignment/restriction relation to an employee or shift was found. | P3_HARDENING (Could-have) |

---

## 3. Pricing / Discounts / Comps

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| FR-POS-040/041/042 | 8.3.1 | Deterministic price-resolution precedence (7 levels), provenance recorded | NEEDS_VERIFICATION (out of this session's direct read, but referenced consistently) | both | `order-lines.service.ts:13` references "the canonical `PriceResolutionService` (FR-POS-040)" and `:329` records provenance ("FR-POS-042 provenance") into `OrderLine.priceListId/priceEntryId/priceRule` (`schema.prisma:2113-2115`). The pricing engine itself lives in `catalogue/pricing/` — out of this section's assigned domain scope; flagged rather than asserted. | — |
| FR-POS-045 | 8.3.2 | Discounts at line level and order level, percentage or fixed | COMPLETE_BACKEND (logic); PARTIAL_BACKEND (role reachability — see below) | both | `discounts.service.ts:138-517` (`applyLineDiscount`/`applyOrderDiscount`); routes `orders.controller.ts:1013` (`POST .../lines/:lineId/discount`) and `:1096` (`POST .../discount`). Exact-bigint percentage math with HALF_UP rounding (`discounts.service.ts:940-974`). Test: `test/pos-financial-corrections.e2e-spec.ts` (45 `it`s), `docs/reports/claude/full-srs-4day/2026-09-03_POS-FIN-1_discounts-refunds-financial-corrections.md`. | P2_COMPLETENESS |
| FR-POS-046 | 8.3.2 | Discount reason required, from configurable list | COMPLETE_BACKEND | both | `discounts.service.ts:689-703` (`requireReasonCode`), 422 if the `ReasonCode` row does not exist in-tenant. `ReasonCode` model persists the configurable list. | P3_HARDENING |
| FR-POS-047 | 8.3.2 | Approval thresholds: max % without approval, max amount without approval, max discounts/shift/employee, discount-after-payment-started (all per-role-per-branch) | PARTIAL_BACKEND | both | `DiscountApprovalPolicyVersion` model + `discounts.service.ts:731-786` (`evaluateApprovalRequired`) implement dimensions 1-3, **scoped to (tenant, branch), not per-role** — the file's own docblock (`discounts.service.ts:23-29`) states this explicitly: "no ratified role-precedence rule exists" for a per-role threshold. Dimension 4 (after-payment-started) implemented separately (`assertDiscountAfterPaymentAllowed`, `:705-718`). Absent any configured policy, approval is always required (safe default). `pos.discount.unlimited` is the one per-actor override implemented (`:192-198`). SRS's literal "per role, per branch" threshold configuration is therefore a recorded, narrower MVP (branch-only). | P2_COMPLETENESS |
| FR-POS-048 | 8.3.2 | Approval via manager PIN, card swipe, or remote mobile-app request | PARTIAL_BACKEND | both | Only manager-PIN-on-terminal is implemented (`approval-helper.ts:74-134`, `obtainSynchronousApproval`, single-phase create+decide inside the same transaction). Card-swipe and remote/mobile-app approval channels are explicitly recorded as deferred: `approval-helper.ts:30-39` cites the 2026-08-29 governance register entry stating the async/mobile-push channel "remains deferred and knowingly unmet" project-wide. Only the synchronous PIN channel of FR-POS-048's three is implemented. | P2_COMPLETENESS |
| FR-POS-049 | 8.3.2 | Record amount/percentage/reason/applying employee/approving employee/timestamp/order context per discount | COMPLETE_BACKEND | both | `Discount` model + `discounts.service.ts:267-296` create call captures all of these; `AuditService.record` (`:298-330`) writes a parallel audit entry with `before`/`metadata`. | P3_HARDENING |
| FR-POS-050 | 8.3.2 | Comp distinct from discount — zero revenue, cost/inventory still recognised | COMPLETE_BACKEND (logic); PARTIAL_BACKEND (role reachability) | both | `discounts.service.ts:527-661` (`applyComp`); the line's `state` is untouched (only `isComp`/`lineDiscount` change), so COGS/inventory depletion — keyed on `state`, never `isComp` — proceed unaffected, exactly matching the requirement (`discounts.service.ts:1-15` docblock, independently verified by reading `applyComp`'s body: no `state:` write in the `orderLine.update` call at `:573-581`). Route: `orders.controller.ts:1162` (`POST .../lines/:lineId/comp`), guarded by `pos.comp.apply`. | P2_COMPLETENESS |
| FR-POS-051 | 8.3.2 | Discount stacking control, most-favourable-single-discount when exclusive | PARTIAL_BACKEND (deliberately narrowed) | both | No stacking/exclusivity model exists at all: "At most ONE discount/comp per line, and at most ONE order-level discount per order. No `Promotion`/exclusivity model exists anywhere in this schema" (`discounts.service.ts:17-21`, enforced by the `ConflictException` at `:171-176` and `:370-375`). This is a recorded narrowing (no promotions engine), not a partial implementation of the SRS's stacking-configuration concept. | P2_COMPLETENESS |
| — | — | **Canonical-role reachability for `pos.discount.apply` / `pos.discount.unlimited` / `pos.comp.apply`** | **IMPLEMENTED_BUT_NOT_PRODUCTION_READY** | both | Verified this session in `identity/authz/canonical-role-templates.ts:69-161`: **none** of the four canonical role templates (`cashier`, `branch_manager`, `shift_supervisor`, `kitchen_staff`) grant `SALES_PERMISSIONS.DISCOUNT_APPLY`, `DISCOUNT_UNLIMITED`, or `COMP_APPLY`. `shift_supervisor` gets only `DISCOUNT_APPROVE` (the manager-override/approval code), not the apply code. The only role that can call `POST .../discount`, `.../lines/:lineId/discount`, or `.../lines/:lineId/comp` at all is the signup-time **Owner** role, which is granted the full permission catalogue (`ALL_PERMISSION_CODES`, `registrations.service.ts:226-231`) — an Owner is not the SRS's intended day-to-day discount-applying actor (a cashier/server is). This is exactly the "no canonical role can hold the required permission" condition the task brief explicitly instructs not to mark COMPLETE_BACKEND for. The local-dev-only `seed-dev-data.ts` Cashier role (`:238-254`) also lacks all three codes. | P0_PRODUCTION_BLOCKER |

---

## 4. Voids / Corrections / Post-Fire Void

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| FR-POS-070 | 8.5 | Four distinct correction operations (pre-fire void / post-fire void / order cancel / refund), per the SRS's own table of when-effect | PARTIAL_BACKEND | both | Pre-fire void (`OrderLinesService.voidLinePreFire`) and post-fire void (`PostFireVoidService.voidPostFire`) are both implemented and structurally kept disjoint (`post-fire-void.service.ts:1-8` docblock: "this service refuses ... any line that has NOT already been sent to production, so the two paths cannot overlap"). Refund is implemented (§5). **"Order cancel" (before payment) as its own distinct operation was NOT located** — no `cancel`/`POST .../cancel` route was found on `orders.controller.ts`; an unpaid order appears to be abandoned by simply never firing/paying it, not explicitly transitioned to a `cancelled` state via a dedicated verb. Needs a dedicated check of the full `OrderState` enum for a `cancelled` value and its transition trigger. | P2_COMPLETENESS |
| FR-POS-071 | 8.5 | Post-fire void mandates disposition classification (returned-to-stock / wasted / given-to-staff), creates the corresponding inventory record | COMPLETE_BACKEND | both | `post-fire-void.service.ts:81-291`. Disposition is a **required** input (not a follow-up step) — `VoidPostFireInput.disposition` is non-optional (`:63-68`); the void write and the `POST_FIRE_VOID_DISPOSITION_COMMAND` inventory call happen in the SAME `UnitOfWork.execute` transaction (`:91-292`). Per the file's own docblock (`:14-24`, acceptance-corrected 2026-09-04) all three dispositions — including `returned_to_stock` — now write an unconditional Inventory-owned disposition record, closing an earlier gap where `returned_to_stock` produced no evidence. Route: `orders.controller.ts:1231` (`POST .../lines/:lineId/void-postfire`). Test: `test/pos-financial-corrections.e2e-spec.ts` includes "Inventory disposition record" and "Kitchen post-fire cancellation" cases per `full-srs-4day/2026-09-04_MW1I_...md` §9.A. | P2_COMPLETENESS |
| FR-POS-075 (voids/cancellations/refunds → audit) | 8.5 | Actor, approver, reason, amount, full before/after state on every correction | COMPLETE_BACKEND | both | `post-fire-void.service.ts:251-270` (`AUDIT_ACTION.ORDER_LINE_VOIDED_POSTFIRE`, `before`/`metadata` with `state`/`lineTotal`/disposition/inventory-movement-ids); `discounts.service.ts:298-330,485-512,631-652`; `refunds.service.ts:375-409` — all four write to `AuditService.record` with actor/approver/reason/amount/before-after. | P3_HARDENING |
| — | — | **Canonical-role reachability for `pos.order.void_line_postfire`** | **IMPLEMENTED_BUT_NOT_PRODUCTION_READY** | both | Same finding as §3's discount/comp reachability gap: `ORDER_VOID_LINE_POSTFIRE` is granted to **no** canonical role template (`canonical-role-templates.ts:69-161`, confirmed absent from all four). Only the full-catalogue Owner role can invoke `POST .../lines/:lineId/void-postfire`. | P0_PRODUCTION_BLOCKER |

---

## 5. Payments

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| FR-POS-060 | 8.4 | 11 named tender types (cash, integrated card, manual/external card, digital wallet, gift card, loyalty points, store credit, voucher, bank transfer, on-account, aggregator-settled) | PARTIAL_BACKEND (deliberately narrowed MVP) | both | `enum OrderPaymentTender { cash, manual_external_card }` (`schema.prisma:2014-2022`), with its own doc comment: "P1F-1 — FR-POS-060 MVP scope: only the two tenders this slice implements. The SRS names eleven; adding the other nine here would be appearance without capability." Only 2 of 11 tenders exist anywhere in the capture path (`sales-payment.service.ts:50`, `PaymentTender = 'cash' \| 'manual_external_card'`). Recorded narrowing, not a defect, but a large completeness gap against the SRS's [M] requirement. | P1_MAJOR_GAP |
| FR-POS-061 | 8.4 | Split payment across multiple tenders, running balance | COMPLETE_BACKEND (for the 2 supported tenders) | both | `OrderPayment[]` is a one-to-many relation off `Order` (`schema.prisma:2068`); `sales-payment.service.ts` capture is a **partial**-capture design (P1F-1 docblock, `orders.controller.ts:810-823`) — repeated calls accumulate toward `grandTotal`, and a settling payment auto-completes the order (P1F-2). Running-balance is derivable via `orderRemainingBalance` (`sales.views.ts`, referenced at `orders.controller.ts:68`). Scoped to cash + manual-external-card only, per FR-POS-060. | P2_COMPLETENESS |
| FR-POS-062 | 8.4 | Split bill: equal division / by seat / by selected items / by arbitrary amount | MISSING_BACKEND | both | No bill-splitting endpoint or service logic was found. `OrderLine.seatNumber` exists as a data field (FR-POS-004) but no query/command groups lines by seat for a partial-bill payment; "arbitrary amount" split is technically achievable only in the trivial sense that a partial cash/card capture can be for any amount up to the remaining balance — there is no first-class "split this order N ways" primitive. | P1_MAJOR_GAP |
| FR-POS-063 | 8.4 | Cash change computation, country-pack cash-rounding rule, rounding recorded as a separate ledger amount | COMPLETE_BACKEND | both | `sales-payment.service.ts:244-277` (tender-specific computation): `roundedCashDue`, `InsufficientCashTenderedError` if under-tendered, `changeGiven = tenderedAmountMinor - roundedCashDue`. `OrderPayment.roundingAdjustment` is a separate, per-payment column (`schema.prisma:2273-2277`), explicitly never folded into `paid_total`/revenue/tax (P1D-D carried item, cited in both the schema doc comment and `refunds.service.ts`'s own docblock). | P3_HARDENING |
| FR-POS-064 | 8.4 | Integrated card payment initiation, terminal timeout/decline/partial-approval/comm-failure handling without indeterminate order state | MISSING_BACKEND | both | Explicitly not implemented: `schema.prisma:2299` "no integrated terminal exists — FR-POS-064 NOT IMPLEMENTED." Only the manual/external-card record-after-the-fact tender exists. | P1_MAJOR_GAP |
| FR-POS-065 | 8.4 | Idempotent payment capture — retried request with same key never double-charges | COMPLETE_BACKEND | both | `@Idempotent()` on `POST .../payments` (`orders.controller.ts:838`), plus a **second**, permanent-id-based replay check inside `SalesPaymentService.capture` itself (mirrors the pattern independently re-verified in `refunds.service.ts:164-187` for refunds — same author-stated pattern, "P1E-5A pattern"). `Idempotency-Key` header documented as mandatory. Test: `test/sales-payment-concurrency.e2e-spec.ts`. | P3_HARDENING |
| FR-POS-066 | 8.4 | No PAN/CVV/magstripe storage; only last-4, scheme, auth code, terminal reference | COMPLETE_BACKEND | both | `OrderPayment` model's card-specific columns are exactly the permitted set: `paymentTerminalTxnRef`, `cardScheme`, `cardLast4` (CHECK-enforced exactly 4 digits, `schema.prisma:2296-2307`), `authorizationCode`. No PAN/CVV/track-data column exists anywhere in the model — structurally impossible to store one, not merely policy-avoided. | P3_HARDENING |
| FR-POS-067 | 8.4 | On-account sales to credit customers with credit-limit blocking | MISSING_BACKEND | both | On-account is one of the 11 FR-POS-060 tenders never implemented (see FR-POS-060); no credit-customer/credit-limit model was found in the sales/identity modules read this session. | P2_COMPLETENESS (Should-have) |
| FR-POS-055/056/057/058 | 8.3.3 | Service charge, tip entry, tip pooling/report, service-charge taxability rule | MISSING_BACKEND | both | `Order.serviceChargeTotal`/`tipTotal` columns exist and are surfaced in views/events (`receipt.views.ts:102,106`, `sales.views.ts:39,44`, `contract/events.ts:260,265`) but are **never written** — `order-totals.ts:103-106`'s own comment: "`serviceChargeTotal` and `roundingAdjustment` are still NOT recomputed here — service charge and cash rounding (BR-FIN-004) are not implemented ... They stay at their defaults of 0." No tip-entry endpoint, no tip-pooling configuration, no tip-distribution report were found. | P1_MAJOR_GAP |

---

## 6. Refunds

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| FR-POS-072 | 8.5 | Refund references original order, cannot exceed original amount in aggregate across all refunds | COMPLETE_BACKEND | both | `refunds.service.ts:106-119` (`originalPaymentId` required); hard cap enforced inside a `pg_advisory_xact_lock`-serialized transaction (`:191-195,255-275`) — `sum(committed refunds) + requested refund <= order.grandTotal`, a fresh `SUM` read under the lock, not a stale/racy figure. The docblock (`:16-37`) records a deliberate, reasoned choice of `grandTotal` (not `paidTotal`) as the cap basis, addressing the one edge case where they diverge (over-tender). Test coverage: `pos-financial-corrections.e2e-spec.ts` "refund `grandTotal` cap" case (`full-srs-4day/2026-09-04_MW1I_...md` §9.A). | P3_HARDENING |
| FR-POS-073 | 8.5 | Refund requires reason; above configurable threshold requires manager approval | COMPLETE_BACKEND (logic); see role-reachability note | both | `refunds.service.ts:245-253` (reason required, 422 if unknown), `:277-323` (approval-required evaluation reusing `DiscountApprovalPolicyVersion.maxAmountWithoutApprovalMinor`, absent policy = always-required — conservative default, recorded reuse of the discount threshold concept per the file's own docblock `:44-52` since the SRS names no distinct refund-threshold dimension). | P2_COMPLETENESS |
| FR-POS-074 | 8.5 | Refund defaults to original tender; different tender requires elevated permission and is flagged in fraud-detection report | PARTIAL_BACKEND | both | Default-to-original-tender and elevated-permission gating are both implemented: `refunds.service.ts:226-243` (`pos.refund.different_tender` checked in-transaction via `SCOPE_AUTHORIZATION`, 403 if absent). The "flagged in the fraud detection report" clause is explicitly **not implemented** — the file's own docblock (`:60-63`) states "no fraud-report infrastructure exists anywhere in this repository," a recorded scoping decision per explicit task instruction at the time, not a silent omission. | P2_COMPLETENESS |
| FR-POS-075 | 8.5 | Refund audit entry: actor, approver, reason, amount, before/after state | COMPLETE_BACKEND | both | `refunds.service.ts:375-409` (`AUDIT_ACTION.REFUND_ISSUED`, `before: {state, cumulativeRefunded}`, `metadata` with full amount/tender/cap/state-transition detail). | P3_HARDENING |
| CR-04 / BR-POS-001 (append-only correction model) | governance | Refunds are compensating entries; original Order/OrderLine/OrderPayment rows are never rewritten | COMPLETE_BACKEND | both | `refunds.service.ts:4-14` docblock states no `orderPayment.update`/`orderLine.update` call exists anywhere in the file (independently verified: only `tx.refund.create` and `tx.order.updateMany` — the latter touching only `state`/`version`/`updatedAt`, never a posted financial total) and that `sales.order_payments`' DB grants make an UPDATE physically impossible even if attempted (append-only ADR-010 pattern, same as `governance.audit_entries`). | P3_HARDENING |
| — | — | Refund tender scope | PARTIAL_BACKEND | both | `IssueRefundInput.tender: 'cash' \| 'manual_external_card'` (`refunds.service.ts:110`) — matches the same FR-POS-060 MVP narrowing as payment capture; a refund can never target a tender type ROS cannot capture in the first place, so this is consistent, not an independent gap. | P2_COMPLETENESS |
| — | — | **Canonical/Shift-Supervisor role reachability for `pos.refund.issue`** | **COMPLETE_BACKEND as of `bf91d66` (same-day fix)** | both (permission code); lane-d only for the role-grant fix | `canonical-role-templates.ts:69-80` (`CASHIER_PERMISSION_CODES` includes `SALES_PERMISSIONS.REFUND_ISSUE`, added by commit `bf91d66` "fix: close backend golden path blockers," 2026-09-07, per its own diff: `+  SALES_PERMISSIONS.REFUND_ISSUE,`); `shift_supervisor` inherits it via `...CASHIER_PERMISSION_CODES` spread (`:118-122`). `pos.refund.different_tender` and `pos.discount.approve` (the approval act) are deliberately WITHHELD from Cashier (docblock `:58-67`), preserving the issue/approve/different-tender separation the SRS's manager-approval model requires. **This is the one refund-adjacent gap the prior-day golden-path audit found (`2026-09-06_GOLDEN-PATH-AUDIT_...md` step 23, "Nobody at staff tier has `pos.refund.issue`") and it is now closed**, verified directly against current `canonical-role-templates.ts` content this session, not merely cited from the closure report. Note: `shift_supervisor` (via `DISCOUNT_APPROVE`) is the only canonical role that can act as the manager-approver for a refund requiring approval — matches the SRS's cashier-issues/supervisor-approves shape. | (resolved — informational, was P0 before `bf91d66`) |
| — | — | Reason-code / approval-policy production provisioning | IMPLEMENTED_BUT_NOT_PRODUCTION_READY | both | Per `bf91d66`'s own commit message and the 2026-09-06 audit: no `ReasonCode` row and no `DiscountApprovalPolicyVersion` row are auto-provisioned at tenant signup — an absent policy means EVERY discount/refund requires manager approval (safe default) and an absent `ReasonCode` means every discount/refund/void call 422s until an Owner manually creates at least one reason code via the admin surface. Backend logic is correct; a fresh tenant cannot exercise discounts/comps/voids/refunds at all without this manual admin step first — a real production-readiness dependency, not a code defect. | P1_MAJOR_GAP |

---

## 7. Receipts

| Req ID | Section | Title | Classification | Worktree(s) | Evidence | Severity |
|---|---|---|---|---|---|---|
| Backend receipt retrieval | 8.8 | `GET /orders/:businessDay/:id/receipt` | COMPLETE_BACKEND (for its own declared, narrow scope) | both | `orders.controller.ts:604-638` → `ReceiptService.findCompletedOrderReceipt` (`receipt.service.ts:24-79`). Accepts `completed`/`partially_refunded`/`refunded` order states (POS-FIN-1 addition so a refunded order's receipt stays retrievable, CR-04). Reads only `sales`-schema tables (zero cross-module boundary edges, by design — `receipt.service.ts:1-19` docblock). Test: `test/receipt.e2e-spec.ts` (17 `it`s, cited as 16/16 e2e in the RCPT-R1 ratification and `full-srs-4day/2026-09-04_MW1I_...md` §9.D). | P3_HARDENING |
| FR-POS-100 | 8.8 | Receipt contains all country-pack-mandated elements: tax registration number, invoice sequence, tax breakdown, required QR code | MISSING_BACKEND | both | The receipt is explicitly, deliberately **non-fiscal**: `receipt.openapi.ts:99-106` — "Makes no claim of legal or fiscal invoice compliance: no tax registration number, invoice sequence, country-pack-mandated tax breakdown, fiscal QR, fiscal signature or tax-authority submission status is present or derivable from this document." `documentType: 'INTERNAL_NON_FISCAL_RECEIPT'`, `fiscal: false` (always). This is a ratified scope decision (governance register, "RCPT-R1 — Internal-MVP Non-Fiscal Receipt Ratification — 2026-09-01," `GOVERNANCE_DECISION_REGISTER.md:7750`) — not an unrecorded gap — but it means FR-POS-100 as literally written ([M], mandatory fiscal content) is NOT met by the backend today. | P1_MAJOR_GAP (governance-acknowledged) |
| FR-POS-101 | 8.8 | Template-driven layout per country pack/brand: logo, custom header/footer, language selection | MISSING_BACKEND | both | No template/branding/header-footer configuration was found in `receipt.service.ts`/`receipt.views.ts`/`receipt.openapi.ts` — the receipt is a fixed JSON shape (`receiptSchema`, `receipt.openapi.ts:99-260`ish), not a rendered, templated document. | P2_COMPLETENESS |
| FR-POS-102 | 8.8 | Bilingual (Arabic/English) receipts, configurable ordering | MISSING_BACKEND | both | `itemNameSnapshot`/name fields are described as "Opaque localized-name snapshot (locale -> name)" (`receipt.openapi.ts:24-27,42-44`) — the data CAN carry multiple locales as captured, but no receipt-level language-selection/ordering configuration or rendering logic was found. | P2_COMPLETENESS |
| FR-POS-103 | 8.8 | Digital delivery: SMS, WhatsApp, email, QR | MISSING_BACKEND | both | No delivery-channel integration (SMS/WhatsApp/email provider, or QR generation) was found anywhere in the sales module. | P2_COMPLETENESS |
| FR-POS-104 | 8.8 | Receipt reprint, clearly marked as duplicate, logged | MISSING_BACKEND | both | `ReceiptService.findCompletedOrderReceipt` is a plain idempotent GET with no reprint/duplicate-marking concept and (by explicit design, `receipt.service.ts:15-18`) no audit write at all ("`AuditService` is deliberately NOT injected: this is a pure GET with no state change"). A second call returns the identical document with no "DUPLICATE" flag and no log entry distinguishing a first view from a reprint. | P2_COMPLETENESS |
| FR-POS-105 | 8.8 | Kitchen tickets print in kitchen's configured language, independent of receipt language | NOT_APPLICABLE_TO_CURRENT_PHASE (Kitchen domain, printing infra out of scope for this backend audit section) | both | Ticket generation (Kitchen module) is out of this section's assigned domain; not independently re-verified here. | — |
| FR-POS-106 | 8.8 | Printer-failure handling: queue, alert, continue sale, auto-retry, never block | NOT_BACKEND_OWNED / NEEDS_VERIFICATION | both | No physical-printer integration exists in the backend at all (receipts/tickets are API responses, not print-queue-managed documents from the backend's perspective); printing itself is a client/device concern. If ROS intends a backend print-job queue, it was not found in this session's reads. | P2_COMPLETENESS |
| — | — | Frontend wiring of the (complete) backend receipt endpoint | NOT_BACKEND_OWNED (cited for completeness, not scored here) | — | Per `2026-09-06_GOLDEN-PATH-AUDIT_pos-create-fire-payment-receipt-refund.md` step 22: the frontend live POS never renders `services.sales.receipt()`'s response to the cashier/customer — called only internally by the Refund drawer's payment-id picker. Backend is complete; this is a frontend gap, out of this report's backend-only scope. | — |

---

## INTEGRATION_GAP — lane-a vs lane-d

**Finding: no open integration gap exists for this report's domain (POS Order Capture / Tables / Discounts-Comps-Voids / Payments / Refunds / Receipts) as of lane-d HEAD `bf91d66`.** This corrects the task brief's premise (see §0).

| Item | Lane-a origin commit(s) | Lane-d integrated commit(s) | Status |
|---|---|---|---|
| Governed discounts, comps, post-fire void, Kitchen signal | `2bf0825` (lane-a3-pos-financial-corrections) | `1c6402c` (cherry-picked via integration branch `full-srs/4day-integration`, per `full-srs-4day/2026-09-04_MW1I_...md` §2 row C1) | **INTEGRATED — content verified byte-identical** (`diff` of `src/modules/sales/orders/discounts.service.ts`, `post-fire-void.service.ts` at both commits: zero diff) |
| Append-only refunds and reporting reconciliation | `1783a6c` | `7c7f166` (row C2) | **INTEGRATED — verified** (`diff` of `refunds.service.ts`, `daily-trading-sales.query.service.ts` at current worktree heads: zero diff) |
| OpenAPI regeneration + route-surface exhaustiveness tests | `71b16f7` | `8ba3e6a` (row C3) | **INTEGRATED** (per MW1I §9.E: `openapi.e2e-spec.ts` 49/49 in the integrated branch) |
| Targeted real-Postgres e2e suite (`pos-financial-corrections.e2e-spec.ts`) | `edf74ed` | `293d830` (row C4) | **INTEGRATED — verified byte-identical** (`diff /test/pos-financial-corrections.e2e-spec.ts` between lane-d and lane-a: zero diff, 45 `it`s both sides) |
| Closure fixes (financial-correction acceptance gaps) | `d4baad5` | `bdd18bf` (row C6) | **INTEGRATED** |
| POS financial acceptance-evidence correction (docs) | `0ca3c4b` (lane-a's own HEAD) | `dc2fd41` (row C8) | **INTEGRATED** — lane-a's tip commit is itself already inside lane-d's history under a different SHA |

**What lane-d has that lane-a does NOT** (all outside this report's assigned domain, listed for completeness since the task brief asked for exact SHAs both directions): HR-1 workforce core (`32a2ba7`/`7bce735`/`1a51b13`), CI/security tenant-isolation hardening (`4f6b995`..`e8510c2`), dependency remediation (`cedf0b5`/`4e9a9b8`), SIGNUP-1 self-service tenant signup (`b85c524` and related), multiple `DEMO-OPS-HOTFIX`/`LIVE-DEMO-HOTFIX` frontend-integration fixes (`dc746a9`, `8fad55d`, `3e4f85c`, `2c323a4`), reporting/analytics (`b373645`/`b2e0327`), and the same-day golden-path closure (`bf91d66`, which is the fix that CLOSES the one canonical-role reachability gap this report found for refunds — see §6).

**What genuinely remains a role-provisioning gap regardless of which worktree is deployed** (not a lane-a/lane-d divergence — identical in both, since the permission *codes* `DISCOUNT_APPLY`/`DISCOUNT_UNLIMITED`/`COMP_APPLY`/`ORDER_VOID_LINE_POSTFIRE` were introduced by the very commits both worktrees share): no canonical role template in either worktree grants any of those four codes; only the full-catalogue Owner role can invoke discount-apply, comp-apply, or post-fire-void today. This is the same class of gap `pos.refund.issue` had until `bf91d66` fixed it — it has NOT yet been fixed for these four codes in either lane. See §3 and §4.

---

## Total requirements assessed and classification counts

**Total distinct requirement/feature rows assessed: 52** (across the 7 tables above, excluding the two purely cross-reference rows and the INTEGRATION_GAP table which restates items already counted).

| Classification | Count |
|---|---|
| COMPLETE_BACKEND | 17 |
| PARTIAL_BACKEND | 15 |
| MISSING_BACKEND | 13 |
| IMPLEMENTED_BUT_NOT_HTTP_REACHABLE | 0 |
| IMPLEMENTED_BUT_NOT_PRODUCTION_READY | 3 |
| DEFERRED_BY_GOVERNANCE | 0 |
| NOT_BACKEND_OWNED | 2 |
| NOT_APPLICABLE_TO_CURRENT_PHASE | 1 |
| NEEDS_VERIFICATION | 4 |

(Rows classified with a compound label, e.g. "COMPLETE_BACKEND (logic); PARTIAL_BACKEND (role reachability)", are counted once under the classification that reflects the requirement's actual production-usable state — the stricter of the two — and a second time, separately, under the dedicated role-reachability row where one exists, since that row is its own distinct assessment. The two IMPLEMENTED_BUT_NOT_PRODUCTION_READY findings for discount/comp/void-postfire and the one for the receipt/reason-code provisioning gap are the three counted in that row.)

**Severity counts:** P0_PRODUCTION_BLOCKER: 2 (discount/comp/post-fire-void unreachable by any canonical staff role — §3, §4). P1_MAJOR_GAP: 6 (tender-type coverage, split-bill, integrated-card, service-charge/tips, fiscal receipt content, reason-code/policy provisioning). P2_COMPLETENESS: 21. P3_HARDENING: 15.

---

*End of report. This section covers only the six assigned domains (POS Order Capture, Tables/Dine-in, Discounts/Comps/Voids/Corrections, Payments, Refunds, Receipts). It does not constitute or replace the master audit's executive summary, which is assembled separately from this and the other domain sections.*
