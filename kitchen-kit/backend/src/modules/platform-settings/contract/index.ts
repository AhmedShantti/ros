/**
 * platform-settings PUBLIC contract barrel — SRS §5.4.
 *
 * Other modules import `modules/platform-settings/contract` and nothing
 * else. `module-boundaries.spec.ts` enforces that mechanically.
 */
export * from './effective-setting.query';
